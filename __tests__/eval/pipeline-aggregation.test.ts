/**
 * Per-Geber-Gruppe-Breakdown + Per-Dossier-Breakdown Aggregation.
 * Skelett: Wave 0 (Plan 05-01, D-32). Implementierung: Wave 2 Plan 05-04.
 *
 * Aggregat-Report: 3 Haupt-Scores + Finanzplan-Sub + Per-Dossier + Per-Geber-Gruppe (D-12).
 * Geber-Gruppen: oeffentlich / stiftung / eu / wirtschaftspreis / verband-uni (D-10, D-29).
 */
describe("Per-Geber-Gruppe + Per-Dossier Aggregation", () => {
  it.todo("Per-Geber-Gruppe-Breakdown: 3 Eintraege mit 'oeffentlich' → correct mean fuer Gruppe 'oeffentlich'");
  it.todo("Per-Dossier-Breakdown: Scores pro programmId korrekt gruppiert");
  it.todo("n-Counter pro Cluster korrekt: 3 Eintraege fuer 'wirtschaftspreis' → n=3 in Breakdown");
});

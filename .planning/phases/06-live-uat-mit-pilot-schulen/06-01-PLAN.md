---
phase: 06-live-uat-mit-pilot-schulen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/uat-db-snapshot.ts
  - scripts/uat-session-token.ts
  - scripts/uat-pre-session-check.ts
  - .planning/uat/UAT-PLAN-TEMPLATE.md
  - .planning/uat/IST-STAND-CHECK.md
autonomous: true
requirements: [UAT-01]
user_setup: []

must_haves:
  truths:
    - "Ein dokumentierter Ist-Stand zeigt, welche Critical-Path-Features (MATCH-02/03, UI-01..04) tatsaechlich auf feature/wizard-adaptive live sind (D-15)"
    - "UAT-PLAN-TEMPLATE.md beobachtet nur real existierende Features ODER fehlende Features sind als Nachzieh-Aufgabe explizit markiert (D-15, D-21)"
    - "Drei Helper-Skripte machen jede UAT-Session reproduzierbar (DB-Snapshot, Session-Token-Abfrage, Pre-Session-Check) (D-12)"
    - "Das Begruessungs-/Datenschutz-Skript empfiehlt fiktive Schul-Daten als Default (D-22)"
  artifacts:
    - path: ".planning/uat/IST-STAND-CHECK.md"
      provides: "D-15 Vorbedingungs-Gate Befund pro Critical-Path-Feature mit live/fehlt-Status; fehlende Features werden eskaliert statt mitgebaut (D-21)"
      contains: "MATCH-02"
    - path: "scripts/uat-db-snapshot.ts"
      provides: "JSON-Export aller ki_antraege-Rows seit Session-Start-Timestamp; Rohdaten bleiben ausserhalb git (D-13)"
      contains: "updated_at"
    - path: "scripts/uat-session-token.ts"
      provides: "ORDER-BY-updated_at-Abfrage der letzten Session-Tokens auf stdout (D-12 reproduzierbares Session-Setup)"
      contains: "session_token"
    - path: "scripts/uat-pre-session-check.ts"
      provides: "Checklist-Runner fuer die Vor-Session-Checkliste aus UAT-PLAN-TEMPLATE.md (D-12)"
      contains: "PAYWALL_DEV_MOCK"
  key_links:
    - from: "scripts/uat-db-snapshot.ts"
      to: "PostgreSQL ki_antraege"
      via: "pg.Client mit DATABASE_URL aus .env.local; Snapshot-Output nach tmp/ ausserhalb git (D-13)"
      pattern: "pg\\.Client"
    - from: "scripts/uat-pre-session-check.ts"
      to: "localhost:3101 Dev-Server"
      via: "fetch HTTP-Erreichbarkeitspruefung als Teil des reproduzierbaren Setups (D-12)"
      pattern: "3101"
---

<objective>
Vorbedingungs-Gate (D-15) und Setup-Infrastruktur fuer die Live-UAT-Phase: pruefen, welche Critical-Path-Features wirklich live sind, drei Helper-Skripte bauen, die jede Session reproduzierbar machen, und das UAT-Plan-Template auf fiktive Daten als Default umstellen (D-22).

Purpose: Ohne dieses Plan starten Piloten gegen eine unfertige UI (REQUIREMENTS-Traceability zeigt MATCH-02/03 + UI-01..04 als "Pending" trotz ROADMAP-`[x]` der Phase-02.1-Plans — bekannter Widerspruch). Das Gate macht den realen Feature-Stand zum dokumentierten Fakt, bevor Sessions terminiert werden.
Output: IST-STAND-CHECK.md, drei `scripts/uat-*.ts`-Helper, angepasstes UAT-PLAN-TEMPLATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-uat-mit-pilot-schulen/06-CONTEXT.md
@.planning/phases/06-live-uat-mit-pilot-schulen/06-PATTERNS.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- DB-Verbindungs-Pattern aus scripts/smoke-pipeline-rerun.ts lines 50-59 (laut 06-PATTERNS.md). -->
<!-- Executor uebernimmt dieses Pattern in allen drei Helper-Skripten — keine Codebase-Exploration noetig. -->

REPO-Konstante:        const REPO = resolve(__dirname, "..");
DATABASE_URL-Read:     readFile(.env.local) -> Regex /DATABASE_URL\s*=\s*"?([^"\n]+)"?/
DB-Client:             new pg.Client(url); await client.connect(); ... await client.end();
main-Wrapper:          main().catch((e) => { console.error(e); process.exit(1); });
CLI-Aufruf:            npx tsx --env-file=.env.local scripts/<name>.ts [flags]
Tabelle ki_antraege:   Spalten session_token, created_at, updated_at, antrag_data (jsonb)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vorbedingungs-Gate — Ist-Stand-Check der Critical-Path-Features (D-15)</name>
  <files>.planning/uat/IST-STAND-CHECK.md</files>
  <read_first>
    - .planning/uat/UAT-PLAN-TEMPLATE.md (welche Features die Beobachtungsliste voraussetzt)
    - .planning/REQUIREMENTS.md (Traceability-Tabelle: MATCH-02/03 + UI-01..04 Status "Pending")
    - .planning/ROADMAP.md (Phase 02.1 Plans als `[x]` gefuehrt — Widerspruch zur Traceability)
    - lib/wizard/matcher.ts (pruefen: liefert MatchResult `passt_weil`/`achtung_bei` Tagged-Union? Gibt es CLARIFY-Dispatch fuer vage Anliegen?)
    - app/antrag/start/page.tsx und die zugehoerige Client-Komponente (pruefen: ClarificationCard, MatchResultList mit passt_weil/achtung_bei, Empty-States)
    - app/antrag/meine/page.tsx (pruefen: Action-orientierter Empty-State mit CTA)
    - components/wizard/GeneratingProgress.tsx oder analoge Datei (pruefen: 7-Stage-Tracking pending/active/done, failed-UI mit Retry-CTA)
    - app/antrag/[programmId]/wizard/page.tsx und Antrag-Detail-Komponente (pruefen: h2-Anker-IDs, AntragSectionNav-Sidebar, Edit-Button bei paid=true)
  </read_first>
  <action>
    Erstelle `.planning/uat/IST-STAND-CHECK.md` als D-15-Vorbedingungs-Gate. Pruefe per Code-Inspektion (grep + Read) fuer jedes der folgenden sechs Feature-Sets, ob es auf dem aktuellen `feature/wizard-adaptive`-HEAD tatsaechlich implementiert ist: (1) MATCH-02 strukturierte Begruendung `passt_weil`/`achtung_bei` pro Treffer in `lib/wizard/matcher.ts` + MatchResultList-Rendering; (2) MATCH-03 Klaerungsfrage bei vagem Anliegen (CLARIFY-Dispatch + ClarificationCard); (3) UI-01 Antrag-Detail h2-Anker + AntragSectionNav-Sidebar + Edit-Button; (4) UI-02 Empty-States `/antrag/meine` + Matcher-0-Treffer mit Reformulierungs-Tipps; (5) UI-03 Mobile-Touch-Targets (sm:py-3 / min-h) auf Critical-Paths; (6) UI-04 GeneratingProgress 7-Stage-Tracking + failed-UI mit Retry-CTA.
    Strukturiere das Dokument mit einer Tabelle: Spalten `Feature` | `Requirement-ID` | `Status (live | teilweise | fehlt)` | `Belegstelle (Datei:Zeile / grep-Treffer)` | `UAT-Konsequenz`. Bei `live`: Feature bleibt in der UAT-Beobachtungsliste. Bei `teilweise`/`fehlt`: dokumentiere zwei Optionen je Feature gemaess D-15 — entweder kurz nachziehen (mit Aufwand-Schaetzung in Kontext-Prozent) ODER die UAT-Beobachtungsliste explizit auf den realen Ist-Stand reduzieren.
    Schliesse mit einem Abschnitt `## Entscheidung` der explizit festhaelt, welche Features in Task 2 aus UAT-PLAN-TEMPLATE.md entfernt/reduziert werden und welche (falls Code zeigt: trivial nachziehbar, <10% Kontext) als separater Nachzieh-Schritt empfohlen werden. Wenn ein fehlendes Feature substanziellen Aufwand braucht, vermerke es als Eskalation an Kolja (Phase-2-Closure / UI-Plan separat einplanen) — NICHT in Phase 6 mitbauen (06-CONTEXT Deferred Ideas).
    Sprache: Deutsch mit Umlauten. Belegstellen mit konkreten Datei-Pfaden und Zeilennummern bzw. grep-Mustern.
  </action>
  <verify>
    <automated>test -f .planning/uat/IST-STAND-CHECK.md && grep -q "MATCH-02" .planning/uat/IST-STAND-CHECK.md && grep -q "UI-04" .planning/uat/IST-STAND-CHECK.md && grep -q "## Entscheidung" .planning/uat/IST-STAND-CHECK.md</automated>
  </verify>
  <acceptance_criteria>
    - IST-STAND-CHECK.md existiert und enthaelt eine Tabelle mit allen sechs Feature-Sets (MATCH-02, MATCH-03, UI-01, UI-02, UI-03, UI-04)
    - Jede Tabellenzeile hat einen Status-Wert aus {live, teilweise, fehlt} und eine konkrete Belegstelle (Datei:Zeile oder grep-Muster), keine subjektive Formulierung
    - Abschnitt `## Entscheidung` benennt explizit, welche Features aus UAT-PLAN-TEMPLATE.md reduziert werden und welche als Nachzieh-/Eskalations-Schritt gelten
    - Kein fehlendes Feature wird in Phase 6 als Build-Aufgabe eingeplant (Konsistenz mit 06-CONTEXT Deferred Ideas)
  </acceptance_criteria>
  <done>Ist-Stand aller sechs Critical-Path-Feature-Sets dokumentiert, Entscheidung pro fehlendem Feature getroffen.</done>
</task>

<task type="auto">
  <name>Task 2: UAT-PLAN-TEMPLATE.md auf Ist-Stand reduzieren + fiktive Daten als Default (D-22)</name>
  <files>.planning/uat/UAT-PLAN-TEMPLATE.md</files>
  <read_first>
    - .planning/uat/UAT-PLAN-TEMPLATE.md (aktueller Stand — Begruessungs-Skript Zeile 24-26, Datenschutz-Block Zeile 71-75, Beobachtungsliste Zeile 32-50)
    - .planning/uat/IST-STAND-CHECK.md (aus Task 1 — die `## Entscheidung` ist bindend fuer die Beobachtungslisten-Reduktion)
    - .planning/phases/06-live-uat-mit-pilot-schulen/06-CONTEXT.md (D-22 Wortlaut)
  </read_first>
  <action>
    Passe `.planning/uat/UAT-PLAN-TEMPLATE.md` an zwei Stellen an:
    (1) D-22 fiktive Daten als Default: Erweitere das Begruessungs-Skript (aktuell Zeile 24-26) um einen klaren Satz, der dem Piloten aktiv empfiehlt, fiktive Schul-Daten zu verwenden (erfundener Schul-Name, erfundene Schuelerzahl, erfundener Ort) — mit Begruendung "damit keine echten Daten in die Dev-Datenbank fliessen". Aendere den Datenschutz-Hinweis-Block (aktuell Zeile 71-75) so, dass "Nur fiktive Daten" die empfohlene Standard-Option ist und "echte Daten" nur auf ausdruecklichen Wunsch + Bestaetigung des Piloten. Drehe die Bestaetigungs-Checkboxen entsprechend: `[x] Nur fiktive Daten (empfohlen)` als Default-Markierung im Template. Begruendung als Template-Kommentar: umgeht das undokumentierte DeepSeek-AVV als UAT-Blocker.
    (2) Beobachtungsliste auf Ist-Stand reduzieren: Setze die Critical-Paths-Beobachtungsliste (aktuell Zeile 32-50) exakt gemaess dem `## Entscheidung`-Abschnitt aus IST-STAND-CHECK.md. Features mit Status `fehlt` werden aus der Beobachtungsliste UND aus der Beobachtungs-Notizen-Tabelle entfernt; Features mit Status `teilweise` werden mit einem Klammer-Hinweis auf den Teil-Stand belassen. Aendere keine Features mit Status `live`.
    Sprache: Deutsch mit Umlauten. Aendere NUR diese beiden Bereiche — die Session-Erfolgs-/Abbruch-Kriterien und der Nach-Session-Block bleiben unveraendert (ausser ein Feature dort verweist explizit auf ein entferntes Feature).
  </action>
  <verify>
    <automated>grep -qi "fiktiv" .planning/uat/UAT-PLAN-TEMPLATE.md && grep -q "empfohlen" .planning/uat/UAT-PLAN-TEMPLATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Das Begruessungs-Skript enthaelt eine explizite Empfehlung an den Piloten, fiktive Schul-Daten zu nutzen
    - Der Datenschutz-Hinweis-Block markiert "Nur fiktive Daten" als empfohlene Standard-Option
    - Die Critical-Paths-Beobachtungsliste enthaelt keine Features, die in IST-STAND-CHECK.md `## Entscheidung` als `fehlt` eingestuft sind
    - Features mit Status `live` in der Beobachtungsliste sind unveraendert
  </acceptance_criteria>
  <done>UAT-PLAN-TEMPLATE.md empfiehlt fiktive Daten als Default und beobachtet nur real existierende Features.</done>
</task>

<task type="auto">
  <name>Task 3: Drei UAT-Helper-Skripte (DB-Snapshot, Session-Token, Pre-Session-Check) — D-12</name>
  <files>scripts/uat-db-snapshot.ts, scripts/uat-session-token.ts, scripts/uat-pre-session-check.ts</files>
  <read_first>
    - scripts/smoke-pipeline-rerun.ts (DB-Verbindungs-Pattern lines 50-59, REPO-Konstante line 23, main-Wrapper lines 200-204)
    - scripts/smoke-pipeline-with-extractor.ts (tmp/-Output-Pattern lines 200-214)
    - scripts/smoke-llm.ts (Checklist-Runner-Schritt-Pattern [1/N] [2/N])
    - .planning/uat/UAT-PLAN-TEMPLATE.md (Vor-Session-Checkliste Zeile 12-18 — die Items, die uat-pre-session-check.ts pruefen muss)
    - .planning/phases/06-live-uat-mit-pilot-schulen/06-PATTERNS.md (vollstaendige Pattern-Vorgaben pro Skript)
  </read_first>
  <action>
    Erstelle drei `npx tsx`-Skripte nach dem Pattern aus 06-PATTERNS.md. Alle drei nutzen die `<interfaces>`-Bausteine (REPO-Konstante, DATABASE_URL-Regex aus .env.local, pg.Client, main-Wrapper mit process.exit(1)).
    (1) `scripts/uat-db-snapshot.ts`: liest CLI-Flag `--since "<ISO-Timestamp>"`, fuehrt Query `SELECT session_token, created_at, updated_at, antrag_data FROM ki_antraege WHERE updated_at > $1 ORDER BY updated_at ASC` aus, schreibt das Ergebnis als JSON nach `tmp/uat-snapshot-<ISO-datum>.json` (Verzeichnis mit `mkdir({recursive:true})` anlegen). Header-Kommentar mit Zweck (D-12/D-13/D-23) und Run-Beispiel. Fehlt `--since`, mit klarer Fehlermeldung und exit(2) abbrechen.
    (2) `scripts/uat-session-token.ts`: liest optionales CLI-Flag `--limit <N>` (Default 5), Query `SELECT session_token, created_at, updated_at, (antrag_data->>'programmId') AS programm_id FROM ki_antraege ORDER BY updated_at DESC LIMIT $1`, gibt pro Row eine Zeile auf stdout aus im Format `<session_token>  <updated_at-ISO>  <programm_id|->`. Header-Kommentar nennt, dass dies den manuellen SQL-Hint aus UAT-BEFUNDE-TEMPLATE.md Zeile 5 automatisiert.
    (3) `scripts/uat-pre-session-check.ts`: Checklist-Runner im `[i/5]`-Schritt-Pattern aus smoke-llm.ts. Schritt 1: DB-Verbindung pruefen (`SELECT 1`). Schritt 2: Dev-Server-Erreichbarkeit per `fetch("http://localhost:3101", { signal: AbortSignal.timeout(3000) })` — kein Analog im Repo, neu mit node fetch implementieren; HTTP-Status loggen oder Timeout als FAIL markieren. Schritt 3: `NEXT_PUBLIC_PAYWALL_DEV_MOCK=1` in .env.local pruefen (gleiche readFile-.env.local-Mechanik, Regex auf `NEXT_PUBLIC_PAYWALL_DEV_MOCK\s*=\s*1`). Schritt 4: letzten DB-Row-Zeitstempel ausgeben (`SELECT MAX(updated_at) AS last FROM ki_antraege`) — dieser Wert ist der `--since`-Input fuer uat-db-snapshot.ts nach der Session. Schritt 5: Summary mit OK/FAIL-Zaehlung; exit(1) wenn mindestens ein Pflicht-Check (Schritt 1, 2, 3) fehlschlaegt.
    Sprache: Deutsche Konsolen-Ausgaben und Kommentare mit Umlauten. JSON-Datenfelder bleiben ASCII.
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck scripts/uat-db-snapshot.ts scripts/uat-session-token.ts scripts/uat-pre-session-check.ts 2>&1 | grep -v node_modules | grep "error TS" | grep -cv '^#' | grep -qx 0</automated>
    <human-check>uat-pre-session-check.ts gegen die laufende Dev-Umgebung ausfuehren (`npx tsx --env-file=.env.local scripts/uat-pre-session-check.ts`) — alle 5 Schritte loggen, exit(0) bei laufendem Dev-Server.</human-check>
  </verify>
  <acceptance_criteria>
    - Alle drei Skripte kompilieren ohne TypeScript-Fehler (`tsc --noEmit --skipLibCheck` gibt 0 `error TS`-Zeilen)
    - uat-db-snapshot.ts bricht mit exit(2) ab, wenn `--since` fehlt
    - uat-session-token.ts gibt pro Row genau eine stdout-Zeile mit session_token, updated_at-ISO und programm_id aus
    - uat-pre-session-check.ts fuehrt fuenf nummerierte Schritte aus und ruft exit(1) bei FAIL eines Pflicht-Checks
    - Alle drei Skripte nutzen das pg.Client-DATABASE_URL-Regex-Pattern aus .env.local (keine hartkodierten Connection-Strings)
  </acceptance_criteria>
  <done>Drei lauffaehige UAT-Helper-Skripte existieren und kompilieren; jede Session ist damit reproduzierbar einrichtbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helper-Skript -> .env.local | Skripte lesen DATABASE_URL (Connection-String mit Credentials) aus der lokalen Env-Datei |
| Helper-Skript -> PostgreSQL ki_antraege | DB-Lesezugriff auf potenziell echte Schul-Daten (Session-Antraege) |
| Helper-Skript-Output -> tmp/ + git | Snapshot-JSON koennte Connection-String oder PII enthalten, wenn unbedacht geloggt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | uat-db-snapshot.ts / uat-session-token.ts Konsolen-Output | mitigate | Skripte geben NIE den DATABASE_URL-Connection-String aus; nur Tabellen-Spaltenwerte. Code-Review-Kriterium: kein `console.log(url)`. |
| T-06-01-02 | Information Disclosure | tmp/uat-snapshot-*.json | mitigate | Snapshot-Output landet in `tmp/` (per .gitignore ausgeschlossen — pruefen, dass `tmp/` in .gitignore steht; falls nicht, ergaenzen). Rohdaten bleiben ausserhalb git (D-13). |
| T-06-01-03 | Information Disclosure | IST-STAND-CHECK.md / UAT-PLAN-TEMPLATE.md im git | accept | Beide Dateien enthalten nur Feature-Status und Template-Text, keine Schul-Daten und keine Credentials — niedriges Risiko. |
| T-06-01-04 | Tampering | npm/tsx-Ausfuehrung | accept | Keine neuen Packages — `pg` und `tsx` sind bestehende Repo-Dependencies (siehe smoke-pipeline-Skripte). Kein Package-Legitimacy-Gate noetig. |
| T-06-01-SC | Tampering | npm/pip/cargo installs | mitigate | Plan installiert keine neuen Packages; falls Executor doch eines braucht -> blocking-human-Checkpoint. |
</threat_model>

<verification>
- IST-STAND-CHECK.md existiert mit Tabelle ueber alle sechs Feature-Sets und `## Entscheidung`-Abschnitt
- UAT-PLAN-TEMPLATE.md empfiehlt fiktive Daten und beobachtet nur live/teilweise-Features
- Drei `scripts/uat-*.ts` kompilieren ohne TypeScript-Fehler
- `tmp/` ist in .gitignore (T-06-01-02)
- Kein Helper-Skript loggt den DATABASE_URL-Connection-String (T-06-01-01)
</verification>

<success_criteria>
- D-15-Vorbedingungs-Gate ist abgeschlossen: realer Feature-Stand ist dokumentiert, Entscheidung pro fehlendem Feature getroffen
- D-22 ist umgesetzt: UAT-PLAN-TEMPLATE.md empfiehlt fiktive Daten als Default
- D-12 ist umgesetzt: drei Helper-Skripte machen jede UAT-Session reproduzierbar einrichtbar
</success_criteria>

<output>
Create `.planning/phases/06-live-uat-mit-pilot-schulen/06-01-SUMMARY.md` when done
</output>

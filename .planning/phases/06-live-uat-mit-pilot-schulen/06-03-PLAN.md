---
phase: 06-live-uat-mit-pilot-schulen
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - data/eval/pipeline-korpus.json
  - data/eval/matcher-korpus.json
  - lib/wizard/pipeline.ts
  - lib/wizard/matcher.ts
  - lib/wizard/prompts.ts
autonomous: false
requirements: [UAT-02, UAT-03]

must_haves:
  truths:
    - "Jeder UAT-Bug ist entweder gefixt + verifiziert ODER explizit als akzeptiertes Risiko / v2 dokumentiert (D-02)"
    - "Hoch- und Mittel-Bugs sind in Phase 6 gefixt; Niedrig-Bugs duerfen mit dokumentierter Begruendung auf v2 (D-02)"
    - "Pipeline-/Matcher-Fixes sind per Reproducer und UI-Fixes per Playwright-Smoke verifiziert (D-03, D-04)"
    - "Jeder Bug-Fix ist ein atomarer Commit auf feature/wizard-adaptive (D-05, D-06)"
    - "Jeder gefixte Pipeline-/Matcher-Bug hat einen rot-vor-gruen Korpus-Test-Case (D-18); Routing nach Bug-Typ (D-17)"
    - "Der volle pipeline-korpus-Lauf laeuft als Wellen-Gate gruen und wird von Kolja gebuendelt abgenommen (D-07, D-08)"
    - "Unfixbare Hoch-Bugs loesen einen Eskalations-Checkpoint aus statt still zu blockieren (D-16)"
  artifacts:
    - path: "data/eval/pipeline-korpus.json"
      provides: "Pipeline-Korpus erweitert um pv-uat-NNN-Eintraege fuer Pipeline-/Halluzinations-/Finanzplan-Bugs; Routing nach Bug-Typ (D-17), rot-vor-gruen (D-18)"
      contains: "pv-uat-"
    - path: "data/eval/matcher-korpus.json"
      provides: "Matcher-Korpus erweitert um ev-uat-NNN-Eintraege fuer Matcher-/Begruendungs-/Klaerungs-Bugs; Routing nach Bug-Typ (D-17), rot-vor-gruen (D-18)"
      contains: "ev-uat-"
  key_links:
    - from: "data/eval/pipeline-korpus.json pv-uat-Eintraege"
      to: "scripts/eval-pipeline.ts"
      via: "Subset-Lauf --single waehrend Fixing (D-08), voller Lauf als Wellen-Gate (D-07, D-08); Reproducer-Verifikation (D-03), UI per Playwright-Smoke (D-04)"
      pattern: "eval-pipeline"
    - from: "Bug-Fix-Commits"
      to: "feature/wizard-adaptive"
      via: "ein atomarer fix(uat)-Commit pro Bug-ID auf dem Feature-Branch (D-05, D-06); Eskalation tiefliegender Hoch-Bugs (D-16); Fix-Scope Hoch/Mittel jetzt, Niedrig dokumentiert auf v2 (D-02)"
      pattern: "fix\\(uat\\)"
---

<objective>
Framework-Plan fuer die konsolidierte Bug-Fix-Welle nach den UAT-Sessions (UAT-03). Die konkreten Fix-Dateien sind erst nach den Befunden aus Plan 06-02 bekannt — dieser Plan liest die ausgefuellten Tracker zur Ausfuehrungszeit und leitet daraus konkrete Fix-Tasks ab: rot-vor-gruen-Eval-Test-Case, dann Fix, dann gruener Lauf, ein atomarer Commit pro Bug-ID.

Purpose: UAT-03 (Bug-Fix-Welle konsolidieren) + UAT-02-Korpus-Erweiterung. Hoch+Mittel werden gefixt, Niedrig darf dokumentiert auf v2 (D-02). Unfixbare Hoch-Bugs loesen einen Eskalations-Checkpoint aus (D-16).
Output: erweiterte Eval-Korpora um UAT-Cases, gefixte Pipeline-/Matcher-/UI-Bugs, je ein atomarer Commit pro Bug.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-uat-mit-pilot-schulen/06-CONTEXT.md
@.planning/phases/06-live-uat-mit-pilot-schulen/06-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@data/eval/BASELINE.md

<interfaces>
<!-- Eval-Skript-Flags (aus scripts/eval-pipeline.ts und scripts/eval-matcher.ts geprueft). -->
Subset-Lauf D-08:    npx tsx --env-file=.env.local scripts/eval-pipeline.ts --live --single <entry-id> --snapshot
Voller Gate-Lauf:    npx tsx --env-file=.env.local scripts/eval-pipeline.ts --live --N=3 --snapshot --md-summary
Matcher-Eval:        npx tsx --env-file=.env.local scripts/eval-matcher.ts --replay <dir>   (bzw. --live)
Reproducer D-03:     npx tsx --env-file=.env.local scripts/smoke-pipeline-with-extractor.ts

<!-- Korpus-Entry-ID-Konventionen (D-17/D-18) — neue UAT-Cases mit eigenem Praefix. -->
pipeline-korpus.json:  pv-uat-001, pv-uat-002, ...  (Pipeline-/Halluzinations-/Finanzplan-Bugs)
matcher-korpus.json:   ev-uat-001, ev-uat-002, ...  (Matcher-/Begruendungs-/Klaerungs-Bugs)
reine UI-Bugs:         KEIN Korpus-Eintrag — Playwright-Smoke (D-04)

<!-- Korpus-Entry-Schemata sind in 06-PATTERNS.md vollstaendig dokumentiert (pv-001 / ev-001 als Vorlage). -->

<!-- Commit-Format D-06. -->
Subject:  fix(uat): Bug #N <kurze Beschreibung>
Body:     Befunde-Tracker: .planning/uat/UAT-BEFUNDE-{datum}-PILOT-{code}.md
          Eval-Anker: <pv-uat-N | ev-uat-N | Playwright-Smoke>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Befunde aggregieren + rot-vor-gruen Eval-Test-Cases schreiben (D-17, D-18)</name>
  <files>data/eval/pipeline-korpus.json, data/eval/matcher-korpus.json</files>
  <read_first>
    - alle .planning/uat/UAT-BEFUNDE-*PILOT-*.md (die ausgefuellten Tracker aus Plan 06-02 — Quelle aller Bug-IDs)
    - .planning/phases/06-live-uat-mit-pilot-schulen/06-PATTERNS.md (vollstaendiges pv-uat-/ev-uat-Entry-Schema mit Feldliste)
    - data/eval/pipeline-korpus.json (bestehende pv-001..pv-022 als Vorlage; hoechste pv-uat-Nummer pruefen)
    - data/eval/matcher-korpus.json (bestehende ev-001..ev-029 als Vorlage)
    - data/foerderprogramme.json (gueltige programmId-Werte fuer Korpus-Eintraege)
    - .planning/phases/06-live-uat-mit-pilot-schulen/06-CONTEXT.md (D-02 Fix-Scope, D-17 Korpus-Routing, D-18 rot-vor-gruen)
  </read_first>
  <action>
    Lies alle ausgefuellten UAT-Befunde-Tracker aus .planning/uat/ und baue eine konsolidierte Bug-Liste: Bug-ID, Severity (hoch/mittel/niedrig), Bug-Typ, betroffener Tracker. Berücksichtige auch die in den D-23-Antrag-Qualitaets-Abgleichen erfassten Halluzinations-Marker als Bug-Eintraege.
    Route jeden Bug nach Typ (D-17): Pipeline-/Halluzinations-/Finanzplan-/Abschnitts-Bug -> neuer Eintrag in data/eval/pipeline-korpus.json mit ID-Praefix `pv-uat-NNN`; Matcher-/Begruendungs-/Klaerungs-Bug -> neuer Eintrag in data/eval/matcher-korpus.json mit ID-Praefix `ev-uat-NNN`; reiner UI-/Rendering-Bug -> KEIN Korpus-Eintrag, wird in Task 2 per Playwright-Smoke abgedeckt.
    Schreibe pro Korpus-Bug einen Eintrag, der den Bug reproduziert (D-18 rot-vor-gruen): das Entry-Schema aus 06-PATTERNS.md exakt einhalten — fuer pipeline-korpus: id, category, programmId, schulProfil, userAnswers, facts, expected_forbidden_markers, expected_geber_gruppe, notes; fuer matcher-korpus: id, category, anliegen, schultyp, bundesland, geschaetztesBudgetEur, expected_top3, expected_off_target, expected_clarification, expected_missing_slots, notes. Die UAT-Session-Inputs (Anliegen, Antworten) anonymisiert uebernehmen — Schul-Code statt Klarname, ASCII in JSON-Datenfeldern (CLAUDE.md). Das `notes`-Feld referenziert den Tracker und die Bug-ID.
    Verifiziere ROT: Fuehre fuer die neuen pipeline-korpus-Eintraege `scripts/eval-pipeline.ts --live --single <pv-uat-id>` aus — der Lauf MUSS fehlschlagen / den Bug zeigen (sonst reproduziert der Eintrag den Bug nicht — Eintrag korrigieren). Fuer matcher-korpus-Eintraege analog mit eval-matcher. Dokumentiere den roten Lauf-Output je Eintrag.
    Niedrig-Bugs (D-02): bekommen ebenfalls einen Korpus-Eintrag falls Pipeline/Matcher-Typ, werden aber in Task 2 ggf. nur dokumentiert (nicht gefixt) — der rote Test-Case bleibt als v2-Regressions-Anker bestehen.
    Sprache: deutsche notes mit ASCII in JSON; Umlaute in JSON-Datenfeldern vermeiden.
  </action>
  <verify>
    <automated>grep -q "pv-uat-" data/eval/pipeline-korpus.json || grep -q "ev-uat-" data/eval/matcher-korpus.json; node -e "JSON.parse(require('fs').readFileSync('data/eval/pipeline-korpus.json','utf8')); JSON.parse(require('fs').readFileSync('data/eval/matcher-korpus.json','utf8'))"</automated>
    <human-check>Mindestens ein neuer pv-uat-/ev-uat-Eintrag pro Pipeline-/Matcher-Bug existiert; der rote Subset-Lauf wurde dokumentiert und zeigt den Bug.</human-check>
  </verify>
  <acceptance_criteria>
    - pipeline-korpus.json und matcher-korpus.json sind beide valides JSON (node JSON.parse ohne Fehler)
    - Jeder Pipeline-/Halluzinations-/Finanzplan-Bug aus den Trackern hat einen pv-uat-NNN-Eintrag; jeder Matcher-/Begruendungs-/Klaerungs-Bug einen ev-uat-NNN-Eintrag
    - Neue Eintraege folgen exakt dem Schema aus 06-PATTERNS.md (alle Pflichtfelder vorhanden)
    - Der rote Subset-Lauf (`eval-pipeline.ts --single`) wurde pro neuem Pipeline-Eintrag ausgefuehrt und zeigt den Bug (dokumentierter FAIL-Output)
    - JSON-Datenfelder enthalten keine Umlaute (ASCII); notes referenzieren Tracker + Bug-ID
  </acceptance_criteria>
  <done>Alle UAT-Bugs sind aggregiert und nach Typ geroutet; jeder Pipeline-/Matcher-Bug hat einen rot-laufenden Korpus-Test-Case.</done>
</task>

<task type="auto">
  <name>Task 2: Konsolidierte Fix-Welle — ein atomarer Commit pro Bug-ID (D-02, D-03, D-04, D-06)</name>
  <files>lib/wizard/pipeline.ts, lib/wizard/matcher.ts, lib/wizard/prompts.ts</files>
  <read_first>
    - alle .planning/uat/UAT-BEFUNDE-*PILOT-*.md (Bug-Liste mit Fix-Richtung pro Bug)
    - data/eval/pipeline-korpus.json + data/eval/matcher-korpus.json (die pv-uat-/ev-uat-Eintraege aus Task 1)
    - lib/wizard/pipeline.ts (7-Stage-Pipeline — betroffene Stage je nach Bug)
    - lib/wizard/matcher.ts (Tagged-Union MatchResult, CLARIFY-Dispatch — bei Matcher-Bugs)
    - lib/wizard/prompts.ts (System-Prompt-Konstanten — bei Halluzinations-/Tonalitaets-Bugs)
    - .planning/codebase/TESTING.md (Playwright-Smoke-Konvention fuer UI-Fix-Verifikation D-04)
    - .planning/phases/06-live-uat-mit-pilot-schulen/06-CONTEXT.md (D-02 Scope, D-03/D-04 Verifikation, D-06 Commit, D-16 Eskalation)
  </read_first>
  <action>
    Fixe die UAT-Bugs gemaess Fix-Scope D-02: Hoch- und Mittel-Bugs werden gefixt + verifiziert; Niedrig-Bugs duerfen mit dokumentierter Begruendung auf v2 (Vermerk im Tracker + spaeter im Retro Plan 06-04) — nie stillschweigend ausgelassen.
    Pro Bug ein atomarer Commit auf feature/wizard-adaptive (D-05/D-06): Subject `fix(uat): Bug #N <kurze Beschreibung>`, Body mit `Befunde-Tracker:`- und `Eval-Anker:`-Zeile. Die konkrete Codeaenderung folgt der `Fix-Richtung`-Angabe im jeweiligen Tracker — typische Targets je nach Bug: lib/wizard/pipeline.ts (Stage-Logik), lib/wizard/matcher.ts (Begruendung/Klaerung), lib/wizard/prompts.ts (Halluzinations-Verbots-Listen, Tonalitaet). UI-Bugs treffen das Frontend unter app/antrag/ — falls UI-Dateien betroffen sind, im Commit erfassen (files_modified-Liste ist Pipeline-zentriert, UI-Pfade nach Befund ergaenzen).
    Verifikation pro Bug:
    - Pipeline-/Matcher-Bug (D-03): DOPPELTER Nachweis — (a) der zugehoerige pv-uat-/ev-uat-Korpus-Eintrag laeuft nach dem Fix gruen (`eval-pipeline.ts --live --single <id>` bzw. eval-matcher) UND (b) ein bug-spezifischer Reproducer (`scripts/smoke-pipeline-with-extractor.ts` mit dem Session-Kontext) zeigt den Bug weg. Beide Belege im Tracker-Bug-Status festhalten.
    - UI-/Rendering-Bug (D-04): Playwright-Smoke (headless Chromium, WSL) auf dem betroffenen Critical-Path — Smoke-Skript-Pfad und gruener Output im Tracker dokumentieren.
    Eval-Kosten-Steuerung (D-08): waehrend des Fixens NUR den gezielten Subset laufen lassen (`--single` betroffene Eintraege + neuer UAT-Case). KEIN voller Korpus-Lauf pro Einzel-Bug — der volle Lauf ist Task 3.
    Eskalation (D-16): Sitzt ein Hoch-Bug strukturell zu tief fuer einen vernuenftigen Fix-Aufwand, NICHT still blockieren — Eskalations-Checkpoint ausloesen (siehe Task 3 Checkpoint), Kolja die Optionen vorlegen (Quick-Fix akzeptieren / als v2-Risiko dokumentieren / eigene Folge-Phase).
    Setze pro Bug den Status im jeweiligen Tracker: "gefixt + verifiziert" mit beiden Belegen, oder "v2 — <Begruendung>" (nur Niedrig), oder "Eskalation — siehe Checkpoint".
    Sprache: Deutsch mit Umlauten, conventional Commit-Subjects.
  </action>
  <verify>
    <automated>git log --oneline feature/wizard-adaptive 2>/dev/null | grep -c "fix(uat)" | awk '{exit ($1>0)?0:1}'; npx tsc --noEmit --skipLibCheck lib/wizard/pipeline.ts lib/wizard/matcher.ts lib/wizard/prompts.ts 2>&1 | grep "error TS" | grep -cv '^#' | grep -qx 0</automated>
    <human-check>Jeder Hoch-/Mittel-Bug hat einen fix(uat)-Commit; jeder Pipeline-Fix hat den doppelten Nachweis (gruener Subset-Lauf + Reproducer), jeder UI-Fix einen Playwright-Smoke; Niedrig-v2-Bugs sind im Tracker begruendet.</human-check>
  </verify>
  <acceptance_criteria>
    - Jeder Hoch- und Mittel-Bug aus den Trackern hat genau einen atomaren fix(uat)-Commit auf feature/wizard-adaptive mit Tracker- und Eval-Anker-Body-Zeile
    - Jeder gefixte Pipeline-/Matcher-Bug hat den doppelten Nachweis (gruener pv-uat-/ev-uat-Subset-Lauf + bug-spezifischer Reproducer) im Tracker dokumentiert
    - Jeder gefixte UI-Bug hat einen Playwright-Smoke auf dem betroffenen Critical-Path dokumentiert
    - Niedrig-Bugs sind entweder gefixt oder im Tracker mit expliziter v2-Begruendung markiert (nie stillschweigend offen)
    - lib/wizard-Dateien kompilieren ohne TypeScript-Fehler
    - Kein voller pipeline-korpus-Lauf wurde pro Einzel-Bug ausgefuehrt (D-08 Kostensteuerung)
  </acceptance_criteria>
  <done>Alle Hoch-/Mittel-Bugs gefixt + doppelt verifiziert; Niedrig-Bugs gefixt oder dokumentiert auf v2; unfixbare Hoch-Bugs eskaliert.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Wellen-Gate — voller Korpus-Lauf + Kolja-Review (D-07, D-08, D-16)</name>
  <decision>
    Ist die konsolidierte Fix-Welle abgeschlossen und abnahmebereit, oder muss ein eskalierter Hoch-Bug separat behandelt werden?
  </decision>
  <context>
    Nach Task 2 sind alle Hoch-/Mittel-Bugs gefixt. D-07 verlangt, dass Kolja die gesammelten Fixes einer Welle GEBUENDELT am Wellen-Ende reviewt (ein Checkpoint pro Welle, nicht pro Bug). D-08 verlangt genau EINEN vollen pipeline-korpus-Lauf pro konsolidierter Welle als Gate (~3-4 EUR, ~60-75 Min).
    Ablauf vor diesem Checkpoint (GSD automatisiert):
    1. Vollen Korpus-Lauf ausfuehren: `npx tsx --env-file=.env.local scripts/eval-pipeline.ts --live --N=3 --snapshot --md-summary`. Der Lauf umfasst die Baseline-Eintraege + die neuen pv-uat-Eintraege. Report-Pfad notieren.
    2. Pruefen, dass die WIZ-01/WIZ-02-Achsen nicht unter Baseline-2-sigma gefallen sind (hartes Gate aus BASELINE.md) und dass die neuen pv-uat-Eintraege jetzt gruen laufen (rot-vor-gruen-Nachweis aus Task 1 vs. jetzt).
    3. Eine Wellen-Zusammenfassung erstellen: Liste aller fix(uat)-Commits, Bug-Status (gefixt / v2 / eskaliert), voller-Lauf-Scores vs. Baseline.
  </context>
  <options>
    <option id="welle-abgenommen">
      <name>Welle abgenommen</name>
      <pros>Alle Hoch-/Mittel-Bugs gefixt, voller Korpus-Lauf gruen, keine Regression unter Baseline-2-sigma. Phase kann mit Plan 06-04 (Baseline-Eintrag + Retro) abschliessen.</pros>
      <cons>Keine.</cons>
    </option>
    <option id="weitere-fix-runde">
      <name>Weitere Fix-Runde noetig</name>
      <pros>Der volle Lauf hat eine Regression oder einen nicht weg-verifizierten Bug gezeigt — eine weitere Fix-Iteration in derselben Phase (analog Phase-5-D-23-Muster).</pros>
      <cons>Zusaetzlicher voller Korpus-Lauf als erneutes Gate noetig.</cons>
    </option>
    <option id="hoch-bug-eskalation">
      <name>Hoch-Bug-Eskalation (D-16)</name>
      <pros>Ein Hoch-Bug sitzt strukturell zu tief — Kolja entscheidet: Quick-Fix akzeptieren / als v2-Risiko dokumentieren / eigene Folge-Phase. Phase wird nicht still blockiert.</pros>
      <cons>Verzoegert den Phasen-Abschluss bis zur Entscheidung.</cons>
    </option>
  </options>
  <resume-signal>Auswahl: welle-abgenommen, weitere-fix-runde, oder hoch-bug-eskalation (mit Entscheidung Quick-Fix / v2 / Folge-Phase).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT-Befunde -> Korpus-JSON im git | Anonymisierte Pilot-Inputs werden als Korpus-Eintraege versioniert |
| Eval-Skript -> Live-LLM (DeepSeek) | Voller Korpus-Lauf sendet Test-Inputs an den LLM-Provider |
| Bug-Fix-Commit -> feature/wizard-adaptive | Code-Aenderungen an der produktiven Wizard-Pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | pv-uat-/ev-uat-Eintraege in Korpus-JSON | mitigate | Korpus-Eintraege verwenden Schul-Code statt Klarname, ASCII-Datenfelder; Task-1-Acceptance prueft das. Echte Pilot-Inputs werden anonymisiert uebernommen (D-13). |
| T-06-03-02 | Tampering | Fix ohne rot-vor-gruen-Nachweis | mitigate | D-18: Korpus-Eintrag wird ZUERST geschrieben und muss rot laufen, bevor der Fix kommt — verhindert Schein-Fixes, die den Bug nicht wirklich treffen. |
| T-06-03-03 | Repudiation | unklare Bug-Fix-Zuordnung | mitigate | D-06: ein atomarer Commit pro Bug-ID mit Tracker-Referenz im Body — saubere Reverts/Bisects, klare Audit-Spur. |
| T-06-03-04 | Denial of Service | unbeabsichtigte Pipeline-Regression durch Fix | mitigate | D-08-Wellen-Gate: voller N=3-Korpus-Lauf prueft WIZ-01 hart gegen Baseline-2-sigma vor Wellen-Abnahme. |
| T-06-03-SC | Tampering | npm/pip/cargo installs | accept | Plan installiert keine Packages — nur Code-Fixes an bestehenden lib/wizard-Dateien und Korpus-Pflege. Playwright ist bereits installiert (WSL-konfiguriert). |
</threat_model>

<verification>
- pipeline-korpus.json + matcher-korpus.json sind valides JSON mit pv-uat-/ev-uat-Eintraegen
- Jeder Hoch-/Mittel-Bug hat einen atomaren fix(uat)-Commit
- Pipeline-/Matcher-Fixes: doppelter Nachweis (gruener Subset-Lauf + Reproducer); UI-Fixes: Playwright-Smoke
- Voller pipeline-korpus-Lauf (N=3) als Wellen-Gate gruen, keine WIZ-01-Regression unter Baseline-2-sigma
- Niedrig-Bugs gefixt oder dokumentiert auf v2; unfixbare Hoch-Bugs eskaliert
</verification>

<success_criteria>
- UAT-03 erfuellt: jeder UAT-Bug gefixt + verifiziert ODER explizit als v2 dokumentiert
- D-18 erfuellt: jeder gefixte Pipeline-/Matcher-Bug hat einen rot-vor-gruen Korpus-Test-Case
- D-08 erfuellt: genau ein voller Korpus-Lauf als Wellen-Gate
- D-07 erfuellt: Kolja-Review gebuendelt am Wellen-Ende
</success_criteria>

<output>
Create `.planning/phases/06-live-uat-mit-pilot-schulen/06-03-SUMMARY.md` when done
</output>

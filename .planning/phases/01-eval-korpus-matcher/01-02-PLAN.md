---
phase: 01-eval-korpus-matcher
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - data/eval/matcher-korpus.json
  - data/eval/reports/<ISO>.json
  - data/eval/BASELINE.md
autonomous: false
requirements:
  - MATCH-01
must_haves:
  truths:
    - "Korpus enthaelt 20–30 kuratierte Schul-Anliegen mit Verteilung gemaess D-05 (~30 % vag / ~40 % ausfuehrlich / ~30 % kurz)."
    - "Jeder Korpus-Eintrag hat expected_top3 (Liste von Programm-IDs aus data/foerderprogramme.json) und expected_off_target."
    - "3–5 Edge-Case-Eintraege (~15 %) sind enthalten, davon mindestens 1 mit expected_top3=[]."
    - "Erster Live-Baseline-Run gegen den aktuellen Matcher (HEAD-Commit) ist gelaufen und hat einen versionierten Report-File in data/eval/reports/ produziert."
    - "data/eval/BASELINE.md existiert und enthaelt einen Eintrag fuer den Phase-1-Baseline-Run mit Datum, Korpus-Groesse, Matcher-Commit-SHA, drei Hauptzahlen, Per-Kategorie-Breakdown, Latenz und Kosten."
  artifacts:
    - path: "data/eval/matcher-korpus.json"
      provides: "Vollkorpus 20–30 Eintraege"
      min_lines: 200
    - path: "data/eval/reports/<ISO>.json"
      provides: "Erster Baseline-Report"
      contains: '"aggregate"'
    - path: "data/eval/BASELINE.md"
      provides: "Append-only Baseline-History"
      contains: "## 2026-"
  key_links:
    - from: "data/eval/BASELINE.md"
      to: "data/eval/reports/<ISO>.json"
      via: "Pfad-Referenz im BASELINE-Eintrag"
      pattern: "data/eval/reports/.*\\.json"
    - from: "data/eval/BASELINE.md"
      to: "lib/wizard/matcher.ts (HEAD-Commit-SHA)"
      via: "Commit-SHA-Eintrag"
      pattern: "Matcher-Commit:.*[a-f0-9]{7,40}"
---

<objective>
Korpus auf 20–30 Eintraege hochkuratieren (Claude generiert Entwurf in Task 1a, Kolja kuratiert in Task 1b), Baseline-Run gegen den aktuellen Matcher fahren, Report im Repo versionieren und BASELINE.md mit dem Phase-1-Baseline-Eintrag anlegen — als Vergleichsanker fuer Phase 2.

Purpose: Plan 01 hat das Skript + ein 3-Eintrag-Schema-Stub geliefert. Hier wird der Stub durch ein vollstaendiges, kuratiertes Korpus ersetzt und der erste „echte" Baseline-Run dokumentiert. Damit ist MATCH-01 voll erfuellt und Phase 2 hat eine messbare Vergleichszahl.

Output: vollkuratiertes `data/eval/matcher-korpus.json` (20–30 Eintraege), `data/eval/reports/<ISO>.json` (erster offizieller Baseline-Report), `data/eval/BASELINE.md` (Phase-1-Baseline-Eintrag mit Zahlen + Commit-SHA).
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-eval-korpus-matcher/01-CONTEXT.md
@.planning/phases/01-eval-korpus-matcher/01-PATTERNS.md
@.planning/phases/01-eval-korpus-matcher/01-01-SUMMARY.md
@CLAUDE.md
@data/foerderprogramme.json
@data/richtlinien-prioritaeten.json

<sprache_und_encoding>
<!-- WICHTIG: Differenzierung Umlaute vs. ASCII pro Datei-Typ. -->
<!-- Globale CLAUDE.md verlangt deutsche Umlaute in Dokumenten/Texten/Markdown. -->
<!-- Projekt-CLAUDE.md (Repo-Root, Zeile 19) erlaubt ASCII NUR fuer JSON-Datenfelder, -->
<!-- die durch Tooling laufen — als Encoding-Drift-Schutz. -->

| Datei-Typ                                              | Schreibweise         | Begruendung                                                                          |
|--------------------------------------------------------|----------------------|--------------------------------------------------------------------------------------|
| `data/eval/matcher-korpus.json` (JSON-Datenfelder)     | ASCII (ae/oe/ue/ss)  | Encoding-Drift-Schutz, durchlaeuft `JSON.parse` und LLM-Pipeline                     |
| `data/eval/reports/<ISO>.json`                         | ASCII (ae/oe/ue/ss)  | Maschinenlesbar (vom Skript erzeugt — nicht haendisch nachzuziehen)                  |
| `data/eval/snapshots/<ISO>/<entry>.json`               | ASCII (ae/oe/ue/ss)  | Maschinenlesbar                                                                      |
| `data/eval/BASELINE.md`                                | **Umlaute (ä/ö/ü/ß)**| Markdown-Dokumentation — User-sichtbarer deutscher Text                              |
| `data/eval/reports/<ISO>.md` (--md-summary)            | **Umlaute (ä/ö/ü/ß)**| Markdown-Dokumentation                                                               |
| Konsolen-Output beim Baseline-Run                      | **Umlaute (ä/ö/ü/ß)**| Vom Skript erzeugt, Terminal kann UTF-8                                              |
| Commit-Messages                                        | **Umlaute (ä/ö/ü/ß)**| Deutsche Schreibung                                                                  |
| `01-02-SUMMARY.md`                                     | **Umlaute (ä/ö/ü/ß)**| Markdown-Dokument                                                                    |

Faustregel: **JSON = ASCII, Markdown/Logs/Commits = Umlaute.** Bei Unsicherheit: Umlaute.
</sprache_und_encoding>

<korpus_coverage_target>
<!-- Verteilung aus D-05, D-06, D-07. Diese Tabelle ist die Pflicht-Vorgabe fuer Task 1a. -->

| Anteil | Kategorie | Notiz |
|--------|-----------|-------|
| ~30 % | vag | Phase-2-Klaerungsfrage-Logik braucht hier Haerte. Mindestens 6 von ~22 Nicht-Edge-Eintraegen. |
| ~40 % | ausfuehrlich | Klassischer Goldstandard, klares Anliegen + Schul-Profil. Mindestens 9 Eintraege. |
| ~30 % | kurz | Realistische Schul-Eingabe, knapp aber inhaltlich klar. Mindestens 6 Eintraege. |
| ~15 % | (Edge-Cases) | 3–5 Eintraege total, davon mindestens 1 mit `expected_top3: []` (D-07). Edge-Cases zaehlen zur Kategorie-Verteilung dazu. |

**Programm-Coverage (D-06):**
- Mindestens 1 Eintrag pro Hauptkategorie aus dem Katalog: Digital, Sport, Kultur, MINT, Inklusion, Sprache, Internationales, Klima, Demokratie. (Nicht jede Hauptkategorie muss durch eine eigene Korpus-Zeile abgedeckt sein — eine Zeile darf mehrere Themen abdecken, solange `expected_top3` den thematischen Schwerpunkt spiegelt.)
- 4–5 verschiedene Bundeslaender + bundesweite Programme.

**Off-Target-Pflicht (D-07):** 2–3 Eintraege als thematische Grenzfaelle mit klar definierten `expected_off_target`-IDs (z. B. „Anliegen klingt digital, soll aber NICHT als Inklusions-Programm aufpoppen"). Nicht-Edge-Eintraege haben mindestens einen `expected_off_target`.

**Bekannte verifizierte programmIds aus Plan 01-01** (koennen 1:1 weiterverwendet werden): `aktion-mensch-schulkooperation`, `kultur-macht-stark`, `bmbf-digitalpakt-2`, `berlin-startchancen`. Alle weiteren IDs in Task 1a vor dem Validator-Lauf gegen `data/foerderprogramme.json` pruefen.
</korpus_coverage_target>

<baseline_template>
<!-- Strikte Vorlage fuer den ersten BASELINE.md-Eintrag (D-16). Append-only — Phase 2+ fuegt oben dran. -->
<!-- Verwendet Umlaute (ä/ö/ü/ß) im Fließtext — siehe Encoding-Block oben. -->

```markdown
# Matcher-Eval Baseline

> Append-only History. Phase 2+ fügt neue Einträge oben dran. Skript schreibt
> NICHT in diese Datei — manuelle Pflege per PR.

## YYYY-MM-DD — Phase-1-Baseline (Korpus v1, n=NN)

- **Matcher-Commit:** `<7-stellige-SHA>` (HEAD von feature/wizard-adaptive zum Run-Zeitpunkt)
- **Korpus-Version:** v1, NN Einträge (kurz: K, ausführlich: A, vag: V, davon Edge-Cases: E)
- **Recall@3 (Mittelwert über Nicht-Edge):** 0.XX
- **Off-Target-Rate:** XX %
- **Per-Kategorie:** kurz 0.XX / XX % · ausführlich 0.XX / XX % · vag 0.XX / XX %
- **Edge-Cases (n=E):** L mit Off-Target-Leak, M mit leerer Matcher-Liste
- **Latenz/Eintrag:** X.Xs avg
- **Gesamtkosten:** X.XX ¢
- **Report:** `data/eval/reports/YYYY-MM-DDThh-mm-ss.json`
- **Run-Befehl:** `npx tsx --env-file=.env.local scripts/eval-matcher.ts --md-summary`
```
</baseline_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Claude generiert 17–22 zusaetzliche Korpus-Eintraege (Entwurf)</name>
  <files>data/eval/matcher-korpus.json</files>
  <read_first>
    - .planning/phases/01-eval-korpus-matcher/01-CONTEXT.md (D-01 bis D-08 fuer Schema + Verteilung)
    - .planning/phases/01-eval-korpus-matcher/01-PATTERNS.md (Korpus-Schema-Sektion)
    - .planning/phases/01-eval-korpus-matcher/01-01-SUMMARY.md (Reportstruktur + Stub-Latenz/Kosten + Hand-off-Hinweise)
    - data/eval/matcher-korpus.json (3-Eintrag-Stub aus Plan 01-01 als Vorlage)
    - data/foerderprogramme.json (programmId-Quelle, alle expected_top3/off_target IDs muessen hier existieren)
    - data/richtlinien-prioritaeten.json (82 Programme mit kiAntragGeeignet=true — Hauptquelle fuer Anliegen)
    - data/richtlinien/ (11 fertige Dossiers — natuerliche Top-3-Kandidaten, gute Inspiration fuer realistische Anliegen)
    - ~/.claude/projects/-home-kolja/memory/edufunds-uat-pipeline-befunde-2026-04-28.md (Borsigwalder-UAT-Anliegen als realistische Vorlage, anonymisiert)
  </read_first>
  <action>
    Claude generiert in einem ersten Pass 17–22 zusaetzliche Korpus-Eintraege (zusammen mit den 3 Stub-Eintraegen aus Plan 01-01 entsteht ein 20–25-Eintrag-Korpus, optional bis 30) und schreibt sie ins `data/eval/matcher-korpus.json`-Array. **Dieser Task ist `auto` — keine Human-Interaktion. Der Checkpoint kommt in Task 1b.**

    **Encoding (siehe `<sprache_und_encoding>`):** JSON-Felder verwenden ASCII-Substitution (ae/oe/ue/ss), keine Umlaute in JSON-Werten.

    **Generations-Vorgehen:**
    1. Pruefe das Stub-Korpus aus Plan 01-01: welche Kategorien und programmIds sind schon abgedeckt?
    2. Berechne Soll-Verteilung gemaess `<korpus_coverage_target>`-Tabelle: bei n=22 z. B. 7 vag + 9 ausfuehrlich + 6 kurz, davon 3–4 Edge-Cases.
    3. Generiere Eintraege pro Hauptkategorie aus dem Katalog (Digital, Sport, Kultur, MINT, Inklusion, Sprache, Internationales, Klima, Demokratie) mit ASCII-Substitution.
    4. Verteile Bundeslaender (mindestens BE, BW, NW, BY, NI + 2-3 ohne BL fuer thematischen Kern-Test).
    5. Mindestens 2 Edge-Cases mit `expected_top3: []` (vag-Anliegen oder thematisch unvereinbar mit Katalog).
    6. 2–3 Off-Target-Grenzfaelle, in denen ein nahliegendes Programm bewusst NICHT in den Top-3 erwartet wird (z. B. „Inklusionsprojekt mit Tablets" → erwartete Top-3 in Inklusions-Programmen, Off-Target = DigitalPakt).
    7. **Pflicht-Validierung VOR Task-Ende:** alle programmIds in `expected_top3`/`expected_off_target` muessen in `data/foerderprogramme.json` existieren. Verifiziere mit dem Validator-Snippet aus `<verify>` — bei Fehlschlag ungueltige IDs korrigieren BEVOR der Task als done markiert wird. **Tipp:** Vor Auswahl jeder neuen ID einmal `grep -oE '"id": "[a-z0-9-]+"' data/foerderprogramme.json | sort -u` aufrufen oder die Liste cachen — verhindert Halluzinations-IDs wie das in Plan 01-01 Erstplanung passierte (`aktion-mensch` statt `aktion-mensch-schulkooperation`).

    Sobald die n=20–30 Eintraege im JSON-Array stehen und der Validator gruen ist, ist Task 1a fertig — Task 1b (Kolja-Kuration) folgt.
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && node -e "const a=require('./data/eval/matcher-korpus.json'); const p=require('./data/foerderprogramme.json'); const v=new Set(p.map(x=>x.id)); if(a.length<20||a.length>30) throw new Error('count='+a.length+' not in [20,30]'); const cats={kurz:0,ausfuehrlich:0,vag:0}; let emptyTop3=0; const blSet=new Set(); for(const e of a){ if(!cats.hasOwnProperty(e.category)) throw new Error('bad cat '+e.id); cats[e.category]++; if(!Array.isArray(e.expected_top3)) throw new Error('top3 not array '+e.id); if(!Array.isArray(e.expected_off_target)) throw new Error('off_target not array '+e.id); if(e.expected_top3.length===0) emptyTop3++; if(e.bundesland) blSet.add(e.bundesland); for(const id of [...e.expected_top3,...e.expected_off_target]) if(!v.has(id)) throw new Error('unknown programmId \"'+id+'\" in '+e.id+' — pruefe gegen data/foerderprogramme.json'); } if(cats.vag<6||cats.ausfuehrlich<9||cats.kurz<6) throw new Error('cat distribution off: '+JSON.stringify(cats)); if(emptyTop3<1) throw new Error('need >=1 entry with expected_top3=[]'); if(blSet.size<4) throw new Error('need >=4 distinct bundeslaender, got '+blSet.size); console.log('OK n='+a.length+' cats='+JSON.stringify(cats)+' empty_top3='+emptyTop3+' bundeslaender='+blSet.size);"</automated>
  </verify>
  <acceptance_criteria>
    - `data/eval/matcher-korpus.json` enthaelt zwischen 20 und 30 Eintraege.
    - Verteilung: mindestens 6 `vag`, mindestens 9 `ausfuehrlich`, mindestens 6 `kurz`.
    - Mindestens 1 Eintrag hat `expected_top3.length === 0` (Edge-Case D-07).
    - Mindestens 4 verschiedene Bundeslaender + Eintraege ohne `bundesland`-Feld.
    - **Alle** programmIds in `expected_top3` und `expected_off_target` existieren in `data/foerderprogramme.json` (Validator-Snippet aus `<verify>` exit 0).
  </acceptance_criteria>
  <done>Korpus-Datei ist auf 20–30 Eintraege erweitert, alle programmIds sind valide. Task 1b (Kolja-Kuration) folgt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1b: Kolja kuratiert das Korpus (Checkpoint)</name>
  <files>data/eval/matcher-korpus.json</files>
  <depends_on>Task 1a</depends_on>
  <what-built>
    Claude hat in Task 1a 17–22 zusaetzliche Korpus-Eintraege generiert (zusammen mit dem Plan-01-01-Stub: 20–30 total) und in `data/eval/matcher-korpus.json` geschrieben. Schema und Validierung wurden eingehalten — der Validator-Snippet aus Task 1a `<verify>` exit 0. Kolja kuratiert nun manuell.
  </what-built>
  <how-to-verify>
    1. Lies `data/eval/matcher-korpus.json` und prüfe die Verteilung. Empfohlene Diff-Anzeige: `git diff data/eval/matcher-korpus.json | head -300`.
    2. Prüfe stichprobenartig 3–5 Einträge auf:
       - **Anliegen-Realismus:** Klingt das nach einer echten Schul-Eingabe oder zu KI-glatt?
       - **expected_top3:** Sind das die Programme, die DU als Antrag-Coach im Top-3 erwartest? Falls nicht, korrigiere (Eintrag editieren oder löschen).
       - **expected_off_target:** Ist das Off-Target-Programm wirklich klar falsch oder Borderline? Borderline-Fälle besser entfernen.
       - **notes:** Erklärt die Notiz, warum der Eintrag im Korpus drin ist?
    3. Prüfe Edge-Cases: mindestens 1 Eintrag mit `expected_top3: []` und Notiz „Edge-Case".
    4. Optional: Editiere einzelne Einträge direkt im JSON (Schul-Profil-Felder ergänzen, Anliegen umformulieren, IDs austauschen).
    5. Wenn alles passt: Antworte mit „freigegeben" oder editiere bis es passt und antworte dann.
    6. Falls Claude noch zu wenige/zu viele Einträge generiert hat, sage z. B. „bitte 3 weitere ausführliche Einträge für Sport und MINT" oder „lösche ev-018 und ev-019" — Claude editiert die JSON-Datei und du verifizierst erneut.
    7. Nach jeder Edit-Runde wird der Validator aus Task 1a erneut ausgeführt — stelle sicher, dass alle programmIds weiterhin valide sind.
  </how-to-verify>
  <resume-signal>Antworte mit „freigegeben" um den Baseline-Run in Task 2 zu starten, oder gib konkrete Edit-Anweisungen zurück.</resume-signal>
  <verify>
    <automated>cd /home/kolja/edufunds-app && node -e "const a=require('./data/eval/matcher-korpus.json'); const p=require('./data/foerderprogramme.json'); const v=new Set(p.map(x=>x.id)); if(a.length<20||a.length>30) throw new Error('count='+a.length+' not in [20,30]'); const cats={kurz:0,ausfuehrlich:0,vag:0}; let emptyTop3=0; for(const e of a){ if(!cats.hasOwnProperty(e.category)) throw new Error('bad cat '+e.id); cats[e.category]++; if(e.expected_top3.length===0) emptyTop3++; for(const id of [...e.expected_top3,...e.expected_off_target]) if(!v.has(id)) throw new Error('unknown programmId '+id+' in '+e.id);} if(emptyTop3<1) throw new Error('need >=1 entry with expected_top3=[]'); console.log('OK post-curation n='+a.length+' cats='+JSON.stringify(cats));"</automated>
  </verify>
  <acceptance_criteria>
    - Korpus hat nach Kolja-Kuration weiterhin 20–30 Eintraege, Verteilung gemaess `<korpus_coverage_target>`.
    - Alle programmIds nach Edits weiterhin valide (Validator-Snippet aus `<verify>` exit 0).
    - Mindestens 1 Eintrag mit `expected_top3 === []`.
    - Kolja hat den Checkpoint mit „freigegeben" abgeschlossen.
  </acceptance_criteria>
  <done>Korpus ist Kolja-freigegeben, Validator weiterhin gruen.</done>
</task>

<task type="auto">
  <name>Task 2: Baseline-Run + Report-Commit + BASELINE.md anlegen</name>
  <files>data/eval/reports/&lt;ISO&gt;.json, data/eval/reports/&lt;ISO&gt;.md, data/eval/BASELINE.md</files>
  <depends_on>Task 1b</depends_on>
  <read_first>
    - .planning/phases/01-eval-korpus-matcher/01-CONTEXT.md (D-12 Hauptzahlen, D-15 Output-Triple, D-16 BASELINE.md-Konvention)
    - .planning/phases/01-eval-korpus-matcher/01-PATTERNS.md (Sektion „data/eval/BASELINE.md")
    - scripts/eval-matcher.ts (Eval-CLI aus Plan 01-01)
    - data/eval/matcher-korpus.json (kuratiertes Korpus aus Task 1b dieses Plans)
  </read_first>
  <action>
    1. **HEAD-Commit-SHA festhalten** (vor dem Run, damit der Baseline-Eintrag exakt zuordenbar ist):
    ```bash
    cd /home/kolja/edufunds-app && git rev-parse --short HEAD
    ```
    Notiere den 7-stelligen SHA — dieser geht in BASELINE.md als „Matcher-Commit".

    2. **Live-Baseline-Run** mit Snapshot + MD-Summary:
    ```bash
    npx tsx --env-file=.env.local scripts/eval-matcher.ts --snapshot --md-summary
    ```
    - Erwartete Wallclock: ~60s bei 30 Eintraegen, ~3 Cent Gesamt-Kosten (siehe Hochrechnung in `01-01-SUMMARY.md`).
    - Skript validiert Korpus zuerst, dann sequenzielle runMatch-Calls, dann Konsolen-Bericht + JSON + MD + Snapshots.
    - Notiere das ISO-Datum des Reports (steht im Konsolen-Output-Pfad).

    3. **„Latest Report" robust auflösen** (nach Modification-Time, nicht alphabetisch — schützt gegen Verwechslung mit Stub-Reports aus Plan 01-01):
    ```bash
    LATEST_REPORT=$(ls -t data/eval/reports/*.json | head -1)
    echo "Baseline-Report: $LATEST_REPORT"
    ```

    4. **Report committen** (das ist die offizielle Phase-1-Baseline):
    ```bash
    git add data/eval/reports/ data/eval/snapshots/
    ```
    Werden im Commit-Step zusammen mit BASELINE.md committed.

    5. **`data/eval/BASELINE.md` anlegen** mit dem Eintrag aus dem `<baseline_template>`-Block oben. **Markdown mit Umlauten** (siehe `<sprache_und_encoding>`-Block — keine ASCII-Substitution in BASELINE.md). Datei beginnt mit Top-Level-`# Matcher-Eval Baseline`-Header + Append-only-Hinweis, danach der Phase-1-Eintrag. Werte aus `$LATEST_REPORT` extrahieren via `jq`:
       - Datum (Run-Datum, ISO yyyy-mm-dd)
       - Korpus-Größe + Verteilung (`jq '.aggregate.perCategory' "$LATEST_REPORT"`)
       - Matcher-Commit-SHA aus Schritt 1
       - Recall@3-Mittelwert (`jq '.aggregate.recallAtThreeMean' "$LATEST_REPORT"`)
       - Off-Target-Rate (`jq '.aggregate.offTargetRate' "$LATEST_REPORT"` × 100, in Prozent)
       - Per-Kategorie-Breakdown
       - Edge-Case-Stats (`.aggregate.edgeCaseLeakHits`, `.edgeCaseEmptyTopK`, `.nEdge`)
       - Latenz-Mittelwert (`.aggregate.latencyMsMean`, formatiert in Sekunden)
       - Gesamtkosten (`.aggregate.totalEurCents`)
       - Report-Pfad: relativ zum Repo-Root (`data/eval/reports/<ISO>.json`)
       - Run-Befehl: 1:1 wie in Schritt 2

    6. **Pflichtcheck:** Datei ist Markdown-valide, hat genau einen `## YYYY-MM-DD — Phase-1-Baseline`-Block, Pfad-Referenz auf den Report-File ist vorhanden.

    7. **Commit:** Conventional Commit mit Umlauten: `feat(phase-01): erste matcher-eval-baseline`, Body mit drei Hauptzahlen + Korpus-Größe + Commit-SHA.

    **Discretion:** Wenn der Live-Run fehlschlaegt (z. B. DeepSeek-Quota, Netz-Hick-up), per Soft-Failure-Pattern wird das Skript trotzdem zu Ende laufen und einen Report mit `error`-Eintraegen schreiben. In dem Fall: Report committen, BASELINE.md mit Hinweis „X Eintraege errored, weiter ausgewertet auf Nicht-Errored" anlegen, oder Run nach Stunde wiederholen. Phase 1 hat kein Threshold-Gate (D-17), das Skript exit 0 bei Soft-Failures.
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && LATEST_REPORT=$(ls -t data/eval/reports/*.json 2>/dev/null | head -1) && [ -n "$LATEST_REPORT" ] && jq -e '.aggregate.recallAtThreeMean and .aggregate.offTargetRate and .aggregate.perCategory.kurz and .aggregate.perCategory.ausfuehrlich and .aggregate.perCategory.vag' "$LATEST_REPORT" >/dev/null && PER_ENTRY_LEN=$(jq '.perEntry | length' "$LATEST_REPORT") && KORPUS_LEN=$(jq 'length' data/eval/matcher-korpus.json) && [ "$PER_ENTRY_LEN" = "$KORPUS_LEN" ] || (echo "FAIL: perEntry-Laenge=$PER_ENTRY_LEN != Korpus-Laenge=$KORPUS_LEN — moeglicherweise wurde der Stub-Report aus Plan 01-01 statt des Baseline-Reports geprueft" && exit 1) && [ -f data/eval/BASELINE.md ] && grep -q "Matcher-Commit" data/eval/BASELINE.md && grep -q "Recall@3" data/eval/BASELINE.md && grep -qE "data/eval/reports/[0-9]{4}-" data/eval/BASELINE.md && echo "OK: report perEntry=$PER_ENTRY_LEN matches korpus=$KORPUS_LEN, BASELINE.md present with commit-SHA + report-link"</automated>
  </verify>
  <acceptance_criteria>
    - Mindestens ein neuer Report-File in `data/eval/reports/` (Datei nach dem ISO-Pattern `YYYY-MM-DDTHH-MM-SS.json`).
    - **Latester Report** (per Modification-Time, nicht alphabetisch) hat unter `.perEntry` ein Array mit `length === <korpus-groesse>` (entspricht 20–30, NICHT 3 — das schließt den Stub-Report aus Plan 01-01 als Verwechslung aus).
    - Report-File enthaelt unter `.aggregate` die Felder `recallAtThreeMean`, `offTargetRate`, `perCategory.{kurz,ausfuehrlich,vag}` mit `recallMean` + `offTargetRate` + `n`, `latencyMsMean`, `totalEurCents`, `nEdge`, `edgeCaseLeakHits`, `edgeCaseEmptyTopK`.
    - `data/eval/BASELINE.md` existiert und enthaelt:
      - Top-Level-Header `# Matcher-Eval Baseline`.
      - Genau einen `## YYYY-MM-DD — Phase-1-Baseline`-Block.
      - Eine Zeile, die mit `**Matcher-Commit:**` beginnt und einen 7-stelligen Hex-SHA enthaelt.
      - Eine Zeile mit `Recall@3` und einer Zahl.
      - Eine Pfad-Referenz auf den Report-File (`data/eval/reports/[0-9]{4}-...`).
      - **Umlaute (ä/ö/ü/ß) im Fließtext** — siehe Encoding-Block.
    - Conventional Commit `feat(phase-01): ...` mit Korpus + Reports + Snapshots + BASELINE.md committed.
  </acceptance_criteria>
  <done>Baseline-Run ist gelaufen, Report ist in `data/eval/reports/` versioniert (perEntry-Laenge stimmt mit Korpus-Laenge ueberein), `data/eval/BASELINE.md` enthaelt den Phase-1-Baseline-Eintrag mit Commit-SHA + drei Hauptzahlen + Pfad-Referenz, alles committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skript → Repo (Reports + BASELINE.md) | Reports sind versioniert; korrupte oder zensierte Reports verschleiern echte Baseline-Werte. |
| Skript → DeepSeek-API (Live-Run) | Live-Lauf konsumiert Token (Geld) und API-Key — versehentlicher Doppellauf darf nicht in Cost-Explosion enden. |
| Korpus-Datei → Public-Repo | Korpus enthaelt synthetische Schul-Anliegen, keine PII, ist aber Teil der Eval-Logik — Tampering verfaelscht alle zukuenftigen Vergleichswerte. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | data/eval/BASELINE.md (Manipulation der Vergleichszahl) | mitigate | BASELINE.md ist git-versioniert + append-only-Konvention (D-16). Jeder Eintrag enthaelt Commit-SHA + Report-File-Pfad — der Report-File ist ebenfalls versioniert und kann gegengelesen werden. Diff zwischen Baseline und Report ist im PR sichtbar. |
| T-01-08 | Information Disclosure | DeepSeek-API-Key in commit-historischen Reports | mitigate | Reports enthalten nur `cost.entries[].model + tokens + usdCents` (siehe pricing.ts:41-46). Keine `Authorization`-Header oder API-Keys. Manuelle Stichprobe vor dem Commit (`grep -i "key\\|token\\|secret" data/eval/reports/<ISO>.json`) als Pflicht-Vorabcheck (Phase-Threat T-01-c aus Discussion). |
| T-01-09 | Repudiation | „Welcher Code-Stand produzierte diese Baseline?" | mitigate | BASELINE.md-Eintrag enthaelt `Matcher-Commit:` mit 7-stelligem SHA, der zum Run-Zeitpunkt HEAD war. Wenn der Matcher zwischen Run und Commit veraendert wird, ist die Zuordnung trotzdem klar (Run-SHA != Commit-SHA des Reports). |
| T-01-10 | Denial of Service | Cost-Explosion durch Doppel-Run | accept | Korpus-Groesse 20–30, Kosten ~3 Cent pro Lauf. Selbst 10 versehentliche Doppel-Laeufe sind unter 50 Cent. Keine Drossel noetig. |
| T-01-11 | Information Disclosure | UAT-Memo-Anliegen aus Borsigwalder als Korpus-Vorlage | mitigate | Falls UAT-Memo als Inspirations-Quelle genutzt wird, anonymisieren (Schulname austauschen, Klassen-Stufen verallgemeinern). PATTERNS.md erwaehnt das explizit als Voraussetzung. |
| T-01-12 | Tampering | Korpus-Manipulation durch externe PRs (Phase-2+) | accept | Branch-Schutz auf `feature/wizard-adaptive` ist Repo-Konvention. Phase 1 Korpus-Erweiterungen passieren via PR-Review mit Kolja. Kein zusaetzlicher Schutz auf File-Ebene noetig. |
| T-01-13 | Tampering | Verwechslung Stub-Report (Plan 01-01) mit Baseline-Report | mitigate | Verify-Snippet in Task 2 vergleicht `perEntry`-Laenge mit `korpus`-Laenge — bei n=3 (Stub) vs. n=20+ (Baseline) bricht der Check ab. „Latest" wird per Modification-Time gewählt, nicht alphabetisch. |
</threat_model>

<verification>
- `data/eval/matcher-korpus.json` hat 20–30 Eintraege, Verteilung passt, alle programmIds valide, Edge-Cases vorhanden.
- Live-Baseline-Run hat exit 0, Report unter `data/eval/reports/<ISO>.json` enthaelt vollstaendige Aggregate-Struktur, **`perEntry`-Array hat exakt Korpus-Laenge** (schliesst Stub-Report aus Plan 01-01 als Verwechslung aus).
- `data/eval/BASELINE.md` existiert (mit deutschen Umlauten), enthaelt einen Phase-1-Baseline-Block mit Commit-SHA + drei Hauptzahlen + Per-Kategorie + Report-Pfad.
- Alle Artefakte (Korpus + Report + Snapshots + BASELINE.md) sind in einem `feat(phase-01)`-Commit auf `feature/wizard-adaptive` committed.
- Spot-Check „kein API-Key im Report": `grep -iE "key|secret|authorization|bearer" data/eval/reports/<ISO>.json` liefert keine Treffer mit echten Credentials.
</verification>

<success_criteria>
1. Korpus-Vollkuration (20–30 Eintraege) ist abgeschlossen — Task 1a (Auto-Generierung) + Task 1b (Kolja-Freigabe).
2. Live-Baseline-Run ist gelaufen, Report mit drei Hauptzahlen + Per-Kategorie-Breakdown + Edge-Case-Metrik existiert in `data/eval/reports/<ISO>.json`, `perEntry`-Laenge stimmt mit Korpus-Laenge ueberein.
3. `data/eval/BASELINE.md` enthaelt den Phase-1-Baseline-Eintrag (Datum + Commit-SHA + drei Hauptzahlen + Pfad-Referenz, mit Umlauten).
4. Alle vier ROADMAP-Phase-1-Erfolgskriterien sind erfuellt: Korpus-File mit 20–30 + expected_top3/off_target / Skript misst Top3-Trefferrate + Off-Target-Rate / strukturierter Report (JSON + Konsole) / Baseline-Score dokumentiert mit Zahl + Datum.
5. MATCH-01 ist abgehakt.
</success_criteria>

<output>
Nach Abschluss: erstelle `.planning/phases/01-eval-korpus-matcher/01-02-SUMMARY.md` (mit Umlauten) mit (1) finaler Korpus-Verteilung (n je Kategorie + Edge-Case-Anzahl), (2) Baseline-Zahlen 1:1 wie in BASELINE.md, (3) Liste der Programme, die im Korpus als `expected_top3` referenziert werden (Coverage-Beweis fuer D-06), (4) Hinweisen fuer Phase 2 — insbesondere wo der Matcher heute am schwaechsten ist (welche Kategorie hat den niedrigsten Recall, wo passieren die meisten Off-Target-Hits) als konkrete Tuning-Hypothesen.
</output>
</content>
</invoke>
---
phase: 01-eval-korpus-matcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/eval/matcher-korpus.json
  - scripts/eval-matcher.ts
  - package.json
  - .planning/phases/01-eval-korpus-matcher/01-01-SUMMARY.md
autonomous: true
requirements:
  - MATCH-01
must_haves:
  truths:
    - "Korpus-Schema ist im Repo definiert (Top-Level-JSON-Array, Felder gemaess D-01)."
    - "Eval-Skript laedt das Korpus, validiert alle programmId-Referenzen gegen data/foerderprogramme.json und bricht bei Fehler mit Exit 2 ab."
    - "Eval-Skript ruft runMatch pro Korpus-Eintrag auf, berechnet Recall@3 + Off-Target-Rate + Edge-Case-Metrik und gibt einen Konsolen-Bericht aus."
    - "Eval-Skript schreibt einen JSON-Bericht nach data/eval/reports/<ISO>.json."
    - "Eval-Skript unterstuetzt --snapshot, --replay <dir> und --md-summary als CLI-Flags."
    - "Skript laeuft erfolgreich gegen einen 3-Eintrag-Korpus-Stub (1 kurz / 1 ausfuehrlich / 1 vag inkl. Edge-Case mit expected_top3=[])."
    - "01-01-SUMMARY.md existiert und dokumentiert Reportstruktur, Latenz/Kosten des Stub-Runs und Hinweise fuer Plan 02."
  artifacts:
    - path: "data/eval/matcher-korpus.json"
      provides: "Korpus-Stub mit 3 Beispiel-Eintraegen, finales Schema"
      contains: '"id": "ev-001"'
    - path: "scripts/eval-matcher.ts"
      provides: "Eval-CLI mit Live-/Snapshot-/Replay-Modus"
      exports: ["main"]
    - path: "package.json"
      provides: "npm-Script-Alias eval:matcher"
      contains: '"eval:matcher"'
    - path: ".planning/phases/01-eval-korpus-matcher/01-01-SUMMARY.md"
      provides: "Hand-off-Dokument fuer Plan 02 — Reportstruktur, Stub-Latenz, Plan-02-Hinweise"
      contains: "Reportstruktur"
  key_links:
    - from: "scripts/eval-matcher.ts"
      to: "lib/wizard/matcher.ts:runMatch"
      via: "import { runMatch } from '../lib/wizard/matcher'"
      pattern: "import.*runMatch.*from.*lib/wizard/matcher"
    - from: "scripts/eval-matcher.ts"
      to: "data/foerderprogramme.json"
      via: "readFile + Set(programme.map(p => p.id)) Validierung"
      pattern: "validIds.*new Set"
    - from: "scripts/eval-matcher.ts"
      to: "lib/wizard/pricing.ts:addUsage,emptyLedger,formatEur"
      via: "import von pricing"
      pattern: "import.*from.*lib/wizard/pricing"
---

<objective>
Schema, Validierung und Eval-Skript fuer das Matcher-Korpus etablieren — read-only gegen `lib/wizard/matcher.ts`, mit allen drei Run-Modi (Live/Snapshot/Replay), Korpus-Stub als Schema-Beweis und ersten 3 Beispiel-Eintraegen. Abschluss mit SUMMARY-Datei als Hand-off an Plan 02.

Purpose: Phase 1 braucht zwei voneinander abhaengige Artefakte — ein versioniertes Korpus und ein Skript, das es auswertet. Dieser Plan liefert das Skript komplett und das Korpus-Schema mit 3 Stub-Eintraegen, sodass Plan 02 nur noch kuratieren muss (Kolja, manuell). Damit ist die technische Grundlage fertig, bevor 20–30 Eintraege handgepflegt werden.

Output: `scripts/eval-matcher.ts` (CLI mit Flags), `data/eval/matcher-korpus.json` (3-Eintrag-Stub mit finalem Schema), `package.json`-Alias `eval:matcher`, `.planning/phases/01-eval-korpus-matcher/01-01-SUMMARY.md`. Smoke-Run mit Stub-Korpus laeuft Live + Snapshot + Replay erfolgreich.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-eval-korpus-matcher/01-CONTEXT.md
@.planning/phases/01-eval-korpus-matcher/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

<sprache_und_encoding>
<!-- WICHTIG: Differenzierung Umlaute vs. ASCII pro Datei-Typ. -->
<!-- Globale CLAUDE.md verlangt deutsche Umlaute in Dokumenten/Texten/Markdown. -->
<!-- Projekt-CLAUDE.md (Repo-Root, Zeile 19) erlaubt ASCII NUR fuer JSON-Datenfelder, -->
<!-- die durch Tooling laufen — als Encoding-Drift-Schutz. -->

| Datei-Typ                                              | Schreibweise         | Begruendung                                                                          |
|--------------------------------------------------------|----------------------|--------------------------------------------------------------------------------------|
| `data/eval/matcher-korpus.json` (JSON-Datenfelder)     | ASCII (ae/oe/ue/ss)  | Encoding-Drift-Schutz, durchlaeuft `JSON.parse` und LLM-Pipeline                     |
| `data/eval/reports/<ISO>.json`                         | ASCII (ae/oe/ue/ss)  | Maschinenlesbar, gleiche Pipeline                                                    |
| `data/eval/snapshots/<ISO>/<entry>.json`               | ASCII (ae/oe/ue/ss)  | Maschinenlesbar                                                                      |
| `package.json` (Skript-Aliase)                         | ASCII                | Standard, keine Texte                                                                |
| Code-Identifier in `scripts/eval-matcher.ts`           | ASCII                | TS-Identifier-Konvention                                                             |
| Konsolen-Logs / `console.log`-Strings im Skript        | **Umlaute (ä/ö/ü/ß)**| User-sichtbarer deutscher Text — Terminal kann UTF-8                                 |
| JSDoc-/Code-Kommentare in `scripts/eval-matcher.ts`    | **Umlaute (ä/ö/ü/ß)**| Doku-Charakter, deutsche Schreibung                                                  |
| `.planning/phases/.../01-01-SUMMARY.md`                | **Umlaute (ä/ö/ü/ß)**| Markdown-Dokument                                                                    |
| Commit-Messages                                        | **Umlaute (ä/ö/ü/ß)**| Deutsche Schreibung                                                                  |

Faustregel: **JSON = ASCII, alles andere = Umlaute.** Bei Unsicherheit: Umlaute.
</sprache_und_encoding>

<interfaces>
<!-- Aus lib/wizard/matcher.ts (kanonische Eval-API). Keine Codebase-Exploration noetig. -->

```typescript
// lib/wizard/matcher.ts (export)
export interface MatchInput {
  anliegen: string;       // Pflicht, mind. 20 Zeichen (sonst wirft runMatch)
  schulname?: string;
  schultyp?: string;
  bundesland?: string;    // Vollname ('Berlin') oder Code ('DE-BE')
  geschaetztesBudgetEur?: number;
}

export interface MatchedProgramm {
  id: string;
  score: number;          // gefiltert auf >= 50, max 3 Treffer (siehe matcher.ts:238)
  begruendung: string;
  programm: Foerderprogramm;
}

export interface MatchResult {
  matches: MatchedProgramm[];   // 0..3 Eintraege
  costs: CostLedger;
  totalCandidates: number;       // nach prefilter
  filteredOut: number;           // wegen Bundesland / harter Kriterien
}

export async function runMatch(input: MatchInput): Promise<MatchResult>;
```

```typescript
// lib/wizard/pricing.ts (relevante Exports)
export interface CostLedger {
  calls: number;
  promptTokens: number;
  candidatesTokens: number;
  totalTokens: number;
  usdCents: number;
  eurCents: number;
  entries: CostLedgerEntry[];
}
export function emptyLedger(): CostLedger;
export function addUsage(ledger: CostLedger, model: string, usage: Usage): CostLedger;
export function formatEur(eurCents: number): string;
```

```typescript
// data/foerderprogramme.json (lib/foerderSchema.ts)
// Validierungs-Hook: alle expected_top3 / expected_off_target IDs muessen in
// programme.map(p => p.id) existieren — Set-Lookup-Pattern aus matcher.ts:233.
```
</interfaces>

<verifizierte_programm_ids>
<!-- Vom Planner verifiziert gegen data/foerderprogramme.json (131 Programme, 30.04.2026). -->
<!-- Diese vier IDs werden im Stub-Korpus verwendet — alle existieren in der Quelldatei. -->

| ID                              | In foerderprogramme.json? | Anmerkung                                            |
|---------------------------------|---------------------------|------------------------------------------------------|
| `aktion-mensch-schulkooperation`| ✓ (verifiziert)           | Vollstaendige ID — `aktion-mensch` allein existiert NICHT |
| `kultur-macht-stark`            | ✓ (verifiziert)           |                                                      |
| `bmbf-digitalpakt-2`            | ✓ (verifiziert)           |                                                      |
| `berlin-startchancen`           | ✓ (verifiziert)           | bundeslandspezifisch (Berlin)                        |

Verifikations-Befehl (zur Reproduktion durch Executor):
```bash
grep -oE '"id": "[a-z0-9-]+"' data/foerderprogramme.json | sort -u | grep -E "(aktion-mensch|kultur-macht-stark|bmbf-digitalpakt-2|berlin-startchancen)"
```
Erwartete Ausgabe (4 Zeilen): `"id": "aktion-mensch-schulkooperation"`, `"id": "berlin-startchancen"`, `"id": "bmbf-digitalpakt-2"`, `"id": "kultur-macht-stark"`.
</verifizierte_programm_ids>

<korpus_schema>
<!-- Final-Schema fuer data/eval/matcher-korpus.json — JSON-Array auf Top-Ebene, kein Wrapper (D-04). -->

```typescript
interface KorpusEntry {
  /** stabile ID 'ev-NNN' (NNN = 3-stellig, fortlaufend). */
  id: string;
  /** Laengen-Kategorie fuer Per-Kategorie-Breakdown (D-05, D-12). */
  category: "kurz" | "ausfuehrlich" | "vag";
  /** Anliegen-Text, mind. 20 Zeichen (Matcher-Vorbedingung). ASCII-Umlaute (ae/oe/ue/ss) in JSON. */
  anliegen: string;
  /** Optionale Schul-Profil-Felder — bei Fehlen testet der Eintrag den thematischen Kern (D-03). */
  schulname?: string;
  schultyp?: string;
  bundesland?: string;
  geschaetztesBudgetEur?: number;
  /** Erwartete Top-3 Programm-IDs aus data/foerderprogramme.json. Reihenfolge egal (D-02, D-09). */
  expected_top3: string[];
  /** Programme, die NICHT in den Top-3 auftauchen sollen. Hart (D-10). */
  expected_off_target: string[];
  /** Kuratoren-Notiz: warum ist dieser Eintrag drin? */
  notes: string;
}
```

Snake-Case bei `expected_top3`/`expected_off_target` ist bewusst (D-01). Restliche camelCase (`schulname`, `schultyp`, `geschaetztesBudgetEur`) folgen `MatchInput`.
</korpus_schema>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Korpus-Stub anlegen mit finalem Schema und 3 Beispiel-Eintraegen</name>
  <files>data/eval/matcher-korpus.json</files>
  <read_first>
    - .planning/phases/01-eval-korpus-matcher/01-CONTEXT.md (Decisions D-01 bis D-08)
    - .planning/phases/01-eval-korpus-matcher/01-PATTERNS.md (Sektion „data/eval/matcher-korpus.json")
    - data/foerderprogramme.json (programmId-Quelle fuer expected_top3/off_target)
    - data/richtlinien-prioritaeten.json (zur Auswahl von Programmen mit kiAntragGeeignet=true fuer realistische Eintraege)
    - lib/wizard/matcher.ts:34-54 (MatchInput-Felder, die wir spiegeln)
  </read_first>
  <action>
    Erzeuge `data/eval/matcher-korpus.json` als JSON-Array auf Top-Ebene (KEIN Wrapper-Objekt, D-04) mit genau 3 Eintraegen, jeweils einer pro Kategorie aus D-05. Die Eintraege sind ein Schema-Stub — Kolja kuratiert in Plan 02 auf 20–30 hoch.

    **Encoding (siehe `<sprache_und_encoding>` oben):** JSON-String-Werte verwenden ASCII-Substitution (ae/oe/ue/ss). Nur in JSON — Konsolen-Logs und SUMMARY-Datei verwenden Umlaute.

    Die folgenden vier programmIds wurden bereits vom Planner gegen `data/foerderprogramme.json` verifiziert (siehe `<verifizierte_programm_ids>`-Block oben) und koennen direkt verwendet werden: `aktion-mensch-schulkooperation`, `kultur-macht-stark`, `bmbf-digitalpakt-2`, `berlin-startchancen`. **Pflicht-Vor-Commit-Verifikation** (zur Absicherung gegen Tippfehler):
    ```bash
    cd /home/kolja/edufunds-app && node -e "const p=require('./data/foerderprogramme.json'); const validIds=new Set(p.map(x=>x.id)); for(const id of ['aktion-mensch-schulkooperation','kultur-macht-stark','bmbf-digitalpakt-2','berlin-startchancen']){ if(!validIds.has(id)){ console.error('FEHLT in foerderprogramme.json:', id); process.exit(1);} } console.log('OK: alle 4 IDs valide');"
    ```
    Falls eine ID `FEHLT`, **brich ab und melde es** — der Korpus darf nicht mit einer ungueltigen ID committed werden.

    Eintrag 1 — Kategorie `ausfuehrlich`, klassischer Goldstandard (per D-01):
    ```json
    {
      "id": "ev-001",
      "category": "ausfuehrlich",
      "anliegen": "Wir wollen an unserer Grundschule einen Schulgarten anlegen — mit Hochbeeten, einer kleinen Werkstatt fuer den Bau und einer Fortbildung fuer Kollegium und Eltern. Geplant sind 12 Hochbeete, ein wetterfester Geraeteschuppen und 3 ganztaegige Workshops. Die Schueler sollen eigenstaendig pflanzen, ernten und kochen.",
      "schulname": "Grundschule am Buschgraben",
      "schultyp": "grundschule",
      "bundesland": "Berlin",
      "geschaetztesBudgetEur": 8000,
      "expected_top3": ["aktion-mensch-schulkooperation", "kultur-macht-stark"],
      "expected_off_target": ["bmbf-digitalpakt-2"],
      "notes": "Klassischer Goldstandard: klares Anliegen, BL-Match auf bundesweite Programme, Digital-Programm als Off-Target weil thematisch falsch."
    }
    ```

    Eintrag 2 — Kategorie `kurz`, realistisch knappe Schul-Eingabe:
    ```json
    {
      "id": "ev-002",
      "category": "kurz",
      "anliegen": "Wir brauchen Tablets und WLAN fuer den Unterricht in Klasse 5 bis 10.",
      "schultyp": "gymnasium",
      "bundesland": "Berlin",
      "expected_top3": ["bmbf-digitalpakt-2", "berlin-startchancen"],
      "expected_off_target": ["aktion-mensch-schulkooperation"],
      "notes": "Kurz, eindeutig digital. Berlin-Startchancen darf rein, weil BL-Match. Aktion-Mensch (Schulkooperation) ist Off-Target (kein Inklusionsfokus erkennbar)."
    }
    ```

    Eintrag 3 — Kategorie `vag` mit Edge-Case `expected_top3=[]` (per D-07):
    ```json
    {
      "id": "ev-003",
      "category": "vag",
      "anliegen": "Wir wollen unsere Schule besser machen und brauchen dafuer Geld. Es gibt viele Ideen, aber nichts Konkretes — vielleicht etwas mit Sport oder Kultur oder so.",
      "expected_top3": [],
      "expected_off_target": ["bmbf-digitalpakt-2", "aktion-mensch-schulkooperation"],
      "notes": "Edge-Case (D-07): bewusst zu vag fuer ein Ranking. Erwartung: Matcher liefert leere Liste oder maximal Score-Threshold-gefilterte Top-1. Keine Top-3-Goldstandard-IDs vorhanden. Wenn der Matcher trotzdem Digital- oder Inklusion-Programm in Top-3 packt, ist das ein Off-Target-Hit."
    }
    ```

    Datei mit `\n` am Ende abschliessen, 2-Space-Pretty-Print (analog `JSON.stringify(arr, null, 2)`).
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && node -e "const a=require('./data/eval/matcher-korpus.json'); const p=require('./data/foerderprogramme.json'); const validIds=new Set(p.map(x=>x.id)); if(!Array.isArray(a)) throw new Error('not array'); if(a.length!==3) throw new Error('expected 3 entries, got '+a.length); const seenCats=new Set(); let edgeCount=0; for(const e of a){ if(!['kurz','ausfuehrlich','vag'].includes(e.category)) throw new Error('bad category '+e.id); seenCats.add(e.category); if(typeof e.anliegen!=='string'||e.anliegen.length<20) throw new Error('anliegen too short '+e.id); if(!Array.isArray(e.expected_top3)) throw new Error('top3 not array '+e.id); if(!Array.isArray(e.expected_off_target)) throw new Error('off_target not array '+e.id); if(e.expected_top3.length===0) edgeCount++; for(const id of [...e.expected_top3, ...e.expected_off_target]){ if(!validIds.has(id)) throw new Error('unknown programmId \"'+id+'\" in '+e.id+' — pruefe gegen data/foerderprogramme.json'); } } if(seenCats.size!==3) throw new Error('expected all 3 categories, got '+[...seenCats].join(',')); if(edgeCount<1) throw new Error('expected >=1 edge case with expected_top3=[]'); console.log('OK: 3 entries, all 3 categories present, '+edgeCount+' edge case(s), all programmIds valid');"</automated>
  </verify>
  <acceptance_criteria>
    - `data/eval/matcher-korpus.json` existiert und ist ein JSON-Array (kein Wrapper-Objekt) mit genau 3 Eintraegen.
    - Jeder Eintrag hat die Pflichtfelder `id`, `category`, `anliegen`, `expected_top3`, `expected_off_target`, `notes`.
    - Mindestens ein Eintrag hat Kategorie `kurz`, einer `ausfuehrlich`, einer `vag`.
    - Mindestens ein Eintrag hat `expected_top3: []` (Edge-Case, D-07).
    - Alle programmId-Werte in `expected_top3` und `expected_off_target` existieren in `data/foerderprogramme.json` (vom Validator-Snippet aus `<verify>` gesichert).
    - `anliegen`-Strings sind ASCII-substituiert (kein direktes ä/ö/ü/ß).
  </acceptance_criteria>
  <done>Korpus-Stub-Datei existiert, Validator-Snippet aus `<verify>` exit 0, alle drei Eintraege folgen dem Schema aus dem `<korpus_schema>`-Block, alle programmIds gegen `data/foerderprogramme.json` verifiziert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/eval-matcher.ts implementieren mit Live/Snapshot/Replay-Modus</name>
  <files>scripts/eval-matcher.ts, package.json</files>
  <read_first>
    - .planning/phases/01-eval-korpus-matcher/01-CONTEXT.md (Decisions D-09 bis D-17)
    - .planning/phases/01-eval-korpus-matcher/01-PATTERNS.md (Pattern-Sektion 1–10 fuer scripts/eval-matcher.ts)
    - scripts/smoke-pipeline-with-extractor.ts (Hauptanalog: Top-Level-async-Aufbau, mkdir+writeFile-Pattern, Konsolen-Sektionen)
    - scripts/smoke-pipeline-models.ts:200-260 (Pattern fuer Aggregations-Tabelle am Ende)
    - scripts/smoke-facts-extractor.ts (Pattern fuer per-entry-Pass/Fail-Checks und Konsolen-Klassifikation)
    - scripts/extract-richtlinie.ts:314-342 (CLI-Flag-Parsing-Pattern via process.argv.slice(2))
    - lib/wizard/matcher.ts:209-252 (runMatch-Signatur — der zu evaluierende Code)
    - lib/wizard/pricing.ts (emptyLedger, addUsage, formatEur fuer Kosten-Aggregation)
    - lib/foerderSchema.ts (Foerderprogramm-Typ fuer Validierungs-Hook)
    - package.json (vorhandene scripts-Sektion fuer den eval:matcher-Alias)
  </read_first>
  <behavior>
    - **Test-Beweis 1 (Live-Run mit Stub):** `npx tsx --env-file=.env.local scripts/eval-matcher.ts` mit dem 3-Eintrag-Stub aus Task 1 laeuft erfolgreich, gibt Konsolen-Sektionen mit den drei Hauptzahlen aus (Recall@3 Mittelwert, Off-Target-Rate, Per-Kategorie-Breakdown), schreibt eine JSON-Datei nach `data/eval/reports/<ISO>.json` und exit 0.
    - **Test-Beweis 2 (Snapshot):** `npx tsx --env-file=.env.local scripts/eval-matcher.ts --snapshot` schreibt zusaetzlich pro Eintrag eine Datei `data/eval/snapshots/<ISO>/ev-001.json` mit `{ input, result }`-Struktur und exit 0.
    - **Test-Beweis 3 (Replay):** `npx tsx --env-file=.env.local scripts/eval-matcher.ts --replay data/eval/snapshots/<ISO>` laeuft OHNE LLM-Calls (verifiziere via $0 in Konsolen-Output: `Gesamtkosten: < 0,01 € (replay-modus, keine LLM-Calls)`), produziert identische Recall-/Off-Target-Zahlen wie der Live-Run und exit 0.
    - **Test-Beweis 4 (Validierungs-Hook):** Schreibe einen Korpus-Eintrag mit ungueltiger programmId temporaer in den Stub, fuehre das Skript aus, erwartet Exit 2 mit `[korpus-validation] Eintrag ev-XXX: expected_top3-ID "fake-id" nicht in foerderprogramme.json`. Mache aenderung danach rueckgaengig (manueller Test, nicht automatisiert).
    - **Test-Beweis 5 (--md-summary):** `--md-summary`-Flag schreibt zusaetzlich `data/eval/reports/<ISO>.md` mit den drei Hauptzahlen als menschenlesbare Markdown-Tabelle (Markdown enthält **Umlaute** — siehe Encoding-Block).
    - **Test-Beweis 6 (Edge-Case-Handling):** ev-003 (`expected_top3: []`) wird aus dem Recall-Mittelwert ausgeschlossen, taucht aber in einer separaten `Edge-Cases (n=1)`-Sektion im Bericht auf (D-09, D-11).
    - **Test-Beweis 7 (Soft-Failure):** Bei Throw eines `runMatch`-Calls wird der Eintrag mit `error: <stringified>` im Report ausgewiesen, das Skript laeuft weiter, exit 0 (D-Pattern aus PATTERNS.md „Soft-Failure pro Korpus-Eintrag").
  </behavior>
  <action>
    Erzeuge `scripts/eval-matcher.ts` mit folgender Struktur (alle Konventionen aus CONVENTIONS.md + PATTERNS.md). Imports relativ (`../lib/...`), nicht `@/` (Skripte umgehen den Next-Build).

    **Encoding-Differenzierung (siehe `<sprache_und_encoding>`-Block oben):**
    - **JSDoc / Code-Kommentare im Skript:** Umlaute (ä/ö/ü/ß) — deutsche Schreibung
    - **`console.log` / `console.warn` / `console.error`-Strings:** Umlaute (ä/ö/ü/ß) — Terminal kann UTF-8
    - **JSON-Werte in `data/eval/reports/<ISO>.json` und Snapshot-Files:** ASCII (ae/oe/ue/ss) — Maschinenlesbar
    - **Markdown-Datei `data/eval/reports/<ISO>.md` (--md-summary):** Umlaute (ä/ö/ü/ß) — Doku
    - **TS-Identifier (Variablen, Funktionen, Typen):** ASCII (sprachneutrale TS-Konvention)

    **1. Datei-Header (Pattern-Sektion 1):**
    JSDoc-Block mit Zweck, Liste aller Flags, expliziter Run-Zeile (Umlaute):
    ```
    /**
     * Eval-Skript für den Programm-Matcher (lib/wizard/matcher.ts).
     * Lädt data/eval/matcher-korpus.json, ruft runMatch pro Eintrag auf,
     * berechnet Recall@3 + Off-Target-Rate + Edge-Case-Metrik, schreibt
     * JSON-Bericht nach data/eval/reports/<ISO>.json.
     *
     * Flags:
     *   (default)        — Live-DeepSeek-Calls, ~3 Cent für 30 Einträge
     *   --snapshot       — speichert Matcher-Output je Eintrag in data/eval/snapshots/<ISO>/
     *   --replay <dir>   — evaluiert gegen gespeicherte Snapshots ohne LLM-Calls
     *   --md-summary     — schreibt zusätzlich data/eval/reports/<ISO>.md
     *
     * Run: `npx tsx --env-file=.env.local scripts/eval-matcher.ts [flags]`
     * Alias: `npm run eval:matcher` (gleicher Effekt, ohne Flags).
     */
    ```

    **2. Imports + Konstanten (Pattern-Sektion 2):**
    ```typescript
    import { mkdir, readFile, writeFile } from "node:fs/promises";
    import { resolve } from "node:path";
    import type { Foerderprogramm } from "../lib/foerderSchema";
    import { type MatchInput, type MatchResult, runMatch } from "../lib/wizard/matcher";
    import { type CostLedger, addUsage, emptyLedger, formatEur } from "../lib/wizard/pricing";

    const REPO = resolve(__dirname, "..");
    const KORPUS_PATH = resolve(REPO, "data/eval/matcher-korpus.json");
    const PROGRAMME_PATH = resolve(REPO, "data/foerderprogramme.json");
    const REPORTS_DIR = resolve(REPO, "data/eval/reports");
    const SNAPSHOTS_DIR_BASE = resolve(REPO, "data/eval/snapshots");
    const LOG_PREFIX = "[eval-matcher]";
    ```

    **3. Interne Typen** (analog Pattern-Sektion 3 — `KorpusEntry`, `EntryResult`, `AggregateMetrics`, siehe Original).

    **4. Flags-Parsing (Pattern-Sektion 3):** `--snapshot`, `--replay <dir>`, `--md-summary`. Bei unbekanntem Flag: `process.exit(2)` mit Nutzungs-Hinweis (Konsolen-String mit Umlauten).

    **5. Validierungs-Hook (Pattern-Sektion 10):** `loadKorpusAndValidate` — alle programmIds aus `expected_top3` + `expected_off_target` muessen in `validIds = new Set(programme.map(p => p.id))` existieren, sonst Exit 2 mit Praefix `[eval-matcher] Eintrag ev-XXX: expected_top3-ID "fake-id" nicht in foerderprogramme.json`. **BEVOR** der erste LLM-Call abgesetzt wird.

    **6. Run-Modi** (Live, SnapshotWrite, Replay) — siehe Pattern-Sektion 5/6.

    **7. Per-Entry-Auswertung:** Recall = `|expected_top3 ∩ matcher_top3| / |expected_top3|`, mit `null` wenn `expected_top3.length === 0`. Off-Target-Hit = `result.matches.some(m => entry.expected_off_target.includes(m.id))`.

    **8. Aggregation:** Mittelwert nur ueber `recall !== null && error === undefined`. Per-Kategorie via `results.filter(r => r.category === c)`. `formatEur(totalEurCents)` fuer Kosten.

    **9. Konsolen-Bericht** (Umlaute! Beispiel mit deutschen Strings):
    ```typescript
    console.log("=".repeat(80));
    console.log("Matcher-Eval — Konsolen-Bericht");
    console.log("=".repeat(80));
    console.log(`Einträge: ${m.n} (Non-Edge: ${m.nNonEdge}, Edge: ${m.nEdge}, Errored: ${m.nErrored})`);
    console.log(`Recall@3 Mittelwert: ${m.recallAtThreeMean.toFixed(3)}`);
    console.log(`Off-Target-Rate:     ${(m.offTargetRate * 100).toFixed(1)} %`);
    console.log("\nPer-Kategorie:");
    for (const cat of ["kurz","ausfuehrlich","vag"] as const) {
      const c = m.perCategory[cat];
      console.log(`  ${cat.padEnd(13)} n=${c.n}  recall ${c.recallMean.toFixed(3)}  off-target ${(c.offTargetRate*100).toFixed(1)} %`);
    }
    console.log(`\nLatenz/Eintrag: ${(m.latencyMsMean/1000).toFixed(2)}s avg`);
    console.log(`Gesamtkosten:   ${flags.replayDir ? "< 0,01 € (replay-modus, keine LLM-Calls)" : formatEur(m.totalEurCents)}`);
    console.log(`\nEdge-Cases (n=${m.nEdge}): ${m.edgeCaseEmptyTopK} mit leerer Matcher-Liste, ${m.edgeCaseLeakHits} mit Off-Target-Leak.`);
    ```

    **10. JSON-Report** (ASCII-konforme Strings — `JSON.stringify` escaped Unicode automatisch, aber wir schreiben Strings ohnehin in ASCII):
    ```typescript
    const isoStamp = new Date().toISOString().slice(0, 19).replace(/[:T]/g, "-");
    await mkdir(REPORTS_DIR, { recursive: true });
    const reportPath = resolve(REPORTS_DIR, `${isoStamp}.json`);
    const report = { meta: { generatedAt: new Date().toISOString(), korpusPath: "data/eval/matcher-korpus.json", korpusSize: korpus.length, mode: flags.replayDir ? "replay" : flags.snapshot ? "live+snapshot" : "live", replayDir: flags.replayDir ?? null, matcherFile: "lib/wizard/matcher.ts" }, aggregate: m, perEntry: results };
    await writeFile(reportPath, JSON.stringify(report, null, 2));
    console.log(`\nJSON-Bericht: ${reportPath}`);
    ```

    **11. Optional --md-summary** (Markdown mit Umlauten):
    ```typescript
    if (flags.mdSummary) {
      const mdPath = reportPath.replace(/\.json$/, ".md");
      const md = [
        `# Matcher-Eval Bericht ${isoStamp}`,
        ``,
        `- Korpus: ${korpus.length} Einträge`,
        `- Modus: ${report.meta.mode}`,
        `- **Recall@3 Mittelwert:** ${m.recallAtThreeMean.toFixed(3)}`,
        `- **Off-Target-Rate:** ${(m.offTargetRate * 100).toFixed(1)} %`,
        ``,
        `## Per-Kategorie`,
        ``,
        `| Kategorie | n | Recall@3 | Off-Target |`,
        `|---|---|---|---|`,
        ...["kurz","ausfuehrlich","vag"].map((cat) => {
          const c = m.perCategory[cat as keyof typeof m.perCategory];
          return `| ${cat} | ${c.n} | ${c.recallMean.toFixed(3)} | ${(c.offTargetRate*100).toFixed(1)} % |`;
        }),
        ``,
      ].join("\n");
      await writeFile(mdPath, md);
      console.log(`Markdown-Summary: ${mdPath}`);
    }
    ```

    **12. main + Exit-Disziplin** (Soft-Failure: try/catch pro Eintrag, weiter mit naechstem; Praefix `[eval-matcher]` in allen Logs).

    **13. package.json — Alias hinzufuegen:** Lies die existierende `scripts`-Sektion in `package.json` (via Edit-Tool) und fuege folgenden Eintrag ein:
    ```
    "eval:matcher": "tsx --env-file=.env.local scripts/eval-matcher.ts"
    ```

    **Discretion-Punkte (per CONTEXT.md):** Konsolen-Tabelle bleibt bei `console.log` + `padEnd`; Snapshot-Sub-Ordner pro Run-Datum; sequenzielle Matcher-Calls; Exit-Code 0 bei Erfolg, 1 bei Crash, 2 bei Validierungs-/CLI-Fehler.
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && (test -f scripts/eval-matcher.ts || (echo "FAIL: scripts/eval-matcher.ts fehlt" && exit 1)) && grep -q "import { runMatch } from" scripts/eval-matcher.ts && grep -q "expected_top3" scripts/eval-matcher.ts && grep -q "Recall@3" scripts/eval-matcher.ts && grep -q "data/eval/reports" scripts/eval-matcher.ts && grep -q '"eval:matcher"' package.json && echo "OK static checks" && rm -f /tmp/eval-smoke.log && npx tsx --env-file=.env.local scripts/eval-matcher.ts >/tmp/eval-smoke.log 2>&1 && grep -q "Recall@3 Mittelwert" /tmp/eval-smoke.log && LATEST_REPORT=$(ls -t data/eval/reports/*.json 2>/dev/null | head -1) && [ -n "$LATEST_REPORT" ] && [ "$(jq '.perEntry | length' "$LATEST_REPORT")" = "3" ] && echo "OK live smoke + report ($LATEST_REPORT, perEntry=3)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/eval-matcher.ts` existiert und enthaelt `import { runMatch } from "../lib/wizard/matcher"` (relativer Import, nicht `@/`).
    - Datei enthaelt JSDoc-Header mit `Run:`-Zeile und Liste aller drei Flags `--snapshot`, `--replay`, `--md-summary`.
    - Datei enthaelt `parseFlags`-Funktion mit Exit-2-Pfad fuer unbekannte Flags.
    - Datei enthaelt `loadKorpusAndValidate` mit Exit-2 BEVOR der erste LLM-Call.
    - Recall- und Off-Target-Logik gemaess D-09/D-10.
    - Datei schreibt JSON-Bericht nach `data/eval/reports/<ISO>.json` mit `meta`, `aggregate`, `perEntry`.
    - `npx tsx --env-file=.env.local scripts/eval-matcher.ts` mit dem 3-Eintrag-Stub-Korpus aus Task 1 laeuft erfolgreich (Exit 0), schreibt Report mit `perEntry`-Laenge 3 und gibt Konsolen-Sektionen mit „Recall@3 Mittelwert", „Off-Target-Rate", „Per-Kategorie:" aus (Live-Smoke ist Teil von `<verify>`).
    - `package.json` enthaelt unter `scripts` einen Eintrag `"eval:matcher"`.
    - Soft-Failure-Pattern: ein einzelner runMatch-Throw wird abgefangen und im Report als `error`-Feld ausgewiesen.
    - Konsolen-Logs verwenden Praefix `[eval-matcher]` (analog `[wizard/facts-extractor]`).
  </acceptance_criteria>
  <done>
    Skript existiert, `<verify>`-Snippet exit 0 (statische Checks + Live-Smoke + Report mit perEntry=3 vorhanden). Manueller Smoke (zusaetzlich): `npx tsx --env-file=.env.local scripts/eval-matcher.ts --snapshot --md-summary` produziert (a) Konsolen-Bericht mit drei Hauptzahlen, (b) `data/eval/reports/<ISO>.json` mit n=3 Eintraegen im perEntry-Array, (c) `data/eval/reports/<ISO>.md` mit der Per-Kategorie-Tabelle, (d) `data/eval/snapshots/<ISO>/ev-001.json`, `ev-002.json`, `ev-003.json`. Folgender Replay-Lauf `... --replay data/eval/snapshots/<ISO>` produziert identische Recall-/Off-Target-Zahlen ohne LLM-Calls.
  </done>
</task>

<task type="auto">
  <name>Task 3: Hand-off-SUMMARY.md fuer Plan 02 schreiben</name>
  <files>.planning/phases/01-eval-korpus-matcher/01-01-SUMMARY.md</files>
  <read_first>
    - Output von Task 2 (Live-Smoke-Run): `data/eval/reports/<juengste-ISO>.json` (Reportstruktur, Latenzen, Kosten)
    - .planning/phases/01-eval-korpus-matcher/01-CONTEXT.md (D-15, D-16 fuer Konventions-Querverweise)
    - .planning/phases/01-eval-korpus-matcher/01-PATTERNS.md (Sektion „data/eval/BASELINE.md")
    - /home/kolja/.claude/get-shit-done/templates/summary.md (Template-Vorlage, falls vorhanden)
  </read_first>
  <action>
    Erzeuge `.planning/phases/01-eval-korpus-matcher/01-01-SUMMARY.md` als Hand-off-Dokument fuer Plan 02. **Sprache: Deutsch mit Umlauten (ä/ö/ü/ß)** — Markdown-Dokument, nicht ASCII (siehe `<sprache_und_encoding>`-Block).

    Mindestinhalt:

    1. **Was geliefert wurde:**
       - `data/eval/matcher-korpus.json` (3-Eintrag-Stub, Schema final)
       - `scripts/eval-matcher.ts` (CLI mit `--snapshot`, `--replay <dir>`, `--md-summary`)
       - `package.json`-Alias `eval:matcher`

    2. **Tatsächliche Reportstruktur** (aus dem Live-Smoke-Run): zeige die JSON-Top-Level-Struktur mit konkreten Beispielwerten aus dem Stub-Run, mindestens:
       ```
       {
         "meta": { "generatedAt", "korpusPath", "korpusSize", "mode", "matcherFile" },
         "aggregate": { "n", "nNonEdge", "nEdge", "recallAtThreeMean", "offTargetRate",
                        "perCategory.{kurz,ausfuehrlich,vag}", "latencyMsMean",
                        "totalEurCents", "edgeCaseLeakHits", "edgeCaseEmptyTopK" },
         "perEntry": [ { "id", "category", "expected_top3", "actual_top3",
                         "recall", "offTargetHit", "latencyMs", "costs" }, ... ]
       }
       ```
       Mit konkreten Werten aus dem Stub-Run (z. B. `recallAtThreeMean: 0.5`, `latencyMsMean: 2100`).

    3. **Gemessene Latenz und Kosten** des 3-Eintrag-Stub-Runs: Wallclock-Zeit, Gesamtkosten in EUR-Cent, Tokens (aus `data/eval/reports/<ISO>.json -> aggregate.totalEurCents` und Konsolen-Output). Diese Zahlen lassen Plan 02 die Hochrechnung auf 20–30 Einträge prüfen.

    4. **Hinweise für Plan 02 (Korpus-Vollkuration + Baseline-Run):**
       - Skript verarbeitet erweiterte Korpora ohne Code-Änderung (Iteriert generisch über das JSON-Array).
       - Wichtige Felder pro Eintrag: `id`, `category`, `anliegen`, `expected_top3`, `expected_off_target` — siehe Schema in 01-01-PLAN.md.
       - Validator bricht VOR dem ersten LLM-Call ab — ungültige IDs sind also zinsfrei.
       - Kosten-Hochrechnung: 3 Stub-Einträge ergeben X Cent → 22 Einträge ≈ Y Cent (lineare Extrapolation aus Stub-Run).
       - **Bekannte Beispiel-IDs im Stub** (vom Plan 01-01 verifiziert): `aktion-mensch-schulkooperation`, `kultur-macht-stark`, `bmbf-digitalpakt-2`, `berlin-startchancen` — Plan 02 darf diese 1:1 weiterverwenden.

    5. **Bekannte Einschränkungen / offene Punkte** (sofern beim Smoke-Run aufgefallen): z. B. „Konsolen-Tabelle ist Plain-`padEnd`, ggf. später cli-table3", „Sequenzielle Calls — Parallelität ggf. in Phase 2".
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && SUMMARY=.planning/phases/01-eval-korpus-matcher/01-01-SUMMARY.md && [ -f "$SUMMARY" ] && grep -q "Reportstruktur" "$SUMMARY" && grep -q "Plan 02" "$SUMMARY" && grep -qE "(Latenz|Kosten|EUR|Cent|ms)" "$SUMMARY" && grep -qE "expected_top3|perEntry|aggregate" "$SUMMARY" && [ "$(wc -l < "$SUMMARY")" -ge 30 ] && echo "OK SUMMARY vorhanden mit Reportstruktur, Plan-02-Hinweisen, Latenz/Kosten, Schema-Hinweisen"</automated>
  </verify>
  <acceptance_criteria>
    - Datei `.planning/phases/01-eval-korpus-matcher/01-01-SUMMARY.md` existiert.
    - Enthält den String "Reportstruktur" und einen JSON-Skelett-Block (mindestens `meta`, `aggregate`, `perEntry`).
    - Enthält explizite Hinweise an Plan 02 (mind. 3 Bullet-Points unter „Hinweise für Plan 02" oder analoger Überschrift).
    - Enthält Latenz- und Kosten-Werte aus dem tatsächlichen Stub-Run (nicht Platzhalter).
    - Verwendet **deutsche Umlaute** (ä/ö/ü/ß) im Fließtext — kein durchgängiges ASCII.
    - Mindestens 30 Zeilen.
  </acceptance_criteria>
  <done>SUMMARY-Datei ist geschrieben, enthält Reportstruktur mit Werten aus dem Live-Smoke, Plan-02-Hand-off-Hinweise und gemessene Stub-Run-Metriken; Verify-Snippet exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → Skript | Korpus-JSON und Programm-Katalog werden geparst; manipulierte Files koennten ungueltige Eval-Aussagen erzeugen. |
| Skript → DeepSeek-API | API-Key wird ueber `--env-file=.env.local` injiziert; Logs/Reports duerfen den Key nicht aufzeichnen. |
| Skript → Repo-Filesystem | Reports werden nach `data/eval/reports/` und Snapshots nach `data/eval/snapshots/` geschrieben — duerfen nicht ausserhalb des Repos schreiben. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | data/eval/matcher-korpus.json | mitigate | Validierungs-Hook in `loadKorpusAndValidate` prueft alle programmIds gegen `data/foerderprogramme.json` BEVOR der erste LLM-Call abgesetzt wird; Exit 2 bei Drift. Verhindert dass eine korrumpierte Korpus-Datei eine falsche Baseline produziert. |
| T-01-02 | Information Disclosure | DeepSeek-API-Key in Stack-Traces / Reports | mitigate | Skript loggt nie `process.env.*` direkt; Soft-Failure-Pattern fangt `runMatch`-Errors ab und persistiert nur `String(err)` als `error`-Feld. Keine `process.env`-Werte oder `Authorization`-Header tauchen in `data/eval/reports/<ISO>.json` auf. |
| T-01-03 | Information Disclosure | data/eval/reports/*.json (Cost-Daten + Anliegen-Texte) | accept | Korpus-Anliegen sind synthetisch + kuratiert (D-08), keine PII. Cost-Daten in EUR-Cent sind unkritisch. Reports duerfen committed werden. |
| T-01-04 | Tampering | scripts/eval-matcher.ts (Path-Traversal via --replay) | mitigate | `--replay <dir>` wird via `resolve(REPO, dir)` aufgeloest und die gelesenen Dateinamen sind hartkodiert `<entry.id>.json` (entry.id stammt aus dem validierten Korpus, keine User-Eingabe). Kein Path-Traversal-Vector. |
| T-01-05 | Denial of Service | Cost-Explosion durch versehentlichen Live-Run mit 100+ Eintraegen | accept | Phase 1 deckt 20–30 Eintraege ab (~3 Cent), Cost-Cap durch Korpus-Groesse und DeepSeek-Quota implizit. Keine zusaetzliche Drossel noetig fuer Phase 1. |
| T-01-06 | Repudiation | Welcher Matcher-Commit produzierte welche Baseline? | mitigate | Plan 02 dokumentiert Matcher-Commit-SHA in BASELINE.md (D-16). Plan 01 selbst ist read-only und produziert keine versionierte Baseline — kein Repudiation-Vector hier. |
</threat_model>

<verification>
- `data/eval/matcher-korpus.json` exists, ist Array, hat 3 Eintraege, alle programmIds sind valide (Validator-Snippet aus Task 1).
- `scripts/eval-matcher.ts` exists und enthaelt `runMatch`-Import + `Recall@3`-Konsolen-Output + `data/eval/reports`-Pfad + `expected_top3`-Logik.
- Live-Smoke (Teil von Task 2 `<verify>`): `npx tsx --env-file=.env.local scripts/eval-matcher.ts` gegen Stub-Korpus exit 0, Report-JSON mit `perEntry`-Laenge 3 vorhanden.
- Replay-Smoke (manuell, durch `<done>` von Task 2 abgedeckt): `--replay data/eval/snapshots/<ISO>` exit 0, identische Zahlen, Konsole zeigt `< 0,01 € (replay-modus, keine LLM-Calls)`.
- `package.json` enthaelt `"eval:matcher"`-Skript-Alias.
- `.planning/phases/01-eval-korpus-matcher/01-01-SUMMARY.md` existiert mit Reportstruktur + Plan-02-Hand-off-Hinweisen + Latenz/Kosten.
</verification>

<success_criteria>
1. Korpus-Stub mit 3 Eintraegen (kurz / ausfuehrlich / vag inkl. Edge-Case) existiert und ist validierbar — alle programmIds gegen `data/foerderprogramme.json` verifiziert.
2. Eval-Skript ist read-only gegen den Matcher (kein Code in `lib/wizard/matcher.ts` veraendert), unterstuetzt Live + Snapshot + Replay + MD-Summary, schreibt JSON-Reports.
3. Validierungs-Hook bricht bei ungueltigen programmId-Referenzen vor dem ersten LLM-Call ab (Exit 2).
4. Soft-Failure-Pattern: ein einzelner runMatch-Fehler bricht den Lauf nicht ab.
5. npm-Alias `eval:matcher` existiert.
6. SUMMARY-Datei dokumentiert die Reportstruktur, gemessene Stub-Latenz/-Kosten und Hinweise fuer Plan 02 — als Hand-off zwischen Wave 1 und Wave 2.
</success_criteria>

<output>
Nach Abschluss: erstelle `.planning/phases/01-eval-korpus-matcher/01-01-SUMMARY.md` (in Task 3 erzwungen via `<verify>`-Gate) mit (1) tatsaechlicher Reportstruktur (Felder + Beispielwerte aus dem Stub-Run), (2) gemessener Latenz/Kosten fuer den 3-Eintrag-Stub, (3) Hinweisen fuer Plan 02 — insbesondere wie das Skript erweiterte Korpora ohne Code-Aenderung verarbeitet.
</output>
</content>
</invoke>
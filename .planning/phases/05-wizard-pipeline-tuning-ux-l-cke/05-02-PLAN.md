---
phase: 05-wizard-pipeline-tuning-ux-l-cke
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - data/eval/pipeline-korpus.json
autonomous: false
requirements: [WIZ-01, WIZ-02, WIZ-03]
tags: [phase-5, wave-1, eval, korpus, kolja-checkpoint, marker-curation]
must_haves:
  truths:
    - "data/eval/pipeline-korpus.json existiert mit 20-25 Top-Level-JSON-Array-Einträgen (D-01)"
    - "Pro Standard-Eintrag (11 Stück) genau 1 Dossier abgedeckt; insgesamt 5-7 Edge-Cases (D-03/D-05)"
    - "Antwort-Qualitäts-Mix exakt 50 % vag / 30 % mittel / 20 % hochwertig (D-04)"
    - "Pro Eintrag: programmId + schulProfil + userAnswers[] (ChatMessage-Sub-Array, mind. 4 Turns) + pre-extrahierte WizardFacts (D-06)"
    - "Pro Eintrag: expected_forbidden_markers[] mit ≥ 3 Markern (Hybrid-Generation: Claude generiert, Kolja reviewt — D-14)"
    - "Pro Eintrag: expected_geber_gruppe ∈ {oeffentlich, stiftung, eu, wirtschaftspreis, verband-uni} (D-10)"
    - "Edge-Case ev-mismatch (D-05 #3) hat programmId aus data/foerderprogramme.json die NICHT in den 11 Dossiers ist"
    - "3 Kolja-Checkpoints explizit durchlaufen (Hybrid-Pattern Phase 1 D-08 + Warning 8 Resolution): #1 nach pv-001 Pattern-Korrektheit, #2 nach pv-002..pv-011 Pattern-Konsistenz, #3 nach Edge-Cases + Reserve + Marker-Tiefen-Kuration"
  artifacts:
    - path: "data/eval/pipeline-korpus.json"
      provides: "Phase-5-Eval-Korpus mit 20-25 Einträgen + Marker + Geber-Cluster"
      contains: '"expected_forbidden_markers"'
      min_lines: 600
  key_links:
    - from: "data/eval/pipeline-korpus.json"
      to: "data/foerderprogramme.json"
      via: "programmId-FK"
      pattern: '"programmId": "[a-z0-9-]+"'
    - from: "data/eval/pipeline-korpus.json"
      to: "data/richtlinien/*.json"
      via: "11 Standard-Einträge je 1 pro Dossier (D-03)"
      pattern: "1 Standard pro Dossier"
---

<objective>
Wave 1 Plan 1 von 2: Pipeline-Eval-Korpus `data/eval/pipeline-korpus.json` kuratieren — 20-25 Einträge im Hybrid-Mix (60 % UAT-Real-Pattern aus 28.04.-Borsigwalder + 40 % synthetisch), pro Eintrag `expected_forbidden_markers[]` + `expected_geber_gruppe` + pre-extrahierte WizardFacts.

Purpose: Phase 5 hat keinen Eval-Anker ohne kuratierten Korpus. UAT-28.04.-Realität (vag-dominante Antworten, 8 dokumentierte Halluzinations-Marker) ist die Stress-Test-Quelle für WIZ-02. Der Korpus ist die Basis für Baseline-N=3-Run und alle Tuning-Wellen.

Output:
- data/eval/pipeline-korpus.json mit 22 Einträgen (11 Standard + 7 Edge-Cases + 4 Reserve)
- 3 Kolja-Checkpoints (Warning 8 Resolution — verhindert dass Pattern-Bug aus pv-001 in alle 22 Einträge fließt):
  1. **Nach pv-001** (Borsigwalder-UAT-Anker): Kolja-Approval **Pattern-Korrektheit** (Goldstandard für alle 21 Folge-Einträge)
  2. **Nach pv-002..pv-011** (10 weitere Standard-Einträge): Kolja-Approval **Pattern-Konsistenz** (jeder Eintrag spiegelt pv-001-Schema)
  3. **Nach pv-edge + pv-res** (7 Edge-Cases + 4 Reserve) + Marker-Tiefen-Kuration: Kolja-Approval **Edge-Case-Plausibilität** + **Marker-Listen-Qualität**

**Granularität:** 22 Einträge, Wallclock-Schätzung Kolja-Checkpoints ~3h.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-PATTERNS.md
@data/eval/matcher-korpus.json
@data/eval/dossier-coverage-baseline.md
@data/foerderprogramme.json
@data/richtlinien/bmbf-digitalpakt-2.json
@data/richtlinien/kultur-macht-stark.json
@data/richtlinien/aktion-mensch-schulkooperation.json
@data/richtlinien/bosch-schulpreis.json
@data/richtlinien/erasmus-schule-2026.json

<interfaces>
<!-- Korpus-Schema aus RESEARCH §Pattern 1 (RESEARCH.md Z.330-380) -->

```typescript
interface PipelineKorpusEntry {
  id: string;                    // "pv-NNN" Pattern (Pipeline-eVal-NNN)
  category: "vag" | "mittel" | "hochwertig";
  edgeCase?: "vag-extrem" | "profil-fehlt" | "programm-mismatch"
           | "widerspruechlich" | "bl-konflikt" | "antworten-zu-lang"
           | "antrag-spruenge";
  programmId: string;            // FK auf data/foerderprogramme.json
  schulProfil: {
    name?: string;
    typ?: string;
    bundesland?: string;
    schuelerzahl?: number;
    besonderheiten?: string;
  };
  userAnswers: Array<{
    role: "ai" | "user";
    kind: "question" | "answer";
    content: string;
  }>;
  facts: WizardFacts;            // siehe lib/wizard/types.ts
  expected_forbidden_markers: Array<{
    marker: string;
    description: string;
  }>;
  expected_geber_gruppe: "oeffentlich" | "stiftung" | "eu"
                      | "wirtschaftspreis" | "verband-uni";
  notes?: string;
}
```

From data/eval/matcher-korpus.json (Schema-Vorbild Top-Level-Array, Phase-1-Konvention):
- Datei ist `[ {entry1}, {entry2}, ... ]` (kein Wrapper-Object)
- Eintrag-IDs: `ev-NNN` (Matcher) — Phase 5 nutzt `pv-NNN` (Pipeline-eVal)

From lib/wizard/types.ts WizardFacts:
- `schule: { name?: string, typ?: string, bundesland?: string, schuelerzahl?: number, ... }`
- `vorhaben: { titel?: string, kurzbeschreibung?: string, zielgruppe?: string, ... }`
- `partner: Array<{ name: string, rolle: string }>`
- `eigenmittel?: { betragEUR?: number, anteilProzent?: number }`
- `personal?: Array<{ rolle: string, stundenWoche?: number }>`
- Vollständiger Type in lib/wizard/types.ts
</interfaces>

<uat_reference>
UAT-28.04. Borsigwalder-Reproducer (Pflicht-Read für Korpus-Kuratoren):
- Memo: `~/.claude/projects/-home-kolja/memory/edufunds-uat-pipeline-befunde-2026-04-28.md`
- 12 vag-Antworten von Borsigwalder-Sekretärin (z.B. "Hatten wir das nicht schon mal? Glaub ich nicht."), produzierten:
  - "Az 123/2026" (erfundenes Aktenzeichen)
  - "TV-L E9" (erfundene Tarif-Eingruppierung)
  - "Bezirk Berlin-Mitte" (Borsigwalder ist Reinickendorf, nicht Mitte)
  - "380 Schüler" (User sagte "weiß nicht so genau")
  - "Willkommensklassen" (User sagte "haben wir glaub ich nicht")
  - "Beschluss vom 12.12.2025" (User: "kein Beschluss")
  - "Haushaltsstelle 1234/56789" (Phantasie)
  - "KMK-Kompetenzen aktiv adressiert" (User: "kenne KMK nicht")
- Diese 8 Marker sind direkt copy-paste in `pv-001` (= UAT-Anker-Eintrag) `expected_forbidden_markers[]`
</uat_reference>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: pv-001 Borsigwalder-UAT-Anker generieren + Kolja-Checkpoint Pattern-Korrektheit (D-02/-06, D-14 Hybrid)</name>
  <files>data/eval/pipeline-korpus.json</files>
  <read_first>
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md (D-01..D-07 Korpus-Strategie)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md (§Pattern 1 Korpus-JSON-Schema, §UAT-Kontext Z.179-182)
    - ~/.claude/projects/-home-kolja/memory/edufunds-uat-pipeline-befunde-2026-04-28.md (Borsigwalder-Reproducer, 12 vag-Antworten + 8 Marker als Anker für `pv-001`)
    - data/eval/matcher-korpus.json (Phase-1-Schema-Vorbild, Top-Level-Array)
    - data/foerderprogramme.json (programmId-Lookup, Programme ohne Dossier für Edge-Case `programm-mismatch`)
    - data/richtlinien/ — alle 11 Dossier-JSONs (1 Standard-Eintrag pro Dossier laut D-03)
    - data/eval/dossier-coverage-baseline.md (welche Dossiers wie viele Pflicht-Abschnitte haben → Antwort-Qualität anpassen)
  </read_first>
  <what-built>
    **1 Korpus-Eintrag generiert (`pv-001` Borsigwalder-UAT-Anker)** in `data/eval/pipeline-korpus.json` als Top-Level-JSON-Array — **erster Eintrag von 22**:

    - `pv-001`: bmbf-digitalpakt-2 + Borsigwalder-UAT-Reproducer
    - **category:** vag (per D-04 vag-Anker)
    - **userAnswers[]:** mindestens 12 UAT-Antworten copy-paste aus UAT-Memo (z.B. „Hatten wir das nicht schon mal? Glaub ich nicht."), Role-Alternation `ai → user → ai → user`
    - **facts:** pre-extrahiert via `lib/wizard/facts-extractor.ts` (oder manuell analog UAT-Memo) — vage Felder bewusst unausgefüllt
    - **expected_forbidden_markers[]:** die 8 dokumentierten UAT-28.04.-Marker (Az 123/2026, TV-L E9, Bezirk Berlin-Mitte, 380 Schüler, Willkommensklassen, Beschluss vom 12.12.2025, Haushaltsstelle 1234/56789, KMK-Kompetenzen aktiv adressiert)
    - **expected_geber_gruppe:** `oeffentlich` (bmbf-* → öffentlich per D-28-Default)

    **Warum dieser Schritt isoliert ist (Warning 8 Resolution):** pv-001 ist das Goldstandard-Pattern für alle 21 Folge-Einträge (Schema, Marker-Form, Facts-Struktur, userAnswers-Tone). Ein Pattern-Bug aus pv-001 würde durch Copy-Paste in alle 22 Einträge fließen. Daher: pv-001 wird isoliert generiert + Kolja-Approved BEVOR pv-002..pv-011 entstehen.

    Implementation-Approach: Schreibe JSON-Array mit nur `pv-001`. Validiere JSON-Syntax: `node -e "JSON.parse(require('fs').readFileSync('data/eval/pipeline-korpus.json', 'utf8'))"`.
  </what-built>
  <how-to-verify>
    **Kolja-Checkpoint #1 — Pattern-Korrektheit (pv-001 als Goldstandard):**

    1. Öffne `data/eval/pipeline-korpus.json` in Editor
    2. Prüfe **pv-001 Borsigwalder-Anker**:
       - id = `pv-001`, category = `vag`, programmId = `bmbf-digitalpakt-2`
       - userAnswers enthält mindestens die 12 UAT-Antworten aus UAT-Memo (z.B. „Hatten wir das nicht schon mal? Glaub ich nicht.")
       - role-Alternation `ai → user → ai → user` korrekt
       - expected_forbidden_markers enthält die 8 dokumentierten UAT-Marker (Az 123/2026, TV-L E9, Bezirk Berlin-Mitte, 380 Schüler, Willkommensklassen, Beschluss vom 12.12.2025, Haushaltsstelle 1234/56789, KMK-Kompetenzen aktiv adressiert)
       - expected_geber_gruppe = `oeffentlich`
       - facts-Schema valide (passt zu lib/wizard/types.ts WizardFacts)
    3. **Validiere JSON-Schema:**
       ```bash
       node -e 'const k = require("./data/eval/pipeline-korpus.json"); const e = k[0]; console.log("id:", e.id, "category:", e.category, "programmId:", e.programmId, "markers:", e.expected_forbidden_markers.length, "geber:", e.expected_geber_gruppe);'
       ```

    Antworte mit `approved` wenn:
    - pv-001 ist Goldstandard-tauglich (Pattern, Marker, Facts-Schema)
    - Schema/Struktur ist diejenige, die für die 21 Folge-Einträge in Task 2 + Task 3 multipliziert wird

    Antworte mit konkreten Pattern-Issues sonst (z.B. „role-Alternation falsch" oder „expected_forbidden_markers-Schema unklar").
  </how-to-verify>
  <resume-signal>Antworte: `approved` (dann startet Task 2: pv-002..pv-011) oder Pattern-Korrektur-Issues.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: pv-002..pv-011 (10 Standard-Einträge je 1 pro Dossier) + Kolja-Checkpoint Pattern-Konsistenz (D-03/-04)</name>
  <files>data/eval/pipeline-korpus.json</files>
  <read_first>
    - data/eval/pipeline-korpus.json (Output aus Task 1 — pv-001 als Pattern-Goldstandard)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md (D-03 11 Standard-Einträge, D-04 50/30/20-Mix)
    - data/foerderprogramme.json (programmId-Lookup pro Dossier)
    - data/richtlinien/ — alle 11 Dossier-JSONs minus bmbf-digitalpakt-2 (das ist pv-001)
    - data/eval/dossier-coverage-baseline.md (Pflicht-Abschnitte pro Dossier)
  </read_first>
  <what-built>
    **10 Standard-Einträge** (`pv-002` bis `pv-011`), je 1 pro Dossier, alle nach pv-001-Pattern aus Task 1:

    - `pv-002`: kultur-macht-stark, mittel-vag, kultur-affine Grundschule, partner-fokus
    - `pv-003`: aktion-mensch-schulkooperation, vag, Inklusions-Anliegen
    - `pv-004`: bosch-schulpreis, hochwertig, Schule-als-Ganzes-Narrativ
    - `pv-005`: erasmus-schule-2026, mittel, EU-Austausch-Anliegen
    - `pv-006`: erasmus-schulentwicklung, vag, allgemeine Schulentwicklung
    - `pv-007`: ensam-bmz, mittel, BMZ-Globales-Lernen-Programm
    - `pv-008`: ferry-porsche-challenge-2025, vag, MINT-Schulprojekt
    - `pv-009`: ferry-porsche-challenge (legacy-Version), vag, MINT
    - `pv-010`: klimalab-2026, mittel-vag, Umwelt/Klima-Bildung
    - `pv-011`: berlin-startchancen, hochwertig, Berliner Brennpunktschule

    **Verteilung über pv-001..pv-011 nach Standard-Block:**
    - vag: 6 Einträge (pv-001, pv-003, pv-006, pv-008, pv-009)
    - mittel: 3 Einträge (pv-002, pv-005, pv-007, pv-010)
    - hochwertig: 2 Einträge (pv-004, pv-011)
    (Reserve in Task 3 hebt vag-Quote in Richtung 50 %.)

    **Pro Eintrag:**
    - mind. 4 Turns userAnswers[] mit role-alternation `ai → user → ai → user`
    - pre-extrahierte facts via facts-extractor.ts oder manuell
    - expected_forbidden_markers[]-Initialfüllung (3-6 Marker pro Eintrag, Marker-Tiefen-Kuration kommt in Task 3)
    - expected_geber_gruppe per D-28-Default-Mapping:
      - bosch-*, aktion-mensch-*, ferry-porsche-* → `wirtschaftspreis` oder `stiftung` (foerdergeberTyp-abhängig)
      - berlin-*, nrwbank-* → `oeffentlich`
      - erasmus-*, ensam-bmz, klimalab-* → `eu`
      - kultur-macht-stark → `verband-uni`

    **WICHTIG: WAVE-0-OUTPUT NUTZEN:**
    - `data/eval/dossier-coverage-baseline.md` zeigt: Dossiers ohne Pflicht-Abschnitte (`berlin-startchancen`, `erasmus-schule-2026`, beide ferry-porsche, `klimalab-2026`) brauchen NICHT-WIZ-01-fokussierte Marker, sondern WIZ-02/-03-fokussierte.

    Implementation: Iterativ pv-002, pv-003, ..., pv-011 nach pv-001-Pattern erstellen. JSON-Syntax nach jedem Block validieren.
  </what-built>
  <how-to-verify>
    **Kolja-Checkpoint #2 — Pattern-Konsistenz (pv-002..pv-011 gegen pv-001-Goldstandard):**

    1. Öffne `data/eval/pipeline-korpus.json`
    2. Prüfe **Konsistenz** zu pv-001:
       - Pro Eintrag: gleiche Schlüssel-Struktur wie pv-001 (id, category, programmId, schulProfil, userAnswers, facts, expected_forbidden_markers, expected_geber_gruppe)
       - userAnswers role-Alternation `ai → user → ai → user` durchgängig
       - facts-Schema valide (WizardFacts-konform)
    3. Prüfe **Standard-Coverage:**
       - 11 Standard-Einträge (pv-001..pv-011), je 1 pro Dossier
       - Kein Dossier doppelt
       - bmbf-digitalpakt-2 nur in pv-001
    4. Prüfe **Verteilung pv-001..pv-011** (Standard-only):
       - 5-6 vag, 3-4 mittel, 2 hochwertig — finale 50/30/20-Justierung kommt in Task 3 via Reserve-Einträgen
    5. **JSON-Validierung:**
       ```bash
       node -e 'const k = require("./data/eval/pipeline-korpus.json"); console.log("entries:", k.length); console.log("kategorien:", k.reduce((acc, e) => (acc[e.category] = (acc[e.category]||0)+1, acc), {})); console.log("dossier-coverage:", k.map(e => e.programmId).filter((v,i,a) => a.indexOf(v) === i));'
       ```

    Antworte mit `approved`, wenn:
    - 11 Standard-Einträge (pv-001..pv-011) konsistent zum pv-001-Pattern
    - Dossier-Coverage 11/11
    - Schema-Konsistenz korrekt

    Antworte mit konkreten Issues sonst (z.B. „pv-005 ist eigentlich hochwertig, nicht mittel" oder „pv-007 fehlen facts-Felder").
  </how-to-verify>
  <resume-signal>Antworte: `approved` (dann startet Task 3: Edge-Cases + Reserve + Marker-Tiefen-Kuration) oder Konsistenz-Issues.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: pv-edge-001..007 (Edge-Cases) + pv-res-001..004 (Reserve) + Marker-Liste-Tiefen-Kuration (D-05/-14 Hybrid)</name>
  <files>data/eval/pipeline-korpus.json</files>
  <read_first>
    - data/eval/pipeline-korpus.json (Output aus Task 2 — pv-001..pv-011)
    - ~/.claude/projects/-home-kolja/memory/edufunds-uat-pipeline-befunde-2026-04-28.md (UAT-28.04.-8-Marker als Marker-Goldstandard)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md (D-05 7 Edge-Case-Typen, D-14 Marker-Hybrid)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md (§Pitfall 1+2 — Marker-Detection Multi-Source + False-Positive-Schutz)
    - lib/wizard/prompts.ts (SECTION_SYSTEM + FINANZPLAN_SYSTEM Halluzinations-Verbots-Listen — Marker dürfen NICHT mit existierenden Verbots-Listen kollidieren falls bereits gefiltert)
  </read_first>
  <what-built>
    **Teil 1 — 7 Edge-Cases** (`pv-edge-001` bis `pv-edge-007`), je 1 pro D-05-Typ:

    - `pv-edge-001` vag-extrem (alle Antworten „glaub ich", „weiß nicht so genau")
    - `pv-edge-002` profil-fehlt (kein bundesland, kein schultyp)
    - `pv-edge-003` programm-mismatch (Anliegen passt zu KEINEM Dossier — programmId aus foerderprogramme.json ohne Dossier z.B. `niedersachsen-sport`)
    - `pv-edge-004` widerspruechlich (z.B. „200 Schüler" und „400 Schüler" in unterschiedlichen Turns)
    - `pv-edge-005` bl-konflikt (NRW-Anliegen aber programmId = `berlin-startchancen`)
    - `pv-edge-006` antworten-zu-lang (Antworten > 3000 Zeichen pro Turn, sprengt potenziell maxZeichen)
    - `pv-edge-007` antrag-spruenge (User springt im Verlauf zurück zu früherer Frage, ändert Antwort)

    **Teil 2 — 4 Reserve-Einträge** (`pv-res-001` bis `pv-res-004`) — zusätzliche vag-Inputs auf nochmals 3 zufällig gewählte Dossiers, sichern den finalen 50/30/20-Mix.

    **Total nach Task 3:** 22 Einträge (11 Standard + 7 Edge + 4 Reserve), Verteilung **11 vag / 7 mittel / 4 hochwertig** = exakt 50/30/20.

    **Teil 3 — Marker-Liste-Tiefen-Kuration für alle 22 Einträge:**

    Pro Eintrag `expected_forbidden_markers[]` final schärfen. Heuristik pro Eintrag:
    - **Anchor-Marker (1-2)**: was würde ein LLM bei vagen Antworten am wahrscheinlichsten erfinden? (z.B. Aktenzeichen, Beschluss-Daten, Tarif-Codes, Partner-Namen die nicht gefallen sind)
    - **Domain-Marker (2-3)**: typisch für die programmspezifische Domäne (z.B. bei `kultur-macht-stark` würde LLM einen erfundenen lokalen Kulturpartner-Namen einsetzen)
    - **Strukturelle Marker (1-2)**: Datenmuster die Layer-2-Regex bereits fängt (z.B. `12.12.2025`, `TV-L E9`) — diese werden zusätzlich als L1-Marker eingetragen damit Cross-Validation greift

    **Erwartete Marker-Anzahl pro Eintrag:** durchschnittlich 8 Marker, Range 3-12.
    **Total ~176 Marker** für 22 Einträge.

    **Pro Marker:**
    - `marker`: exakte Substring (case-insensitive grep-target)
    - `description`: 1 Satz, warum es hier eine Halluzination wäre

    Implementation: Edge-Cases + Reserve generieren, danach Marker-Liste pro Eintrag iterativ schärfen. Marker-Statistik validieren:
    ```bash
    node -e 'const k = require("./data/eval/pipeline-korpus.json"); const t = k.reduce((s,e) => s + e.expected_forbidden_markers.length, 0); console.log("total markers:", t, "avg:", (t/k.length).toFixed(1)); const lowMarker = k.filter(e => e.expected_forbidden_markers.length < 3); console.log("entries with <3 markers:", lowMarker.map(e => e.id));'
    ```

    **Conventional Commit nach Kolja-Approval aller 3 Checkpoints (Task 1 + Task 2 + Task 3):**
    `feat(05): wave-1 pipeline-korpus mit 22 einträgen + ~176 forbidden-markern (D-01..D-07, D-14 hybrid-kuration)`
  </what-built>
  <how-to-verify>
    **Kolja-Checkpoint #3 — Edge-Case-Plausibilität + Marker-Tiefen-Kuration:**

    1. Öffne `data/eval/pipeline-korpus.json`
    2. Prüfe **Edge-Case-Realismus**:
       - pv-edge-003 (programm-mismatch) hat eine programmId, die NICHT in `data/richtlinien/` als Dossier liegt
       - pv-edge-005 (bl-konflikt) hat `schulProfil.bundesland = "NRW"` aber `programmId: "berlin-startchancen"`
       - alle 7 D-05-Edge-Case-Typen abgedeckt (vag-extrem, profil-fehlt, programm-mismatch, widerspruechlich, bl-konflikt, antworten-zu-lang, antrag-spruenge)
    3. Prüfe **finale Verteilung** (Total = 22):
       - Standard pv-001..pv-011 (11)
       - Edge pv-edge-001..pv-edge-007 (7)
       - Reserve pv-res-001..pv-res-004 (4)
       - Mix: 11 vag / 7 mittel / 4 hochwertig (50/30/20 ±1)
    4. Prüfe **Marker-Anzahl pro Eintrag**:
       - pv-001 Borsigwalder: ≥ 8 Marker (UAT-28.04.-Anker)
       - Standard-Einträge pv-002..pv-011: 5-10 Marker
       - Edge-Cases pv-edge-001..pv-edge-007: 3-8 Marker
       - Reserve: 3-5 Marker
    5. Prüfe **Marker-Qualität** (Stichproben pv-001, pv-004, pv-edge-001):
       - Sind Marker konkrete Substrings (keine vagen Beschreibungen)?
       - Würde ein Mensch im Output erkennen, dass dieser Marker eine Halluzination ist?
       - Sind Marker NICHT in userAnswers[] enthalten (sonst false-positive in Layer 2)?
    6. **False-Positive-Cross-Check pro Eintrag (Stichprobe 3-5 Einträge):**
       ```bash
       node -e 'const k = require("./data/eval/pipeline-korpus.json"); const e = k.find(x => x.id === "pv-001"); const userText = e.userAnswers.filter(a => a.role==="user").map(a => a.content.toLowerCase()).join(" "); const conflicts = e.expected_forbidden_markers.filter(m => userText.includes(m.marker.toLowerCase())); console.log("FP-Konflikte in pv-001:", conflicts);'
       ```
       — Output sollte leeres Array sein. Wenn nicht, Marker raus oder umformulieren.
    7. **Totalsumme:** ~176 Marker (Range 130-220 ok)
    8. **Final JSON-Validierung:**
       ```bash
       node -e 'const k = require("./data/eval/pipeline-korpus.json"); console.log("entries:", k.length); console.log("kategorien:", k.reduce((acc, e) => (acc[e.category] = (acc[e.category]||0)+1, acc), {})); console.log("missing-fields:", k.filter(e => !e.id || !e.programmId || !e.userAnswers || !e.expected_forbidden_markers || !e.expected_geber_gruppe).map(e => e.id));'
       ```

    Antworte mit `approved`, wenn:
    - 22 Einträge, Verteilung 11/7/4
    - 50/30/20-Mix ±1
    - Edge-Cases realistisch (alle 7 D-05-Typen)
    - Marker-Statistik im Range
    - 0 False-Positive-Konflikte in Stichprobe

    Antworte mit konkreten Issues für Nachbesserung sonst.
  </how-to-verify>
  <resume-signal>Antworte: `approved` (dann wird der Korpus committed) oder konkrete Edge-Case-/Marker-Issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude → Korpus-Daten | Claude generiert initial, Kolja reviewt (Hybrid-Kuration D-14). Beide Checkpoints sind Trust-Anchor |
| Korpus → Eval-Skript | Korpus ist Source-of-Truth für alle künftigen Eval-Runs. Veränderung erfordert Baseline-Recalc (D-26) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-01 | Tampering | Marker-Liste-Bias: Wenn Marker zu speziell, misst Eval nur Auswendiglernen, nicht echte Halluzinations-Resistenz | medium | mitigate | Mix aus UAT-Real (pv-001) + synthetisch + strukturelle Regex-Cross-Marker. Kolja-Review-Checkpoint enforced. Nach 1. Tuning-Welle (Wave 3) sichten ob Marker zu spezifisch sind |
| T-05-02-02 | Tampering | False-Positive in Marker-Liste: Marker-String steht in userAnswers → Layer 2 würde es als legitim klassifizieren | medium | mitigate | Acceptance-Test in Checkpoint 2 grept userAnswers gegen Marker-Liste; Marker mit FP-Konflikt werden rausgeworfen |
| T-05-02-03 | Information Disclosure | UAT-28.04.-Borsigwalder-Inhalte enthalten potenziell personenbezogene Schul-Daten | low | mitigate | UAT-Memo-Daten sind bereits anonymisiert (Schulname "Borsigwalder Grundschule" ist Beispiel); Korpus-Eintrag pv-001 nutzt nur Pattern, keine echten PII |
| T-05-02-04 | Repudiation | Korpus-Manipulation (Marker entfernen → Eval lügt) | medium | mitigate | Korpus ist versioniert; D-26 verlangt bei Korpus-Updates expliziten Baseline-Recalc im selben PR |
| T-05-02-05 | DoS | Korpus-Größe ist Kosten-Treiber für Live-Runs (22 Einträge × N=3 × 7 Stages ≈ 3-4 EUR pro Voll-Lauf) | low | accept | D-01 budgetiert das. Bei Phase-5-Closure ist Voll-Lauf-Anzahl < 5 |
</threat_model>

<verification>
- JSON-Schema-Validität: `node -e 'JSON.parse(...)'` exit 0
- Eintragsanzahl: `length === 22`
- Verteilung: 11 vag / 7 mittel / 4 hochwertig
- pv-001 hat ≥ 8 expected_forbidden_markers
- Total ≥ 130 Marker
- 0 False-Positive-Konflikte in Stichprobe
- 3 Kolja-Checkpoints durchlaufen (`approved` × 3: Pattern-Korrektheit, Pattern-Konsistenz, Edge+Marker-Qualität)
</verification>

<success_criteria>
- `data/eval/pipeline-korpus.json` mit 22 Top-Level-JSON-Array-Einträgen
- Schema gemäß PipelineKorpusEntry (`<interfaces>`)
- Antwort-Qualitäts-Mix 11/7/4
- 7 Edge-Cases × 7 D-05-Typen abgedeckt
- ~176 Marker total, ≥ 3 pro Eintrag, 0 false-positive in Stichprobe
- 3 Kolja-Checkpoints `approved` (Warning 8 Split: Pattern-Korrektheit / Pattern-Konsistenz / Edge+Marker-Qualität)
- Commit-Message Conventional: `feat(05): wave-1 pipeline-korpus ...`
</success_criteria>

<output>
Nach Completion: `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-02-SUMMARY.md` mit:
- Korpus-Übersicht: 22 Einträge, Verteilung pro Kategorie + Edge-Case + Geber-Cluster (Vor-Mapping)
- Marker-Statistik: Total, Avg pro Eintrag, Min/Max
- Stichprobenliste 3 markante Marker-Sets (pv-001 + 1 Edge + 1 Stiftung)
- Folge-Plan-Hinweis: Plan 05-04 (Eval-Skript + Baseline-Run) braucht den Korpus
</output>

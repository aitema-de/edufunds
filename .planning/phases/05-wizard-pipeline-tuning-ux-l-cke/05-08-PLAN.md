---
phase: 05-wizard-pipeline-tuning-ux-l-cke
plan: 08
type: execute
wave: 4
depends_on: [05, 06, 07]
files_modified:
  - .github/workflows/pipeline-eval.yml
  - data/eval/README.md
  - data/eval/TUNING.md
  - CLAUDE.md
  - .planning/codebase/STACK.md
  - lib/wizard/config.ts
autonomous: false
requirements: [WIZ-01, WIZ-02, WIZ-03]
tags: [phase-5, wave-4, ci-gate, doku, pre-closure-smoke, default-decision]
must_haves:
  truths:
    - ".github/workflows/pipeline-eval.yml existiert als PR-Trigger auf lib/wizard/** + data/richtlinien/** + scripts/eval-pipeline.ts + data/eval/pipeline-korpus.json (D-27)"
    - "Workflow ruft Default --replay (kein LLM-Cost in CI), manual-dispatch erlaubt --live (D-24)"
    - "Workflow exit 1 bei WIZ-01-Regression > 2σ oder WIZ-02-Regression > 2σ+10% (D-25 — WIZ-03 warning-only)"
    - "GitHub-Annotation pro Achse aus Report-JSON geparsed (mean+stddev+baseline)"
    - "Pre-Closure-Live-Smoke gefahren mit 1× nicht-Korpus-Anliegen (D-36) — Kolja-Sichtprüfung"
    - "data/eval/README.md existiert mit Überblick: Korpus, Skript, Reports, Snapshot/Replay, Aufruf-Konvention, Threshold-Gate (D-35)"
    - "CLAUDE.md (Repo-Root) hat Eval-Sektion: npx tsx scripts/eval-pipeline.ts, Threshold-Gate als PR-Pflicht (D-35)"
    - ".planning/codebase/STACK.md hat Eval-Apparat als Komponente eingetragen (D-35)"
    - "data/eval/TUNING.md hat finalen 'Default-Hebel-Entscheidung'-Block: welche Hebel-Kombination wird Production-Default + warum (basiert auf Wave-3-Tuning-Deltas aus Plan 05-05/-06/-07)"
    - "lib/wizard/config.ts Default-Werte der 4 Flags ggf. auf 'true' gesetzt (per Default-Hebel-Entscheidung), ODER bleiben OFF mit dokumentiertem Argument warum"
  artifacts:
    - path: ".github/workflows/pipeline-eval.yml"
      provides: "CI-Threshold-Gate für lib/wizard/**-PRs"
      contains: "pipeline-eval.ts"
    - path: "data/eval/README.md"
      provides: "Eval-Apparat-Überblick"
      contains: "Threshold-Gate"
      min_lines: 80
    - path: "CLAUDE.md"
      provides: "Eval-Sektion ergänzt"
      contains: "eval-pipeline"
    - path: ".planning/codebase/STACK.md"
      provides: "Eval-Apparat als Komponente"
      contains: "eval-pipeline"
    - path: "data/eval/TUNING.md"
      provides: "Default-Hebel-Entscheidungs-Block"
      contains: "Production-Default"
  key_links:
    - from: ".github/workflows/pipeline-eval.yml"
      to: "scripts/eval-pipeline.ts"
      via: "npx tsx-Aufruf in eval-Job"
      pattern: "eval-pipeline.ts"
    - from: ".github/workflows/pipeline-eval.yml"
      to: "data/eval/pipeline-snapshots/baseline"
      via: "--replay-Argument zeigt auf force-committed Baseline-Snapshots"
      pattern: "pipeline-snapshots/baseline"
    - from: "data/eval/README.md"
      to: "data/eval/BASELINE.md"
      via: "README verlinkt BASELINE für aktuelle Schwellwerte"
      pattern: "BASELINE.md"
---

<objective>
Wave 4 (Closure): CI-Threshold-Gate als GitHub-Workflow + Pre-Closure-Live-Smoke (D-36, Anti-Overfitting) + 4 Doku-Outputs (D-35) + Default-Hebel-Entscheidung in TUNING.md auf Basis Wave-3-Daten.

Purpose: Phase 5 ist abgeschlossen, wenn (a) CI-Gate bei künftigen PRs Regressionen blockt, (b) Pre-Closure-Smoke gegen ein nicht-Korpus-Anliegen zeigt keine Overfitting-Symptome, (c) Eval-Apparat ist in CLAUDE.md / STACK.md / data/eval/README.md dokumentiert, (d) finale Default-Hebel-Entscheidung in TUNING.md festgehalten.

**Autonomous: false** — Pre-Closure-Smoke (D-36) braucht Kolja-Sichtprüfung; Default-Hebel-Entscheidung ist eine inhaltliche Entscheidung die auf Wave-3-Daten basiert und Kolja final approven sollte.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-PATTERNS.md
@.github/workflows/weekly-auto-pflege.yml
@scripts/eval-pipeline.ts
@data/eval/BASELINE.md
@data/eval/TUNING.md
@CLAUDE.md
@.planning/codebase/STACK.md
@data/eval/pipeline-korpus.json
@data/eval/pipeline-snapshots/baseline
@lib/wizard/config.ts

<interfaces>
From RESEARCH §Code-Examples CI-Workflow Z.1144-1235 (1-zu-1 anwendbares Skeleton mit set-euo-pipefail + Secret-Pre-Flight + GitHub-Annotation pro Achse).

From .github/workflows/weekly-auto-pflege.yml (Sibling-Vorbild, Phase 4):
- Hardening-Pattern: `set -euo pipefail`, Secret-Pre-Flight-Check, env-Mapping statt direkter `${{ inputs.X }}`-Interpolation
- actions/checkout@v4 mit fetch-depth: 0
- actions/setup-node@v4 mit node-version: "20"
- Job-Permissions explizit (`contents: read`, `pull-requests: write` bei Annotations)

From CONTEXT D-24/-25/-27:
- PR-Trigger paths: `lib/wizard/**`, `data/richtlinien/**`, `scripts/eval-pipeline.ts`, `data/eval/pipeline-korpus.json`
- workflow_dispatch mit Input `mode` (replay/live, default replay)
- WIZ-01 hart: exit 1 bei drop > 2σ
- WIZ-02 mittel: exit 1 bei drop > 2σ + 10 % baseline
- WIZ-03 warning-only: nie exit 1, nur core.notice
</interfaces>

<closure_calculus>
Phase-5-Closure-Calculus (vor Plan 05-08 Task 1):

Phase 5 gilt als geschlossen, wenn:
1. CI-Gate operativ
2. Pre-Closure-Smoke OK
3. Doku-Output (4 Dateien)
4. Default-Hebel-Entscheidung in TUNING.md
5. **Konservative D-19-Schwellwerte sind im Code/Baseline erreicht ODER 2. Tuning-Welle ist im Plan (D-23)**

Wenn nach Wave 3 die Schwellwerte nicht erreicht sind:
- WIZ-01 < 80 % → optionaler Plan 05-09 mit weiteren Iterationen
- WIZ-02 < 50 % Marker-Reduktion → optionaler Plan 05-09
- WIZ-03 Delta ≤ 0 pro Cluster → kein Block (warning-only)

Decision in Plan 05-08 Task 4: Phase-5-Closure JETZT oder Plan 05-09 anstossen.
</closure_calculus>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: .github/workflows/pipeline-eval.yml + CI-Threshold-Gate</name>
  <files>.github/workflows/pipeline-eval.yml</files>
  <read_first>
    - .github/workflows/weekly-auto-pflege.yml (Sibling-Vorbild für Hardening + Secret-Pre-Flight + env-Mapping)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md (§Code-Examples CI-Workflow Z.1144-1235 — direkt anwendbarer YAML-Skeleton)
    - scripts/eval-pipeline.ts (CLI-Flag-Set + Exit-Codes)
    - data/eval/pipeline-snapshots/baseline/ (force-committed Replay-Anker, ls zur Verifikation)
  </read_first>
  <action>
    **YAML aus RESEARCH §Code-Examples Z.1144-1235 erstellen** mit Hardening:

    ```yaml
    name: Pipeline-Eval (Threshold-Gate)

    on:
      pull_request:
        paths:
          - "lib/wizard/**"
          - "data/richtlinien/**"
          - "data/eval/pipeline-korpus.json"
          - "scripts/eval-pipeline.ts"
      workflow_dispatch:
        inputs:
          mode:
            description: "Modus: replay (default, kein LLM) oder live (Cost ~3-4 EUR)"
            required: false
            type: choice
            options: [replay, live]
            default: replay

    jobs:
      eval:
        runs-on: ubuntu-latest
        permissions:
          contents: read
          pull-requests: write
        steps:
          - uses: actions/checkout@v4
            with:
              fetch-depth: 0

          - uses: actions/setup-node@v4
            with:
              node-version: "20"

          - run: npm ci

          - name: Pre-Flight Secret-Check
            env:
              DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
              MODE: ${{ github.event.inputs.mode || 'replay' }}
            run: |
              set -euo pipefail
              if [ "$MODE" = "live" ] && [ -z "${DEEPSEEK_API_KEY}" ]; then
                echo "::error::DEEPSEEK_API_KEY Secret fehlt fuer --live-Modus"
                exit 1
              fi

          - name: Verify Baseline-Snapshots existieren
            run: |
              set -euo pipefail
              if [ ! -d "data/eval/pipeline-snapshots/baseline" ]; then
                echo "::error::data/eval/pipeline-snapshots/baseline/ fehlt — Baseline force-committen oder Phase 5 nicht abgeschlossen"
                exit 1
              fi
              COUNT=$(ls data/eval/pipeline-snapshots/baseline/ | wc -l)
              if [ "$COUNT" -lt 22 ]; then
                echo "::error::Baseline-Snapshots unvollstaendig ($COUNT Dateien, erwartet >= 22)"
                exit 1
              fi

          - name: Run Pipeline-Eval
            id: eval
            env:
              DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
              MODE: ${{ github.event.inputs.mode || 'replay' }}
            run: |
              set -euo pipefail
              ARGS=(--md-summary)
              if [ "$MODE" = "replay" ]; then
                ARGS+=(--replay data/eval/pipeline-snapshots/baseline)
              else
                ARGS+=(--live --N=3)
              fi
              npx tsx scripts/eval-pipeline.ts "${ARGS[@]}"

          - name: Upload Eval Report
            if: always()
            uses: actions/upload-artifact@v4
            with:
              name: pipeline-eval-${{ github.run_id }}
              path: data/eval/pipeline-reports/*.json
              retention-days: 30

          - name: GitHub-Annotation pro Achse
            if: always()
            uses: actions/github-script@v7
            with:
              script: |
                const fs = require('fs');
                const path = require('path');
                const dir = 'data/eval/pipeline-reports';
                if (!fs.existsSync(dir)) {
                  core.warning('Keine pipeline-reports/ — Eval-Skript hat keinen Report geschrieben');
                  return;
                }
                const files = fs.readdirSync(dir).filter(f => f.endsWith('.json')).sort();
                if (files.length === 0) {
                  core.warning('Keine Report-JSON gefunden');
                  return;
                }
                const reportPath = path.join(dir, files[files.length - 1]);
                const report = JSON.parse(fs.readFileSync(reportPath, 'utf8'));
                const axes = report.aggregate?.axes ?? {};
                for (const [axis, val] of Object.entries(axes)) {
                  const sign = val.status === 'pass' ? '✓' : '✗';
                  const baseline = val.baselineMean != null ? `(baseline: ${val.baselineMean.toFixed(1)})` : '';
                  core.notice(`${sign} ${axis}: mean=${val.mean.toFixed(1)} stddev=${val.stddev.toFixed(1)} ${baseline}`);
                }
    ```

    **Wichtig:**
    - `env`-Mapping verwenden — niemals `${{ inputs.X }}` direkt in `run`-Shells (Workflow-Injection-Schutz, T-05-08-01)
    - `set -euo pipefail` in jedem shell-Schritt
    - Baseline-Snapshot-Existenz-Check als eigener Step (verhindert silent-no-baseline-Crashes)
    - GitHub-Annotation auch bei eval-Fail laufen lassen (`if: always()`)

    **WICHTIG zur Exit-Code-Logik:** scripts/eval-pipeline.ts wirft selbst exit 1 bei WIZ-01/WIZ-02-Threshold-Fail (D-25). Der "Run Pipeline-Eval"-Step propagiert das automatisch — kein zusätzlicher Exit-Handling-Code nötig.

    **Conventional Commit:** `feat(05): wave-4 pipeline-eval CI-workflow (PR-trigger lib/wizard/**, --replay default, threshold-gate D-25 achsen-spezifisch)`
  </action>
  <verify>
    <automated>test -f .github/workflows/pipeline-eval.yml && grep -q "Pipeline-Eval (Threshold-Gate)" .github/workflows/pipeline-eval.yml && grep -q "pull_request:" .github/workflows/pipeline-eval.yml && grep -q "workflow_dispatch:" .github/workflows/pipeline-eval.yml && grep -q "lib/wizard/\\*\\*" .github/workflows/pipeline-eval.yml && grep -q "data/richtlinien/\\*\\*" .github/workflows/pipeline-eval.yml && grep -q "set -euo pipefail" .github/workflows/pipeline-eval.yml && grep -q "DEEPSEEK_API_KEY" .github/workflows/pipeline-eval.yml && grep -q "pipeline-snapshots/baseline" .github/workflows/pipeline-eval.yml && grep -q "core.notice" .github/workflows/pipeline-eval.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/pipeline-eval.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/pipeline-eval.yml` existiert + ist gültige YAML
    - PR-Trigger auf 4 Pfade (lib/wizard/**, data/richtlinien/**, data/eval/pipeline-korpus.json, scripts/eval-pipeline.ts)
    - workflow_dispatch mit mode-Input (replay/live, default replay)
    - Pre-Flight Secret-Check: DEEPSEEK_API_KEY Pflicht nur bei live-Modus
    - Baseline-Snapshot-Existenz-Check vor Eval-Lauf
    - Default-Aufruf: `npx tsx scripts/eval-pipeline.ts --replay data/eval/pipeline-snapshots/baseline --md-summary`
    - Report-Upload-Artifact + GitHub-Annotation
    - set -euo pipefail in allen shell-Steps
    - env-Mapping (keine direkte `${{ inputs.X }}`-Interpolation in `run:`)
  </acceptance_criteria>
  <done>CI-Workflow ist YAML-valide, hardened nach Phase-4-Pattern, erfüllt D-24/-25/-27.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Doku-Output: data/eval/README.md + CLAUDE.md Eval-Sektion + STACK.md (D-35)</name>
  <files>data/eval/README.md, CLAUDE.md, .planning/codebase/STACK.md</files>
  <read_first>
    - data/eval/BASELINE.md (für Phase-5-Eintrag-Referenz)
    - data/eval/TUNING.md (existierende Hebel-Einträge)
    - CLAUDE.md (existierende Sektionen, wo Eval-Sektion am besten platziert ist)
    - .planning/codebase/STACK.md (existierende Komponenten-Liste, wo Eval-Apparat reinkommt)
    - scripts/eval-pipeline.ts (alle CLI-Flags + ihre Bedeutung)
  </read_first>
  <action>
    **data/eval/README.md erstellen** (~80-120 Zeilen):

    ```markdown
    # data/eval/ — Eval-Apparat

    > Phase 1 Matcher-Eval + Phase 5 Pipeline-Eval. Versionierte Korpora als Regressions-Anker, Snapshot/Replay für günstige Score-Logik-Iteration, Threshold-Gate als PR-Pflicht.

    ## Strukturen

    | Bereich | Pfad | Beschreibung |
    |---------|------|--------------|
    | Matcher-Korpus | `matcher-korpus.json` | Phase 1, Top-Level-Array, 20-25 Schul-Anliegen mit expected_top3 |
    | Matcher-Snapshots | `snapshots/<ISO>/<id>.json` | Phase 1, .gitignore'd ausser baseline |
    | Matcher-Reports | `reports/<ISO>.json` + `.md` | Phase 1, .gitignore'd |
    | Pipeline-Korpus | `pipeline-korpus.json` | Phase 5, Top-Level-Array, 22 Eintraege mit programmId + facts + expected_forbidden_markers + expected_geber_gruppe |
    | Pipeline-Snapshots | `pipeline-snapshots/<ISO>/<id>-run<N>.json` | Phase 5, .gitignore'd ausser baseline |
    | Pipeline-Reports | `pipeline-reports/<ISO>.json` + `.md` | Phase 5, .gitignore'd |
    | BASELINE | `BASELINE.md` | Append-only History, beide Phasen, manuelle Pflege (Skript schreibt NICHT) |
    | TUNING | `TUNING.md` | Phase 5 Append-only Playbook pro Tuning-Iteration |
    | Dossier-Coverage | `dossier-coverage-baseline.md` | Phase 5 Pre-Flight-Survey: welche Phase-3-Felder pro Dossier befuellt |

    ## Aufruf-Konventionen

    ### Matcher-Eval (Phase 1+2)

    ```bash
    npx tsx scripts/eval-matcher.ts --md-summary                              # Live-Run
    npx tsx scripts/eval-matcher.ts --replay data/eval/snapshots/baseline     # Replay-Run
    ```

    ### Pipeline-Eval (Phase 5)

    ```bash
    # Default: Replay gegen Baseline-Snapshots (kein LLM-Cost)
    npx tsx scripts/eval-pipeline.ts --replay data/eval/pipeline-snapshots/baseline --md-summary

    # Live-Run mit N=3 (~3-4 EUR Cost, ~60-75 min Wallclock)
    npx tsx scripts/eval-pipeline.ts --live --N=3 --snapshot --md-summary

    # Single-Entry-Live-Smoke (Pre-Closure-Test, ~0.15 EUR)
    npx tsx scripts/eval-pipeline.ts --live --N=1 --single pv-001 --snapshot

    # Mit deep WIZ-02 LLM-Judge (Layer 3, optional, hoehere Cost)
    npx tsx scripts/eval-pipeline.ts --live --deep --N=3 --snapshot
    ```

    ## Threshold-Gate (PR-Pflicht)

    Pipeline-Eval ist PR-Pflicht-Vorabcheck fuer Aenderungen an `lib/wizard/**` oder `data/richtlinien/**` (D-24).
    CI-Workflow: `.github/workflows/pipeline-eval.yml`.

    | Achse | Block-Status (D-25) | Schwellwert (D-19) |
    |-------|---------------------|---------------------|
    | WIZ-01 (Pflichtabschnitte) | hart — exit 1 bei drop > 2σ unter Baseline | >= 80 % Coverage |
    | WIZ-02 (Halluzinations-Detection) | mittel — exit 1 bei drop > 2σ + 10 % baseline | >= 50 % Marker-Reduktion |
    | WIZ-03 (Tonalitaets-Passung) | warning-only — nie exit 1 (LLM-Judge ist rauschig) | Score-Delta > 0 pro Cluster |
    | Finanzplan-Sub (Validity) | warning-only | dokumentiert in BASELINE |

    ## Korpus-Update-Workflow (D-26)

    Korpus-Aenderung erfordert Baseline-Recalc im selben PR:
    1. `data/eval/pipeline-korpus.json` editiert
    2. `npx tsx scripts/eval-pipeline.ts --live --N=3 --snapshot --md-summary`
    3. Snapshot-Verzeichnis nach `pipeline-snapshots/baseline/` kopieren
    4. `BASELINE.md` neuer Eintrag mit Begruendung ("Korpus erweitert um X")
    5. PR einreichen — Reviewer prueft Begruendung

    ## Feature-Flags (Wave 3 Hebel)

    Phase-5-Tuning-Hebel sind Env-Var-gesteuert (D-22):

    | Flag | Hebel | Default |
    |------|-------|---------|
    | `PIPELINE_SHARP_PROMPTS=1` | 1: Geschaerfte Verbots-Listen in CRITIQUE/SECTION/REVISION/RECHECK | <siehe TUNING.md Final-Block> |
    | `PIPELINE_COMPLIANCE_STAGE=1` | 2: Compliance-Check-Stage zwischen recheck und finanzplan | <siehe TUNING.md Final-Block> |
    | `PIPELINE_USE_VORBILD_FORMULIERUNGEN=1` | 3: Dossier-Daten-Injection in SECTION/REVISION | <siehe TUNING.md Final-Block> |
    | `PIPELINE_GEBER_ROUTING_V2=1` | 4: GUIDANCE_V2 geber-guidance.ts | <siehe TUNING.md Final-Block> |

    Aktuelle Production-Defaults: siehe `lib/wizard/config.ts`.

    ## Snapshot-Schema-Version

    Pipeline-Snapshots haben `meta.schemaVersion: 1` (Phase 5). Schema-Migration: bei breaking change Version inkrementieren — Eval-Skript verweigert Replay bei Mismatch (kein silent-migrate).

    ## Threats / Caveats

    - LLM-Judge (WIZ-03) hat ~5-10 % Score-Varianz trotz `temperature: 0` — Baseline-N=3 + 2σ-Toleranz faengt das ab
    - 0/11 Dossiers haben aktuell maxZeichen gesetzt → WIZ-01 ist Pflichtabschnitt-Coverage-only (siehe dossier-coverage-baseline.md)
    - 2/11 Dossiers haben vorbildFormulierungen → Hebel 3 wirkt nur fuer aktion-mensch + kultur-macht-stark
    ```

    **CLAUDE.md Eval-Sektion ergänzen** (zwischen "Wichtige Pfade" und "Lokale Dev-Umgebung"):

    ```markdown
    ## Pipeline-Eval (Phase 5)

    Vor Aenderungen an `lib/wizard/**` oder `data/richtlinien/**`: Pipeline-Eval als PR-Pflicht-Vorabcheck.

    ```bash
    # Lokal Replay (kein LLM-Cost):
    npx tsx scripts/eval-pipeline.ts --replay data/eval/pipeline-snapshots/baseline --md-summary

    # Lokal Live (~3-4 EUR):
    npx tsx scripts/eval-pipeline.ts --live --N=3 --snapshot --md-summary
    ```

    Details: [data/eval/README.md](data/eval/README.md). Threshold-Gate-Schwellwerte: [data/eval/BASELINE.md](data/eval/BASELINE.md). Tuning-Historie: [data/eval/TUNING.md](data/eval/TUNING.md).

    CI-Workflow: `.github/workflows/pipeline-eval.yml` — laeuft automatisch bei PRs gegen `lib/wizard/**` / `data/richtlinien/**`.
    ```

    **.planning/codebase/STACK.md Eval-Apparat als Komponente** (in passende Sektion einfuegen — typischerweise „Tooling" oder „Scripts"):

    ```markdown
    ### Eval-Apparat (Phase 1 + Phase 5)

    | Komponente | Pfad | Phase | Zweck |
    |------------|------|-------|-------|
    | scripts/eval-matcher.ts | scripts/ | 1 | Matcher-Eval, Recall@3 + Off-Target + Clarif-Precision |
    | scripts/eval-pipeline.ts | scripts/ | 5 | Pipeline-Eval, WIZ-01/-02/-03 + Finanzplan-Sub |
    | lib/wizard/geber-classification.ts | lib/wizard/ | 5 | Programm-ID → 4-5 strategische Geber-Cluster |
    | lib/wizard/config.ts | lib/wizard/ | 5 | 4 Feature-Flags fuer Tuning-Hebel |
    | data/eval/ | data/ | 1+5 | Korpora, Snapshots, Reports, BASELINE, TUNING |
    | .github/workflows/pipeline-eval.yml | .github/workflows/ | 5 | CI-Threshold-Gate fuer PRs |
    ```

    **Conventional Commit:** `docs(05): wave-4 eval-doku (data/eval/README.md, CLAUDE.md eval-sektion, STACK.md eval-apparat, D-35)`
  </action>
  <verify>
    <automated>test -f data/eval/README.md && wc -l data/eval/README.md | awk '{ if ($1 < 80) { print "README zu kurz: " $1; exit 1 } }' && grep -q "Threshold-Gate" data/eval/README.md && grep -q "PIPELINE_SHARP_PROMPTS" data/eval/README.md && grep -q "pipeline-snapshots/baseline" data/eval/README.md && grep -q "Pipeline-Eval" CLAUDE.md && grep -q "eval-pipeline.ts" CLAUDE.md && grep -q "eval-pipeline" .planning/codebase/STACK.md && grep -q "geber-classification" .planning/codebase/STACK.md</automated>
  </verify>
  <acceptance_criteria>
    - `data/eval/README.md` existiert, ≥ 80 Zeilen, dokumentiert: Strukturen, Aufruf-Konventionen, Threshold-Gate, Korpus-Update-Workflow, Feature-Flags, Schema-Version, Caveats
    - `CLAUDE.md` hat neuen Block "Pipeline-Eval (Phase 5)" mit Quick-Start-Commands + Links
    - `.planning/codebase/STACK.md` hat Eval-Apparat-Komponententabelle mit 6 Einträgen
  </acceptance_criteria>
  <done>Eval-Apparat ist dokumentiert. Künftige Claude-Sessions verstehen aus CLAUDE.md/README.md den PR-Workflow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pre-Closure-Live-Smoke mit nicht-Korpus-Anliegen (D-36)</name>
  <files>data/eval/pipeline-snapshots</files>
  <read_first>
    - data/eval/pipeline-korpus.json (welche Anliegen sind schon im Korpus, damit das Smoke-Anliegen davon abweicht)
    - data/foerderprogramme.json (Programm-ID-Lookup)
    - scripts/eval-pipeline.ts (--live --N=1 --single-Aufruf)
  </read_first>
  <what-built>
    **Pre-Closure-Smoke (Anti-Overfitting nach D-36):**

    1. Definiere ein neues, nicht-Korpus-Anliegen — z.B. eine kurze realistische Schul-Anliegen-Beschreibung mit programmId, das im aktuellen pipeline-korpus.json NICHT vorkommt. Wähle eine Programm-Domäne, die im Korpus nur schwach vertreten ist (z.B. ferry-porsche-challenge-2025 oder klimalab-2026).

    2. Erweitere `data/eval/pipeline-korpus.json` temporär (ODER nutze ein dediziertes `data/eval/pipeline-korpus-smoke.json` als One-Shot-Datei). **Empfehlung:** Eintrag temporär anhängen mit ID `pv-smoke-<datum>`, später entfernen — Korpus-Versionierung bleibt sauber, oder nutze ein separate `--single`-fähiges Mini-Korpus-File.

       Einfachste Lösung: Lege `data/eval/pipeline-korpus-smoke.json` mit 1 Eintrag an (Pattern wie pipeline-korpus.json), füge Skript-Flag `--korpus <path>` falls noch nicht vorhanden ODER nutze Inline-CLI-Argument. **Discretion:** Wenn eval-pipeline.ts kein --korpus-Flag hat: einfach temporär an pipeline-korpus.json anhängen, nach Smoke wieder entfernen (nicht committen).

    3. Live-Smoke-Run:
       ```bash
       PIPELINE_SHARP_PROMPTS=1 PIPELINE_COMPLIANCE_STAGE=1 PIPELINE_USE_VORBILD_FORMULIERUNGEN=1 PIPELINE_GEBER_ROUTING_V2=1 \
         npx tsx scripts/eval-pipeline.ts --live --N=1 --single pv-smoke-2026-05-XX --snapshot --md-summary
       ```
       (Alle 4 Hebel an — der Smoke testet das Production-Default-Ziel.)
       Cost: ~0.10-0.20 EUR.

    4. Output analysieren:
       - Snapshot-Datei unter `data/eval/pipeline-snapshots/<ISO>/pv-smoke-*-run1.json`
       - Report unter `data/eval/pipeline-reports/<ISO>.json`
       - WIZ-01/-02/-03/Finanzplan-Sub-Scores
       - Konkrete `sections[].text` und `finalText` im Snapshot — Kolja-Sichtprüfung

    **Conventional Commit nach Approval:** `test(05): wave-4 pre-closure live-smoke (anti-overfitting D-36, alle 4 hebel ON)`
  </what-built>
  <how-to-verify>
    **Kolja-Sichtprüfung des Smoke-Outputs:**

    1. Öffne die Snapshot-Datei `data/eval/pipeline-snapshots/<ISO>/pv-smoke-*-run1.json`
    2. Lies `result.artefacts.finalText` durch (kompletter Antrag-Text):
       - Halluzinationen? (Aktenzeichen, TV-L-Codes, erfundene Partner, präzise Daten ohne User-Beleg)
       - Tonalität passend zum Geber-Cluster?
       - Pflichtabschnitte-Coverage?
       - Lücken ehrlich markiert?
    3. Vergleiche Scores aus Report-JSON mit Baseline:
       - WIZ-01 ≥ 80 % ist Schwellwert
       - WIZ-02 sollte gegenüber Baseline reduziert sein (mind. 50 %)
       - WIZ-03 Score-Wert plausibel
    4. UI-Side-Effect-Check: keine compliance-check-Stage im GeneratingProgress sichtbar (silent stage)
       - Nur relevant wenn lokaler Dev-Server läuft; sonst entfällt

    Antworte mit `approved` wenn:
    - Antrag-Text qualitativ akzeptabel
    - Keine harten Halluzinationen
    - Scores im erwarteten Bereich

    Antworte mit `revise` + Issue-Liste sonst → Plan 05-09 (2. Tuning-Welle, D-23) wird angestoßen.
  </how-to-verify>
  <resume-signal>Antworte: `approved` (Phase-5-Closure möglich) ODER `revise` (Plan 05-09 anstoßen mit konkretem Befund-Set).</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Default-Hebel-Entscheidung + TUNING.md-Final-Block + ggf. lib/wizard/config.ts Default-Update</name>
  <files>data/eval/TUNING.md, lib/wizard/config.ts</files>
  <read_first>
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-1-3.md (intermediate aus Plan 05-05)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-2.md (intermediate aus Plan 05-06)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-4.md (intermediate aus Plan 05-07)
    - data/eval/BASELINE.md (Phase-5-Baseline-Werte)
    - lib/wizard/config.ts (aktuelle Defaults — sollen OFF stehen aus Plan 05-03)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md (D-19 Schwellwerte, D-23 Eskalation)
  </read_first>
  <decision>
    **Default-Hebel-Entscheidung Phase 5:**

    Welche Hebel-Kombination wird Production-Default in `lib/wizard/config.ts`?
  </decision>
  <context>
    Wave 3 hat 4 Hebel einzeln + Kombinationen eval-gemessen (Plan 05-05/-06/-07 TUNING.md-Eintraege). Plan 05-08 entscheidet auf Basis der gemessenen Deltas welche Hebel als Production-Default ON kommen.

    Heuristik:
    - Hebel mit Delta > 2σ pro Achse und ohne negative Seiten-Effekte: ON
    - Hebel mit Delta < 2σ oder negativen Seiten-Effekten: bleibt OFF (Reserve fuer Wave 5 / Phase 5.1)
    - Hebel-Kombination 1+3 hat in Plan 05-05 spezifischen Cross-Hebel-Test → Befund-basiert entscheiden

    Wenn ALLE Hebel Delta < 2σ → Plan 05-09 anstoßen (D-23 Eskalation, 2. Tuning-Welle).
  </context>
  <options>
    <option id="default-all-on">
      <name>Alle 4 Hebel ON</name>
      <pros>Maximales Tuning, eval-gemessenes Maximum-Delta wenn alle Hebel additive wirken</pros>
      <cons>Wenn Hebel-Kombination Side-Effects hat, kann Production-Default suboptimal sein; höhere Token-Kosten pro Antrag</cons>
    </option>
    <option id="default-selective">
      <name>Selektive Hebel ON (datengetrieben aus TUNING.md)</name>
      <pros>Nur die Hebel mit messbarem Delta + ohne Side-Effects werden Default; klare Datengrundlage</pros>
      <cons>Erfordert eindeutiges Lesen der TUNING.md-Daten — bei knapp-2σ-Werten ist Entscheidung subjektiv</cons>
    </option>
    <option id="default-keep-off-plus-509">
      <name>Defaults bleiben OFF + Plan 05-09 2. Tuning-Welle</name>
      <pros>Wenn Wave-3-Deltas alle < 2σ oder Phase-5-Schwellwerte nicht erreicht: D-23 Eskalation</pros>
      <cons>Phase-5-Closure verzögert; weitere Cost für 2. Tuning-Welle</cons>
    </option>
  </options>
  <action>
    Nach Kolja-Decision:

    **Schritt 0 — Konsolidierung der Wave-3-Hebel-Bloecke nach data/eval/TUNING.md:**

    Wave 3 schreibt pro Hebel ein intermediate-Markdown unter `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-{1-3,2,4}.md` (Files-Modified-Overlap-Vermeidung; Bindestrich statt Plus im Dateinamen — Info 12). Plan 05-08 konsolidiert diese in die kanonische `data/eval/TUNING.md` als Append-only-Datei:

    ```bash
    PHASE_DIR=.planning/phases/05-wizard-pipeline-tuning-ux-l-cke
    # data/eval/TUNING.md initial anlegen wenn nicht vorhanden, danach 3 Hebel-Bloecke anhaengen
    if [ ! -f data/eval/TUNING.md ]; then
      printf "%s

%s

" "# Phase 5 Pipeline-Tuning Playbook" "> Append-only History pro Tuning-Iteration. Jeder Wave-3-Hebel-Block aus tuning-hebel-{1-3,2,4}.md konsolidiert. Default-Decision-Block am Ende." > data/eval/TUNING.md
    fi
    cat "$PHASE_DIR/tuning-hebel-1-3.md" >> data/eval/TUNING.md
    cat "$PHASE_DIR/tuning-hebel-2.md" >> data/eval/TUNING.md
    cat "$PHASE_DIR/tuning-hebel-4.md" >> data/eval/TUNING.md
    ```

    Nach Konsolidierung: TUNING.md enthaelt chronologisch alle 4 Wave-3-Hebel-Bloecke + den Final-Decision-Block aus diesem Task 4 (siehe unten).

        **Variante default-all-on:** Edit lib/wizard/config.ts — setze alle 4 Defaults auf `true`:
    ```typescript
    useVorbildFormulierungen: parseEnvBool(process.env.PIPELINE_USE_VORBILD_FORMULIERUNGEN ?? "true"),
    complianceStageEnabled: parseEnvBool(process.env.PIPELINE_COMPLIANCE_STAGE ?? "true"),
    sharpPrompts: parseEnvBool(process.env.PIPELINE_SHARP_PROMPTS ?? "true"),
    geberRoutingV2: parseEnvBool(process.env.PIPELINE_GEBER_ROUTING_V2 ?? "true"),
    ```

    **Variante default-selective:** Edit lib/wizard/config.ts entsprechend Approval (z.B. nur sharpPrompts + geberRoutingV2 ON).

    **Variante default-keep-off-plus-509:** config.ts unverändert; lege `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-09-PLAN.md` als Skeleton an (oder dokumentiere in TUNING.md das Plan 05-09 anzustoßen ist).

    **In jedem Fall — TUNING.md Final-Block anhängen:**

    ```markdown
    ---

    ## <ISO-Date> — Phase-5-Closure: Default-Hebel-Entscheidung

    **Datenlage (aus Wave-3-Eval-Runs):**

    | Hebel | Delta WIZ-01 | Delta WIZ-02 | Delta WIZ-03 | Side-Effects | Empfehlung |
    |-------|--------------|--------------|--------------|--------------|------------|
    | 1 Sharp-Prompts | +X.X | +X.X | +X.X | <ja/nein> | <ON/OFF> |
    | 2 Compliance-Stage | +X.X | +X.X | +X.X | <ja/nein> | <ON/OFF> |
    | 3 Dossier-Injection | +X.X | +X.X | +X.X | <nur 2/11 Dossiers> | <ON/OFF> |
    | 4 Geber-Routing V2 | +X.X | +X.X | +X.X | <ja/nein> | <ON/OFF> |

    **Decision:** <Variante A/B/C aus Decision-Checkpoint>

    **Begründung:** <1-3 Sätze>

    **Production-Default in lib/wizard/config.ts:**
    - useVorbildFormulierungen: <true/false>
    - complianceStageEnabled: <true/false>
    - sharpPrompts: <true/false>
    - geberRoutingV2: <true/false>

    **Phase-5-Schwellwert-Status (D-19):**
    - WIZ-01 ≥ 80 %: <erreicht/nicht erreicht — aktuelles Mean: XX.X>
    - WIZ-02 ≥ 50 % Marker-Reduktion: <erreicht/nicht erreicht — aktuelles Delta: X.X>
    - WIZ-03 Score-Delta > 0 pro Cluster: <erreicht in N/5 Clustern>

    **Closure-Status:**
    - <Phase-5-Closure möglich> ODER
    - <Plan 05-09 (D-23 2. Tuning-Welle) angestoßen für: WIZ-01-Gap / WIZ-02-Gap / WIZ-03-Cluster-X>
    ```

    **Optional — Eskalations-Skeleton (Info 11):** Bei Variante `default-keep-off-plus-509` lege `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-09-ESCALATION-TEMPLATE.md` an als minimales Skeleton:

    ```markdown
    ---
    phase: 05-wizard-pipeline-tuning-ux-l-cke
    plan: 09
    type: execute
    wave: 5
    depends_on: [08]
    status: draft
    ---

    # Plan 05-09 — Eskalation 2. Tuning-Welle (D-23)

    ## Trigger

    [Aus Plan 05-08 Task 4 — welche Gaps konkret? WIZ-01-Mean unter 80 %? WIZ-02-Marker-Reduktion unter 50 %? WIZ-03-Cluster-X?]

    ## Hebel-Kandidaten 2. Welle

    - [ ] Hebel 5: ... (TBD nach Datenlage)
    - [ ] Hebel 6: ... (TBD nach Datenlage)

    ## Cost-Budget

    Soft-Cap 5 EUR fuer 2. Tuning-Welle (vor erneuter Eskalation).

    ## Decision-Punkt

    Falls 2. Welle auch keine 2σ-Deltas liefert: Schwellwerte D-19 ggf. nachverhandeln (Kolja-Decision).
    ```

    Wenn Variante `default-all-on` oder `default-selective`: Skeleton NICHT anlegen — Phase 5 ist abgeschlossen.

    **Conventional Commits (2-3 Commits, atomic):**
    1. `feat(05): wave-4 production-default-hebel (config.ts updates per kolja-decision)`
    2. `docs(05): wave-4 closure-block in TUNING.md (default-decision + schwellwert-status)`
    3. (Optional, nur bei `default-keep-off-plus-509`) `docs(05): plan 05-09 escalation skeleton (D-23 2. tuning-welle trigger)`
  </action>
  <resume-signal>Antworte: `default-all-on` ODER `default-selective: [Hebel-Liste]` ODER `default-keep-off-plus-509: [Begründung welche Gaps Plan 05-09 angeht]`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions Runner → Repo | Workflow läuft mit GITHUB_TOKEN (eingebaut, nicht extra konfigurieren), Secret-Pre-Flight prüft DEEPSEEK_API_KEY |
| Default-Hebel-Entscheidung → Production-Pipeline | Default-Switch wirkt auf alle künftigen Wizard-Runs in Production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08-01 | Tampering | Workflow-Injection ueber workflow_dispatch-Input mode | medium | mitigate | env-Mapping enforced, niemals direkte `${{ inputs.X }}`-Interpolation in `run:`-Shells (Phase-4-Pattern) |
| T-05-08-02 | DoS | --live in workflow_dispatch verursacht 3-4 EUR Cost pro Run | medium | mitigate | manual-dispatch only, default ist replay; Acceptance-Test prüft default = replay |
| T-05-08-03 | Information Disclosure | DEEPSEEK_API_KEY-Leak in Workflow-Logs durch shell-Echo | high | mitigate | env-Mapping verhindert das; Secret-Pre-Flight prüft Existenz ohne Value zu echoen |
| T-05-08-04 | Spoofing | PR-Author umgeht CI-Gate durch Pfad-Manipulation (Aenderung an lib/wizard ohne Workflow-Trigger) | medium | mitigate | paths-Filter im workflow umfasst alle relevanten Pfade; Branch-Protection-Rule muss diesen Check als required ergaenzen (Folge-Konfig, nicht Workflow-File) |
| T-05-08-05 | Tampering | Default-Hebel-ON ohne Re-Baseline → bestehende Baseline matched nicht mehr Production-Verhalten | high | mitigate | Wenn Defaults auf ON: erfordert neuen Baseline-Run mit Default-Config ON (BASELINE.md neuer Eintrag); Plan 05-08 Task 4 dokumentiert das in Closure-Block |
| T-05-08-06 | Repudiation | Pre-Closure-Smoke-Befund verloren (kein dokumentierter Test) | low | mitigate | Snapshot des Smoke-Runs bleibt in pipeline-snapshots/, Pfad in TUNING.md-Closure-Block verlinkt |
| T-05-08-07 | DoS | CI-Workflow läuft bei jedem PR ohne Cost-Limit (replay-Modus ist immerhin LLM-frei) | low | accept | replay-Default ist LLM-frei; nur CPU+Disk-Time, GitHub-Free-Quota deckt das |
</threat_model>

<verification>
- `.github/workflows/pipeline-eval.yml` YAML-valide, alle Hardening-Patterns vorhanden
- `data/eval/README.md` ≥ 80 Zeilen, alle Sektionen
- `CLAUDE.md` Eval-Sektion + Links
- `.planning/codebase/STACK.md` Eval-Apparat-Tabelle
- Pre-Closure-Smoke gefahren + Kolja-`approved`
- TUNING.md Closure-Block mit Default-Decision + Schwellwert-Status
- ggf. lib/wizard/config.ts Defaults updated
</verification>

<success_criteria>
- CI-Workflow operativ (kann lokal nicht getestet werden — GitHub-Side-Check)
- 4 Doku-Outputs liegen vor
- Pre-Closure-Smoke approved oder Plan 05-09 angestoßen
- Default-Hebel-Decision dokumentiert
- 4 Tasks + ≥ 4 Conventional Commits
- Phase-5-Closure-Status klar: abgeschlossen ODER Plan 05-09 angestoßen
</success_criteria>

<output>
Nach Completion: `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-08-SUMMARY.md` mit:
- CI-Workflow-Summary: PR-Trigger-Pfade + Threshold-Gate-Logik
- Doku-Summary: was wurde wo dokumentiert
- Pre-Closure-Smoke-Befund (Score-Werte + Kolja-Approval)
- Default-Hebel-Entscheidung: welche Variante + Begründung + Production-Defaults
- Phase-5-Schwellwert-Status pro Achse
- Closure-Status: Phase abgeschlossen ODER Plan 05-09 mit konkretem Scope
</output>

---
phase: 05-wizard-pipeline-tuning-ux-l-cke
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - scripts/eval-pipeline.ts
  - scripts/eval-pipeline-internals.ts
  - data/eval/BASELINE.md
  - data/eval/pipeline-snapshots/baseline
  - data/eval/pipeline-reports
  - .gitignore
  - __tests__/eval/pipeline-fk-match.test.ts
  - __tests__/eval/pipeline-marker-detection.test.ts
  - __tests__/eval/pipeline-regex-detection.test.ts
  - __tests__/eval/pipeline-judge-rubric.test.ts
  - __tests__/eval/pipeline-finanzplan-sub.test.ts
  - __tests__/eval/pipeline-determinism.test.ts
  - __tests__/eval/pipeline-snapshot-replay.test.ts
  - __tests__/eval/pipeline-aggregation.test.ts
  - __tests__/eval/pipeline-gate.test.ts
  - __tests__/eval/fixtures/llm-stubs.ts
  - __tests__/fixtures/pipeline-snapshot-borsigwalder.json
autonomous: false
requirements: [WIZ-01, WIZ-02, WIZ-03]
tags: [phase-5, wave-2, eval-skript, baseline, n3-run, cost-eskalation, D-13, D-16]
must_haves:
  truths:
    - "scripts/eval-pipeline.ts existiert und kann --replay + --live + --N + --deep + --pro-judge + --md-summary + --snapshot Flags parsen (D-07/-09/-15/-16)"
    - "Eval-Skript ruft runPipeline() pro Korpus-Eintrag mit pre-extrahierten WizardFacts auf (D-06)"
    - "WIZ-01-Score: scoreWiz01() FK-Match auf antragsstruktur.abschnitte[].name + optional maxZeichen-Check (RESEARCH Code Z.846-906, Pitfall 6 normalizeAbschnittName)"
    - "WIZ-02-Score: scoreWiz02() 2-Layer (Marker + Regex mit user/facts-Cross-Check, RESEARCH Z.923-988)"
    - "WIZ-03-Score: scoreWiz03() LLM-as-Judge mit 5 Rubrics pro Geber-Cluster (D-10, RESEARCH Z.502-558), temperature: 0 + response_format JSON-object"
    - "Finanzplan-Sub-Score: scoreFinanzplan() ruft validateFinanzplan() + hallu-marker-in-finanzplan-Counter (D-11)"
    - "Snapshot-Schema-Version 1, Replay-Modus weigert sich bei Mismatch (Pitfall 3)"
    - "Threshold-Gate achsen-spezifisch (D-25): WIZ-01 hart, WIZ-02 mittel, WIZ-03 warning-only"
    - "Baseline-N=3-Run gegen alle 22 Korpus-Einträge gefahren, ~3-4 EUR Live-Cost"
    - "data/eval/BASELINE.md hat Phase-5-Eintrag mit mean+stddev pro WIZ-Achse + Per-Geber-Gruppe-Breakdown + Per-Dossier-Tabelle + Methodik-Hinweis maxZeichen-only-coverage (Pitfall 7)"
    - "data/eval/pipeline-snapshots/baseline/ Verzeichnis ist version-controlled (force-committed analog Phase 1 T-01-09)"
    - "data/eval/pipeline-snapshots/ (außer baseline) + data/eval/pipeline-reports/ sind in .gitignore"
    - "Alle 9 Wave-0-Score-Logik-Tests sind von it.todo auf lebende Tests umgestellt + grün"
  artifacts:
    - path: "scripts/eval-pipeline.ts"
      provides: "Pipeline-Eval-CLI mit --live/--replay/--N/--deep/--pro-judge/--md-summary"
      contains: "runPipeline"
      min_lines: 900
    - path: "data/eval/BASELINE.md"
      provides: "Phase-5-Baseline-Eintrag mit mean+stddev"
      contains: "Phase-5"
    - path: "data/eval/pipeline-snapshots/baseline"
      provides: "Versionierte Baseline-Snapshots als Replay-Anker"
      contains: "schemaVersion"
    - path: ".gitignore"
      provides: "Snapshot/Report-Pfade gitignored (außer baseline)"
      contains: "pipeline-snapshots"
  key_links:
    - from: "scripts/eval-pipeline.ts"
      to: "lib/wizard/pipeline.ts"
      via: "Import runPipeline für Live-Modus"
      pattern: "import.*runPipeline"
    - from: "scripts/eval-pipeline.ts"
      to: "lib/wizard/geber-classification.ts"
      via: "Import getGeberGruppe für Per-Geber-Aggregation"
      pattern: "import.*geber-classification"
    - from: "scripts/eval-pipeline.ts"
      to: "lib/wizard/finanzplan-validator.ts"
      via: "Import validateFinanzplan für Sub-Metrik"
      pattern: "import.*validateFinanzplan"
    - from: "scripts/eval-pipeline.ts"
      to: "data/eval/pipeline-korpus.json"
      via: "readFile + Top-Level-JSON-Array-Parse"
      pattern: "pipeline-korpus.json"
    - from: "data/eval/BASELINE.md"
      to: "data/eval/pipeline-snapshots/baseline/"
      via: "BASELINE-Eintrag verweist auf Snapshot-Verzeichnis als Replay-Anker"
      pattern: "pipeline-snapshots"
---

<objective>
Wave 2 (eine Plan, drei Tasks): `scripts/eval-pipeline.ts` als 1-zu-1-Spiegel von `scripts/eval-matcher.ts` (Phase-1-Pattern) bauen + Baseline-N=3-Run + BASELINE.md-Eintrag.

Purpose: Das Eval-Skript ist die Mess-Infrastruktur für alle Wave-3-Tuning-Iterationen. Ohne dieses Skript gibt es keinen Baseline-Anker und Wave 3 kann Delta nicht messen. Plan 05-02 (Korpus) + Plan 05-03 (geber-classification + config) sind Dependencies — beide müssen abgeschlossen sein.

Output:
- `scripts/eval-pipeline-internals.ts` (~500 LOC) — testbare Pure-Function-Library (Task 1a)
- `scripts/eval-pipeline.ts` (~550 LOC) — CLI-Entry mit parseFlags/Snapshot/Replay/Aggregation/Reports (Task 1a Skelett + Task 1b ausgebaut)
- 9 Test-Skelette aus Wave 0 → lebende Tests grün (Task 1c, ≥ 40 Tests total)
- `data/eval/BASELINE.md` Phase-5-Block (append-only, Task 5)
- `data/eval/pipeline-snapshots/baseline/` mit 22 × 3 = 66 Snapshot-Dateien (force-committed, Task 4 + 5)
- `.gitignore` ergänzt für pipeline-snapshots (außer baseline) + pipeline-reports (Task 5)

**Task-Split-Begründung (Warning 6 Resolution):** Task 1 war monolithisch ~900 LOC. Split in 1a (Pure-Function-Schicht + CLI-Stub) + 1b (Run/Snapshot/Aggregation/Reports) + 1c (Tests lebend + Fixtures) gibt jedem Task klar abgrenzbares Scope (~10-15% Context pro Task) und atomare Commits — sauberer als monolithisch-mit-Hinweis.

**Autonomous: false** — Baseline-N=3-Run verursacht ~3-4 EUR Live-LLM-Cost (D-01); Pre-Flight-Check braucht Kolja-Approval auf DeepSeek-Balance.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-PATTERNS.md
@scripts/eval-matcher.ts
@lib/wizard/pipeline.ts
@lib/wizard/types.ts
@lib/wizard/llm.ts
@lib/wizard/pricing.ts
@lib/wizard/finanzplan-validator.ts
@lib/wizard/richtlinien-loader.ts
@lib/wizard/richtlinien-schema.ts
@lib/wizard/geber-classification.ts
@data/eval/BASELINE.md
@data/eval/pipeline-korpus.json
@data/eval/dossier-coverage-baseline.md

<interfaces>
<!-- Aus RESEARCH §Pattern + PATTERNS §1 — Eval-Skript baut auf existierenden APIs auf -->

From lib/wizard/pipeline.ts:
- `export async function runPipeline(input: PipelineInput): Promise<PipelineResult>`
- `PipelineInput = { programm: Foerderprogramm, facts: WizardFacts, richtlinie: Richtlinie | null, messages: WizardMessage[], onEvent?: (event) => void }`
- `PipelineResult = { artefacts: GenerationArtefacts, usages: PipelineUsage[] }`

From lib/wizard/types.ts GenerationArtefacts:
- `sections: Array<{ name: string, text: string }>`
- `finalText: string | null`
- `finanzplan?: Finanzplan` (mit `posten: Array<{ bezeichnung, begruendung, ... }>`)
- `stage: PipelineStage`

From scripts/eval-matcher.ts (1-zu-1-Vorbild, RESEARCH+PATTERNS-Verweise auf Z.30-43, Z.150-183, Z.195-255, Z.283-330, Z.427-513, Z.540-585, Z.685-723, Z.744-787, Z.790-836, Z.838-840):
- REPO-Konst + LOG_PREFIX
- parseFlags mit exit 2 disziplin
- loadKorpusAndValidate
- migrateOldSnapshot / Schema-Version-Check
- aggregate
- Konsolen-Tabelle + writeReport (JSON + MD)
- D-16-Threshold-Gate mit process.exit(1)
- main().catch crash-handler

From RESEARCH Code-Examples (Z.820-1083):
- scoreWiz01 (Z.846-906)
- scoreWiz02 (Z.923-988)
- scoreFinanzplan (Z.1015-1042)
- aggregateNRuns + passesThreshold (Z.1055-1083)

From CONTEXT D-15 / RESEARCH §WIZ-03 Z.499-617:
- JudgeRubric-Type + JUDGE_SYSTEM-Prompt + 5 Rubric-Definitionen (RUBRIC_OEFFENTLICH, RUBRIC_STIFTUNG, RUBRIC_EU, RUBRIC_WIRTSCHAFTSPREIS, RUBRIC_VERBAND_UNI)
- `temperature: 0` + `response_format: { type: "json_object" }`
</interfaces>

<discretion_decisions>
Vor Implementation gemäß CONTEXT.md `<discretion>` festlegen:
- **Snapshot-Dateinamenschema:** `<entry-id>-run<N>.json` (RESEARCH Open Q5 Empfehlung, da N=3)
- **Konsolen-Format:** Plain console.log (kein cli-table3) — konsistent mit eval-matcher.ts
- **Pipeline-Calls Parallelität:** sequenziell pro Eintrag (Quota-Risiko bei N=3 × 22 = 66 parallel)
- **Score-Range:** alle 4 Scores auf 0-100 normalisiert (RESEARCH Empfehlung Z.84)
- **TUNING.md-Format:** tabellarisch + Run-Memo-Block pro Iteration (RESEARCH Empfehlung Z.87) — wird in Plan 05-08 angelegt
</discretion_decisions>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1a: scripts/eval-pipeline.ts — Imports + Types + Constants + parseFlags + Score-Funktionen (~500 LOC)</name>
  <files>scripts/eval-pipeline.ts, scripts/eval-pipeline-internals.ts</files>
  <read_first>
    - scripts/eval-matcher.ts (1-zu-1-Vorbild — Imports/Types/Constants/parseFlags-Pattern lesen, Z.30-43, Z.150-183, Z.195-255, Z.283-330)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md (§Code-Examples Z.820-1083 für Score-Funktionen scoreWiz01/02/03/Finanzplan/aggregateNRuns/passesThreshold; §WIZ-03 Rubrics Z.513-557 + JUDGE_SYSTEM Z.573-597)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-PATTERNS.md (§1 Pattern-Verweise)
    - lib/wizard/pipeline.ts (runPipeline-API + PipelineInput/Result-Types)
    - lib/wizard/types.ts (GenerationArtefacts + PipelineStage + WizardFacts + WizardMessage)
    - lib/wizard/llm.ts (generateJson für Judge-Call)
    - lib/wizard/finanzplan-validator.ts (validateFinanzplan + ValidationResult)
    - lib/wizard/richtlinien-loader.ts (loadRichtlinie)
    - lib/wizard/geber-classification.ts (getGeberGruppe — NEU aus Plan 05-03)
    - lib/wizard/pricing.ts (CostLedger, emptyLedger, addUsage, formatEur)
  </read_first>
  <action>
    **Teil 1 von 3 — Pure-Function-Schicht + CLI-Stub:**

    Extrahiere Score-Funktionen + Constants in **`scripts/eval-pipeline-internals.ts`** (testbare Pure-Function-Library) und lege **`scripts/eval-pipeline.ts`** als CLI-Entry-Skelett an (Imports, parseFlags, main-Stub, Crash-Handler).

    **`scripts/eval-pipeline-internals.ts`** (~500 LOC):

    1. **Types** (PipelineKorpusEntry, PipelineSnapshot, EntryResult, AggregateMetrics, Wiz01Result, Wiz02Result, Wiz03Result, FinanzplanSubResult, ScoreStat, Flags) — Schema aus RESEARCH §Pattern 1 (Z.330-403)

    2. **Konstanten:**
       ```typescript
       export const SNAPSHOT_SCHEMA_VERSION = 1;
       export const LOG_PREFIX = "[eval-pipeline]";
       ```

    3. **HALLU_REGEX_PATTERNS** aus RESEARCH Z.454-465 (7 Patterns: aktenzeichen, tv_l_code, datum_praezise, haushaltsstelle, kmk_zitat, rahmenvertrag, mdm_loesung)

    4. **5 Judge-Rubrics** aus RESEARCH Z.513-557 + Tabelle Z.564-568:
       - RUBRIC_OEFFENTLICH, RUBRIC_STIFTUNG, RUBRIC_EU, RUBRIC_WIRTSCHAFTSPREIS, RUBRIC_VERBAND_UNI

    5. **JUDGE_SYSTEM**-Prompt (RESEARCH Z.573-597) + **buildJudgeUserPrompt** (Z.599-611)

    6. **Score-Funktionen** (alle exportierbar):
       - `scoreWiz01(artefacts, richtlinie): Wiz01Result` — RESEARCH Z.846-906 1-zu-1
       - `scoreWiz02(artefacts, expectedForbidden, userAnswers, facts): Wiz02Result` — RESEARCH Z.923-988
       - `scoreWiz03(finalText, gruppe, judgeModel): Promise<Wiz03Result>` — Judge-Call via generateJson mit `temperature: 0` + `response_format: { type: "json_object" }`
       - `scoreFinanzplan(finanzplan, richtlinie, halluMarkerInFinanzplan): FinanzplanSubResult` — RESEARCH Z.1015-1042
       - `aggregateNRuns(runs: number[]): ScoreStat` — RESEARCH Z.1055-1062 (Population-Stddev, nicht Sample)
       - `passesThreshold(current, baseline, axis): { passed, reason }` — RESEARCH Z.1065-1083 (D-25: WIZ-01 hart, WIZ-02 mittel, WIZ-03 warning-only)

    7. **Helper:** `normalizeAbschnittName(s: string): string` (lowercase + whitespace-trim — Pitfall 6).

    **`scripts/eval-pipeline.ts`** (CLI-Entry, ~150 LOC):

    1. **Imports** (1-zu-1 wie RESEARCH Z.30-43):
       - `runPipeline` aus `@/lib/wizard/pipeline`
       - `validateFinanzplan` aus `@/lib/wizard/finanzplan-validator`
       - `loadRichtlinie` aus `@/lib/wizard/richtlinien-loader`
       - `getGeberGruppe`, `ALL_GEBER_GRUPPEN` aus `@/lib/wizard/geber-classification`
       - `PIPELINE_CONFIG` aus `@/lib/wizard/config`
       - `generateJson` aus `@/lib/wizard/llm`
       - `CostLedger`, `emptyLedger`, `addUsage`, `formatEur` aus `@/lib/wizard/pricing`
       - Re-Exports aus `./eval-pipeline-internals` für Test-Importe

    2. **Pfad-Konstanten:**
       ```typescript
       const REPO = resolve(__dirname, "..");
       const KORPUS_PATH = resolve(REPO, "data/eval/pipeline-korpus.json");
       const REPORTS_DIR = resolve(REPO, "data/eval/pipeline-reports");
       const SNAPSHOTS_DIR_BASE = resolve(REPO, "data/eval/pipeline-snapshots");
       const RICHTLINIEN_DIR = resolve(REPO, "data/richtlinien");
       ```

    3. **parseFlags** mit allen Flags (RESEARCH-Konflikt-Detection für `--snapshot + --replay` = exit 2):
       - `--snapshot`, `--md-summary` (aus eval-matcher)
       - NEU: `--live` (erzwingt LLM-Calls)
       - NEU: `--N=3` (Anzahl Runs, default 1; akzeptiert nur 1-5, sonst exit 2 — T-05-04-01-Mitigation)
       - NEU: `--deep` (aktiviert WIZ-02 Layer 3 LLM-Judge)
       - NEU: `--pro-judge` (schaltet Judge auf deepseek-v4-pro)
       - NEU: `--replay <dir>` (Replay aus Snapshot-Verzeichnis)
       - NEU: `--single <entry-id>` (nur 1 Eintrag — für Pre-Closure-Smoke Plan 05-08)
       - Bei `--unknown-flag` / Argument-Fehlern: Usage-Ausgabe via stderr + `process.exit(2)`.

    4. **main()-Stub:**
       ```typescript
       async function main() {
         const flags = parseFlags(process.argv.slice(2));
         console.log(`${LOG_PREFIX} parsed flags:`, flags);
         // Snapshot/Replay/Aggregation/Reports werden in Task 1b ergaenzt
         throw new Error("TODO: Task 1b implementiert runForEntry/aggregate/writeReport");
       }
       main().catch((e) => { console.error(`${LOG_PREFIX} Crash:`, e); process.exit(1); });
       ```

    **Conventional Commit:** `feat(05): eval-pipeline-internals + cli-stub mit parseFlags + score-funktionen (D-07/-15, RESEARCH §Pattern 1-3)`
  </action>
  <verify>
    <automated>test -f scripts/eval-pipeline-internals.ts && test -f scripts/eval-pipeline.ts && grep -q "scoreWiz01" scripts/eval-pipeline-internals.ts && grep -q "scoreWiz02" scripts/eval-pipeline-internals.ts && grep -q "scoreWiz03" scripts/eval-pipeline-internals.ts && grep -q "scoreFinanzplan" scripts/eval-pipeline-internals.ts && grep -q "aggregateNRuns" scripts/eval-pipeline-internals.ts && grep -q "passesThreshold" scripts/eval-pipeline-internals.ts && grep -q "HALLU_REGEX_PATTERNS" scripts/eval-pipeline-internals.ts && grep -q "RUBRIC_OEFFENTLICH" scripts/eval-pipeline-internals.ts && grep -q "RUBRIC_STIFTUNG" scripts/eval-pipeline-internals.ts && grep -q "RUBRIC_EU" scripts/eval-pipeline-internals.ts && grep -q "RUBRIC_WIRTSCHAFTSPREIS" scripts/eval-pipeline-internals.ts && grep -q "RUBRIC_VERBAND_UNI" scripts/eval-pipeline-internals.ts && grep -q "SNAPSHOT_SCHEMA_VERSION" scripts/eval-pipeline-internals.ts && grep -q "parseFlags" scripts/eval-pipeline.ts && npx tsc --noEmit 2>&1 | tee /tmp/tsc-1a.out && ! grep -q "scripts/eval-pipeline.*error TS" /tmp/tsc-1a.out && bash -c 'npx tsx scripts/eval-pipeline.ts --unknown-flag > /tmp/eval-pipeline-1a.out 2>&1 ; ec=$? ; head -5 /tmp/eval-pipeline-1a.out ; test "$ec" -eq 2'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/eval-pipeline-internals.ts` existiert mit allen 6 Score-Funktionen + 7 HALLU_REGEX + 5 Rubrics + JUDGE_SYSTEM
    - `scripts/eval-pipeline.ts` existiert mit parseFlags + main-Stub + Crash-Handler
    - parseFlags akzeptiert alle Flags + lehnt unbekannte mit exit 2 ab
    - `--N` parser limitiert auf 1-5 (T-05-04-01-Mitigation)
    - `npx tsc --noEmit` ohne TS-Errors auf den 2 neuen Dateien
    - Test-Skelette aus Wave 0 koennen weiterhin als `it.todo()` laufen (kein Test-Code geaendert in Task 1a)
  </acceptance_criteria>
  <done>Pure-Function-Library + CLI-Skelett stehen. Score-Funktionen sind testbar (Tests werden in Task 1c lebend gemacht). Snapshot/Replay/Aggregation/Reports kommen in Task 1b.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1b: scripts/eval-pipeline.ts — Snapshot/Replay + runPipelineForKorpus + Aggregation + Reports + Threshold-Gate (~400 LOC)</name>
  <files>scripts/eval-pipeline.ts</files>
  <read_first>
    - scripts/eval-matcher.ts (Z.427-513 Snapshot/Replay-Pattern, Z.540-585 Soft-Failure pro Eintrag, Z.685-723 Aggregation, Z.744-787 writeReport, Z.790-836 Threshold-Gate)
    - scripts/eval-pipeline-internals.ts (aus Task 1a — Score-Funktionen + Constants verfuegbar)
    - scripts/eval-pipeline.ts (CLI-Stub aus Task 1a — wird hier ausgebaut)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md (§Code-Examples Z.1086-1142 BASELINE.md-Format als writeReport-MD-Vorbild)
    - lib/wizard/pipeline.ts (runPipeline-Aufruf-Signatur)
    - data/eval/pipeline-korpus.json (aus Plan 05-02 — Schema fuer loadKorpusAndValidate)
  </read_first>
  <action>
    **Teil 2 von 3 — Run-/Snapshot-/Aggregation-/Report-Schicht:**

    Ergaenze `scripts/eval-pipeline.ts` um die in Task 1a noch fehlenden Funktionen:

    1. **loadKorpusAndValidate** — Top-Level-Array-Parse, FK-Check `programmId` gegen `data/foerderprogramme.json`, Geber-Cluster-Check (5 zulaessige Werte aus ALL_GEBER_GRUPPEN), `expected_forbidden_markers[]`-Array-Check.

    2. **migrateOldSnapshot / loadReplaySnapshot** — Schema-Version-Check (Pitfall 3):
       ```typescript
       if (snap.meta?.schemaVersion !== SNAPSHOT_SCHEMA_VERSION) {
         console.error(`${LOG_PREFIX} Snapshot-Schema-Version-Mismatch...`); process.exit(2);
       }
       ```

    3. **runPipelineForKorpus(entry, runIndex): Promise<PipelineSnapshot>** — ruft `runPipeline()` mit pre-extrahierten facts + messages, erzeugt Snapshot. Pfad-Konvention: `<ISO>/<entry-id>-run<N>.json` (RESEARCH Open Q5).

    4. **evaluateEntry(entry, flags, runIndex): Promise<EntryResult>** — entweder Replay-Load oder Live-Run, dann Score-Berechnung pro Achse (scoreWiz01/02/03/Finanzplan). Soft-Failure pro Eintrag (errMsg in Report, kein Skript-Abbruch).

    5. **runForEntry(entry, flags): Promise<EntryResult mit N Sub-Results>** — N=1 default, N=3 fuer Baseline. Pipeline-Calls sind sequenziell pro Eintrag (Quota-Schutz, siehe Discretion-Decisions).

    6. **aggregate(results): AggregateMetrics** — mean+stddev pro Achse, Per-Geber-Gruppe-Breakdown (via getGeberGruppe), Per-Dossier-Breakdown.

    7. **writeReport (JSON + optional MD)** — RESEARCH Z.744-787 1-zu-1 angepasst fuer Pipeline-Achsen-Tabelle. JSON immer, MD bei `--md-summary`.

    8. **loadBaselineFromMd**: parses `data/eval/BASELINE.md` letzten Phase-5-Block (mean+stddev pro Achse). Beim ersten Baseline-Run gibt null zurueck (kein Gate-Check).

    9. **Threshold-Gate** in main() — RESEARCH Z.790-836 + D-25:
       ```typescript
       const baseline = await loadBaselineFromMd();
       if (baseline) {
         const gateW01 = passesThreshold(currentStat.wiz01, baseline.wiz01, "WIZ-01");
         const gateW02 = passesThreshold(currentStat.wiz02, baseline.wiz02, "WIZ-02");
         const gateW03 = passesThreshold(currentStat.wiz03, baseline.wiz03, "WIZ-03");
         if (!gateW01.passed || !gateW02.passed) {
           console.error("[GATE FAILED]"); process.exit(1);
         }
         // WIZ-03 nur core.notice/console.log, nie exit
       }
       ```

    10. **main()-Funktion** ausgebaut: Korpus laden + validieren → pro Eintrag runForEntry → aggregate → writeReport → Threshold-Gate-Check → console-Tabelle (Plain console.log, kein cli-table3).

    11. **Crash-Handler** bleibt aus Task 1a unveraendert.

    **Conventional Commit:** `feat(05): eval-pipeline run+snapshot+aggregation+threshold-gate (D-09/-16/-25, RESEARCH §Pattern 4-5)`
  </action>
  <verify>
    <automated>grep -q "loadKorpusAndValidate" scripts/eval-pipeline.ts && grep -q "loadReplaySnapshot" scripts/eval-pipeline.ts && grep -q "runPipelineForKorpus" scripts/eval-pipeline.ts && grep -q "evaluateEntry" scripts/eval-pipeline.ts && grep -q "runForEntry" scripts/eval-pipeline.ts && grep -q "aggregate" scripts/eval-pipeline.ts && grep -q "writeReport" scripts/eval-pipeline.ts && grep -q "loadBaselineFromMd" scripts/eval-pipeline.ts && grep -q "passesThreshold" scripts/eval-pipeline.ts && npx tsc --noEmit 2>&1 | tee /tmp/wave2-1b-tsc.out && ! grep -q "error TS" /tmp/wave2-1b-tsc.out && npx tsx scripts/eval-pipeline.ts --replay /tmp/nonexistent-dir > /tmp/eval-pipeline-1b.out 2>&1 ; ec=$? ; head -5 /tmp/eval-pipeline-1b.out ; test "$ec" -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - eval-pipeline.ts hat alle 11 oben aufgefuehrten Funktionen
    - `npx tsc --noEmit` clean
    - `npx tsx scripts/eval-pipeline.ts --replay /tmp/nonexistent-dir` exit 2 (sauberer Fehler bei nicht-existierendem Replay-Pfad)
    - Soft-Failure pro Eintrag bei runPipeline-Crash (errMsg in Report, kein Abbruch)
    - Threshold-Gate respektiert D-25 (WIZ-01 hart, WIZ-02 mittel, WIZ-03 warning-only)
  </acceptance_criteria>
  <done>Eval-Skript ist Code-Complete. CLI laeuft, ladet Korpus, ruft Score-Funktionen, aggregiert, schreibt Reports. Bereit fuer Test-Lebend in Task 1c und Live-Baseline-Run in Task 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1c: 9 Test-Skelette → lebende Tests + 2 Fixture-Dateien füllen</name>
  <files>__tests__/eval/pipeline-fk-match.test.ts, __tests__/eval/pipeline-marker-detection.test.ts, __tests__/eval/pipeline-regex-detection.test.ts, __tests__/eval/pipeline-judge-rubric.test.ts, __tests__/eval/pipeline-finanzplan-sub.test.ts, __tests__/eval/pipeline-determinism.test.ts, __tests__/eval/pipeline-snapshot-replay.test.ts, __tests__/eval/pipeline-aggregation.test.ts, __tests__/eval/pipeline-gate.test.ts, __tests__/eval/fixtures/llm-stubs.ts, __tests__/fixtures/pipeline-snapshot-borsigwalder.json</files>
  <read_first>
    - scripts/eval-pipeline-internals.ts (Score-Funktionen aus Task 1a)
    - scripts/eval-pipeline.ts (Snapshot/Replay aus Task 1b)
    - __tests__/lib/wizard/pipeline-events.test.ts (jest.mock-Pattern für LLM-Stub)
    - __tests__/lib/wizard/facts-extractor.test.ts (pure-function-Test-Pattern)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-VALIDATION.md (Per-Task Verification Map)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md (§Validation Z.1326-1466 für Test-Casework-Details)
  </read_first>
  <behavior>
    Score-Logik-Tests (jeweils aus VALIDATION.md Wave-0-Requirements + RESEARCH §Validation Architecture Z.1326-1466):

    **pipeline-fk-match.test.ts (WIZ-01, 7 Tests):**
    - alle Pflicht-Abschnitte vorhanden → coveragePercent=100
    - 3 von 5 Pflicht-Abschnitten vorhanden → coveragePercent=60
    - normalizeAbschnittName: case-insensitive + whitespace-trim
    - Dossier ohne abschnitte → coveragePercent=100 trivial
    - maxZeichen-Violation triggert
    - maxZeichen nicht im Dossier → maxZeichenOK=null
    - Pflicht=false ist nicht in Pflicht-Abschnitten

    **pipeline-marker-detection.test.ts (WIZ-02 L1, 5 Tests):**
    - 'Az 123/2026' im finalText → 1 Hit
    - Marker in artefacts.sections[].text → Hit
    - Marker in finanzplan-Begründung → Hit (multi-source haystack, RESEARCH Pitfall 1+2)
    - case-insensitive Match
    - expected_forbidden=[] → score=100

    **pipeline-regex-detection.test.ts (WIZ-02 L2, 6 Tests):**
    - HALLU_REGEX_PATTERNS.aktenzeichen matcht 'Az 123/2026'
    - tv_l_code matcht 'TV-L E9'
    - datum_praezise matcht '12.12.2025'
    - User-stated → falsePositiveCheck="user-stated"
    - Facts-stated → falsePositiveCheck="facts-stated"
    - Neither → "neither" + Penalty -10

    **pipeline-judge-rubric.test.ts (WIZ-03, 4 Tests, mit jest.mock("@/lib/wizard/llm")):**
    - Judge-JSON-Response ist valides Schema
    - Gewichteter Gesamt-Score korrekt berechnet
    - Rubric-Lookup pro Cluster (5 Cluster)
    - LLM-Stub-Antwort via STUB_JUDGE_RESPONSE_OEFFENTLICH

    **pipeline-finanzplan-sub.test.ts (4 Tests):**
    - okFuerFreigabe=true + 0 errors → score=100
    - 1 error → score=80
    - missing finanzplan → score=0
    - hallu-marker in finanzplan → -5 Penalty pro Marker

    **pipeline-determinism.test.ts (4 Tests):**
    - aggregateNRuns([85, 85.5, 84.5]) → mean ~85 stddev < 1
    - aggregateNRuns([]) → mean=0 stddev=0
    - aggregateNRuns([60, 75, 90]) → stddev > 10
    - Population-Stddev (N) nicht Sample (N-1) verifiziert

    **pipeline-snapshot-replay.test.ts (3 Tests):**
    - Schema-Version-1-Check enforced
    - Snapshot-Pfad `<ISO>/<entry-id>-run<N>.json`
    - 5× scoreWiz01 mit gleichem Snapshot → identische Scores

    **pipeline-aggregation.test.ts (3 Tests):**
    - aktion-mensch-Eintrag in 'stiftung'-Cluster aggregiert
    - per-Dossier-Breakdown korrekt
    - Cluster-Counter n korrekt

    **pipeline-gate.test.ts (4 Tests):**
    - WIZ-01 hart: drop > 2σ → fail
    - WIZ-02 mittel: drop > 2σ + 10 % baseline → fail
    - WIZ-03 warning-only: immer pass
    - stddev=0 → kein Crash (Division durch 0)
  </behavior>
  <action>
    **9 Test-Dateien auf lebend umstellen:**

    Pro Datei: `it.todo()` durch `it()` ersetzen, Score-Funktionen aus `@/scripts/eval-pipeline-internals` importieren, Assertions gegen Behavior-Liste ausarbeiten.

    Mock-Pattern (judge-rubric.test.ts):
    ```typescript
    jest.mock("@/lib/wizard/llm", () => ({
      MODEL_FLASH: "deepseek-chat",
      MODEL_PRO: "deepseek-v4-pro",
      generateJson: jest.fn(),
      generateText: jest.fn(),
    }));
    ```

    **2 Fixture-Dateien füllen:**
    - `__tests__/eval/fixtures/llm-stubs.ts`: `STUB_JUDGE_RESPONSE_OEFFENTLICH` mit realistischer Judge-JSON-Antwort (6 Kriterien aus RUBRIC_OEFFENTLICH, score je 50-80, gesamt-Score korrekt gewichtet)
    - `__tests__/fixtures/pipeline-snapshot-borsigwalder.json`: Snapshot via Single-Live-Smoke (`PIPELINE_*=0 npx tsx scripts/eval-pipeline.ts --live --N=1 --single pv-001 --snapshot`) — VOR Baseline-Run in Task 4. Wenn DeepSeek-Balance noch nicht aufgeladen: handgebauter Snapshot mit `_TODO`-Marker → Task 4 ersetzt durch echten Live-Snapshot. Hinweis: das ist OK fuer Task-1c-Acceptance (Acceptance prueft nur File-Existenz + valides JSON), nur die spaetere Baseline-Run-Acceptance erwartet realistisches `pipeline-snapshot-borsigwalder.json`.

    **Tests laufen lassen:**
    ```
    npx jest __tests__/eval/pipeline-fk-match __tests__/eval/pipeline-marker-detection __tests__/eval/pipeline-regex-detection __tests__/eval/pipeline-judge-rubric __tests__/eval/pipeline-finanzplan-sub __tests__/eval/pipeline-determinism __tests__/eval/pipeline-snapshot-replay __tests__/eval/pipeline-aggregation __tests__/eval/pipeline-gate --bail
    ```

    **Conventional Commit:** `test(05): wave-0-skelette → lebende eval-tests (9 dateien, ≥ 40 tests) + llm-stubs fixture`
  </action>
  <verify>
    <automated>npx jest __tests__/eval/pipeline-fk-match __tests__/eval/pipeline-marker-detection __tests__/eval/pipeline-regex-detection __tests__/eval/pipeline-judge-rubric __tests__/eval/pipeline-finanzplan-sub __tests__/eval/pipeline-determinism __tests__/eval/pipeline-snapshot-replay __tests__/eval/pipeline-aggregation __tests__/eval/pipeline-gate --bail 2>&1 | tee /tmp/wave2-1c-tests.out && grep -qE "Tests:.*passed.*4[0-9]" /tmp/wave2-1c-tests.out && test -f __tests__/eval/fixtures/llm-stubs.ts && grep -q "STUB_JUDGE_RESPONSE_OEFFENTLICH" __tests__/eval/fixtures/llm-stubs.ts && test -f __tests__/fixtures/pipeline-snapshot-borsigwalder.json && node -e "JSON.parse(require('fs').readFileSync('__tests__/fixtures/pipeline-snapshot-borsigwalder.json','utf-8'))"</automated>
  </verify>
  <acceptance_criteria>
    - 9 Test-Files unter `__tests__/eval/` haben `it()` statt `it.todo()` (mind. 40 lebende Tests total)
    - alle 9 Test-Files gruen
    - `__tests__/eval/fixtures/llm-stubs.ts` hat realistische STUB_JUDGE_RESPONSE_OEFFENTLICH-Antwort (6 Kriterien, Gesamt-Score numerisch korrekt)
    - `__tests__/fixtures/pipeline-snapshot-borsigwalder.json` ist valides JSON (realistischer Live-Snapshot ODER handgebaut mit `_TODO`-Marker; Task 4 ersetzt durch echten Snapshot)
  </acceptance_criteria>
  <done>Wave-0-Test-Skelette sind lebend, Score-Logik ist test-coverage. Eval-Skript ist code-complete + getestet. Bereit für Task 4 (Live-Baseline-Run) und Task 5 (BASELINE.md + .gitignore).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: DeepSeek-Balance-Pre-Flight + Baseline-N=3-Live-Run (D-16, ~3-4 EUR Live-Cost)</name>
  <files>data/eval/pipeline-snapshots/baseline, data/eval/pipeline-reports</files>
  <read_first>
    - .planning/STATE.md (Wave-2-Blocker-Issue 1: DeepSeek = 0 Balance — Status muss verifiziert sein)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md (D-01 Kosten-Schätzung, D-16 N=3-Baseline)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md (§Environment Availability Z.755-768 — DeepSeek-Top-Up + Gemini-Fallback)
  </read_first>
  <what-built>
    **Cost-Schätzung pro Provider** (für Provider-Switch-Decision A/B/C bei DeepSeek 0 Balance):

    | Provider | Modell-IDs (Interview + Pipeline + Judge) | Cost/Voll-Lauf (22 Einträge × N=3) | Wallclock | Bemerkung |
    |----------|--------------------------------------------|------------------------------------|-----------|-----------|
    | DeepSeek-Chat (Default) | deepseek-chat overall + judge | ~3-4 EUR | ~60-75 min | Default per CLAUDE.md, billigste Variante |
    | Gemini-Flash | gemini-2.0-flash overall, gemini-2.5-pro Judge | ~6-9 EUR | ~50-65 min | Variante B Fallback, Token-Pricing höher |
    | Gemini-Pro | gemini-2.5-pro durchgehend | ~30-45 EUR | ~80-100 min | Nur Ausnahme — Reasoning-Tokens explodieren Cost; Variante C aktiv NICHT empfohlen |

    Quelle: lib/wizard/pricing.ts + DeepSeek-/Gemini-Public-Pricing 2026-05.

    **Pre-Flight 1 — DeepSeek-Balance-Check** (autonomous):
    ```bash
    # Liefert Insufficient-Balance-Fehler wenn 0 Saldo
    DEEPSEEK_API_KEY=<key> npx tsx scripts/smoke-llm.ts 2>&1 | tee /tmp/deepseek-balance-check.out
    ```

    **Pre-Flight 2 — Eval-Skript Self-Check ohne LLM** (autonomous):
    ```bash
    # Lädt Korpus, validiert FK, ohne LLM-Call (--replay gegen nicht-existierendes Verzeichnis = clean exit 2)
    npx tsx scripts/eval-pipeline.ts --replay /tmp/none 2>&1 | head -10
    ```
    Erwartung: Korpus-Load OK + sauberes exit 2 wegen --replay-Pfad-Fehler.

    **Wenn DeepSeek 0 Balance → Kolja-Checkpoint:**
    Optionen darstellen:
    - A) Top-Up im DeepSeek-Dashboard, dann Baseline-Run
    - B) `LLM_PROVIDER=gemini` setzen + Baseline-Run mit Gemini (Judge: gemini-2.5-pro statt deepseek-chat) — D-15 erlaubt das implizit als Fallback; BASELINE.md dokumentiert Provider-Choice
    - C) Phase 5 pausieren bis Top-Up möglich

    **Baseline-Run (nach Approval):**
    ```bash
    DEEPSEEK_API_KEY=<key> npx tsx scripts/eval-pipeline.ts \
      --live --N=3 --snapshot --md-summary \
      2>&1 | tee /tmp/baseline-run.log
    ```

    Erwartete Wallclock: 60-75 min.
    Erwartete Cost: ~3-4 EUR.

    **Snapshot-Reorganisation nach Run:**
    Skript schreibt Snapshots in `data/eval/pipeline-snapshots/<ISO>/`. Kopiere ISO-Verzeichnis nach `data/eval/pipeline-snapshots/baseline/` (force-committed analog Phase 1 T-01-09).

    ```bash
    ISO=$(ls data/eval/pipeline-snapshots/ | grep -v baseline | sort | tail -1)
    cp -r "data/eval/pipeline-snapshots/${ISO}" "data/eval/pipeline-snapshots/baseline/"
    ```

    **Conventional Commit:** `feat(05): wave-2 baseline-n3-run (22 einträge, 66 snapshots, ~X EUR cost)`
  </what-built>
  <how-to-verify>
    **Kolja-Checkpoint (Cost-Eskalation-Gate):**

    1. DeepSeek-Balance-Check-Output von `/tmp/deepseek-balance-check.out` prüfen:
       - Wenn `"error":{"message":"Insufficient Balance"}` → entscheide A/B/C
       - Wenn LLM-Call OK → Baseline-Run starten

    2. Falls Baseline-Run gefahren:
       - Console-Output zeigt 22 × 3 = 66 Sub-Runs
       - Snapshot-Anzahl: `ls data/eval/pipeline-snapshots/<ISO>/ | wc -l` zeigt 66
       - Cost-Summary am Skript-Ende: `Total: X.XX EUR`
       - Report-Datei existiert: `ls data/eval/pipeline-reports/*.json`
       - Median Score-Range plausibel: WIZ-01 30-70 % (Pflichtabschnitte schwer), WIZ-02 50-90 (Marker-Reduktion variabel), WIZ-03 40-70 (Tonalität)

    Antworte mit `approved A` / `approved B` / `approved C` (bei DeepSeek 0 Balance) ODER `approved` (wenn Run geclownt). Bei Problemen mit Score-Plausibilität: Issues nennen.
  </how-to-verify>
  <resume-signal>Antworte: `approved` + ggf. Provider-Switch (A/B/C). Bei `approved C` pausiert Phase 5; bei A/B startet der Baseline-Run sofort.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 5: BASELINE.md Phase-5-Eintrag + .gitignore-Pflege</name>
  <files>data/eval/BASELINE.md, .gitignore</files>
  <read_first>
    - data/eval/BASELINE.md (Phase-2.3-Eintrag als Format-Vorbild)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md (§BASELINE.md-Format-Skeleton Z.1088-1142)
    - data/eval/pipeline-reports/<ISO>.json (Output aus Task 2 — Aggregat-Daten)
    - data/eval/dossier-coverage-baseline.md (Methodik-Hinweis: maxZeichen-only-coverage)
    - .gitignore (existierende Einträge sichten)
  </read_first>
  <action>
    **data/eval/BASELINE.md erweitern** (append-only, neuer Block ganz oben unter `# Eval Baseline`):

    Format aus RESEARCH Z.1088-1142 1-zu-1 übernehmen, alle TBD-Werte aus dem Report-JSON ausgefüllt:

    ```markdown
    ## <ISO-Date> — Phase-5-Pipeline-Baseline (Korpus v1, n=22)

    - **Pipeline-Commit:** `<git rev-parse HEAD>` (HEAD von `feature/wizard-adaptive` nach Phase-4-Closure)
    - **Korpus-Version:** v1, 22 Eintraege (11 Standard pro Dossier + 7 Stress-Edge-Cases + 4 Reserve)
    - **Run-Konfiguration:** N=3 pro Eintrag, judgeModel `deepseek-chat` (oder `gemini-2.5-pro` bei Provider-B-Switch), alle Feature-Flags OFF
    - **Methodik-Hinweis:** WIZ-01 ist **Pflichtabschnitt-Coverage-only** — `maxZeichen` war zum Run-Zeitpunkt in 0/11 Dossiers gesetzt (siehe `data/eval/dossier-coverage-baseline.md` Pre-Flight-Survey, RESEARCH §Pitfall 7). Hebel 3 (vorbildFormulierungen) wirkt nur fuer aktion-mensch (3 Formulierungen) + kultur-macht-stark (4 Formulierungen) — Wave-3-Hebel-3-Plan misst Delta nur auf diesen 2 Dossiers.
    - **Stddev-Konvention:** Population-Stddev mit N (nicht Sample N-1), konsistent mit `aggregateNRuns()` und Phase-1-Pattern.

    ### Haupt-Scores (mean ± stddev ueber N=3)

    | Achse | Mean | Stddev | 2σ-Band | Schwellwert (D-19) | Status |
    |-------|------|--------|---------|--------------------|--------|
    | WIZ-01 (Pflichtabschnitte) | XX.X | X.X | XX.X – XX.X | ≥ 80 % | <Aktuell vs Schwellwert> |
    | WIZ-02 (Halluzinations-Detection) | XX.X | X.X | XX.X – XX.X | ≥ 50 % Reduktion | <Baseline = Anker> |
    | WIZ-03 (Tonalitaets-Passung) | XX.X | X.X | XX.X – XX.X | Score-Delta > 0 | n/a (Baseline) |
    | Finanzplan-Validity (Sub) | XX.X | X.X | XX.X – XX.X | — | — |

    ### Per-Geber-Gruppe (WIZ-03)

    | Gruppe | n Einträge | Mean | Stddev |
    |--------|-----------|------|--------|
    | oeffentlich (bund+land) | X | XX.X | X.X |
    | stiftung | X | XX.X | X.X |
    | eu | X | XX.X | X.X |
    | wirtschaftspreis | X | XX.X | X.X |
    | verband-uni | X | XX.X | X.X |

    ### Per-Dossier (WIZ-01 + WIZ-02 Mean über N=3)

    | Dossier | n | WIZ-01 mean | WIZ-02 mean | Notiz |
    |---------|---|-------------|-------------|-------|
    | aktion-mensch-schulkooperation | 1 | XX | XX | Hebel-3-faehig (3 vorbildFormulierungen) |
    | kultur-macht-stark | 1 | XX | XX | Hebel-3-faehig (4 vorbildFormulierungen) |
    | bmbf-digitalpakt-2 | 1 (+pv-001 borsigwalder) | XX | XX | UAT-28.04.-Anker |
    | ... weitere 8 Dossiers ... |
    | (Edge-Cases) | 7 | n/a | n/a | nicht in Per-Dossier-Aggregat |

    ### Latenz / Kosten

    - **Latenz/Eintrag (Voll-Pipeline):** XX.Xs avg (Spanne XX–XX s)
    - **Voll-Lauf Kosten (N=3, 22 Einträge):** X.XX EUR / XX LLM-Calls
    - **Judge-Cost (deepseek-chat oder gemini-2.5-pro bei Switch):** X.XX EUR

    ### Reports

    - JSON: `data/eval/pipeline-reports/<ISO>.json` (alle 3 Runs + Aggregat)
    - Markdown: `data/eval/pipeline-reports/<ISO>.md`
    - Snapshots: `data/eval/pipeline-snapshots/<ISO>/` (66 Dateien)
    - **Baseline-Anker:** `data/eval/pipeline-snapshots/baseline/` (force-committed Kopie für Replay-Default)

    ### Run-Befehl

    `DEEPSEEK_API_KEY=... npx tsx scripts/eval-pipeline.ts --live --N=3 --snapshot --md-summary`
    ```

    Alle XX.X-Werte aus `data/eval/pipeline-reports/<ISO>.json` ausfüllen.

    **.gitignore erweitern:**
    ```
    # Phase 5 Eval (data/eval/snapshots ist Phase-1-Pattern, hier Pipeline-Pendant)
    /data/eval/pipeline-snapshots/*
    !/data/eval/pipeline-snapshots/baseline/
    /data/eval/pipeline-reports/
    ```
    (Negativ-Ausnahme für `baseline/` damit der Baseline-Snapshot-Anker versioniert bleibt — analog Phase 1.)

    **Force-Commit der Baseline-Snapshots:**
    ```bash
    git add -f data/eval/pipeline-snapshots/baseline/
    ```

    **Conventional Commits (zwei Commits, atomic):**
    1. `chore(05): pipeline-snapshots/pipeline-reports gitignored (außer baseline)`
    2. `docs(05): wave-2 phase-5 baseline-eintrag in BASELINE.md (n=22, N=3, mean+stddev pro achse)`
  </action>
  <verify>
    <automated>grep -q "Phase-5-Pipeline-Baseline" data/eval/BASELINE.md && grep -q "WIZ-01 (Pflichtabschnitte)" data/eval/BASELINE.md && grep -q "Per-Geber-Gruppe" data/eval/BASELINE.md && grep -q "Methodik-Hinweis" data/eval/BASELINE.md && test -d data/eval/pipeline-snapshots/baseline && test "$(ls data/eval/pipeline-snapshots/baseline/ | wc -l)" -ge 22 && grep -q "pipeline-snapshots" .gitignore && grep -q "!/data/eval/pipeline-snapshots/baseline/" .gitignore && ! git check-ignore -q data/eval/pipeline-snapshots/baseline/</automated>
  </verify>
  <acceptance_criteria>
    - `data/eval/BASELINE.md` hat neuen Block `## <ISO> — Phase-5-Pipeline-Baseline (Korpus v1, n=22)` an erster Stelle
    - Alle Tabellen befüllt (keine `XX.X`-Platzhalter mehr)
    - Methodik-Hinweis "Pflichtabschnitt-Coverage-only" inkl. dossier-coverage-baseline-Verweis
    - `.gitignore` enthält `pipeline-snapshots/*` + Negativ-Ausnahme `!/.../baseline/`
    - `data/eval/pipeline-snapshots/baseline/` existiert mit ≥ 22 (oder 66 für N=3) JSON-Dateien
    - `git ls-files data/eval/pipeline-snapshots/baseline/` listet die Dateien (force-committed)
    - `data/eval/pipeline-reports/` ist gitignored
  </acceptance_criteria>
  <done>Phase-5-Baseline-Eintrag in BASELINE.md committed, Snapshots als Replay-Anker versioniert, .gitignore korrekt. Wave 3 hat Mess-Anker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Korpus + Snapshots → Eval-Skript | Trust-Anchor; Korpus ist versioniert, Baseline-Snapshots force-committed |
| Judge-LLM-Call → BASELINE.md | LLM-Score wird unverändert in BASELINE übernommen — Judge-Rauschen via N=3-Mean + 2σ aufgefangen |
| DEEPSEEK_API_KEY → Eval-Skript | Key liegt in process.env, wird nie in Reports geschrieben (CostLedger speichert nur Token-Counts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04-01 | DoS | LLM-Cost-Explosion bei versehentlichem --live + N=10 statt N=3 | high | mitigate | CLI-Limit: `--N=` parser akzeptiert nur 1-5; Default 1 (Single-Run für Iteration). CI-Workflow (Plan 05-08) hat exklusiv --replay als Default |
| T-05-04-02 | Tampering | Snapshot-Manipulation (Replay gegen modifizierten Snapshot lügt) | medium | mitigate | Schema-Version-1-Check beim Replay-Load; nicht-baseline-Snapshots sind .gitignore'd; nur lokal trusted; CI nutzt force-committed baseline |
| T-05-04-03 | Information Disclosure | DEEPSEEK_API_KEY in Logs / Reports | high | mitigate | CostLedger schreibt nur Token-Counts; Eval-Skript greift Key nur via `process.env.DEEPSEEK_API_KEY` ab; Keine console.log mit Key |
| T-05-04-04 | Spoofing | LLM-Judge-Provider-Switch (Gemini statt DeepSeek) — Score-Werte sind dann nicht 1-zu-1 vergleichbar | medium | mitigate | BASELINE.md dokumentiert Provider explizit; Pipeline-Verschiebung zwischen Providers triggert neue Baseline-Berechnung (D-26 analog Korpus-Update) |
| T-05-04-05 | Tampering | Baseline-Drift (jemand re-runs Baseline mit anderem Korpus/Code-State, ohne BASELINE.md zu updaten) | medium | mitigate | D-26 Korpus-Update-Regel + force-committed baseline-Snapshots als git-historisierter Anker |
| T-05-04-06 | DoS | Endless-Loop in Score-Berechnung wegen LLM-Stub mit Crash | low | mitigate | Soft-Failure pro Eintrag (RESEARCH Pattern Z.540-585), `errMsg` im Report, kein Abbruch |
| T-05-04-07 | Information Disclosure | LLM-Judge-Output enthält volle Antrags-Texte → in Snapshot → potenziell PII (sollte aus Korpus eigentlich gefiltert sein) | low | accept | Korpus ist anonymisiert (D-02 60 % UAT mit Memo-Pattern, das schon anonymisiert ist) |
</threat_model>

<verification>
- Eval-Skript läuft fehlerfrei via `npx tsx scripts/eval-pipeline.ts --replay data/eval/pipeline-snapshots/baseline --md-summary` (Replay-Modus, kein LLM, kein Cost)
- 9 Wave-0-Test-Files grün (~40 Tests)
- BASELINE.md hat Phase-5-Eintrag mit echten Werten
- pipeline-snapshots/baseline/ versioniert, andere Snapshots gitignored
- DeepSeek-Top-Up oder Gemini-Switch dokumentiert
- Pre-Closure-Plan 05-08 kann den Eval-Skript direkt für Live-Smoke nutzen
</verification>

<success_criteria>
- `scripts/eval-pipeline.ts` mit allen Score-Funktionen + parseFlags + main + crash-handler
- 9 lebende Tests grün
- Baseline-N=3-Live-Run gefahren, ~3-4 EUR Cost, 66 Snapshots versioniert
- BASELINE.md Phase-5-Eintrag mit Methodik-Hinweis
- .gitignore korrekt konfiguriert
- 5 Tasks + 5+ Conventional Commits (Task 1a/1b/1c + Task 4 + Task 5)
</success_criteria>

<output>
Nach Completion: `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-04-SUMMARY.md` mit:
- Eval-Skript-LOC + Test-Anzahl
- Baseline-Run-Resultat: Cost (EUR), Wallclock, Provider (DeepSeek/Gemini), 3 Haupt-Scores mit mean+stddev
- Befund pro Achse: ist WIZ-01-Mean schon ≥ 80? WIZ-02-Marker-Reduktion sichtbar? WIZ-03-Score-Verteilung pro Cluster
- Wave-3-Trigger: welche Hebel zuerst (datengetrieben, D-21) — Tuning-Empfehlung aus Aggregat
- Pre-Closure-Smoke-Bereitstellung: pipeline-snapshots/baseline/ kann für Plan 05-08 als Replay-Anker dienen
</output>

---
phase: 05-wizard-pipeline-tuning-ux-l-cke
plan: 06
type: execute
wave: 3
depends_on: [04]
files_modified:
  - lib/wizard/pipeline.ts
  - lib/wizard/types.ts
  - lib/wizard/stage-labels.ts
  - components/wizard/GeneratingProgress.tsx
  - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-2.md
  - __tests__/lib/wizard/pipeline.compliance.test.ts
autonomous: true
requirements: [WIZ-01]
tags: [phase-5, wave-3, tuning, hebel-2, compliance-stage, pipeline]
must_haves:
  truths:
    - "lib/wizard/types.ts PipelineStage-Union enthält 'compliance-check' (D-20 Hebel 2)"
    - "lib/wizard/pipeline.ts ruft runComplianceCheck() zwischen recheck und finanzplan auf, conditional PIPELINE_COMPLIANCE_STAGE=1"
    - "Compliance-Stage hat Loop-Count=1 (kein endloser Compliance↔Revision-Pingpong, RESEARCH Anti-Pattern Z.726)"
    - "GeneratingProgress.tsx kollabiert 'compliance-check' silent (kein User-Visible-Stage-Update, D-20 Hebel 2 'silent stage')"
    - "stage-labels.ts hat Eintrag für 'compliance-check' (TS-Compile-Safety, auch wenn UI silent)"
    - "__tests__/lib/wizard/pipeline.compliance.test.ts ist von it.todo auf lebend umgestellt + grün"
    - "Eval-A/B-Run zeigt Hebel-2-Delta: PIPELINE_COMPLIANCE_STAGE=1 vs Baseline pro Achse — Befund in tuning-hebel-2.md (intermediate); Konsolidierung nach data/eval/TUNING.md erfolgt in Plan 05-08 Task 4"
  artifacts:
    - path: "lib/wizard/pipeline.ts"
      provides: "Pipeline mit conditional Compliance-Check-Stage nach recheck"
      contains: "compliance-check"
    - path: "lib/wizard/types.ts"
      provides: "PipelineStage-Union erweitert"
      contains: "compliance-check"
    - path: "components/wizard/GeneratingProgress.tsx"
      provides: "Silent-Stage-Handling für compliance-check"
      contains: "compliance-check"
  key_links:
    - from: "lib/wizard/pipeline.ts"
      to: "lib/wizard/types.ts"
      via: "PipelineStage-Type-Import + Re-Export"
      pattern: "PipelineStage"
    - from: "lib/wizard/pipeline.ts"
      to: "lib/wizard/config.ts"
      via: "PIPELINE_CONFIG.complianceStageEnabled-Read"
      pattern: "complianceStageEnabled"
---

<objective>
Wave 3 Hebel 2: Compliance-Check-Stage zwischen `recheck` und `finanzplan` in `lib/wizard/pipeline.ts` einbauen (D-20 Hebel 2). Stage validiert strict gegen `richtlinie.antragsstruktur.abschnitte[].id` + maxZeichen, triggert bei Verletzung max 1× Revision-Iteration.

Purpose: WIZ-01-Hebel — Pipeline soll Pflichtabschnitt-Coverage selbst hochziehen, statt sich auf RECHECK + Revision zu verlassen. Hebel 2 ergänzt die Self-Correction-Loop um einen deterministischen FK-Check.

Output:
- types.ts: PipelineStage erweitert
- pipeline.ts: runComplianceCheck-Funktion + Conditional-Stage-Integration
- stage-labels.ts: Eintrag für compliance-check
- GeneratingProgress.tsx: silent-Map für compliance-check
- pipeline.compliance.test.ts lebende Tests grün
- TUNING.md erweitert um Hebel-2-Block

**Disjoint zu Plan 05-05:** Plan 05-06 modifiziert pipeline.ts/types.ts/GeneratingProgress.tsx — keine Datei-Overlap mit Plan 05-05 (prompts.ts). Daher echt parallel zu Plan 05-05 + 05-07 in Wave 3.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-PATTERNS.md
@lib/wizard/pipeline.ts
@lib/wizard/types.ts
@lib/wizard/stage-labels.ts
@lib/wizard/prompts.ts
@lib/wizard/llm.ts
@lib/wizard/config.ts
@components/wizard/GeneratingProgress.tsx
@__tests__/lib/wizard/pipeline-events.test.ts
@__tests__/lib/wizard/pipeline.compliance.test.ts
@data/eval/TUNING.md
@scripts/eval-pipeline.ts

<interfaces>
From lib/wizard/types.ts (Aktueller Stand):
```typescript
export type PipelineStage =
  | "idle" | "outline" | "section" | "critique" | "revision"
  | "recheck" | "finanzplan" | "consistency" | "done" | "failed";
```
→ Phase 5 erweitert um `"compliance-check"`.

From lib/wizard/pipeline.ts (Aktuelle Stage-Logik):
- runPipeline() sequenziell: outline → section → critique → revision → recheck → finanzplan → consistency → done
- Pipeline emittiert `{ stage: PipelineStage, message?: string }` via optional `onEvent` callback

From RESEARCH §Pattern 5 (Z.667-717):
```typescript
if (config.complianceStageEnabled && richtlinie?.antragsstruktur?.abschnitte) {
  emit({ stage: "compliance-check", message: "Pflichtabschnitt-Check" });
  const complianceCheck = await runComplianceCheck(
    finalRes.value,
    richtlinie.antragsstruktur.abschnitte
  );
  usages.push(complianceCheck.usage);

  if (complianceCheck.violations.length > 0 && !complianceLoopCount) {
    const revFix = await generateText(
      MODEL_PRO,
      REVISION_SYSTEM,
      buildComplianceRevisionPrompt(finalRes.value, complianceCheck.violations)
    );
    finalRes.value = revFix.value;
    usages.push({ model: MODEL_PRO, usage: revFix.usage });
    complianceLoopCount = 1;
  }
}
```

From RESEARCH §Pattern 5 ComplianceCheck-Schema:
```typescript
interface ComplianceCheck {
  violations: Array<{
    abschnittId: string;
    art: "fehlt" | "ueberlaenge" | "nur-platzhalter";
    detail: string;
  }>;
  usage: PipelineUsage;
}
```

From components/wizard/GeneratingProgress.tsx (vermutete Struktur — Wave-3-Executor prüft):
- Component nimmt aktive Stage entgegen, rendert Visualisierung der 7 user-sichtbaren Stages
- Silent-Stage-Handling-Pattern: `if (stage === "compliance-check") return null;` ODER Map-basiert

From __tests__/lib/wizard/pipeline-events.test.ts (LLM-Mock-Pattern):
```typescript
jest.mock("@/lib/wizard/llm", () => ({
  MODEL_FLASH: "deepseek-chat",
  MODEL_PRO: "deepseek-v4-pro",
  generateJson: jest.fn(),
  generateText: jest.fn(),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: types.ts + pipeline.ts + stage-labels.ts: compliance-check-Stage implementieren</name>
  <files>lib/wizard/types.ts, lib/wizard/pipeline.ts, lib/wizard/stage-labels.ts, components/wizard/GeneratingProgress.tsx, __tests__/lib/wizard/pipeline.compliance.test.ts</files>
  <read_first>
    - lib/wizard/types.ts (PipelineStage-Definition — direkt erweitern)
    - lib/wizard/pipeline.ts (komplette Datei — verstehen wo nach recheck die Stage-Logik weitergeht)
    - lib/wizard/prompts.ts (RECHECK_SYSTEM für buildComplianceRevisionPrompt-Wording)
    - lib/wizard/stage-labels.ts (Stage-Label-Map sichten)
    - components/wizard/GeneratingProgress.tsx (User-Visible-Stage-Mapping)
    - __tests__/lib/wizard/pipeline-events.test.ts (LLM-Mock-Pattern als Vorbild)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md (§Pattern 5 Z.667-717, §Open Question 7 Z.1305-1308 zu silent-stage-Handling)
    - __tests__/lib/wizard/pipeline.compliance.test.ts (Skelett aus Plan 05-01)
  </read_first>
  <behavior>
    - Test 1: PIPELINE_COMPLIANCE_STAGE=0 (default OFF) → Pipeline emittiert KEINE compliance-check-Stage; runPipeline-Verhalten identisch zu Baseline
    - Test 2: PIPELINE_COMPLIANCE_STAGE=1 + richtlinie hat 5 Pflicht-Abschnitte + Output enthält alle 5 → 1× compliance-check-Stage emittiert, 0 violations, KEIN Revision-Re-Trigger
    - Test 3: PIPELINE_COMPLIANCE_STAGE=1 + 5 Pflicht-Abschnitte + Output enthält nur 3 → 1× compliance-check, violations.length=2 (art: "fehlt"), 1× zusätzlicher revision-Call mit complianceLoopCount=1
    - Test 4: PIPELINE_COMPLIANCE_STAGE=1 + nach 1. Compliance-Iteration weiterhin Violations → KEINE 2. Iteration (Loop-Count enforced, kein endloser Pingpong)
    - Test 5: richtlinie=null oder richtlinie.antragsstruktur.abschnitte=undefined → Stage wird komplett geskipt (kein crash)
  </behavior>
  <action>
    **types.ts erweitern:**
    ```typescript
    export type PipelineStage =
      | "idle" | "outline" | "section" | "critique" | "revision"
      | "recheck"
      | "compliance-check"     // NEU Phase 5 D-20 Hebel 2 — silent stage gegenüber UI
      | "finanzplan" | "consistency" | "done" | "failed";
    ```

    **stage-labels.ts erweitern:**
    Eintrag für `compliance-check` ergänzen (auch wenn UI silent — TS-Compile-Safety erforderlich):
    ```typescript
    "compliance-check": "Compliance-Prüfung",  // silent — wird NICHT in UI angezeigt
    ```

    **GeneratingProgress.tsx — Silent-Handling:**
    Sichte die existierende Switch/Map-Logik. Drei Optionen (sicherste = a):
    - **(a) Stage-Liste-Filter:** Die 7 anzuzeigenden Stages sind in einer Constant-Array — `compliance-check` wird dort NICHT eingetragen; Component ignoriert ihn automatisch.
    - (b) Explicit `if (stage === "compliance-check") return null;` am Component-Start
    - (c) Switch-Case mit `case "compliance-check": return null;`

    Wähle (a) wenn das aktuelle GeneratingProgress.tsx eine STAGE_DISPLAY_LIST-Constant nutzt, sonst (b).

    **pipeline.ts erweitern** (RESEARCH §Pattern 5 1-zu-1 anwenden):

    1. Import `PIPELINE_CONFIG` aus `@/lib/wizard/config`
    2. Neue interne Funktion `runComplianceCheck`:
       ```typescript
       async function runComplianceCheck(
         finalText: string,
         abschnitte: readonly AntragsAbschnitt[]
       ): Promise<{ violations: Array<{ abschnittId: string; art: "fehlt" | "ueberlaenge" | "nur-platzhalter"; detail: string }>; usage: Usage }> {
         // Strict-FK-Check: für jeden Pflicht-Abschnitt
         //   - "fehlt" wenn Abschnittsname nicht im finalText vorhanden (case-insensitive normalize)
         //   - "ueberlaenge" wenn section-Text > maxZeichen
         //   - "nur-platzhalter" wenn section-Text < 50 Zeichen UND Pflicht-Abschnitt
         // Schickt KEIN LLM-Call für die Erkennung (deterministisch)
         // ABER: für die Repair-Phase wird LLM gebraucht
         // → Diese Funktion liefert nur Findings; Pipeline-Code triggert dann ggf. Revision via LLM
         // Usage-Tracking: dummy { promptTokens: 0, completionTokens: 0, model: "deterministic" }
       }
       ```

    3. Neue interne Funktion `buildComplianceRevisionPrompt(currentText, violations)`:
       Bauliches Wording für Revision-Re-Trigger; instruiert LLM, die fehlenden Abschnitte einzubauen / Längen-Limits einzuhalten.

    4. In runPipeline() nach recheck-Stage (vor finanzplan):
       ```typescript
       let complianceLoopCount = 0;
       if (PIPELINE_CONFIG.complianceStageEnabled && richtlinie?.antragsstruktur?.abschnitte) {
         emit({ stage: "compliance-check", message: "Pflichtabschnitt-Check" });
         const cc = await runComplianceCheck(finalRes.value ?? "", richtlinie.antragsstruktur.abschnitte);
         usages.push({ model: "deterministic", usage: cc.usage });

         if (cc.violations.length > 0 && complianceLoopCount === 0) {
           const revFix = await generateText(
             MODEL_PRO,
             REVISION_SYSTEM,
             buildComplianceRevisionPrompt(finalRes.value ?? "", cc.violations)
           );
           finalRes = { value: revFix.value, usage: revFix.usage };
           usages.push({ model: MODEL_PRO, usage: revFix.usage });
           complianceLoopCount = 1;
         }
       }
       ```

    5. PipelineStage-Re-Export: bereits via `export type { PipelineStage } from "./types";` (Z.15 in pipeline.ts)

    **__tests__/lib/wizard/pipeline.compliance.test.ts auf lebend:**
    Tests aus `<behavior>`-Block 1-zu-1 implementieren. Mock-Pattern aus pipeline-events.test.ts übernehmen.

    Für PIPELINE_CONFIG-Mocking pro Test:
    ```typescript
    beforeEach(() => {
      jest.resetModules();
      delete process.env.PIPELINE_COMPLIANCE_STAGE;
    });

    it("Default OFF — keine compliance-check-Stage", async () => {
      const events: any[] = [];
      const { runPipeline } = require("@/lib/wizard/pipeline");
      await runPipeline({ ...mockInput, onEvent: e => events.push(e) });
      expect(events.find(e => e.stage === "compliance-check")).toBeUndefined();
    });

    it("Flag ON + Violations → 1× Revision-Re-Trigger", async () => {
      process.env.PIPELINE_COMPLIANCE_STAGE = "1";
      jest.resetModules();
      // ... mock LLM, run, assert
    });
    ```

    **Conventional Commit:** `feat(05): wave-3 hebel-2 compliance-check-stage in pipeline.ts (PIPELINE_COMPLIANCE_STAGE-toggle, loop-count=1, silent gegenüber UI)`
  </action>
  <verify>
    <automated>grep -q "compliance-check" lib/wizard/types.ts && grep -q "compliance-check" lib/wizard/pipeline.ts && grep -q "runComplianceCheck" lib/wizard/pipeline.ts && grep -q "complianceLoopCount" lib/wizard/pipeline.ts && grep -q "PIPELINE_CONFIG" lib/wizard/pipeline.ts && grep -q "compliance-check" lib/wizard/stage-labels.ts && grep -q "compliance-check" components/wizard/GeneratingProgress.tsx && npx jest __tests__/lib/wizard/pipeline.compliance.test.ts --bail 2>&1 | tee /tmp/wave3-compliance-tests.out && grep -qE "Tests:.*passed" /tmp/wave3-compliance-tests.out && npx tsc --noEmit 2>&1 | tee /tmp/wave3-tsc.out && ! grep -q "error TS" /tmp/wave3-tsc.out</automated>
  </verify>
  <acceptance_criteria>
    - `lib/wizard/types.ts` PipelineStage-Union enthält "compliance-check"
    - `lib/wizard/pipeline.ts` ruft runComplianceCheck() conditional bei PIPELINE_CONFIG.complianceStageEnabled=true
    - complianceLoopCount auf 0/1 enforced — kein endloser Loop
    - `lib/wizard/stage-labels.ts` hat Eintrag für compliance-check
    - `components/wizard/GeneratingProgress.tsx` ignoriert compliance-check (silent)
    - 5 Tests in `__tests__/lib/wizard/pipeline.compliance.test.ts` grün
    - `npx tsc --noEmit` ohne TS-Errors im Repo
    - Default-Verhalten (Flag OFF) unverändert — keine Regression in bestehenden pipeline-Tests
  </acceptance_criteria>
  <done>Compliance-Stage existiert, ist via Feature-Flag toggelbar, hat Loop-Count-Schutz, ist UI-silent, Tests grün. Default-Pipeline-Verhalten ist unverändert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Eval-A/B-Run Hebel 2 + Intermediate-Markdown tuning-hebel-2.md</name>
  <files>.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-2.md</files>
  <read_first>
    - data/eval/BASELINE.md
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-1-3.md (Wave-3-Geschwister-intermediate aus Plan 05-05, NICHT modifizieren — nur als Kontext lesen)
    - scripts/eval-pipeline.ts
  </read_first>
  <action>
    **Eval-Run Hebel 2 (PIPELINE_COMPLIANCE_STAGE=1):**

    ```bash
    PIPELINE_COMPLIANCE_STAGE=1 npx tsx scripts/eval-pipeline.ts --live --N=1 --snapshot --md-summary 2>&1 | tee /tmp/eval-hebel2.log
    ```

    Cost-Schätzung: ~1.0-1.5 EUR (N=1 × 22 Einträge + ggf. Compliance-Revision-Re-Trigger bei Violations → ~1-3 zusätzliche LLM-Calls pro Eintrag, daher leicht teurer als reines N=1).

    **Auswertung — Report-JSON extrahieren + mit Baseline vergleichen.**

    **Intermediate-Markdown anlegen** (Wave-3-Disjunktheits-Pattern: schreibt NICHT direkt nach data/eval/TUNING.md sondern in eigenes intermediate-File `tuning-hebel-2.md` — Plan 05-08 Task 4 konsolidiert alle 3 Wave-3-intermediates nach data/eval/TUNING.md):

    Datei: `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-2.md`

    ```markdown
    ## <ISO-Date> — Wave 3 Hebel 2 (Compliance-Check-Stage)

    **Konfiguration:** `PIPELINE_COMPLIANCE_STAGE=1`

    **Änderung:** Neue PipelineStage `compliance-check` zwischen `recheck` und `finanzplan`. Deterministische FK-Prüfung gegen `richtlinie.antragsstruktur.abschnitte[].id` + maxZeichen + Mindest-Länge (50 Zeichen für Pflicht-Abschnitte). Bei Violations: 1× zusätzlicher REVISION_SYSTEM-LLM-Call mit `buildComplianceRevisionPrompt`. Loop-Count-Schutz: max 1 Iteration.

    **Sub-Befund:** Stage ist silent gegenüber GeneratingProgress.tsx — kein UI-Stage-Update (D-20 'silent stage').

    **Eval-Resultat (Variante D vs Baseline):**

    | Achse | Baseline Mean | Hebel-2 Mean | Delta | Innerhalb 2σ? |
    |-------|---------------|--------------|-------|---------------|
    | WIZ-01 | XX.X | XX.X | +X.X | <ja/nein> |
    | WIZ-02 | XX.X | XX.X | +X.X | <ja/nein> |
    | WIZ-03 | XX.X | XX.X | +X.X | <ja/nein> |
    | Finanzplan-Sub | XX.X | XX.X | +X.X | — |

    **Wave-Cost:** XX.X ct/Eintrag Aufschlag durch Compliance-Re-Trigger
    **Compliance-Stats:** N Einträge hatten Violations → davon N abgefangen durch Loop-Count=1

    **Empfehlung:** <Hebel beibehalten / verfeinern / verwerfen>

    **Plan-Phase Cross-Reference:** Wenn Hebel 1 (Plan 05-05) bereits WIZ-01 stark anhebt, ist Hebel-2-Delta kleiner — beide tunen ähnliche Schwäche. Decision in Plan 05-08 (Wave 4): welche Hebel-Kombination wird Default.
    ```

    **Conventional Commit:** `docs(05): wave-3 hebel-2 compliance-stage eval-delta in tuning-hebel-2.md (intermediate)`
  </action>
  <verify>
    <automated>INTERMEDIATE=.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-2.md && test -f "$INTERMEDIATE" && grep -q "Hebel 2" "$INTERMEDIATE" && grep -q "Compliance-Check-Stage" "$INTERMEDIATE" && grep -q "PIPELINE_COMPLIANCE_STAGE=1" "$INTERMEDIATE" && grep -q "Loop-Count" "$INTERMEDIATE" && test "$(ls data/eval/pipeline-reports/*.json 2>/dev/null | wc -l)" -ge 4</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-2.md` (intermediate) hat Hebel-2-Block
    - Achsen-Tabelle befüllt mit Baseline/Hebel-2/Delta/2σ
    - Compliance-Stats: Violations-Count, Loop-Count-Abfangung
    - Empfehlung dokumentiert
    - Mindestens 4 Report-JSONs in `data/eval/pipeline-reports/`
  </acceptance_criteria>
  <done>Hebel 2 ist eval-gemessen, Plan 05-08 hat Daten für finale Default-Entscheidung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline-State → Compliance-Check | runComplianceCheck ist deterministisch (kein LLM) — gleicher Output bei gleichem Input |
| LLM-Repair-Call → Pipeline-Output | Genau wie REVISION_SYSTEM (60s timeout, Cost-Logged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06-01 | DoS | Compliance↔Revision-Pingpong endless loop ohne Loop-Count | high | mitigate | complianceLoopCount=1 enforced als Code-Pflicht; Test 4 verifiziert das explizit (kein 2. Iteration) |
| T-05-06-02 | Tampering | TypeScript-Type-Erweiterung PipelineStage trifft alle Verwender → Compile-Errors in unbeteiligten UI-Komponenten | medium | mitigate | `npx tsc --noEmit` als Acceptance-Test enforced — keine TS-Errors |
| T-05-06-03 | DoS | Cost-Aufschlag pro Antrag durch Compliance-Re-Trigger (zusätzlicher LLM-Call wenn Violations) | medium | accept | LLM-Call ist nur bei Violations (selten bei kleinen Dossiers); Cost im Cent-Bereich |
| T-05-06-04 | Information Disclosure | runComplianceCheck schreibt violations[]-Detail in Logs → könnte sensitive Antrags-Inhalte enthalten | low | accept | Logs sind auf Dev/CI-Maschinen, kein User-facing |
| T-05-06-05 | Tampering | Default-Drift: jemand schaltet complianceStageEnabled-Default auf true → bricht Default-Pipeline-Verhalten | medium | mitigate | Plan 05-03 Test "Default-PIPELINE_CONFIG: alle 4 Flags = false" enforced; Wave-4-Plan 05-08 prüft Defaults nochmal vor PR-Closure |
| T-05-06-06 | Spoofing | runComplianceCheck uses Section-Name-String-Match — bei Pipeline-Output mit umformuliertem Section-Name (z.B. "Bedarfsanalyse und Ausgangslage" statt "Bedarfsanalyse") wäre die Compliance "fehlt" falsch-positiv (RESEARCH Pitfall 6) | medium | mitigate | normalizeAbschnittName mit case-insensitive + whitespace-Trim + optional Substring-Match (z.B. Section-Name ENTHÄLT abschnittsname als Substring). Test ergänzt falls Eval-Befund das zeigt |
</threat_model>

<verification>
- types.ts + pipeline.ts + stage-labels.ts + GeneratingProgress.tsx tragen "compliance-check"
- 5 Tests grün
- `npx tsc --noEmit` clean
- Eval-Run gefahren, Report in `pipeline-reports/`
- TUNING.md erweitert
</verification>

<success_criteria>
- Hebel 2 implementiert mit Loop-Count=1
- Stage ist UI-silent
- Eval-Delta gegen Baseline gemessen
- 2 Conventional Commits
</success_criteria>

<output>
Nach Completion: `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-06-SUMMARY.md` mit:
- Hebel-2-Implementations-Übersicht
- Eval-Delta-Tabelle WIZ-01 / WIZ-02 / WIZ-03 / Finanzplan
- Compliance-Stats: wie viele Einträge hatten Violations
- Hinweis Cross-Reference zu Hebel 1: ob beide gemeinsam größeres Delta liefern als Einzeln
</output>

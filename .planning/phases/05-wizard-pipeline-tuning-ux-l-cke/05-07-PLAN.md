---
phase: 05-wizard-pipeline-tuning-ux-l-cke
plan: 07
type: execute
wave: 3
depends_on: [04]
files_modified:
  - lib/wizard/geber-guidance.ts
  - lib/wizard/programm-kriterien.ts
  - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-4.md
autonomous: true
requirements: [WIZ-03]
tags: [phase-5, wave-3, tuning, hebel-4, geber-routing, programm-kriterien]
must_haves:
  truths:
    - "lib/wizard/geber-guidance.ts hat GUIDANCE_V2-Variante mit geschaerften critiqueFocus + sectionStyle pro 8 GeberTyp-Werten (D-20 Hebel 4)"
    - "GUIDANCE-Export waehlt zwischen GUIDANCE_BASE und GUIDANCE_V2 ueber PIPELINE_CONFIG.geberRoutingV2 (Default OFF -> Base = aktuelles Verhalten)"
    - "lib/wizard/programm-kriterien.ts hat ExtraGuidance-Eintraege fuer mindestens 7 weitere der 11 Dossier-Programme — Coverage erreicht 10/11 oder besser"
    - "Eval-A/B-Run zeigt Hebel-4-Delta WIZ-03 pro Cluster (oeffentlich/stiftung/eu/wirtschaftspreis/verband-uni) — Befund in tuning-hebel-4.md (intermediate); Konsolidierung nach data/eval/TUNING.md erfolgt in Plan 05-08 Task 4"
    - "Existierende Tests bleiben gruen — Default-Verhalten unveraendert wenn Flag OFF"
  artifacts:
    - path: "lib/wizard/geber-guidance.ts"
      provides: "Geschaerfte GUIDANCE_V2-Rubrics + Selector ueber PIPELINE_CONFIG"
      contains: "GUIDANCE_V2"
    - path: "lib/wizard/programm-kriterien.ts"
      provides: "ExtraGuidance-Records fuer >= 10 der 11 Dossier-Programme"
      contains: "kultur-macht-stark"
  key_links:
    - from: "lib/wizard/geber-guidance.ts"
      to: "lib/wizard/config.ts"
      via: "PIPELINE_CONFIG.geberRoutingV2-Read"
      pattern: "geberRoutingV2"
    - from: "lib/wizard/programm-kriterien.ts"
      to: "data/richtlinien/"
      via: "Programm-IDs der 11 Dossiers muessen abgedeckt sein"
      pattern: "Record<string"
---

<objective>
Wave 3 Hebel 4: Geber-Typ-Routing schaerfen (D-20 Hebel 4) — `geber-guidance.ts` critiqueFocus/sectionStyle pro GeberTyp praezisieren UND `programm-kriterien.ts` ExtraGuidance um die ~7 Dossiers ergaenzen, die bislang keinen Eintrag haben.

Purpose: WIZ-03-Hebel — Pipeline soll pro Geber-Typ tonalisch passend formulieren. Aktuell sind Rubrics zu generisch. Hebel 4 schaerft das Wording mit Klein-Beispielen + ergaenzt programm-spezifische Hinweise pro Dossier.

Output:
- geber-guidance.ts: Conditional-Erweiterung mit GUIDANCE_V2 bei `PIPELINE_GEBER_ROUTING_V2=1`
- programm-kriterien.ts: >= 7 neue ExtraGuidance-Eintraege fuer Dossiers ohne bisherige Records
- TUNING.md: Hebel-4-Eval-Delta pro Geber-Cluster

**Disjoint zu Plan 05-05 + 05-06:** Plan 05-07 modifiziert NUR geber-guidance.ts + programm-kriterien.ts — keine Datei-Overlap. Echt parallel zu Plan 05-05 + 05-06 in Wave 3.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md
@lib/wizard/geber-guidance.ts
@lib/wizard/programm-kriterien.ts
@lib/wizard/config.ts
@lib/wizard/geber-classification.ts
@data/richtlinien/aktion-mensch-schulkooperation.json
@data/richtlinien/kultur-macht-stark.json
@data/richtlinien/erasmus-schulentwicklung.json
@data/richtlinien/ensam-bmz.json
@data/richtlinien/klimalab-2026.json
@data/richtlinien/berlin-startchancen.json
@data/eval/BASELINE.md
@scripts/eval-pipeline.ts

<interfaces>
From lib/wizard/geber-guidance.ts (Aktueller Stand, Wave-3-Executor verifiziert):
- `export type GeberTyp = "bund" | "land" | "eu" | "stiftung" | "verband" | "programm" | "uni" | "sonstige"`
- `interface GeberGuidance` mit Feldern wie `critiqueFocus`, `sectionStyle` (ggf. weitere)
- `const GUIDANCE: Record<GeberTyp, GeberGuidance> = { ... }`

From lib/wizard/programm-kriterien.ts (~Z.25-139):
- `const KRITERIEN: Record<string, ExtraGuidance>` mit Eintraegen pro Programm-ID
- `export function getExtraGuidance(programmId: string): ExtraGuidance | null`

Aktueller Coverage-Status (welche der 11 Dossiers bereits einen ExtraGuidance-Eintrag haben — Wave-3-Executor verifiziert via grep):
- Wahrscheinlich vorhanden: bmbf-digitalpakt-2, bosch-schulpreis, ferry-porsche-challenge (legacy)
- Wahrscheinlich fehlend: aktion-mensch-schulkooperation, kultur-macht-stark, ensam-bmz, erasmus-schule-2026, erasmus-schulentwicklung, klimalab-2026, berlin-startchancen, ferry-porsche-challenge-2025

From CONTEXT.md D-20 Hebel 4 (Z.73):
> Geber-Typ-Routing ausbauen — `geber-guidance.ts` Rubrics schaerfen, zusaetzliche `programm-kriterien.ts`-`ExtraGuidance`-Eintraege fuer die 11 Dossiers ergaenzen.

From RESEARCH §WIZ-03 Rubric-Vorschlaege Z.560-568 (pro strategischer Cluster):
- oeffentlich: messbare-wirkung / strategiebezug / transferfaehigkeit / kooperationen / nachhaltigkeit-struktur / tonalitaet
- stiftung: mission-passung / konkrete-szene / zielgruppe-spezifisch / wirkung-narrativ / ehrlichkeit / tonalitaet
- eu: europaeischer-mehrwert / querschnittsthemen / partnerschaft-konkret / evaluation-dissemination / innovation / tonalitaet
- wirtschaftspreis: story-driven / vorhaben-praegnant / wirkung-konkret / glaubwuerdigkeit / preis-eignung / tonalitaet
- verband-uni: fachlich-belegt / methodik-explizit / zielgruppe-spezifisch / wirkung-evidenz / kooperationen / tonalitaet
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: geber-guidance.ts GUIDANCE_V2 + Selector via PIPELINE_GEBER_ROUTING_V2</name>
  <files>lib/wizard/geber-guidance.ts</files>
  <read_first>
    - lib/wizard/geber-guidance.ts (komplette Datei lesen — alle 8 GeberTyp-Werte mit aktuellen Feld-Inhalten erfassen)
    - lib/wizard/config.ts (PIPELINE_CONFIG.geberRoutingV2)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md §WIZ-03 Rubric-Vorschlaege Z.560-568
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md D-10/-29 (Cluster-Mapping)
  </read_first>
  <action>
    **Schritt 1 — Bestand sichern:**
    Lese komplette geber-guidance.ts. Identifiziere die Type-Definition von `GeberGuidance` (welche Felder genau hat sie?) und benenne den existierenden Record-Export um:

    ```typescript
    // Bestehender Record bleibt — nur umbenannt:
    const GUIDANCE_BASE: Record<GeberTyp, GeberGuidance> = { /* bisherige 8 Eintraege 1-zu-1 */ };
    ```

    **Schritt 2 — GUIDANCE_V2 als parallel-Record:**
    Neuer Record mit allen 8 GeberTyp-Werten, geschaerfter Wording. Wording-Vorlage (alle Felder ASCII mit ae/oe/ue/ss):

    - `bund`: critiqueFocus = sachlich-strategiebezogen, vermeide PR-Glanz, Strategiebezug konkret (KMK, Bildungsplan, DigitalPakt) NUR wenn aus User-Antworten belegbar; quantifizierbare Indikatoren statt "verbessert Lernerfolg". sectionStyle = Struktur-Nachhaltigkeit + Kooperationspartner mit Namen + Transferfaehigkeit explizit.
    - `land`: wie bund + landesspezifische Bildungsstrategie + kommunale Verankerung; Foederalismus-Kontext (Bundesland muss bekannt sein, sonst Luecken-Marker).
    - `eu`: formell, EU-Konventionen ("Europaeischer Mehrwert", "Querschnittsthemen Gender/Inklusion/Klima"); transnationale Partnerschaft mit ≥ 1 EU-Land + Partnername; Dissemination-Plan + Evaluation-Methodik + Innovation-Charakter.
    - `stiftung`: Mission der Stiftung treffen; erzaehlerisch beginnen mit konkreter Szene (1 Schueler oder 1 Klassen-Situation); Zielgruppe sehr spezifisch (nicht "benachteiligte Schueler"); Wirkungs-Narrativ statt Wirkungs-Tabelle; ehrlich zu Luecken.
    - `verband`: Fachterminologie OK; Methodik explizit; Wirkungs-Evidenz aus User-Antworten / Dossier zitieren (NIE erfinden).
    - `programm`: knapp + story-driven; Vorhaben in 2-3 Saetzen erklaerbar; Schul-als-Ganzes-Perspektive (nicht 1 Lehrer / 1 Klasse); Preis-Eignung-Argument.
    - `uni`: wissenschaftliche Methodik; Hypothesen + Untersuchungs-Design + Erwartete Erkenntnisse; Praxis-Transfer in Schul-Praxis beschrieben.
    - `sonstige`: neutral-sachlich; lieber generisch-praezise als spezifisch-falsch.

    Wenn `GeberGuidance` zusaetzliche Felder hat (z.B. `examples`, `forbiddenPhrases`, `successStories`), GUIDANCE_V2-Werte auch dort schaerfen — am besten 1-2 konkrete Beispiele aus den Dossiers nehmen.

    **Schritt 3 — Selector als Export:**

    ```typescript
    import { PIPELINE_CONFIG } from "./config";

    export const GUIDANCE: Record<GeberTyp, GeberGuidance> = PIPELINE_CONFIG.geberRoutingV2
      ? GUIDANCE_V2
      : GUIDANCE_BASE;
    ```

    **Schritt 4 — Existierende Importeure validieren:**
    ```bash
    grep -r "from.*geber-guidance" lib/ app/ scripts/ __tests__/
    ```
    Pruefe ob jemand `GUIDANCE` direkt importiert — falls ja, der Selector-Export bleibt named `GUIDANCE` und alle bestehenden Importer funktionieren weiter (Default ist GUIDANCE_BASE = bisheriges Verhalten).

    **Schritt 5 — Type-Check:**
    `npx tsc --noEmit` muss ohne Errors laufen — alle 8 GeberTyp-Keys muessen in GUIDANCE_V2 vorhanden sein (TypeScript-Record-Type erzwingt das).

    **Conventional Commit:** `feat(05): wave-3 hebel-4 geber-guidance v2-rubrics + selector (PIPELINE_GEBER_ROUTING_V2-toggle, 8 GeberTyp geschaerft)`
  </action>
  <verify>
    <automated>grep -q "GUIDANCE_V2" lib/wizard/geber-guidance.ts && grep -q "GUIDANCE_BASE" lib/wizard/geber-guidance.ts && grep -q "geberRoutingV2" lib/wizard/geber-guidance.ts && grep -q "PIPELINE_CONFIG" lib/wizard/geber-guidance.ts && npx tsc --noEmit 2>&1 | tee /tmp/wave3-h4-tsc.out ; if grep -q "error TS" /tmp/wave3-h4-tsc.out; then exit 1; fi && npx jest __tests__/lib/wizard/ --bail 2>&1 | tee /tmp/wave3-h4-tests.out && grep -qE "(Tests:.*passed|0 failed)" /tmp/wave3-h4-tests.out</automated>
  </verify>
  <acceptance_criteria>
    - geber-guidance.ts enthaelt GUIDANCE_BASE (alt) + GUIDANCE_V2 (neu) + Selector-Export GUIDANCE
    - GUIDANCE_V2 hat alle 8 GeberTyp-Keys mit geschaerftem Wording
    - Wording fuer alle 8 Typen ist substantiell geschaerft (≥ 100 Zeichen pro Feld)
    - Default-Verhalten (Flag OFF) ist Selector → GUIDANCE_BASE → unveraendert
    - `npx tsc --noEmit` ohne TS-Errors
    - Existierende Wizard-Tests gruen (keine Regressionen)
  </acceptance_criteria>
  <done>geber-guidance.ts hat V2-Variante, Feature-Flag-Toggle funktioniert, Default-Verhalten unveraendert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: programm-kriterien.ts ExtraGuidance-Eintraege fuer >= 7 fehlende Dossier-Programme</name>
  <files>lib/wizard/programm-kriterien.ts</files>
  <read_first>
    - lib/wizard/programm-kriterien.ts (komplette Datei — KRITERIEN-Record sichten + welche Programm-IDs schon Eintraege haben)
    - data/richtlinien/aktion-mensch-schulkooperation.json (relevante Felder fuer ExtraGuidance: rejectGruende, bestPractices, kiAntragGeeignet-Kontext)
    - data/richtlinien/kultur-macht-stark.json
    - data/richtlinien/ensam-bmz.json
    - data/richtlinien/erasmus-schule-2026.json
    - data/richtlinien/erasmus-schulentwicklung.json
    - data/richtlinien/klimalab-2026.json
    - data/richtlinien/berlin-startchancen.json
    - data/richtlinien/ferry-porsche-challenge-2025.json
    - data/foerderprogramme.json (Programm-Beschreibung als Kontext)
  </read_first>
  <action>
    **Schritt 1 — Identifiziere die fehlenden Dossier-Programme:**

    ```bash
    # Welche Dossiers existieren?
    ls data/richtlinien/*.json | xargs -I{} basename {} .json

    # Welche programmIds sind in KRITERIEN gemappt?
    grep -E '"[a-z-]+":' lib/wizard/programm-kriterien.ts | head -20
    ```

    Die Differenzmenge sind die fehlenden Eintraege. Erwartung gemaess RESEARCH: ~7-8 fehlend.

    **Schritt 2 — Pro fehlendes Dossier einen ExtraGuidance-Eintrag bauen:**

    Lese die ExtraGuidance-Type-Definition oben in programm-kriterien.ts — typischerweise:
    ```typescript
    interface ExtraGuidance {
      gewichtet?: string[];   // worauf legt der Geber besonderen Wert
      pflichten?: string[];   // was MUSS im Antrag stehen
      fallen?: string[];      // typische Fall-Pattern
    }
    ```

    Inhalt aus den Dossier-Feldern ableiten:
    - `gewichtet[]` = aus `bestPractices[]` + Foerderschwerpunkten
    - `pflichten[]` = aus `antragsstruktur.abschnitte[]` (Pflicht-Abschnitte) + `formaleVoraussetzungen[]`
    - `fallen[]` = aus `rejectGruende[]` (direkt uebernehmen, paraphrasieren)

    **Beispiel fuer aktion-mensch-schulkooperation (D-20 Hebel 4 Konkretisierung):**

    ```typescript
    "aktion-mensch-schulkooperation": {
      gewichtet: [
        "Inklusion mit Sub-Gruppe ≥ 1 Mensch mit Behinderung als Beteiligter",
        "Wirkungs-Narrativ mit konkreten Szenen (Aktion Mensch praeferiert Geschichten ueber Statistik)",
        "Kontinuitaet ueber Foerderzeitraum hinaus strukturell beschrieben",
      ],
      pflichten: [
        "Kooperationspartner (Verein oder Initiative) mit konkretem Namen + Rolle benannt",
        "Zielgruppen-Definition mit messbarer Beteiligungs-Quote",
        "Bezug zur Aktion-Mensch-Foerdergrundsatz 'Mitten im Leben'",
      ],
      fallen: [
        "Reine Sachausstattung ohne Personen-Engagement",
        "PR-Glanz-Sprache ('zukunftsweisende Massnahme')",
        "Unspezifische Zielgruppe ('benachteiligte Jugendliche')",
      ],
    },
    ```

    Analog fuer kultur-macht-stark, ensam-bmz, erasmus-schule-2026, erasmus-schulentwicklung, klimalab-2026, berlin-startchancen, ferry-porsche-challenge-2025.

    **Schritt 3 — Coverage validieren:**
    Nach Implementation: alle 11 Dossiers haben einen Eintrag in KRITERIEN ODER mindestens 10/11. ferry-porsche-challenge-legacy kann referenzieren ferry-porsche-challenge-2025 (oder bleibt eigener Eintrag).

    **Schritt 4 — Toggle-Pattern (Empfehlung):**
    `programm-kriterien.ts` hat aktuell `KRITERIEN` direkt exportiert. Plan 05-07 erweitert das Mapping additive — neue Eintraege sind defacto nur aktiv wenn LLM-Pipeline sie konsumiert; getExtraGuidance liefert sie automatisch.
    Wenn Toggle gewuenscht (analog zu GUIDANCE_V2), kann ein `KRITERIEN_V2` analog erstellt werden — aber RESEARCH/CONTEXT verlangen das nicht explizit. **Entscheidung Discretion:** Additive-Pattern (neue Eintraege landen direkt im KRITERIEN-Record, gefiltert nur bei `PIPELINE_GEBER_ROUTING_V2=1` ueber den Aufrufer (Pipeline-Code)).
    Falls Pipeline-Code aktuell `getExtraGuidance(programmId)` direkt aufruft (ohne Flag), bleibt Default ON — neue Eintraege wirken sofort. Das ist gewollt fuer Wave 3 Hebel 4 (D-20 sagt "ExtraGuidance ergaenzen" als Tuning-Massnahme, nicht als Feature-Flag).

    **Pruefen ob Pipeline-Code Toggle braucht:**
    ```bash
    grep -r "getExtraGuidance" lib/ scripts/
    ```
    Wenn der Caller schon einen Toggle hat (z.B. `if (PIPELINE_CONFIG.geberRoutingV2) const extra = getExtraGuidance(...)`), passt das. Sonst additive Default-ON.

    **Klare Implementation-Decision:** ExtraGuidance-Eintraege sind additive ohne Toggle. Hebel-4-Toggle wirkt nur fuer GUIDANCE_V2-Selector aus Task 1. Diese Vereinfachung in TUNING.md notieren.

    **Conventional Commit:** `feat(05): wave-3 hebel-4-teil-2 programm-kriterien fuer 7+ dossier-programme ergaenzt`
  </action>
  <verify>
    <automated>python3 -c "import json,glob,re,subprocess; dossiers = sorted([p.split('/')[-1].replace('.json','') for p in glob.glob('data/richtlinien/*.json')]); kfile=open('lib/wizard/programm-kriterien.ts').read(); covered = [d for d in dossiers if f'\"{d}\"' in kfile]; print('total dossiers:', len(dossiers)); print('covered in KRITERIEN:', len(covered)); print('missing:', [d for d in dossiers if d not in covered]); exit(0 if len(covered) >= 10 else 1)" && npx tsc --noEmit 2>&1 | tee /tmp/wave3-h4-task2-tsc.out ; if grep -q "error TS" /tmp/wave3-h4-task2-tsc.out; then exit 1; fi && npx jest __tests__/lib/wizard/ --bail 2>&1 | tee /tmp/wave3-h4-task2-tests.out && grep -qE "(Tests:.*passed|0 failed)" /tmp/wave3-h4-task2-tests.out</automated>
  </verify>
  <acceptance_criteria>
    - mindestens 10 der 11 Dossier-Programme haben einen Eintrag in `KRITERIEN`
    - Pro Eintrag: gewichtet[]/pflichten[]/fallen[] sind nicht leer (mind. 2 Eintraege pro Feld)
    - Inhalte sind aus den Dossier-bestPractices/rejectGruende/formaleVoraussetzungen abgeleitet (nicht erfunden)
    - `npx tsc --noEmit` clean
    - Existierende Tests gruen
  </acceptance_criteria>
  <done>ExtraGuidance-Coverage ist >= 10/11 Dossiers, Pipeline kann pro Dossier praezisere Hinweise liefern.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Eval-A/B-Run Hebel 4 + Intermediate-Markdown tuning-hebel-4.md</name>
  <files>.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-4.md</files>
  <read_first>
    - data/eval/BASELINE.md
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-1-3.md (Wave-3-Geschwister-intermediate aus Plan 05-05, NICHT modifizieren — nur als Kontext lesen)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-2.md (Wave-3-Geschwister-intermediate aus Plan 05-06, NICHT modifizieren — nur als Kontext lesen)
    - scripts/eval-pipeline.ts
  </read_first>
  <action>
    **Eval-Run Hebel 4:**

    ```bash
    PIPELINE_GEBER_ROUTING_V2=1 npx tsx scripts/eval-pipeline.ts --live --N=1 --snapshot --md-summary 2>&1 | tee /tmp/eval-hebel4.log
    ```

    Cost-Schaetzung: ~1.0-1.5 EUR.

    **Auswertung — pro Geber-Cluster Delta WIZ-03 berechnen:**

    ```bash
    node -e '
    const fs = require("fs");
    const files = fs.readdirSync("data/eval/pipeline-reports").filter(f => f.endsWith(".json")).sort();
    const latest = JSON.parse(fs.readFileSync("data/eval/pipeline-reports/" + files[files.length-1], "utf-8"));
    console.log("Per-Geber WIZ-03:", JSON.stringify(latest.aggregate.perGeberGruppe?.wiz03 ?? {}, null, 2));
    '
    ```

    **Intermediate-Markdown anlegen** (Wave-3-Disjunktheits-Pattern: schreibt NICHT direkt nach data/eval/TUNING.md sondern in eigenes intermediate-File `tuning-hebel-4.md` — Plan 05-08 Task 4 konsolidiert alle 3 Wave-3-intermediates nach data/eval/TUNING.md):

    Datei: `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-4.md`

    ```markdown
    ## <ISO-Date> — Wave 3 Hebel 4 (Geber-Typ-Routing V2 + ExtraGuidance-Erweiterung)

    **Konfiguration:** `PIPELINE_GEBER_ROUTING_V2=1`

    **Aenderung Teil 1 (geber-guidance.ts):** GUIDANCE_V2-Record mit geschaerfter critiqueFocus + sectionStyle pro 8 GeberTyp-Werten. Selector ueber PIPELINE_CONFIG.geberRoutingV2.

    **Aenderung Teil 2 (programm-kriterien.ts):** ExtraGuidance-Records fuer 7+ weitere Dossier-Programme (aktion-mensch, kultur-macht-stark, ensam-bmz, erasmus-*, klimalab, berlin-startchancen, ferry-porsche-challenge-2025) — additive ohne Toggle (Decision Plan 05-07).

    **Erwartung:** WIZ-03 Score-Delta > 0 pro Cluster, insbesondere Stiftung + Wirtschaftspreis profitieren (RESEARCH §Pattern 3 — diese Cluster brauchen am meisten Tonalitaets-Spezifik). Oeffentlich + EU profitieren moderat (waren schon relativ konservativ).

    **Eval-Resultat (Variante E vs Baseline):**

    | Cluster | Baseline Mean | Hebel-4 Mean | Delta | Innerhalb 2σ? |
    |---------|---------------|--------------|-------|---------------|
    | oeffentlich | XX.X | XX.X | +X.X | <ja/nein> |
    | stiftung | XX.X | XX.X | +X.X | <ja/nein> |
    | eu | XX.X | XX.X | +X.X | <ja/nein> |
    | wirtschaftspreis | XX.X | XX.X | +X.X | <ja/nein> |
    | verband-uni | XX.X | XX.X | +X.X | <ja/nein> |
    | **WIZ-03 Gesamt** | XX.X | XX.X | +X.X | <ja/nein> |

    **WIZ-01/-02 Seiteneffekt (sollte ~0 sein):**

    | Achse | Baseline Mean | Hebel-4 Mean | Delta |
    |-------|---------------|--------------|-------|
    | WIZ-01 | XX.X | XX.X | +X.X |
    | WIZ-02 | XX.X | XX.X | +X.X |

    **Empfehlung:** <Hebel beibehalten / verfeinern / verwerfen>

    **Cross-Reference zu Hebel 1+2+3:** Hebel 4 ist disjoint zu allen anderen Hebeln (modifiziert unterschiedliche Code-Pfade). Plan 05-08 (Wave 4) trifft Default-Entscheidung welche Hebel-Kombination Production-Default wird.
    ```

    Werte aus Report-JSON eintragen.

    **Conventional Commit:** `docs(05): wave-3 hebel-4 geber-routing eval-delta in tuning-hebel-4.md (intermediate)`
  </action>
  <verify>
    <automated>INTERMEDIATE=.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-4.md && test -f "$INTERMEDIATE" && grep -q "Hebel 4" "$INTERMEDIATE" && grep -q "Geber-Typ-Routing V2" "$INTERMEDIATE" && grep -q "PIPELINE_GEBER_ROUTING_V2=1" "$INTERMEDIATE" && grep -qE "oeffentlich.*stiftung.*eu.*wirtschaftspreis.*verband-uni" "$INTERMEDIATE" && test "$(ls data/eval/pipeline-reports/*.json 2>/dev/null | wc -l)" -ge 5</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/tuning-hebel-4.md` (intermediate) hat Hebel-4-Block
    - Per-Cluster-Delta-Tabelle (5 Cluster) befuellt
    - WIZ-01/-02-Seiteneffekt-Tabelle vorhanden (sollte ~0 sein, sonst Cross-Hebel-Problem)
    - Empfehlung dokumentiert
    - >= 5 Report-JSONs in `data/eval/pipeline-reports/` (Baseline + Hebel 1 + Hebel 3 + Hebel 1+3 + Hebel 2 + Hebel 4 = mind. 5)
  </acceptance_criteria>
  <done>Hebel 4 ist eval-gemessen, Pro-Cluster-Befund dokumentiert. Plan 05-08 hat alle 4 Hebel-Daten fuer finale Default-Decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dossier-Inhalt → ExtraGuidance | Inhalte werden manuell aus Dossier-Feldern abgeleitet, Kolja-reviewbar via PR |
| Geber-Type-String → GUIDANCE_V2-Lookup | Type-Safe via TS-Record-Type |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07-01 | Tampering | GUIDANCE_V2 fehlt einen GeberTyp-Key → TS-Compile-Fehler | low | mitigate | `npx tsc --noEmit` als Acceptance-Test |
| T-05-07-02 | Tampering | Bisherige Importeure verwenden `GUIDANCE` direkt — Selector-Refactor koennte Verhalten ueberraschend aendern | medium | mitigate | Default-Selector zeigt auf GUIDANCE_BASE (= alte Werte) wenn Flag OFF. Existierende Tests gruen → keine Regression |
| T-05-07-03 | Information Disclosure | ExtraGuidance-Eintraege enthalten Dossier-Wortlaute → ggf. Stiftungs-Insider-Info in LLM-Prompts | low | accept | Dossiers sind oeffentlich (Foerderrichtlinien), kein Schutzbedarf |
| T-05-07-04 | Tampering | ExtraGuidance-Eintraege haben generische Floskeln statt programmspezifischer Tipps → keine Hebel-Wirkung | medium | mitigate | Acceptance-Test prueft Mindestlaenge pro Feld; Inhalte aus Dossier-bestPractices/rejectGruende abgeleitet (nicht erfunden) |
| T-05-07-05 | Spoofing | Default-Drift wie in Plan 05-05/05-06: jemand setzt geberRoutingV2-Default auf true | medium | mitigate | Plan 05-03-Test enforced Defaults OFF; Plan 05-08-Lint nochmal |
</threat_model>

<verification>
- geber-guidance.ts kompiliert clean, GUIDANCE_V2 + Selector funktional
- programm-kriterien.ts >= 10/11 Dossier-Coverage
- Eval-Run gefahren
- TUNING.md erweitert
</verification>

<success_criteria>
- Hebel 4 implementiert in 2 Files (geber-guidance.ts + programm-kriterien.ts)
- Eval-A/B-Delta pro Cluster gemessen
- TUNING.md mit Hebel-4-Block
- 3 Conventional Commits (Task 1 + Task 2 + Task 3)
</success_criteria>

<output>
Nach Completion: `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-07-SUMMARY.md` mit:
- Coverage-Tabelle vorher/nachher fuer programm-kriterien.ts
- GUIDANCE_V2-Schaerfung pro GeberTyp als Vergleichs-Tabelle
- WIZ-03-Delta pro Cluster
- Empfehlung fuer Plan 05-08: welcher Hebel wird Production-Default
</output>

---
phase: 05-wizard-pipeline-tuning-ux-l-cke
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - lib/wizard/geber-classification.ts
  - lib/wizard/config.ts
  - __tests__/eval/geber-classification.test.ts
  - __tests__/lib/wizard/config.test.ts
autonomous: true
requirements: [WIZ-03]
tags: [phase-5, wave-1, geber-classification, feature-flags, tdd]
must_haves:
  truths:
    - "lib/wizard/geber-classification.ts existiert und mappt alle 11 Dossier-IDs auf 4-5 strategische Geber-Cluster (D-28)"
    - "getGeberGruppe(programmId) liefert für nicht gemappte programmId Fallback 'unknown' und loggt Console-Warning (Pitfall 4)"
    - "lib/wizard/config.ts existiert und liest 4 Env-Vars (PIPELINE_USE_VORBILD_FORMULIERUNGEN, PIPELINE_COMPLIANCE_STAGE, PIPELINE_SHARP_PROMPTS, PIPELINE_GEBER_ROUTING_V2) — Default OFF (D-22, Anti-Pattern RESEARCH Z.727)"
    - "Tests __tests__/eval/geber-classification.test.ts + __tests__/lib/wizard/config.test.ts sind grün (von it.todo auf lebende Tests umgestellt)"
  artifacts:
    - path: "lib/wizard/geber-classification.ts"
      provides: "Mapping Programm-ID → strategische Geber-Gruppe + Fallback-Logik"
      contains: "getGeberGruppe"
      min_lines: 60
    - path: "lib/wizard/config.ts"
      provides: "Env-Var-gesteuerte Feature-Flags (4 Hebel-Toggles), Modul-globaler PIPELINE_CONFIG"
      contains: "PIPELINE_CONFIG"
      min_lines: 30
    - path: "__tests__/eval/geber-classification.test.ts"
      provides: "11-Dossier-Mapping verifiziert + unknown-Fallback"
      contains: "describe"
    - path: "__tests__/lib/wizard/config.test.ts"
      provides: "Env-Var-Parsing 'true'/'1'/'false'/'0'/'' verifiziert"
      contains: "describe"
  key_links:
    - from: "lib/wizard/geber-classification.ts"
      to: "lib/wizard/geber-guidance.ts"
      via: "GeberGruppe-Type Re-Export / Import (4-5 strategische Cluster überlagern 8 GeberTyp-Werte aus geber-guidance.ts)"
      pattern: "export type GeberGruppe"
    - from: "scripts/eval-pipeline.ts (Wave 2)"
      to: "lib/wizard/geber-classification.ts"
      via: "Import für Per-Geber-Gruppe-Aggregation (D-12)"
      pattern: "import.*geber-classification"
    - from: "lib/wizard/pipeline.ts (Wave 3)"
      to: "lib/wizard/config.ts"
      via: "Import für Feature-Flag-Reads (4 Hebel)"
      pattern: "import.*config"
---

<objective>
Wave 1 Plan 2 von 2: Zwei neue lib/wizard-Module + ihre Tests — `geber-classification.ts` (D-28 Mapping 11 Dossiers → 4-5 strategische Cluster) und `config.ts` (D-22 Env-Var-gesteuerte Feature-Flags für Wave-3-Hebel-A/B-Eval).

Purpose: Wave 2 (Eval-Skript) braucht `getGeberGruppe()` für Per-Geber-Aggregation. Wave 3 (Tuning-Hebel) braucht 4 Env-Var-Toggles für isoliertes Hebel-Eval-Delta-Messen (D-21).

Output:
- lib/wizard/geber-classification.ts mit GeberGruppe-Type + KLASSIFIKATION-Record + getGeberGruppe()-Funktion
- lib/wizard/config.ts mit PIPELINE_CONFIG (frozen) + parseEnvBool()-Helper
- 2 Tests umgestellt von it.todo auf lebende Tests, alle grün

**Discretion:** Feature-Flag-Defaults alle OFF (RESEARCH Empfehlung Z.85-86, verhindert Wave-3-Hebel-Delta-Verzerrung) — kann in Wave 4 finalem Tuning-Plan auf ON gedreht werden.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md
@.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-PATTERNS.md
@lib/wizard/programm-kriterien.ts
@lib/wizard/geber-guidance.ts
@lib/wizard/llm.ts
@data/foerderprogramme.json

<interfaces>
<!-- Aus PATTERNS.md §3 (geber-classification) + RESEARCH §Pattern 3-Vorbild lib/wizard/programm-kriterien.ts -->

From lib/wizard/programm-kriterien.ts (Record-basiertes Mapping-Pattern):
```typescript
const KRITERIEN: Record<string, ExtraGuidance> = {
  "bmbf-digitalpakt-2": { gewichtet: [...], pflichten: [...], fallen: [...] },
  // pro Programm-ID ein Eintrag
};
export function getExtraGuidance(programmId: string): ExtraGuidance | null {
  return KRITERIEN[programmId] ?? null;
}
```

From lib/wizard/llm.ts (Env-Var-Read am Modul-Start, frozen exports):
```typescript
const PROVIDER = process.env.LLM_PROVIDER ?? "deepseek";
export const MODEL_FLASH = PROVIDER === "gemini" ? "gemini-2.0-flash" : "deepseek-chat";
```

From CONTEXT.md D-28 (Initial-Mapping der 11 Dossiers):
- bmbf-digitalpakt-2 → bundesfoerderung (→ Cluster oeffentlich)
- bosch-schulpreis → wirtschaftspreis
- ferry-porsche-challenge-2025 → wirtschaftspreis
- aktion-mensch-schulkooperation → stiftung
- erasmus-schule-2026 → eu
- kultur-macht-stark → verband (→ Cluster verband-uni)
- berlin-startchancen → landesfoerderung (→ Cluster oeffentlich)
- erasmus-schulentwicklung → eu
- ensam-bmz → bundesfoerderung (→ Cluster oeffentlich)
- ferry-porsche-challenge → wirtschaftspreis (legacy)
- klimalab-2026 → ? (Recherche: foerdergeberTyp aus foerderprogramme.json — wahrscheinlich stiftung oder eu)

From RESEARCH Pitfall 4 (Z.793-798): Fallback-Logik für nicht gemappte programmId:
```typescript
export function getGeberGruppe(programmId: string): GeberGruppe | "unknown" {
  return MAPPING[programmId] ?? "unknown";
  // Bei "unknown" loggt Eval-Skript Console-Warning und überlegt foerdergeberTyp-Default
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/wizard/geber-classification.ts + Test umstellen auf lebend (D-28)</name>
  <files>lib/wizard/geber-classification.ts, __tests__/eval/geber-classification.test.ts</files>
  <read_first>
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md (D-10 4-5 Cluster, D-28 Mapping, D-29 verband+programm-Aggregation)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md (§Pitfall 4 Fallback-Logik Z.793-798)
    - lib/wizard/programm-kriterien.ts (Record-Mapping + Getter-Pattern als Vorbild, ~Z.25-160)
    - lib/wizard/geber-guidance.ts (existierende GeberTyp-Union mit 8 Werten — geber-classification.ts überlagert das mit 4-5 strategischen Clustern)
    - data/foerderprogramme.json (foerdergeberTyp pro Programm-ID für Mapping-Recherche, insbesondere klimalab-2026)
    - __tests__/eval/geber-classification.test.ts (Skelett aus Plan 05-01 mit it.todo) — wird in diesem Task umgestellt
  </read_first>
  <behavior>
    - Test 1: `getGeberGruppe("bmbf-digitalpakt-2")` → `"oeffentlich"`
    - Test 2: `getGeberGruppe("bosch-schulpreis")` → `"wirtschaftspreis"`
    - Test 3: `getGeberGruppe("aktion-mensch-schulkooperation")` → `"stiftung"`
    - Test 4: `getGeberGruppe("erasmus-schule-2026")` → `"eu"`
    - Test 5: `getGeberGruppe("kultur-macht-stark")` → `"verband-uni"`
    - Test 6: `getGeberGruppe("ferry-porsche-challenge-2025")` → `"wirtschaftspreis"`
    - Test 7: `getGeberGruppe("xyz-unbekannt-2099")` → `"unknown"` + console.warn-Call (jest.spyOn(console, "warn"))
    - Test 8: Alle 11 Dossier-IDs in `data/richtlinien/*.json` sind im Mapping enthalten (programmatisch via readdir-Vorbereitung — entweder im Test selbst oder als data-Constant import)
  </behavior>
  <action>
    **lib/wizard/geber-classification.ts erstellen:**

    ```typescript
    /**
     * Phase 5 D-28/-29: Mapping Programm-ID → strategische Geber-Gruppe.
     *
     * Überlagert die 8 `GeberTyp`-Werte aus `geber-guidance.ts` mit 4-5
     * strategischen Clustern für WIZ-03-Tonalitäts-Eval (D-10):
     * - "oeffentlich" (bund + land aggregiert)
     * - "stiftung"
     * - "eu"
     * - "wirtschaftspreis"
     * - "verband-uni" (verband + uni/wissenschaft aggregiert)
     *
     * Begründung: `verband` und `programm` als GeberTyp sind semantisch
     * heterogen (bosch nominal "verband" aber praktisch Wirtschaftspreis;
     * ENSA-BMZ nominal "programm" aber EU-formalisiert). Strategische
     * Cluster liefern klarere Tonalitäts-Signale.
     */

    export type GeberGruppe =
      | "oeffentlich"
      | "stiftung"
      | "eu"
      | "wirtschaftspreis"
      | "verband-uni";

    const MAPPING: Readonly<Record<string, GeberGruppe>> = Object.freeze({
      "bmbf-digitalpakt-2": "oeffentlich",
      "berlin-startchancen": "oeffentlich",
      "ensam-bmz": "oeffentlich",            // BMZ ist Bundesförderung trotz globalem Lernen-Fokus
      "aktion-mensch-schulkooperation": "stiftung",
      "klimalab-2026": "stiftung",           // verify via foerderprogramme.json — wenn EU, dann "eu"
      "erasmus-schule-2026": "eu",
      "erasmus-schulentwicklung": "eu",
      "bosch-schulpreis": "wirtschaftspreis",
      "ferry-porsche-challenge-2025": "wirtschaftspreis",
      "ferry-porsche-challenge": "wirtschaftspreis",  // legacy-Version
      "kultur-macht-stark": "verband-uni",
    });

    /**
     * Gibt die strategische Geber-Gruppe für eine Programm-ID zurück.
     * Bei nicht gemappten IDs wird "unknown" geliefert + Console-Warning
     * (RESEARCH Pitfall 4 — Eval-Skript darf nicht silent skipen).
     */
    export function getGeberGruppe(programmId: string): GeberGruppe | "unknown" {
      const g = MAPPING[programmId];
      if (g) return g;
      console.warn(
        `[geber-classification] programmId "${programmId}" nicht gemappt — Fallback "unknown"`
      );
      return "unknown";
    }

    /** Liefert alle bekannten Cluster-Namen (für Eval-Aggregation D-12) */
    export const ALL_GEBER_GRUPPEN: readonly GeberGruppe[] = Object.freeze([
      "oeffentlich",
      "stiftung",
      "eu",
      "wirtschaftspreis",
      "verband-uni",
    ]);

    /** Liefert das gesamte Mapping (für Tests + Wave-3-Hebel-4 ExtraGuidance-Lookup) */
    export function listMapping(): Readonly<Record<string, GeberGruppe>> {
      return MAPPING;
    }
    ```

    **VOR Schreiben des Mappings:** klimalab-2026 in data/foerderprogramme.json nachschauen — `foerdergeberTyp` lesen (bund/land/eu/stiftung/sonstige). Wenn `eu` → klimalab-2026 mappt auf `"eu"`, sonst `"stiftung"`. Korrekten Wert ins MAPPING setzen.

    **__tests__/eval/geber-classification.test.ts auf lebende Tests umstellen:**
    Existierende it.todo() durch konkrete it() ersetzen — siehe `<behavior>`-Block. 8 Tests, jest exit 0.

    Beispiel-Test:
    ```typescript
    import { getGeberGruppe, ALL_GEBER_GRUPPEN } from "@/lib/wizard/geber-classification";
    import { readdirSync } from "node:fs";
    import { resolve } from "node:path";

    describe("geber-classification: 11-Dossier-Mapping (D-28)", () => {
      it("bmbf-digitalpakt-2 → oeffentlich", () => {
        expect(getGeberGruppe("bmbf-digitalpakt-2")).toBe("oeffentlich");
      });
      // ... 7 weitere Tests
      it("alle 11 Dossier-IDs sind gemappt", () => {
        const dossierDir = resolve(__dirname, "../../data/richtlinien");
        const ids = readdirSync(dossierDir).filter(f => f.endsWith(".json")).map(f => f.replace(/\.json$/, ""));
        const unknownIds = ids.filter(id => getGeberGruppe(id) === "unknown");
        expect(unknownIds).toEqual([]);
      });
    });
    ```

    **Conventional Commit:** `feat(05): geber-classification module mit 11-dossier-mapping + unknown-fallback (D-28/-29, wave-1)`
  </action>
  <verify>
    <automated>npx jest __tests__/eval/geber-classification.test.ts --bail 2>&1 | tee /tmp/geber-test.out && grep -qE "Tests:.*passed.*8" /tmp/geber-test.out && grep -qE "ALL_GEBER_GRUPPEN" lib/wizard/geber-classification.ts && grep -qE "verband-uni" lib/wizard/geber-classification.ts && test "$(grep -c '\"oeffentlich\"\\|\"stiftung\"\\|\"eu\"\\|\"wirtschaftspreis\"\\|\"verband-uni\"' lib/wizard/geber-classification.ts)" -ge 16</automated>
  </verify>
  <acceptance_criteria>
    - `lib/wizard/geber-classification.ts` existiert, ≥ 60 Zeilen
    - Exports: `GeberGruppe`-Type, `getGeberGruppe`-Funktion, `ALL_GEBER_GRUPPEN`-Constant, `listMapping`-Funktion
    - MAPPING enthält alle 11 Dossier-IDs aus `data/richtlinien/*.json`
    - klimalab-2026 ist auf korrekten Cluster gemappt nach foerdergeberTyp-Lookup (recherchiert in foerderprogramme.json)
    - `unknown`-Fallback loggt Console-Warning
    - 8 Tests in `__tests__/eval/geber-classification.test.ts` grün (Tests umgestellt von it.todo auf it)
  </acceptance_criteria>
  <done>Geber-Classification-Modul existiert, alle 11 Dossier-IDs gemappt, unknown-Fallback funktioniert, Tests grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/wizard/config.ts + Test umstellen auf lebend (D-22 Feature-Flags)</name>
  <files>lib/wizard/config.ts, __tests__/lib/wizard/config.test.ts</files>
  <read_first>
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-CONTEXT.md (D-22 Feature-Flags + RESEARCH-Empfehlung Defaults OFF)
    - .planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-RESEARCH.md (Anti-Pattern Z.727 — Defaults müssen OFF sein für Wave-3-Hebel-Delta)
    - lib/wizard/llm.ts (Env-Var-Read-Pattern am Modul-Start)
    - __tests__/lib/wizard/config.test.ts (Skelett aus Plan 05-01)
  </read_first>
  <behavior>
    - Test 1: `parseEnvBool("true")` → true
    - Test 2: `parseEnvBool("1")` → true
    - Test 3: `parseEnvBool("TRUE")` → true (case-insensitive)
    - Test 4: `parseEnvBool("false")` → false
    - Test 5: `parseEnvBool("0")` → false
    - Test 6: `parseEnvBool("")` → false (default)
    - Test 7: `parseEnvBool(undefined)` → false (default)
    - Test 8: Default `PIPELINE_CONFIG` ohne Env-Vars: alle 4 Flags = false
    - Test 9: Mit Env-Var-Setting via `process.env.PIPELINE_USE_VORBILD_FORMULIERUNGEN="1"` (vor require-Reload) → useVorbildFormulierungen=true
  </behavior>
  <action>
    **lib/wizard/config.ts erstellen:**

    ```typescript
    /**
     * Phase 5 D-22: Env-Var-gesteuerte Feature-Flags für Wave-3-Tuning-Hebel.
     *
     * Defaults sind alle OFF — Wave 3 misst Delta pro Hebel ein/aus
     * (RESEARCH Anti-Pattern Z.727 — Defaults ON würde Per-Hebel-A/B-Eval
     * unmöglich machen). Nach erfolgreichem Tuning werden die Defaults in
     * einem finalem Plan auf ON gesetzt (Phase 5 Wave 4 oder Phase 5.1).
     */

    /**
     * Parsed einen Env-Var-String zu boolean.
     * "true"/"1"/"TRUE" → true ; "false"/"0"/""/undefined → false
     */
    export function parseEnvBool(value: string | undefined): boolean {
      if (value === undefined) return false;
      const v = value.trim().toLowerCase();
      return v === "true" || v === "1";
    }

    /**
     * Phase-5-Pipeline-Konfiguration. Frozen at module load.
     * Reads from process.env at startup.
     *
     * **Default state Phase 5:** alle 4 Flags = false.
     */
    export const PIPELINE_CONFIG = Object.freeze({
      /** D-20 Hebel 3: vorbildFormulierungen + bestPractices + rejectGruende in SECTION/REVISION-Prompts injizieren */
      useVorbildFormulierungen: parseEnvBool(process.env.PIPELINE_USE_VORBILD_FORMULIERUNGEN),

      /** D-20 Hebel 2: neue Compliance-Check-Stage zwischen recheck und done aktivieren */
      complianceStageEnabled: parseEnvBool(process.env.PIPELINE_COMPLIANCE_STAGE),

      /** D-20 Hebel 1: geschärfte CRITIQUE/SECTION/REVISION/RECHECK-Prompts mit erweiterten Verbots-Listen */
      sharpPrompts: parseEnvBool(process.env.PIPELINE_SHARP_PROMPTS),

      /** D-20 Hebel 4: geber-guidance.ts Rubrics geschärft + erweiterte programm-kriterien.ts-Records */
      geberRoutingV2: parseEnvBool(process.env.PIPELINE_GEBER_ROUTING_V2),
    });

    /** Typed alias für externe Verwender (Test-Mocking, etc.) */
    export type PipelineConfig = typeof PIPELINE_CONFIG;
    ```

    **__tests__/lib/wizard/config.test.ts auf lebende Tests umstellen:**
    Test-Pattern für Modul-globalen Env-Read: `jest.resetModules()` + `process.env.PIPELINE_X="1"` + `require("@/lib/wizard/config")`.

    ```typescript
    describe("PIPELINE_CONFIG (D-22)", () => {
      const ORIGINAL_ENV = { ...process.env };
      beforeEach(() => {
        delete process.env.PIPELINE_USE_VORBILD_FORMULIERUNGEN;
        delete process.env.PIPELINE_COMPLIANCE_STAGE;
        delete process.env.PIPELINE_SHARP_PROMPTS;
        delete process.env.PIPELINE_GEBER_ROUTING_V2;
        jest.resetModules();
      });
      afterAll(() => { process.env = ORIGINAL_ENV; });

      it("parseEnvBool: 'true' → true", () => {
        const { parseEnvBool } = require("@/lib/wizard/config");
        expect(parseEnvBool("true")).toBe(true);
      });
      // ... 8 weitere Tests
      it("Default-PIPELINE_CONFIG: alle 4 Flags = false", () => {
        const { PIPELINE_CONFIG } = require("@/lib/wizard/config");
        expect(PIPELINE_CONFIG.useVorbildFormulierungen).toBe(false);
        expect(PIPELINE_CONFIG.complianceStageEnabled).toBe(false);
        expect(PIPELINE_CONFIG.sharpPrompts).toBe(false);
        expect(PIPELINE_CONFIG.geberRoutingV2).toBe(false);
      });
      it("PIPELINE_USE_VORBILD_FORMULIERUNGEN=1 → useVorbildFormulierungen=true", () => {
        process.env.PIPELINE_USE_VORBILD_FORMULIERUNGEN = "1";
        const { PIPELINE_CONFIG } = require("@/lib/wizard/config");
        expect(PIPELINE_CONFIG.useVorbildFormulierungen).toBe(true);
      });
    });
    ```

    **Conventional Commit:** `feat(05): pipeline-config feature-flags (4 hebel-toggles, defaults OFF, D-22 wave-1)`
  </action>
  <verify>
    <automated>npx jest __tests__/lib/wizard/config.test.ts --bail 2>&1 | tee /tmp/config-test.out && grep -qE "Tests:.*passed" /tmp/config-test.out && grep -q "useVorbildFormulierungen" lib/wizard/config.ts && grep -q "complianceStageEnabled" lib/wizard/config.ts && grep -q "sharpPrompts" lib/wizard/config.ts && grep -q "geberRoutingV2" lib/wizard/config.ts && grep -q "Object.freeze" lib/wizard/config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/wizard/config.ts` existiert, ≥ 30 Zeilen
    - Exports: `parseEnvBool`-Funktion, `PIPELINE_CONFIG`-Object (Object.freeze), `PipelineConfig`-Type
    - 4 Flags: useVorbildFormulierungen, complianceStageEnabled, sharpPrompts, geberRoutingV2 — alle default false
    - parseEnvBool erkennt "true"/"1"/"TRUE" als true, alles andere als false
    - 9 Tests in `__tests__/lib/wizard/config.test.ts` grün
  </acceptance_criteria>
  <done>Config-Modul existiert, 4 Feature-Flags default OFF, lebende Tests grün. Wave-3-Hebel-Plans (05-05/-06/-07) können Env-Var-getoggelt eingespielt werden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env-Var → PIPELINE_CONFIG | Env-Vars werden am Modul-Load gelesen; gewollte Trust-Anchor für CI/Dev-Override |
| programmId-String → getGeberGruppe | nicht-gemappte IDs liefern "unknown" + Warning; kein Crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Tampering | Feature-Flag-Default-Drift: jemand setzt Default in config.ts auf true → Wave 3 misst Hebel-Delta falsch | medium | mitigate | Acceptance-Test "Default-PIPELINE_CONFIG: alle 4 Flags = false" enforces Defaults OFF; CI-Lint kann das später ergänzen |
| T-05-03-02 | Information Disclosure | console.warn in getGeberGruppe könnte sensitive programmId leaken in CI-Logs | low | accept | programmIds sind aus public foerderprogramme.json, kein PII |
| T-05-03-03 | Tampering | Mapping-Drift: neues Dossier kommt rein (Phase 4 Vollautomation), wird nicht gemappt → "unknown" Cluster verschluckt Eval | medium | mitigate | Test "alle 11 Dossier-IDs sind gemappt" verifiziert das gegen `data/richtlinien/*.json` zur Test-Laufzeit; bricht wenn neues Dossier ohne Mapping reinkommt |
| T-05-03-04 | Spoofing | Env-Var-Injection in CI (PIPELINE_SHARP_PROMPTS="1" gesetzt obwohl nicht beabsichtigt) | low | mitigate | CI-Workflow (Plan 05-08) setzt Flags explizit pro Job; Eval-Skript loggt PIPELINE_CONFIG-Snapshot pro Run (Audit-Trail) |
</threat_model>

<verification>
- Tests grün: `npx jest __tests__/eval/geber-classification.test.ts __tests__/lib/wizard/config.test.ts`
- Module existieren und exportieren erwartete Symbole
- 11/11 Dossier-IDs gemappt
- 4 Feature-Flags default OFF
</verification>

<success_criteria>
- `lib/wizard/geber-classification.ts` (≥ 60 Zeilen) — Mapping vollständig, getGeberGruppe + ALL_GEBER_GRUPPEN + listMapping exportiert
- `lib/wizard/config.ts` (≥ 30 Zeilen) — PIPELINE_CONFIG mit 4 Flags, parseEnvBool exportiert
- 2 Tests umgestellt von it.todo auf lebende Tests, alle 17 Tests (8+9) grün
- 2 Conventional Commits (`feat(05): geber-classification ...`, `feat(05): pipeline-config ...`)
</success_criteria>

<output>
Nach Completion: `.planning/phases/05-wizard-pipeline-tuning-ux-l-cke/05-03-SUMMARY.md` mit:
- Module-Snapshot: Anzahl LOC, Exports
- 11-Dossier-Mapping als Tabelle (programmId → Cluster)
- 4 Feature-Flag-Defaults bestätigt OFF
- Hinweis: Wave 2 Plan 05-04 importiert geber-classification.ts für Per-Geber-Aggregation; Wave 3 Plans (05-05..05-07) importieren config.ts für Hebel-Toggles
</output>

---
phase: 02.1-frontend-polish-stripe-vorbereitung
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - components/Wizard/AntragResult.tsx
  - components/Wizard/AntragSectionNav.tsx
  - __tests__/components/Wizard/AntragSectionNav.test.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "h2-Headings im Antrag-Markdown haben deterministische, kollisionsfreie Anker-IDs (Slug aus h2-Text mit Umlaut-Mapping, max 60 Zeichen, Index-Suffix bei Duplikaten)"
    - "Neue Component `components/Wizard/AntragSectionNav.tsx` rendert eine sticky Sidebar (hidden md:block) mit den h2-Headings als anklickbare Links — IntersectionObserver markiert das aktuell sichtbare Heading mit accent-Klassen (Gold-Border + text-orange-400)"
    - "AntragSectionNav returns null wenn keine h2-Headings im article gefunden werden (kein leerer Sidebar-Container)"
    - "PenLine-Edit-Button erscheint on-hover rechts neben h2 NUR wenn paid=true; Klick verlinkt auf den Wizard-Edit-Pfad `/antrag/{programmId}/wizard?editAnswer=true` (D-14 — KEIN neuer Backend-Endpoint)"
    - "PRINT_MARKDOWN_COMPONENTS bleibt unveraendert (PDF braucht keine Anker)"
    - "PDF-Download-Button hat sm:py-3 fuer 44px Touch-Target (D-03 Path 4)"
  artifacts:
    - path: components/Wizard/AntragSectionNav.tsx
      provides: "Neue Sidebar-Component mit IntersectionObserver"
      min_lines: 50
    - path: components/Wizard/AntragResult.tsx
      provides: "MARKDOWN_COMPONENTS.h2 mit slug-id + scroll-mt-24; AntragSectionNav-Integration; PenLine-Edit-Button bei paid"
      contains: "AntragSectionNav"
    - path: __tests__/components/Wizard/AntragSectionNav.test.tsx
      provides: "4 gruene Tests (Slug-Generation + Umlaut-Mapping + null-Branch + active-State)"
  key_links:
    - from: components/Wizard/AntragResult.tsx
      to: components/Wizard/AntragSectionNav.tsx
      via: "import { AntragSectionNav } + JSX-Render mit articleRef"
      pattern: "AntragSectionNav"
    - from: components/Wizard/AntragSectionNav.tsx
      to: DOM
      via: "querySelectorAll('h2[id]') + IntersectionObserver"
      pattern: "h2\\[id\\]|IntersectionObserver"
---

<objective>
UI-Polish Block B: Antrag-Detail-Lese-UX (D-02 + D-14). Konkret: h2-Anker-IDs deterministisch erzeugen, neue `AntragSectionNav`-Component mit sticky Sidebar + IntersectionObserver-Active-State, on-hover PenLine-Edit-Button neben h2 (nur bei paid=true), Mobile-Touch-Polish auf PDF-Button.

Purpose: Auf langen Antraegen (8+ Sektionen) findet der User Sektionen schneller; Edit-Pfad bleibt im bestehenden Wizard-Edit-Flow (D-14: KEIN neuer regenerate-section-Endpoint). Sticky-Sidebar nur ab md: — Mobile bleibt sequentielles Scrollen (UI-SPEC-Vorgabe).

Output: 1 neue Component + modifizierte AntragResult.tsx + 1 Test-File komplett gruen.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/kolja/edufunds-app/CLAUDE.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-CONTEXT.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-UI-SPEC.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-RESEARCH.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-PATTERNS.md
@/home/kolja/edufunds-app/components/Wizard/CheckoutSuccessClient.tsx

<interfaces>
<!-- Aus AntragResult.tsx extrahiert. Plan modifiziert nur MARKDOWN_COMPONENTS.h2 + Layout-Wrapper. -->

From `components/Wizard/AntragResult.tsx` Z. 1–55 (Imports + MARKDOWN_COMPONENTS):
```typescript
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";
// (erweitern um lucide PenLine + neuen AntragSectionNav-Import)

interface Props {
  programm: Foerderprogramm;
  generation: GenerationArtefacts;
  costs?: CostLedger | null;
  sessionToken?: string;
  paidToken?: string | null;
  onRestart?: () => void;
  onFinanzplanChange?: (plan: Finanzplan) => void;
}

const MARKDOWN_COMPONENTS = {
  h1: ({ children }) => <h1 className="mb-6 text-2xl font-semibold text-slate-100">{children}</h1>,
  h2: ({ children }) => <h2 className="mb-3 mt-8 text-lg font-semibold text-[#c9a227]">{children}</h2>,
  // ... weitere
};

const PRINT_MARKDOWN_COMPONENTS = { /* PDF-Variante — bleibt unveraendert */ };
```

`AntragResult` rendert heute Markdown bei Z. 268 (`<ReactMarkdown remarkPlugins={[remarkGfm]} components={MARKDOWN_COMPONENTS}>{...}`). PDF nutzt PRINT_MARKDOWN_COMPONENTS bei Z. 418.

Slug-Generator (RESEARCH.md §"Sektions-Anker-Slug"):
```typescript
function slugifyHeading(text: string): string {
  return text.toLowerCase()
    .replace(/[äöüß]/g, c => ({ä:"ae",ö:"oe",ü:"ue",ß:"ss"}[c]!))
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-|-$/g, "")
    .slice(0, 60);
}
```

UI-SPEC §"Antrag-Detail Lesemodus — Sektions-Navigation":
- Sidebar: `hidden md:block sticky top-24 max-h-[calc(100vh-8rem)] overflow-y-auto space-y-1 text-xs`
- Aktiver Link: `text-orange-400 border-l-2 border-orange-500 pl-2`
- Inaktiver Link: `text-slate-400 hover:text-slate-200 pl-2`
- Edit-Button: `PenLine`-Icon (Lucide, 14px), opacity-0 group-hover:opacity-100, only paid=true
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AntragSectionNav-Component anlegen + Tests gruen</name>
  <read_first>
    - /home/kolja/edufunds-app/__tests__/components/Wizard/AntragSectionNav.test.tsx (Wave-0-Skelett, 4 todos)
    - /home/kolja/edufunds-app/test/setup.tsx (IntersectionObserver-Mock aus Plan 02.1-01)
    - /home/kolja/edufunds-app/__tests__/components/MatchResultList.test.tsx (RTL-Stil-Vorlage)
    - /home/kolja/edufunds-app/components/Wizard/CheckoutSuccessClient.tsx Z. 25–80 (Cancel-Flag-Pattern fuer useEffect-Cleanup)
    - /home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-RESEARCH.md §4.1 (komplettes AntragSectionNav-Vorbild)
  </read_first>
  <behavior>
    - Test 1: slugifyHeading("Vorhaben und Anliegen") === "vorhaben-und-anliegen"
    - Test 2: slugifyHeading("Maßnahmen und Förderung") === "massnahmen-und-foerderung"
    - Test 3: AntragSectionNav mit articleRef der KEIN h2 enthaelt rendert null (kein DOM-Output)
    - Test 4: AntragSectionNav mit 2 h2-Elementen rendert 2 Links; nach Mock-IntersectionObserver-Trigger ist `text-orange-400`-Klasse auf dem aktiven Link
    - Test 5 (real, neu): Duplikat-h2-Texte erhalten Suffix `-2`, `-3` etc.
  </behavior>
  <files>components/Wizard/AntragSectionNav.tsx, __tests__/components/Wizard/AntragSectionNav.test.tsx</files>
  <action>
**A. `components/Wizard/AntragSectionNav.tsx` (neu):**

```typescript
"use client";

import { useEffect, useState } from "react";

interface Section {
  id: string;
  label: string;
}

interface Props {
  /** Ref auf den article-Container, in dem die h2-Headings stehen. */
  articleRef: React.RefObject<HTMLElement | null>;
}

/**
 * Slug-Generator fuer h2-Heading-Anker-IDs.
 * - Umlaut-Mapping (ae/oe/ue/ss)
 * - lowercase + non-alphanum durch '-'
 * - max 60 Zeichen
 * Exportiert fuer Unit-Tests.
 */
export function slugifyHeading(text: string): string {
  return text
    .toLowerCase()
    .replace(/[äöüß]/g, (c) => ({ ä: "ae", ö: "oe", ü: "ue", ß: "ss" }[c]!))
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-|-$/g, "")
    .slice(0, 60);
}

/**
 * Sticky Sidebar mit Liste der h2-Headings im Antrag-Article.
 * - hidden md:block — auf Mobile keine Sidebar (UI-SPEC D-03)
 * - IntersectionObserver markiert das aktuell sichtbare Heading
 * - returns null wenn keine h2 vorhanden
 */
export function AntragSectionNav({ articleRef }: Props) {
  const [sections, setSections] = useState<Section[]>([]);
  const [activeId, setActiveId] = useState<string | null>(null);

  useEffect(() => {
    const article = articleRef.current;
    if (!article) return;
    const h2s = Array.from(article.querySelectorAll("h2[id]")) as HTMLElement[];
    setSections(
      h2s.map((h) => ({
        id: h.id,
        label: (h.textContent ?? "").trim(),
      }))
    );

    if (h2s.length === 0) return;

    let cancelled = false;
    const observer = new IntersectionObserver(
      (entries) => {
        if (cancelled) return;
        const visible = entries
          .filter((e) => e.isIntersecting)
          .sort((a, b) => a.boundingClientRect.top - b.boundingClientRect.top);
        if (visible.length > 0) setActiveId(visible[0].target.id);
      },
      { rootMargin: "-100px 0px -50% 0px" }
    );
    h2s.forEach((h) => observer.observe(h));
    return () => {
      cancelled = true;
      observer.disconnect();
    };
  }, [articleRef]);

  if (sections.length === 0) return null;

  return (
    <nav
      aria-label="Antrag-Sektionen"
      className="hidden md:block sticky top-24 max-h-[calc(100vh-8rem)] overflow-y-auto space-y-1 text-xs"
    >
      {sections.map((s) => (
        <a
          key={s.id}
          href={`#${s.id}`}
          className={
            s.id === activeId
              ? "block border-l-2 border-orange-500 pl-2 text-orange-400"
              : "block pl-2 text-slate-400 hover:text-slate-200"
          }
        >
          {s.label}
        </a>
      ))}
    </nav>
  );
}
```

**B. `__tests__/components/Wizard/AntragSectionNav.test.tsx` (it.todos unskippen + 1 neuer Test):**

```typescript
import { render, screen } from "@testing-library/react";
import { createRef, useRef } from "react";
import { AntragSectionNav, slugifyHeading } from "@/components/Wizard/AntragSectionNav";

describe("slugifyHeading — D-02", () => {
  it("generiert 'vorhaben-und-anliegen' aus 'Vorhaben und Anliegen' — D-02", () => {
    expect(slugifyHeading("Vorhaben und Anliegen")).toBe("vorhaben-und-anliegen");
  });

  it("mapped Umlaute: 'Maßnahmen und Förderung' → 'massnahmen-und-foerderung' — D-02", () => {
    expect(slugifyHeading("Maßnahmen und Förderung")).toBe("massnahmen-und-foerderung");
  });

  it("kuerzt auf 60 Zeichen — D-02", () => {
    const long = "Lange Ueberschrift " + "x".repeat(100);
    expect(slugifyHeading(long).length).toBeLessThanOrEqual(60);
  });
});

describe("AntragSectionNav — D-02", () => {
  it("returns null wenn keine h2-Elemente im article — D-02", () => {
    function Wrapper() {
      const ref = useRef<HTMLElement>(null);
      return (
        <div>
          <article ref={ref}>{/* keine h2 */}</article>
          <AntragSectionNav articleRef={ref} />
        </div>
      );
    }
    const { container } = render(<Wrapper />);
    expect(container.querySelector("nav")).toBeNull();
  });

  it("rendert einen Link pro h2-Element — D-02", () => {
    function Wrapper() {
      const ref = useRef<HTMLElement>(null);
      return (
        <div>
          <article ref={ref}>
            <h2 id="vorhaben">Vorhaben</h2>
            <h2 id="finanzierung">Finanzierung</h2>
          </article>
          <AntragSectionNav articleRef={ref} />
        </div>
      );
    }
    render(<Wrapper />);
    expect(screen.getByText("Vorhaben")).toBeInTheDocument();
    expect(screen.getByText("Finanzierung")).toBeInTheDocument();
  });

  // W9 — Duplikat-Suffix-Test fuer den buildMarkdownComponents-Code-Pfad in AntragResult.tsx (Pitfall 5).
  // Der Test wickelt h2-Elemente wie der Markdown-Render und verifiziert, dass Duplikate -2/-3-Suffixe bekommen.
  it("vergibt -2/-3-Suffix bei Duplikat-h2 — D-02", () => {
    // Mock-Markdown-Render: simuliert zwei identische h2-Texte ("Maintenance"),
    // schreibt Anker-IDs nach dem gleichen Pitfall-5-Pattern wie buildMarkdownComponents().
    const usedIds = new Map<string, number>();
    const mkId = (text: string) => {
      const base = slugifyHeading(text) || "section";
      const count = (usedIds.get(base) ?? 0) + 1;
      usedIds.set(base, count);
      return count === 1 ? base : `${base}-${count}`;
    };

    const id1 = mkId("Maintenance");
    const id2 = mkId("Maintenance");
    const id3 = mkId("Maintenance");

    expect(id1).toBe("maintenance");
    expect(id2).toBe("maintenance-2");
    expect(id3).toBe("maintenance-3");
    // Eindeutigkeit der DOM-Anker-IDs garantieren:
    expect(new Set([id1, id2, id3]).size).toBe(3);
  });
});
```

Hinweis IntersectionObserver-Active-State-Test: Der Mock aus `test/setup.tsx` triggert kein echtes Intersection-Event. Der active-State-Test wird daher vereinfacht: Wir testen nur das initiale Rendering (kein activeId, alle Links inaktiv). Der active-State-Branch wird via Code-Review verifiziert (RESEARCH §4.1 zitiert).

Wenn der Test mit IntersectionObserver-Mock instabil ist (z. B. observe-Aufruf wirft), dann den active-State-Test als `it.todo` belassen mit Begruendungs-Kommentar — die anderen 4 Tests reichen fuer Acceptance.

Alle vorigen `it.todo`-Bloecke aus diesem File entfernen oder durch obige Tests ersetzen.
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && npm test -- --runTestsByPath __tests__/components/Wizard/AntragSectionNav.test.tsx --silent 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/kolja/edufunds-app/components/Wizard/AntragSectionNav.tsx` exit 0
    - `grep -q "export function slugifyHeading" /home/kolja/edufunds-app/components/Wizard/AntragSectionNav.tsx` exit 0
    - `grep -q "export function AntragSectionNav" /home/kolja/edufunds-app/components/Wizard/AntragSectionNav.tsx` exit 0
    - `grep -q "IntersectionObserver" /home/kolja/edufunds-app/components/Wizard/AntragSectionNav.tsx` exit 0
    - `grep -q "hidden md:block sticky" /home/kolja/edufunds-app/components/Wizard/AntragSectionNav.tsx` exit 0
    - `grep -q "border-orange-500\|text-orange-400" /home/kolja/edufunds-app/components/Wizard/AntragSectionNav.tsx` exit 0
    - `grep -q "vorhaben-und-anliegen" /home/kolja/edufunds-app/__tests__/components/Wizard/AntragSectionNav.test.tsx` exit 0
    - `grep -q "massnahmen-und-foerderung" /home/kolja/edufunds-app/__tests__/components/Wizard/AntragSectionNav.test.tsx` exit 0
    - `grep -q "maintenance-2\|maintenance-3" /home/kolja/edufunds-app/__tests__/components/Wizard/AntragSectionNav.test.tsx` exit 0  # W9 — Duplikat-Suffix-Test
    - `npm test -- --runTestsByPath __tests__/components/Wizard/AntragSectionNav.test.tsx` Exit 0
    - `npx tsc --noEmit --skipLibCheck` Exit 0
  </acceptance_criteria>
  <done>Component existiert mit slugifyHeading + AntragSectionNav-Export, 4+ Tests gruen, alle todos entfernt.</done>
</task>

<task type="auto">
  <name>Task 2: AntragResult MARKDOWN_COMPONENTS.h2 mit Anker-IDs + AntragSectionNav-Integration + Edit-Button</name>
  <read_first>
    - /home/kolja/edufunds-app/components/Wizard/AntragResult.tsx (volle 427 Zeilen, vor allem Z. 1–55 Imports + MARKDOWN_COMPONENTS, Z. 192–280 Render-Block, Z. 220–232 PDF-Button)
    - /home/kolja/edufunds-app/components/Wizard/AntragSectionNav.tsx (aus Task 1)
    - /home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-UI-SPEC.md §"Antrag-Detail Lesemodus — Sektions-Navigation"
    - /home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-RESEARCH.md §4.1 + Pitfall 5 (Duplikat-Slugs)
  </read_first>
  <files>components/Wizard/AntragResult.tsx</files>
  <action>
**A. Imports erweitern (oberste Zeilen):**

- Lucide-Import um `PenLine` ergaenzen.
- Neuen Import: `import { AntragSectionNav, slugifyHeading } from "./AntragSectionNav";`
- React-Import um `useRef` ergaenzen.

**B. `MARKDOWN_COMPONENTS.h2` (Z. 30–32) ersetzen — mit Slug-ID + scroll-mt-24 + group-Wrapper fuer Edit-Button:**

Aktueller Code:
```typescript
h2: ({ children }: { children?: React.ReactNode }) => (
  <h2 className="mb-3 mt-8 text-lg font-semibold text-[#c9a227]">{children}</h2>
),
```

Ersetzen durch (Closure ueber `paid` und `programmId` benoetigt — siehe Schritt C):
```typescript
// MARKDOWN_COMPONENTS muss Closure ueber paid + programmId haben.
// → Die Const wird zur Funktion `buildMarkdownComponents(paid, programmId)`.
function buildMarkdownComponents(paid: boolean, programmId: string) {
  // Duplikat-Tracking pro Render: gleicher h2-Text bekommt -2, -3 Suffix.
  const usedIds = new Map<string, number>();
  return {
    h1: ({ children }: { children?: React.ReactNode }) => (
      <h1 className="mb-6 text-2xl font-semibold text-slate-100">{children}</h1>
    ),
    h2: ({ children }: { children?: React.ReactNode }) => {
      const text =
        typeof children === "string"
          ? children
          : Array.isArray(children)
            ? children.filter((c) => typeof c === "string").join("")
            : String(children ?? "");
      const baseSlug = slugifyHeading(text) || "section";
      const count = (usedIds.get(baseSlug) ?? 0) + 1;
      usedIds.set(baseSlug, count);
      const id = count === 1 ? baseSlug : `${baseSlug}-${count}`;
      return (
        <h2
          id={id}
          className="group mb-3 mt-8 flex items-center gap-2 text-lg font-semibold text-[#c9a227] scroll-mt-24"
        >
          <span>{children}</span>
          {paid && (
            <a
              href={`/antrag/${programmId}/wizard?editAnswer=true`}
              className="opacity-0 group-hover:opacity-100 text-slate-500 hover:text-slate-200 transition"
              title="Antwort zurueck und neu beantworten"
              aria-label="Sektion bearbeiten"
            >
              <PenLine className="h-3.5 w-3.5" />
            </a>
          )}
        </h2>
      );
    },
    h3: ({ children }: { children?: React.ReactNode }) => (
      <h3 className="mb-2 mt-6 text-base font-semibold text-slate-100">{children}</h3>
    ),
    p: ({ children }: { children?: React.ReactNode }) => (
      <p className="mb-4 leading-relaxed text-slate-200">{children}</p>
    ),
    strong: ({ children }: { children?: React.ReactNode }) => (
      <strong className="font-semibold text-slate-100">{children}</strong>
    ),
    em: ({ children }: { children?: React.ReactNode }) => (
      <em className="italic text-slate-200">{children}</em>
    ),
    ul: ({ children }: { children?: React.ReactNode }) => (
      <ul className="mb-4 ml-6 list-disc space-y-1 text-slate-200">{children}</ul>
    ),
    ol: ({ children }: { children?: React.ReactNode }) => (
      <ol className="mb-4 ml-6 list-decimal space-y-1 text-slate-200">{children}</ol>
    ),
    li: ({ children }: { children?: React.ReactNode }) => (
      <li className="leading-relaxed">{children}</li>
    ),
  };
}
```

Die alte `const MARKDOWN_COMPONENTS = { ... }` (Z. 26–54) **komplett entfernen** und durch obige `buildMarkdownComponents`-Function ersetzen.

`PRINT_MARKDOWN_COMPONENTS` (Z. 56–116) **bleibt unveraendert** (PDF braucht keine Anker und keinen Edit-Button).

**C. Im Component-Body (`AntragResult({...}: Props)`) Markdown-Components per Closure herstellen:**

Im Funktions-Body, oberhalb des JSX-Returns, neue Konstante:
```typescript
const paid = !!paidToken;
const markdownComponents = buildMarkdownComponents(paid, programm.id);
const articleRef = useRef<HTMLElement>(null);
```

Den ReactMarkdown-Aufruf bei Z. 268 anpassen:
- `components={MARKDOWN_COMPONENTS}` → `components={markdownComponents}`

**D. Layout-Wrapper fuer Sidebar (md:+):**

Den bestehenden Container, der das `<article>`-Element fuer Markdown haelt, in ein Grid-Layout einwickeln. Heute ist `<article ref={...}>` Teil eines `<div className="rounded-xl border ...">`-Wrappers. Aenderung:

1. Identifiziere den Bereich im Render, wo `<article>` mit dem ReactMarkdown den Hauptinhalt rendert (Z. ~257–278).
2. Wickele das `<article>` in ein Grid-Wrapper:
   ```jsx
   <div className="md:grid md:grid-cols-[1fr_180px] md:gap-8">
     <article ref={articleRef} className="...bestehende Klassen...">
       <ReactMarkdown remarkPlugins={[remarkGfm]} components={markdownComponents}>
         {fullText}
       </ReactMarkdown>
     </article>
     <AntragSectionNav articleRef={articleRef} />
   </div>
   ```
3. Pruefe: das `<article>` braucht `ref={articleRef}` damit AntragSectionNav die h2-Elemente findet. Falls heute schon ein anderes Ref auf das article-Element gesetzt ist (z. B. fuer `printRef`-Logik), nutze `useRef` mit zwei separaten Refs ODER `mergeRefs`-Hack — wahrscheinlicher Fall: heutiges `printRef` ist auf einem **separaten** ausgeblendeten DOM fuer PDF (siehe Z. 410–425), der article fuer den Browser-Render hat noch keinen Ref. → einfach articleRef anhaengen.

**E. PDF-Download-Button Mobile-Touch (D-03 Path 4):**

- Im Header-Block des Antrags (Z. 220–232) den PDF-Download-Button: Klassen-String enthaelt heute `px-3 py-2 text-sm` → ergaenze `sm:py-3` direkt nach `py-2`.

**F. Nicht-Regress-Pruefung:**
- `PRINT_MARKDOWN_COMPONENTS`-Map bleibt 1:1 erhalten — `grep -A 70 "const PRINT_MARKDOWN_COMPONENTS" components/Wizard/AntragResult.tsx` muss noch das gleiche Schema zeigen.
- Bestehende Render-Faelle (Paywall-Blur, Critique-Block, Finanzplan) bleiben unangetastet.
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && npx tsc --noEmit --skipLibCheck 2>&1 | tail -15 && npm test -- --silent 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "buildMarkdownComponents" /home/kolja/edufunds-app/components/Wizard/AntragResult.tsx` exit 0
    - `grep -q "AntragSectionNav" /home/kolja/edufunds-app/components/Wizard/AntragResult.tsx` exit 0
    - `grep -q "scroll-mt-24" /home/kolja/edufunds-app/components/Wizard/AntragResult.tsx` exit 0
    - `grep -q "PenLine" /home/kolja/edufunds-app/components/Wizard/AntragResult.tsx` exit 0
    - `grep -q "editAnswer=true" /home/kolja/edufunds-app/components/Wizard/AntragResult.tsx` exit 0
    - `grep -q "PRINT_MARKDOWN_COMPONENTS" /home/kolja/edufunds-app/components/Wizard/AntragResult.tsx` exit 0
    - `grep -q "articleRef" /home/kolja/edufunds-app/components/Wizard/AntragResult.tsx` exit 0
    - `grep -q "sm:py-3" /home/kolja/edufunds-app/components/Wizard/AntragResult.tsx` exit 0
    - `npx tsc --noEmit --skipLibCheck` Exit 0
    - `npm test -- --silent` Exit 0 (kein Regress)
  </acceptance_criteria>
  <done>AntragResult rendert h2-Anker-IDs, Sidebar-Nav nur md:+, PenLine-Edit-Button bei paid, PDF-Button hat sm:py-3. Volle Test-Suite gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → AntragSectionNav | querySelectorAll auf article-Inhalt — Inhalt kommt aus serverseitigem Markdown-Render von AI-erzeugtem Text |
| Edit-Button-Link → Wizard-Edit-Pfad | Hard-Link auf `/antrag/{programmId}/wizard?editAnswer=true` — keine User-Eingabe in URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1.04-01 | Information Disclosure | h2-Anker-IDs | accept | Anker-IDs sind Slugs aus AI-erzeugtem h2-Text — kein PII, keine Tokens |
| T-02.1.04-02 | Tampering | querySelector-Injection ueber AI-h2-Texte | mitigate | `slugifyHeading` whitelistet `[a-z0-9]+`, alles andere wird zu `-`. Kein DOM-XSS-Vector ueber id-Attribut |
| T-02.1.04-03 | Information Disclosure | PenLine-Link sichtbar nur bei paid | mitigate | `paid` aus `paidToken`-Closure, kein localStorage-Read im Component (Server-Side gerendert) |
| T-02.1.04-04 | Denial of Service | IntersectionObserver auf vielen h2 | accept | Max ~10 h2 pro Antrag (DigitalPakt 2.0 hat 8 Sektionen), kein Performance-Konzern |
</threat_model>

<verification>
- Tests: `npm test -- __tests__/components/Wizard/AntragSectionNav.test.tsx` → 4+ gruen
- Volle Suite: `npm test -- --silent` Exit 0
- TypeScript: `npx tsc --noEmit --skipLibCheck` Exit 0
- Lint: `npm run lint -- components/Wizard/AntragResult.tsx components/Wizard/AntragSectionNav.tsx` ohne Fehler
- Visuell (manuell, nicht-blockierend): Antragsseite mit existierendem Antrag laden, Sidebar erscheint auf md+, Klick scrollt zu Anker
</verification>

<success_criteria>
- AntragSectionNav existiert + 4 Tests gruen
- AntragResult.tsx rendert h2-Anker-IDs + Edit-Button bei paid + PDF-Button hat sm:py-3
- PRINT_MARKDOWN_COMPONENTS unveraendert
- Volle Test-Suite gruen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-04-SUMMARY.md` mit:
- AntragSectionNav-Loc + Test-Anzahl
- AntragResult.tsx-Diff-Summary
- Hinweise zu Layout-Grid + ggf. Issues mit articleRef-Mehrfach-Belegung
</output>

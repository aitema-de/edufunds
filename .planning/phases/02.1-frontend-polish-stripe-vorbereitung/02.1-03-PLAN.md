---
phase: 02.1-frontend-polish-stripe-vorbereitung
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - components/Wizard/MyAntraegeClient.tsx
  - components/Wizard/MatchResultList.tsx
  - components/Wizard/StartClient.tsx
  - components/Wizard/AnliegenForm.tsx
  - components/Wizard/QuestionCard.tsx
  - __tests__/components/Wizard/MatchResultList.test.tsx
  - __tests__/components/Wizard/MyAntraegeClient.test.tsx
autonomous: true
requirements: [UI-02, UI-03]
must_haves:
  truths:
    - "/antrag/meine zeigt bei leerem sessions-Array Heading 'Noch kein Antrag begonnen' + CTA 'Anliegen schildern' verlinkt auf /antrag/start (D-05)"
    - "MatchResultList zeigt bei matches=[] Heading 'Kein Programm passt zu diesem Anliegen' + 3 Reformulierungs-Tipps + CTA 'Anliegen neu formulieren' (D-05)"
    - "Klick auf 'Anliegen neu formulieren' ruft onReset, das in StartClient den matchState zurueck auf null setzt — User kann erneut Anliegen schildern (D-05)"
    - "AnliegenForm-Textarea hat sm:min-h-[140px] (D-03 Mobile)"
    - "QuestionCard-Submit-Button hat sm:py-3 fuer 44px Touch-Target (D-03 Mobile)"
    - "MatchResultList Programm-Details-Link + 'Antrag starten'-Button haben sm:py-3 (D-03 Mobile)"
    - "MyAntraegeClient Session-Cards stacken auf Mobile (flex-col sm:flex-row sm:flex-wrap, D-03)"
  artifacts:
    - path: components/Wizard/MyAntraegeClient.tsx
      provides: "Action-orientierter Empty-State + Mobile-Stack-Layout"
      contains: "Anliegen schildern"
    - path: components/Wizard/MatchResultList.tsx
      provides: "Empty-State mit SearchX-Icon + 3 Tipps + onReset; Mobile-Touch sm:py-3"
      contains: "onReset"
    - path: components/Wizard/StartClient.tsx
      provides: "handleReset-Callback + onReset-Prop durchgereicht"
      contains: "handleReset"
    - path: components/Wizard/AnliegenForm.tsx
      provides: "sm:min-h-[140px] auf Textarea"
      contains: "sm:min-h-"
    - path: components/Wizard/QuestionCard.tsx
      provides: "sm:py-3 auf Submit-Button"
      contains: "sm:py-3"
  key_links:
    - from: components/Wizard/MatchResultList.tsx
      to: components/Wizard/StartClient.tsx
      via: "onReset-Prop ruft handleReset, das matchState=null setzt"
      pattern: "onReset"
    - from: components/Wizard/MyAntraegeClient.tsx
      to: app/antrag/start
      via: "Empty-State-CTA href=/antrag/start"
      pattern: "/antrag/start"
---

<objective>
UI-Polish Block A: Action-orientierte Empty-States (D-05) + Mobile-Touch-Optimierung der Critical-Paths Anliegen/Match/Wizard-Antwort (D-03). Reine Frontend-Aenderungen in 5 Components, keine API-Erweiterungen.

Purpose: User mit 0 Antraegen oder 0 Match-Treffern bekommt einen klaren naechsten Schritt statt einer Sackgasse — wichtig fuer Pilot-UAT-Sessions in Phase 6 (Pilot soll nicht stehenbleiben). Mobile-Touch-Targets >= 44px senken Eingabefehler-Rate auf iPhone-Safari/Android-Chrome.

Output: 5 modifizierte Components + 2 Component-Test-Files unskipped + gruen.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/kolja/edufunds-app/CLAUDE.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-CONTEXT.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-UI-SPEC.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-RESEARCH.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-PATTERNS.md

<interfaces>
<!-- Aktueller State der zu modifizierenden Components — Plan haengt an diese Signaturen. -->

From `components/Wizard/MatchResultList.tsx` Z. 24–28 (vor Polish):
```typescript
interface Props {
  matches: MatchEntry[];
  onStartAntrag: (entry: MatchEntry) => void;
}
export function MatchResultList({ matches, onStartAntrag }: Props) { ... }
```

Heutiger Empty-State (Z. 36–50, wird ersetzt):
```typescript
if (matches.length === 0) {
  return (
    <div className="rounded-xl border border-slate-700/50 bg-slate-800/40 p-8 text-center">
      <h3 className="mb-2 text-lg font-semibold text-slate-200">
        Keine passenden Programme gefunden
      </h3>
      <p className="mx-auto max-w-md text-sm text-slate-400">
        Versuch es noch einmal mit einer anderen Formulierung ...
      </p>
    </div>
  );
}
```

From `components/Wizard/StartClient.tsx` Z. 32–40 + Z. 155–169 (StateMachine + JSX):
```typescript
const [matchState, setMatchState] = useState<MatchState>(null);
const [lastInput, setLastInput] = useState<AnliegenValues | null>(null);
const [isSecondRound, setIsSecondRound] = useState(false);
// ...
{matchState !== null && (
  <div className="pt-2">
    {matchState.kind === "ranking" && (
      <MatchResultList matches={matchState.matches} onStartAntrag={startAntrag} />
    )}
    ...
  </div>
)}
```

From `components/Wizard/MyAntraegeClient.tsx` Z. 133–154 (Empty-State, wird modifiziert):
```typescript
if (sessions.length === 0) {
  return (
    <div className="rounded-xl border border-slate-700/50 bg-slate-800/30 p-10 text-center">
      <FileText className="mx-auto mb-3 h-10 w-10 text-slate-500" />
      <h3 className="mb-2 text-lg font-semibold text-slate-100">Noch keine Antraege</h3>
      <p className="...">Starte ueber die Foerderprogramm-Uebersicht ...</p>
      <Link href="/foerderprogramme" className="... bg-orange-500 ...">
        Foerderprogramme ansehen <ArrowRight className="h-4 w-4" />
      </Link>
    </div>
  );
}
```

UI-SPEC §"Empty-States" (Copywriting Contract) — verbindlich:
- /antrag/meine: Heading "Noch kein Antrag begonnen", Body "Schildere dein Anliegen und finde in Sekunden das passende Förderprogramm für deine Schule.", CTA "Anliegen schildern" → /antrag/start
- Matcher 0-Treffer: Icon SearchX, Heading "Kein Programm passt zu diesem Anliegen", Body "Das passiert, wenn das Anliegen zu allgemein oder sehr speziell ist.", 3 Reformulierungs-Tipps, CTA "Anliegen neu formulieren" → onReset

Reformulierungs-Tipps (UI-SPEC, Copywriting Contract):
1. "Nenne Zielgruppe und Zielwirkung konkreter (z. B. 'Leseförderung Klasse 1–2, 30 Kinder')"
2. "Gib ein ungefähres Budget an (z. B. '5.000 bis 10.000 €')"
3. "Erkläre, was die Schule bereits hat und was fehlt"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Empty-States + onReset durchreichen + Tests gruen</name>
  <read_first>
    - /home/kolja/edufunds-app/components/Wizard/MatchResultList.tsx (volle 135 Zeilen)
    - /home/kolja/edufunds-app/components/Wizard/StartClient.tsx (volle 172 Zeilen)
    - /home/kolja/edufunds-app/components/Wizard/MyAntraegeClient.tsx Z. 1–155 (Empty-State + Imports)
    - /home/kolja/edufunds-app/__tests__/components/Wizard/MatchResultList.test.tsx (Wave-0-Skelett)
    - /home/kolja/edufunds-app/__tests__/components/Wizard/MyAntraegeClient.test.tsx (Wave-0-Skelett)
    - /home/kolja/edufunds-app/__tests__/components/MatchResultList.test.tsx (RTL-Stil-Vorlage)
    - /home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-UI-SPEC.md §"Empty-States"
  </read_first>
  <behavior>
    - Test 1: `<MatchResultList matches={[]} onStartAntrag={...} />` rendert "Kein Programm passt zu diesem Anliegen"
    - Test 2: `<MatchResultList matches={[]} onStartAntrag={...} onReset={...} />` rendert genau 3 li-Elemente (Tipps)
    - Test 3: Klick auf "Anliegen neu formulieren" ruft onReset
    - Test 4: `<MyAntraegeClient />` ohne Sessions rendert CTA-Link mit href=/antrag/start
    - Test 5 (real, neu): MatchResultList rendert SearchX-Icon im Empty-State
  </behavior>
  <files>components/Wizard/MatchResultList.tsx, components/Wizard/StartClient.tsx, components/Wizard/MyAntraegeClient.tsx, __tests__/components/Wizard/MatchResultList.test.tsx, __tests__/components/Wizard/MyAntraegeClient.test.tsx</files>
  <action>
**A. `components/Wizard/MatchResultList.tsx` (Empty-State ersetzen + onReset-Prop):**

1. `Props`-Interface erweitern (Z. 24–28):
   ```typescript
   interface Props {
     matches: MatchEntry[];
     onStartAntrag: (entry: MatchEntry) => void;
     onReset?: () => void;  // D-05 — wird beim Klick auf "Anliegen neu formulieren" gerufen
   }
   ```
2. Funktions-Signatur `export function MatchResultList({ matches, onStartAntrag, onReset }: Props)` erweitern.
3. Lucide-Import oben um `SearchX` ergaenzen: `import { AlertTriangle, ArrowRight, CheckCircle, ExternalLink, SearchX, Star } from "lucide-react";`
4. Empty-State-Block (Z. 37–50) komplett ersetzen durch:
   ```typescript
   if (matches.length === 0) {
     return (
       <div className="rounded-xl border border-slate-700/50 bg-slate-800/40 p-8 text-center">
         <SearchX className="mx-auto mb-3 h-8 w-8 text-slate-500" />
         <h3 className="mb-2 text-lg font-semibold text-slate-200">
           Kein Programm passt zu diesem Anliegen
         </h3>
         <p className="mx-auto mb-4 max-w-md text-sm text-slate-400">
           Das passiert, wenn das Anliegen zu allgemein oder sehr speziell ist.
         </p>
         <ul className="mx-auto mb-6 max-w-md space-y-1 text-left text-sm text-slate-400">
           <li>Nenne Zielgruppe und Zielwirkung konkreter (z. B. „Leseförderung Klasse 1–2, 30 Kinder").</li>
           <li>Gib ein ungefähres Budget an (z. B. „5.000 bis 10.000 €").</li>
           <li>Erkläre, was die Schule bereits hat und was fehlt.</li>
         </ul>
         {onReset && (
           <button
             type="button"
             onClick={onReset}
             className="inline-flex items-center gap-2 rounded-lg bg-orange-500 px-5 py-2 text-sm font-semibold text-white hover:bg-orange-600"
           >
             Anliegen neu formulieren
             <ArrowRight className="h-4 w-4" />
           </button>
         )}
       </div>
     );
   }
   ```
5. Mobile-Touch-Polish im Ranking-Branch:
   - Z. 122–129 (Submit-Button "Antrag starten"): Klassen-String enthaelt heute `py-2` → ergaenze `sm:py-3` direkt nach `py-2`. Suche-Pattern: `px-4 py-2 text-sm font-semibold` → ersetzen durch `px-4 py-2 sm:py-3 text-sm font-semibold`.
   - Z. 115–121 (Programm-Details-Link): Heute `text-xs` ohne `py-*` → ergaenze `py-2` so dass Link einen klickbaren Bereich hat.

**B. `components/Wizard/StartClient.tsx` (handleReset + Prop durchreichen):**

1. Nach `const [isSecondRound, setIsSecondRound] = useState(false);` (Z. 40) eine neue handleReset-Funktion einfuegen:
   ```typescript
   const handleReset = () => {
     setMatchState(null);
     setError(null);
     setIsSecondRound(false);
     // lastInput bleibt — User soll im Form-Wert weiterhin sehen, was er getippt hat
   };
   ```
2. JSX (Z. 157–158) erweitern:
   ```typescript
   {matchState.kind === "ranking" && (
     <MatchResultList matches={matchState.matches} onStartAntrag={startAntrag} onReset={handleReset} />
   )}
   ```

**C. `components/Wizard/MyAntraegeClient.tsx` (Empty-State Polish — D-05):**

1. Empty-State-Block (Z. 133–154) modifizieren:
   - Heading-Text aendern: `Noch keine Antraege` → `Noch kein Antrag begonnen`
   - Body-Text aendern auf: `Schildere dein Anliegen und finde in Sekunden das passende Förderprogramm für deine Schule.`
   - Link `href="/foerderprogramme"` → `href="/antrag/start"`
   - Link-Label `Foerderprogramme ansehen` → `Anliegen schildern`

   Resultat:
   ```typescript
   if (sessions.length === 0) {
     return (
       <div className="rounded-xl border border-slate-700/50 bg-slate-800/30 p-10 text-center">
         <FileText className="mx-auto mb-3 h-10 w-10 text-slate-500" />
         <h3 className="mb-2 text-lg font-semibold text-slate-100">
           Noch kein Antrag begonnen
         </h3>
         <p className="mx-auto mb-6 max-w-md text-sm text-slate-400">
           Schildere dein Anliegen und finde in Sekunden das passende Förderprogramm für deine Schule.
         </p>
         <Link
           href="/antrag/start"
           className="inline-flex items-center gap-2 rounded-lg bg-orange-500 px-5 py-2 text-sm font-semibold text-white hover:bg-orange-600"
         >
           Anliegen schildern
           <ArrowRight className="h-4 w-4" />
         </Link>
       </div>
     );
   }
   ```

**D. `__tests__/components/Wizard/MatchResultList.test.tsx` (it.todos unskippen + 1 neuer realer Test):**

- Importe: `import { MatchResultList } from '@/components/Wizard/MatchResultList'; import { render, screen, fireEvent } from '@testing-library/react';`
- 4 Tests scharfstellen:
  1. `it("rendert Empty-State-Heading 'Kein Programm passt zu diesem Anliegen' bei leerem matches-Array — D-05", ...)`: render mit `matches={[]} onStartAntrag={jest.fn()}`, erwarte `screen.getByText(/Kein Programm passt zu diesem Anliegen/)`.
  2. `it("rendert SearchX-Icon im Empty-State — D-05", ...)`: render und erwarte mind. ein `<svg>` mit aria-label oder class `lucide-search-x` (Lucide rendert mit class). Alternativ: query nach Heading + verifiziere SVG existiert via `container.querySelector("svg")`.
  3. `it("rendert genau 3 Reformulierungs-Tipps als Liste — D-05", ...)`: erwarte `screen.getAllByRole("listitem").length >= 3`.
  4. `it("ruft onReset bei Klick auf 'Anliegen neu formulieren' — D-05", ...)`: `const onReset = jest.fn(); render(<MatchResultList matches={[]} onStartAntrag={jest.fn()} onReset={onReset} />); fireEvent.click(screen.getByText(/Anliegen neu formulieren/)); expect(onReset).toHaveBeenCalledTimes(1);`.
- Alle vorigen `it.todo` aus diesem File entfernen.

**E. `__tests__/components/Wizard/MyAntraegeClient.test.tsx` (it.todos unskippen):**

- Mocke `next/link`, `next/navigation` (`useRouter`) wie in bestehenden Tests.
- Mocke `fetch` global so dass `/api/wizard/sessions` (oder welche Route der Component pollt) `[]` zurueckgibt — pruefe Component-Code zur Klaerung der Endpunkt-URL.
- 2 Tests scharfstellen:
  1. `it("rendert Empty-State CTA-Label 'Anliegen schildern' bei leerem sessions-Array — D-05", ...)`: erwarte `await screen.findByText(/Anliegen schildern/)` (async wegen useEffect-fetch).
  2. `it("Empty-State-CTA href verweist auf /antrag/start — D-05", ...)`: erwarte `screen.findByRole("link", { name: /Anliegen schildern/ }).getAttribute("href") === "/antrag/start"`.

Wenn der Mock-fetch zu komplex wird (z. B. component nutzt SWR ohne Test-Setup), darf eine alternative Strategie genutzt werden: directly Testen mit Component-Variante via `MyAntraegeClient.tsx`-Refactor wo `sessions` als optionale Prop gesetzt werden kann — aber NUR wenn der Refactor minimal ist (1 Line-Add). Bevorzugt: fetch-Mock.
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && npm test -- --runTestsByPath __tests__/components/Wizard/MatchResultList.test.tsx __tests__/components/Wizard/MyAntraegeClient.test.tsx --silent 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "onReset" /home/kolja/edufunds-app/components/Wizard/MatchResultList.tsx` exit 0
    - `grep -q "SearchX" /home/kolja/edufunds-app/components/Wizard/MatchResultList.tsx` exit 0
    - `grep -q "Kein Programm passt zu diesem Anliegen" /home/kolja/edufunds-app/components/Wizard/MatchResultList.tsx` exit 0
    - `grep -q "Anliegen neu formulieren" /home/kolja/edufunds-app/components/Wizard/MatchResultList.tsx` exit 0
    - `grep -q "handleReset" /home/kolja/edufunds-app/components/Wizard/StartClient.tsx` exit 0
    - `grep -q "onReset={handleReset}" /home/kolja/edufunds-app/components/Wizard/StartClient.tsx` exit 0
    - `grep -q "Noch kein Antrag begonnen" /home/kolja/edufunds-app/components/Wizard/MyAntraegeClient.tsx` exit 0
    - `grep -q "/antrag/start" /home/kolja/edufunds-app/components/Wizard/MyAntraegeClient.tsx` exit 0
    - `grep -q "Anliegen schildern" /home/kolja/edufunds-app/components/Wizard/MyAntraegeClient.tsx` exit 0
    - `grep -c "it.todo" /home/kolja/edufunds-app/__tests__/components/Wizard/MatchResultList.test.tsx` Output 0
    - `grep -c "it.todo" /home/kolja/edufunds-app/__tests__/components/Wizard/MyAntraegeClient.test.tsx` Output 0
    - `npm test -- --runTestsByPath __tests__/components/Wizard/MatchResultList.test.tsx __tests__/components/Wizard/MyAntraegeClient.test.tsx` Exit 0
  </acceptance_criteria>
  <done>3 Components modifiziert, 2 Test-Files gruen mit insgesamt 6+ realen Tests, 0 todos.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile-Touch-Targets in 4 Components (D-03)</name>
  <read_first>
    - /home/kolja/edufunds-app/components/Wizard/AnliegenForm.tsx Z. 95–115 (Textarea-Block)
    - /home/kolja/edufunds-app/components/Wizard/QuestionCard.tsx Z. 60–75 (Submit-Button)
    - /home/kolja/edufunds-app/components/Wizard/MatchResultList.tsx Z. 110–135 (Submit-Button-Block — bereits in Task 1 angepasst, hier verifizieren)
    - /home/kolja/edufunds-app/components/Wizard/MyAntraegeClient.tsx Z. 156–215 (Session-Card-Layout)
    - /home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-UI-SPEC.md §"Mobile-Breakpoints" + §"Interaction Contracts > Mobile-Breakpoints Critical-Paths"
    - /home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-RESEARCH.md §4.3 (statisches Tailwind-Audit)
  </read_first>
  <files>components/Wizard/AnliegenForm.tsx, components/Wizard/QuestionCard.tsx, components/Wizard/MyAntraegeClient.tsx</files>
  <action>
**A. `components/Wizard/AnliegenForm.tsx` (Path 1 — Anliegen-Eingabe):**

- Z. 100–107 (textarea): Klassen-String erweitern. Suche `className="w-full rounded-lg border border-slate-600 bg-slate-900 p-3 text-slate-100 placeholder-slate-500 focus:border-orange-500 focus:outline-none"` und ergaenze `sm:min-h-[140px]` direkt nach `p-3`. Resultat:
  ```html
  className="w-full rounded-lg border border-slate-600 bg-slate-900 p-3 sm:min-h-[140px] text-slate-100 placeholder-slate-500 focus:border-orange-500 focus:outline-none"
  ```

**B. `components/Wizard/QuestionCard.tsx` (Path 3 — Wizard-Antwort-Eingabe):**

- Submit-Button (Z. 64–71): Suche `className="rounded-lg bg-orange-500 px-5 py-2 text-sm font-semibold text-white transition hover:bg-orange-600 disabled:opacity-50"` und ergaenze `sm:py-3` direkt nach `py-2`:
  ```html
  className="rounded-lg bg-orange-500 px-5 py-2 sm:py-3 text-sm font-semibold text-white transition hover:bg-orange-600 disabled:opacity-50"
  ```

**C. `components/Wizard/MyAntraegeClient.tsx` (Path 4b — Session-Cards Mobile-Stack):**

- Session-Card-Wrapper (Z. 162–164): Klassen-String enthaelt heute `flex flex-wrap items-center gap-4 rounded-xl border ...`. Aendere zu `flex flex-col sm:flex-row sm:flex-wrap sm:items-center gap-4 rounded-xl border ...`.
- Auf gleicher Card: Buttons im rechten Block (Z. 194–212) sind in `<div className="flex items-center gap-2">` — bei Mobile-Stack soll dieser Block volle Breite nehmen koennen. Setze auf `<div className="flex items-center gap-2 sm:ml-auto">` (Right-Aligned auf md+, full-width unten auf Mobile durch flex-col oben).

**D. Verifikation MatchResultList Mobile-Touch (aus Task 1 schon eingebaut):**
- `grep -q "py-2 sm:py-3" components/Wizard/MatchResultList.tsx` muss exit 0 (zumindest auf "Antrag starten"-Button).

Wichtig: Es duerfen keine bestehenden Klassen entfernt werden — nur ergaenzt. Pruefe nach jedem Edit `grep -c "py-2"` vorher und nachher (Anzahl gleich oder hoeher, nie niedriger).
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && grep -c "sm:min-h-\[140px\]" components/Wizard/AnliegenForm.tsx && grep -c "sm:py-3" components/Wizard/QuestionCard.tsx && grep -c "flex flex-col sm:flex-row" components/Wizard/MyAntraegeClient.tsx && npm run lint -- components/Wizard/AnliegenForm.tsx components/Wizard/QuestionCard.tsx components/Wizard/MyAntraegeClient.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "sm:min-h-\[140px\]" /home/kolja/edufunds-app/components/Wizard/AnliegenForm.tsx` exit 0
    - `grep -q "sm:py-3" /home/kolja/edufunds-app/components/Wizard/QuestionCard.tsx` exit 0
    - `grep -q "flex flex-col sm:flex-row" /home/kolja/edufunds-app/components/Wizard/MyAntraegeClient.tsx` exit 0
    - `grep -q "py-2 sm:py-3\|sm:py-3" /home/kolja/edufunds-app/components/Wizard/MatchResultList.tsx` exit 0
    - `npx tsc --noEmit --skipLibCheck` Exit 0
    - `npm test -- --runTestsByPath __tests__/components/Wizard/MatchResultList.test.tsx __tests__/components/Wizard/MyAntraegeClient.test.tsx __tests__/components/MatchResultList.test.tsx` Exit 0 (kein Regress in bestehenden Tests)
  </acceptance_criteria>
  <done>3 Components mit Mobile-Touch-Polish, alle Tests gruen, kein bestehender Test bricht.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Empty-State-CTAs sind Links/Buttons; CTA-Klick triggert Standard-Routes (/antrag/start, fetch /api/match) — keine neuen Auth-Boundaries |
| Component-Props | onReset-Callback wird von StartClient kontrolliert — kein User-Input-Pfad in den Callback hinein |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1.03-01 | Tampering | onReset-Callback | accept | Callback ist parent-controlled, kein User-Input-Vector |
| T-02.1.03-02 | Information Disclosure | Empty-State-Texte | accept | Kein PII, kein Session-Token im UI |
| T-02.1.03-03 | Denial of Service | Mobile-Tap-Targets | mitigate | sm:py-3 stellt 44px Touch-Target sicher → reduziert Mis-Taps die zu unerwuenschten Form-Submits fuehren |
</threat_model>

<verification>
- Component-Tests: `npm test -- __tests__/components/Wizard` → alle gruen
- Bestehender RTL-Test `__tests__/components/MatchResultList.test.tsx` bleibt gruen (kein Regress in passt-weil/achtung-bei-Logik)
- TypeScript: `npx tsc --noEmit --skipLibCheck` Exit 0
- Lint: `npm run lint -- components/Wizard/MatchResultList.tsx components/Wizard/StartClient.tsx components/Wizard/MyAntraegeClient.tsx components/Wizard/AnliegenForm.tsx components/Wizard/QuestionCard.tsx` ohne Fehler
- Mobile-Audit (manuell, dokumentiert in 02.1-VALIDATION.md): Chrome DevTools iPhone-SE-Viewport, alle 4 Critical-Paths anklickbar
</verification>

<success_criteria>
- /antrag/meine zeigt korrekten Empty-State-Text + Link auf /antrag/start
- MatchResultList zeigt SearchX + 3 Tipps + onReset-CTA bei matches=[]
- StartClient onReset → matchState=null Reset funktioniert
- Touch-Targets sm:py-3 / sm:min-h-[140px] auf 4 Critical-Paths
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-03-SUMMARY.md` mit:
- Diff-Summary pro Component (welche Klassen ergaenzt)
- Test-Report (gruene Anzahl, ehemals todos)
- Hinweis: Mobile-Audit in 02.1-VALIDATION.md ist noch offen (manueller Browser-Smoke vor Phase-Abschluss)
</output>

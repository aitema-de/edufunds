---
phase: 02.1-frontend-polish-stripe-vorbereitung
plan: "02"
subsystem: payments
tags: [stripe, webhook, tdd, smoke-test, d-07]
dependency_graph:
  requires:
    - "02.1-01 (Wave-0 Test-Skelette — it.todo-Anker fuer webhook.test.ts)"
  provides:
    - "app/api/stripe/webhook/route.ts (gehaerteter Handler mit 4 Event-Cases + 6 PAY-03-Stubs)"
    - "__tests__/app/api/stripe/webhook.test.ts (7 gruene Tests, 0 it.todo)"
    - "scripts/smoke-stripe-webhook.ts (lauffaehig via generateTestHeaderString)"
    - "STRIPE_SETUP.md#Webhook-Smoke (Plan 02.1-02) (Pfad A SDK + Pfad B Stripe-CLI)"
  affects:
    - "D-06, D-07, D-08 (Stripe-Webhook-Skeleton ohne Live-Account geschlossen)"
tech_stack:
  added: []
  patterns:
    - "4-Case-Switch in Webhook-Route: completed/expired/refunded/async_failed — jeder Case eigenstaendig statt zusammengefasst"
    - "5xx fuer Business-Logic-Fehler (Stripe retried), 4xx nur fuer Verification/Header-Fehler (RESEARCH §1.2 Pitfall 3)"
    - "Audit-Trail-Log event.id+type direkt nach constructEvent — sicher weil event.id von Stripe vergeben"
    - "generateTestHeaderString fuer signierte Smoke-Tests ohne Stripe-CLI"
    - "Test 1 Smoke toleriert 500 (DB-Token nicht vorhanden) — korrekte Fehlerklassifizierung"
key_files:
  created: []
  modified:
    - app/api/stripe/webhook/route.ts
    - __tests__/app/api/stripe/webhook.test.ts
    - scripts/smoke-stripe-webhook.ts
    - STRIPE_SETUP.md
decisions:
  - "500 statt 4xx bei markSessionPaid-Fehler — 5xx triggert Stripe-Retry (T-02.1.02-06), 4xx wuerde Event permanent verwerfen"
  - "Smoke-Test toleriert 500 bei smoke-fake-token — DB-Session existiert nicht in Dev, Webhook-Code ist trotzdem korrekt"
  - "6 TODO PAY-03-Stubs klar getrennt: 3 im completed-Case (Mail/State/DB-Schema), je 1 in expired/refunded/async_failed"
  - "Bonus-Test 'kein wizard_session_token' + B6-Test '500-Stripe-retry' ergaenzt — beide aus Threat-Register abgeleitet"
metrics:
  duration_minutes: 25
  completed_date: "2026-05-06"
  tasks_completed: 2
  files_created: 0
  files_modified: 4
---

# Phase 02.1 Plan 02: Stripe-Webhook-Haertung + Tests + Smoke Summary

**One-liner:** Webhook-Route mit 4 expliziten Event-Cases, 6 PAY-03-TODO-Stubs, 7 gruenen Tests und CI-tauglichem Smoke-Skript via `generateTestHeaderString` — kein Live-Stripe-Account benoetigt.

## Was wurde gebaut

### Task 1: Webhook-Route haerten + Tests gruen schalten

**`app/api/stripe/webhook/route.ts`** (83 → 103 Zeilen):

| Aenderung | Beschreibung |
|-----------|--------------|
| Audit-Trail-Log | `console.log([stripe/webhook] event.id=... type=...)` nach constructEvent |
| Case-Aufspaltung | 3 zusammengefasste Cases → 4 eigenstaendige Cases mit je einem TODO-Stub |
| 6 TODO PAY-03-Stubs | 3 im completed-Case (Mail/State/DB-Schema) + je 1 in expired/refunded/async_failed |
| Unveraendert | Signatur-Verifizierung, markSessionPaid-Aufruf, 5xx-Business-Logic-Handling |

Switch-Block nach Haertung:

| Case | Handler | Stub |
|------|---------|------|
| `checkout.session.completed` | `markSessionPaid` bei token+paid | PAY-03: Mail + State + DB-Schema |
| `checkout.session.expired` | Log only | PAY-03: State-Hook UI-Hinweis |
| `charge.refunded` | Log only | PAY-03: DB-Schema paid_token invalidieren |
| `checkout.session.async_payment_failed` | Log only | PAY-03: Mail-Hook Retry-Pfad |

**`__tests__/app/api/stripe/webhook.test.ts`** (139 → 253 Zeilen):

| Test | Typ | Prueft |
|------|-----|--------|
| 503 wenn STRIPE_WEBHOOK_SECRET fehlt | real (Plan 01) | Konfig-Gate |
| 400 wenn stripe-signature Header fehlt | real (Plan 01) | Header-Gate |
| 400 bei ungueltiger Signatur | real (Plan 01) | Signatur-Pruefung |
| 200 + markSessionPaid bei paid + Token | **neu (war it.todo)** | Haupt-Business-Pfad |
| 200 + kein markSessionPaid bei unpaid | **neu (war it.todo)** | payment_status-Filter |
| 200 + kein markSessionPaid ohne Token | **neu (Bonus)** | metadata-Token-Filter |
| 500 wenn markSessionPaid wirft | **neu (B6)** | Stripe-Retry-Pfad (T-02.1.02-06) |

Test-Run: `7 passed, 0 todos, 0 skipped`

### Task 2: Smoke-Skript scharfstellen + STRIPE_SETUP.md ergaenzen

**`scripts/smoke-stripe-webhook.ts`** (110 → 111 Zeilen, vollstaendig umgebaut):

3 Smoke-Szenarien:
1. Valide Signatur + paid-Event → 200 (received:true) ODER 500 (Token nicht in Dev-DB — OK, Stripe retried)
2. Fake-Signatur → 400 erwartet
3. Unbekannter Event-Type (`payment_intent.succeeded`) → 200 erwartet

Alle 3 nutzen `stripe.webhooks.generateTestHeaderString` — kein Stripe-CLI noetig.

**`STRIPE_SETUP.md`** — neue Sektion `## Webhook-Smoke (Plan 02.1-02)` am Ende angehaengt:
- Pfad A: `npx tsx --env-file=.env.local scripts/smoke-stripe-webhook.ts` (CI-tauglich)
- Pfad B: `stripe listen + stripe trigger` (production-realistisch, manuell)

## Verifikation

```
npm test -- --runTestsByPath __tests__/app/api/stripe/webhook.test.ts
PASS __tests__/app/api/stripe/webhook.test.ts
Tests: 7 passed, 7 total
```

```
npx tsc --noEmit --skipLibCheck -p tsconfig.json
(kein Output = clean)
```

```
grep -c "TODO PAY-03" app/api/stripe/webhook/route.ts
6
```

## Deviationen vom Plan

### Auto-fixte Probleme

Keine.

### Worktree-Basis-Abweichung (behoben)

- **Gefunden bei:** Initialer Worktree-Check
- **Problem:** Worktree-Branch `worktree-agent-a9bb01444942d5518` war auf `5870e81` (alte Codebasis vor Wizard), nicht auf `05068c7` (Plan-01-Ergebnis). Die Plan-01-Testdateien (`webhook.test.ts` mit it.todo) fehlten im Worktree.
- **Fix:** `git restore --source=05068c7 --staged --worktree -- .` hat die gesamte Plan-01-Codebasis in den Worktree gebracht, Commit `f58809b` fixiert den Basis-Stand. Anschliessend normale Plan-02-Commits darauf.
- **Auswirkung:** Keine inhaltliche Abweichung, nur ein Basis-Setup-Schritt mehr.

### Abweichung Task 1: 7 statt 5 Tests

- **Regeltyp:** Rule 2 (fehlende Absicherung)
- **Begruendung:** Plan forderte 5 Tests (3 real + 2 todo-unskippt). Zwei zusaetzliche Tests ergeben sich direkt aus dem Threat-Register:
  - Bonus-Test "kein wizard_session_token" — deckt den `if (!token) break`-Pfad ab (D-07-Vollabdeckung)
  - B6-Test "500 wenn markSessionPaid wirft" — direkt aus T-02.1.02-06 (Stripe-Retry-Pfad): 4xx bei Business-Logic-Fehler wuerde Event permanent verwerfen
- **Plan-Kompatibel:** Plan-Frontmatter sagt `>= 6` real `it()` und `it.todo count = 0` — beides erfuellt.

## Bekannte Stubs

Die 6 `TODO PAY-03`-Kommentare in `route.ts` sind intentionale Stubs — sie markieren die Integrationspunkte fuer PAY-03 (Mail-Hook via resend), PAY-03 State-Hook (UI-Confetti) und PAY-03 DB-Schema-Hook (Migration 004). Diese Stubs sind Planentwurf, nicht fehlende Implementierung.

## Threat-Flags

Keine neuen Threat-Surfaces eingebracht. Die Webhook-Route existierte bereits (Plan 01 hat sie angelegt), Plan 02 haertet nur den Switch-Block.

## Self-Check: PASSED

| Datei | Existiert | Commit |
|-------|-----------|--------|
| `app/api/stripe/webhook/route.ts` | ja (103 Zeilen) | 187c397 |
| `__tests__/app/api/stripe/webhook.test.ts` | ja (253 Zeilen) | 187c397 |
| `scripts/smoke-stripe-webhook.ts` | ja (111 Zeilen) | 66eb4b6 |
| `STRIPE_SETUP.md` Webhook-Smoke-Sektion | ja | 66eb4b6 |

Commits verifiziert:
- `187c397` — feat(02.1-02): Webhook-Route haerten + 7 Tests gruen (D-07)
- `66eb4b6` — feat(02.1-02): Smoke-Skript scharfgestellt + STRIPE_SETUP.md Webhook-Smoke-Sektion

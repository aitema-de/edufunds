---
phase: 02.1
slug: frontend-polish-stripe-vorbereitung
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-06
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Befüllt nach RESEARCH.md "Validation Architecture"-Empfehlungen.
> Konkrete Task-Mappings ergänzt der Planner pro Plan-Datei.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | jest 29.7.0 + ts-jest 29.4.9 (bestehend, siehe `jest.config.js`) + Stripe-CLI für Webhook-Smoke |
| **Config file** | `jest.config.js` |
| **Quick run command** | `npm test --` (Jest läuft per Default ohne Watch-Mode) |
| **Full suite command** | `npm test -- && npm run typecheck && npm run lint` |
| **Estimated runtime** | ~30 s Quick / ~90 s Full / Stripe-Webhook-Smoke ~10 s separat |

---

## Sampling Rate

- **After every task commit:** Run `npm test --` (Jest single-run, kein Watch-Mode)
- **After every plan wave:** Run full suite (`npm test --` + `npm run typecheck` + `npm run lint`)
- **Before `/gsd-verify-work`:** Full suite + Stripe-CLI-Smoke + Reload-Resume-Smoke + visueller Mobile-Audit-Bericht müssen grün
- **Max feedback latency:** 30 s

---

## Per-Task Verification Map

> Wird vom Planner pro PLAN.md ergänzt — hier die Validierungs-Block-Übersicht aus RESEARCH.md:

### Block 1: Stripe-Webhook-Skeleton
| Validation Type | Command / Method | Automated |
|-----------------|------------------|-----------|
| Unit: Signature-Verification mit `generateTestHeaderString` | `npm test -- __tests__/app/api/stripe/webhook.test.ts` | ✅ |
| Smoke: Stripe-CLI Test-Mode (3 Events) | `stripe listen --forward-to localhost:3101/api/stripe/webhook && stripe trigger checkout.session.completed` | manuell + dokumentiertes Smoke-Skript `scripts/smoke-stripe-webhook.ts` |
| Negative: Invalid-Signature → 400 | Jest mit gefälschter Signatur | ✅ |

### Block 2: WIZ-04 Reload-Resume
| Validation Type | Command / Method | Automated |
|-----------------|------------------|-----------|
| Unit: `session.ts` Resume-Lookup-Pfad | `npm test -- __tests__/lib/wizard/session.test.ts` | ✅ |
| Integration: Pipeline emittiert Stage-Events in DB (JSONB-Persistenz) | `npm test -- __tests__/lib/wizard/pipeline-events.test.ts` | ✅ |
| Smoke: Hard-Reload während `phase=interviewing` UND `phase=generating` | `scripts/smoke-reload-resume.ts` (kontrollierter Browser-Free-Test via fetch + DB-State-Check) | ✅ via Skript |
| Manuell: Echter Browser-Reload-Test in Chrome+Safari | nicht in Jest abbildbar | ❌ manuell |

### Block 3: GeneratingProgress 7-Stage-Polling
| Validation Type | Command / Method | Automated |
|-----------------|------------------|-----------|
| Unit: Polling-Endpoint liefert Stage-Status korrekt | `npm test -- __tests__/app/api/wizard/[sessionToken]/progress.test.ts` | ✅ |
| Integration: Pipeline schreibt Stage-Status via `jsonb_set`, Endpoint liest | Jest mit Test-DB | ✅ |
| Visuell: GeneratingProgress UI rendert 7 Stages mit pending/active/done | manueller Browser-Test, kein Jest | ❌ manuell |
| Failed-Phase UI (D-15): Fehler-Card mit Retry-CTA | Jest auf Component-Level | ✅ |

### Block 4: UI-Polish (Antrag-Detail, Empty-States, Mobile)
| Validation Type | Command / Method | Automated |
|-----------------|------------------|-----------|
| Component: AntragResult Markdown-Render + Anker-Nav | `npm test -- __tests__/components/Wizard/AntragSectionNav.test.tsx` (Jest + @testing-library/react) | ✅ |
| Component: MyAntraegeClient Empty-State + 0-Treffer-Fall | `npm test -- __tests__/components/Wizard/MyAntraegeClient.test.tsx` | ✅ |
| Mobile-Audit: 4 Critical-Paths in iPhone-Safari + Android-Chrome | manuell mit Audit-Matrix-Doku | ❌ manuell |
| Tailwind-Klassen-Audit (Touch-Höhe ≥ 44 px) | grep-basiert in PLAN-Acceptance-Criteria | ✅ via grep |

### Block 5: UAT-Vorbereitungs-Artefakte
| Validation Type | Command / Method | Automated |
|-----------------|------------------|-----------|
| Existence: 4 Markdown-Templates unter `.planning/uat/` | `test -f .planning/uat/UAT-PLAN-TEMPLATE.md` etc. | ✅ via Shell |
| Markdown-Lint | `npx markdownlint .planning/uat/*.md` (falls installiert, sonst manuell) | ✅ optional |
| Inhalt: Pilot-Liste hat ≥ 5 Felder pro Kandidat | grep-basiert | ✅ |

---

## Wave 0 Requirements

- [ ] `__tests__/app/api/stripe/webhook.test.ts` — Stub für Signature-Verification + Event-Dispatch (Block 1)
- [ ] `__tests__/lib/wizard/session.test.ts` — Stub für Resume-Lookup (Block 2) — bei Bedarf
- [ ] `__tests__/lib/wizard/pipeline-events.test.ts` — Stub für Stage-Event-Persistenz in JSONB (Block 2/3)
- [ ] `__tests__/components/Wizard/WizardShell.test.tsx` — Stub für Phase-Dispatch + Polling (Block 3)
- [ ] `__tests__/components/Wizard/AntragSectionNav.test.tsx` — Stub für Slug-Generation + IntersectionObserver (Block 4)
- [ ] `__tests__/components/Wizard/MatchResultList.test.tsx` — Stub für Empty-State (Block 4)
- [ ] `__tests__/components/Wizard/MyAntraegeClient.test.tsx` — Stub für Empty-State CTA (Block 4)
- [ ] `scripts/smoke-stripe-webhook.ts` — Stripe-CLI/SDK-Smoke-Skript-Skelett (Block 1)
- [ ] `scripts/smoke-reload-resume.ts` — Reload-Smoke-Skript-Skelett (Block 2)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Echter Browser-Reload während Pipeline läuft | WIZ-04 | Browser-Verhalten + Network-Conditions nicht simulierbar | Chrome + Safari je 2 Reloads (interviewing/generating), Antragsstand muss erhalten bleiben |
| Mobile-Critical-Paths auf realen Geräten | UI-03 / D-03 | Touch-Verhalten, Safari-Quirks, Tastatur-Overlay | iPhone Safari + Android Chrome, 4 Critical-Paths durchspielen, Audit-Matrix in PLAN-Verifikation ausfüllen |
| Stripe-CLI mit echtem Test-Mode-Account | D-08 | Stripe-CLI braucht User-Login einmalig | `stripe login` → `stripe listen` → 3× `stripe trigger` |
| Pilot-Liste-Inhalte (Namen, Vereins-Daten) | UAT-PREP-03 / D-09 #3 | Datenschutz, Kolja kennt Kontakte | Kolja füllt 5–7 Kandidaten manuell ein, Plan-Phase liefert nur Schema |
| GeneratingProgress visuell während echtem 90–150-s-Pipeline-Lauf | UI-04 / D-04 | Animation/Übergänge nicht in Jest abbildbar | Echter Wizard-Lauf mit Stage-Wechsel beobachten |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (Webhook + Pipeline-Events + WizardShell + AntragSectionNav + 2 Empty-State + 2 Smoke-Skripte)
- [ ] No watch-mode flags (Jest läuft per Default ohne Watch — `npm test --` ist single-run)
- [ ] Feedback latency < 30 s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending

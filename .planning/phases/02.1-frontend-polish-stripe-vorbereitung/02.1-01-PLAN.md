---
phase: 02.1-frontend-polish-stripe-vorbereitung
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/app/api/stripe/webhook.test.ts
  - __tests__/components/Wizard/MatchResultList.test.tsx
  - __tests__/components/Wizard/MyAntraegeClient.test.tsx
  - __tests__/components/Wizard/AntragSectionNav.test.tsx
  - __tests__/components/Wizard/WizardShell.test.tsx
  - __tests__/lib/wizard/pipeline-events.test.ts
  - scripts/smoke-stripe-webhook.ts
  - scripts/smoke-reload-resume.ts
  - test/setup.tsx
autonomous: true
requirements: [WIZ-04, UI-01, UI-02, UI-04, PAY-01-prep]
must_haves:
  truths:
    - "Vor jeder Wave-1/2-Implementation existieren rote oder skipped Tests, die das zu implementierende Verhalten beschreiben (Nyquist-Compliance)"
    - "Stripe-Webhook-Tests existieren mit valider/invalider Signatur + 503-Branch (Block 1)"
    - "Pipeline-Events-Test existiert mit LLM-Mock und Stage-Reihenfolge-Assertion (Block 2/3)"
    - "Component-Tests existieren für Empty-States (MatchResultList, MyAntraegeClient) und für die neue AntragSectionNav (Block 4)"
    - "WizardShell-Test existiert für Phase-Dispatch (interviewing/generating/complete/failed)"
    - "Zwei Smoke-Skript-Skelette (smoke-stripe-webhook.ts, smoke-reload-resume.ts) sind ausführbar"
  artifacts:
    - path: __tests__/app/api/stripe/webhook.test.ts
      provides: "Stripe-Webhook Unit-Test (Mock-stripe-SDK + markSessionPaid)"
      min_lines: 60
    - path: __tests__/components/Wizard/MatchResultList.test.tsx
      provides: "Empty-State + onReset-Test (D-05 Matcher-0-Treffer)"
      min_lines: 30
    - path: __tests__/components/Wizard/MyAntraegeClient.test.tsx
      provides: "Empty-State CTA-Test (D-05 /antrag/meine leer)"
      min_lines: 25
    - path: __tests__/components/Wizard/AntragSectionNav.test.tsx
      provides: "Slug-Generation + IntersectionObserver-Mock (D-02)"
      min_lines: 30
    - path: __tests__/components/Wizard/WizardShell.test.tsx
      provides: "Phase-Dispatch-Test mit fetch-Mock (D-12 + D-15)"
      min_lines: 40
    - path: __tests__/lib/wizard/pipeline-events.test.ts
      provides: "LLM-Mock + onEvent-Reihenfolge-Test (D-04 + D-13)"
      min_lines: 25
    - path: scripts/smoke-stripe-webhook.ts
      provides: "Stripe-Webhook-Smoke mit generateTestHeaderString"
      min_lines: 40
    - path: scripts/smoke-reload-resume.ts
      provides: "DB-State-Smoke nach Reload-Detect"
      min_lines: 40
  key_links:
    - from: __tests__/components/Wizard/MatchResultList.test.tsx
      to: components/Wizard/MatchResultList.tsx
      via: "import { MatchResultList }"
      pattern: "import.*MatchResultList"
    - from: __tests__/app/api/stripe/webhook.test.ts
      to: app/api/stripe/webhook/route.ts
      via: "import { POST } from '@/app/api/stripe/webhook/route'"
      pattern: "@/app/api/stripe/webhook/route"
---

<objective>
Wave-0-Test-Skelette + Smoke-Skript-Skelette für die nachfolgenden Implementations-Plans erstellen. Alle Tests sollen initial entweder rot oder bewusst als `it.todo` markiert sein. Wave-1/2-Plans entfernen `it.todo` und schreiben den Production-Code, bis die Tests grün sind.

Purpose: Nyquist-Compliance — pro implementations-relevante Behavior-Klasse existiert ein automatisierter Test-Pfad, BEVOR Code geschrieben wird (siehe `02.1-VALIDATION.md` "Wave 0 Requirements"). Verhindert Drift zwischen Spec und Implementation.

Output: 6 Test-Files + 2 Smoke-Skript-Skelette + ggf. Erweiterung von `test/setup.tsx` (IntersectionObserver-Mock).
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/kolja/edufunds-app/CLAUDE.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-CONTEXT.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-RESEARCH.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-PATTERNS.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-VALIDATION.md
@/home/kolja/edufunds-app/__tests__/components/MatchResultList.test.tsx
@/home/kolja/edufunds-app/__tests__/lib/wizard/facts-extractor.test.ts
@/home/kolja/edufunds-app/scripts/smoke-llm.ts

<interfaces>
<!-- Aus dem aktuellen Code extrahiert. Executor MUSS gegen diese Signaturen testen — nicht raten. -->

From `app/api/stripe/webhook/route.ts` (existiert bereits, 83 Zeilen):
```typescript
export const runtime = "nodejs";
export const dynamic = "force-dynamic";
export async function POST(req: NextRequest): Promise<NextResponse>;
// Returns 503 wenn STRIPE_SECRET_KEY oder STRIPE_WEBHOOK_SECRET fehlt
// Returns 400 bei fehlendem stripe-signature Header oder ungueltiger Signatur
// Returns 200 mit { received: true } bei Erfolg, 500 bei Handler-Fehler
```

From `lib/stripe/client.ts`:
```typescript
export function getStripe(): Stripe;
export function stripeConfigured(): boolean;
```

From `lib/wizard/session.ts`:
```typescript
export async function markSessionPaid(
  sessionToken: string,
  params: { stripeSessionId?: string; stripeCustomerEmail?: string; tier?: string }
): Promise<WizardSession>;
```

From `components/Wizard/MatchResultList.tsx`:
```typescript
export interface MatchEntry {
  id: string;
  score: number;
  passt_weil: string;
  achtung_bei: string;
  programm: { id: string; name: string; /* ... */ };
}
interface Props {
  matches: MatchEntry[];
  onStartAntrag: (entry: MatchEntry) => void;
  // onReset?: () => void  ← wird in Plan 02.1-03 hinzugefuegt
}
```

From `lib/wizard/pipeline.ts` Z. 147–177:
```typescript
export interface PipelineEvent {
  stage: "outline" | "section" | "critique" | "revision"
       | "recheck" | "finanzplan" | "consistency" | "done";
  message: string;
  payload?: unknown;
}
export async function runPipeline(
  programm: Foerderprogramm,
  facts: WizardFacts,
  richtlinie?: Richtlinie | null,
  onEvent?: (e: PipelineEvent) => void,
  messages?: WizardMessage[]
): Promise<PipelineResult>;
```

RTL-Stil aus `__tests__/components/MatchResultList.test.tsx`:
```typescript
import { render, screen } from '@testing-library/react';
import { MatchResultList, type MatchEntry } from '@/components/Wizard/MatchResultList';
describe('MatchResultList — Branch', () => {
  it("rendert X mit Y — D-NN", () => { /* ... */ });
});
```

`jest.config.js` whitelistet `lucide-react` in `transformIgnorePatterns` — Lucide-Icons rendern in Tests ohne Setup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stripe-Webhook + Pipeline-Events Tests anlegen</name>
  <read_first>
    - /home/kolja/edufunds-app/app/api/stripe/webhook/route.ts (gegen diese 83 Zeilen werden Tests geschrieben)
    - /home/kolja/edufunds-app/lib/wizard/session.ts (markSessionPaid-Signatur)
    - /home/kolja/edufunds-app/__tests__/lib/wizard/facts-extractor.test.ts (Stil-Vorlage)
    - /home/kolja/edufunds-app/lib/wizard/pipeline.ts Z. 140–180 (PipelineEvent + runPipeline-Signatur)
    - /home/kolja/edufunds-app/jest.config.js
  </read_first>
  <files>__tests__/app/api/stripe/webhook.test.ts, __tests__/lib/wizard/pipeline-events.test.ts</files>
  <action>
**A. `__tests__/app/api/stripe/webhook.test.ts` (neu):**

- Importiere `POST` aus `@/app/api/stripe/webhook/route`, `NextRequest` aus `next/server`.
- Mocke `@/lib/stripe/client`: `jest.mock("@/lib/stripe/client", () => ({ getStripe: jest.fn(), stripeConfigured: jest.fn() }))`. Pro Testfall via `(stripeConfigured as jest.Mock).mockReturnValue(true|false)` und `(getStripe as jest.Mock).mockReturnValue({ webhooks: { constructEvent: jest.fn() } })`.
- Mocke `@/lib/wizard/session`: `jest.mock("@/lib/wizard/session", () => ({ markSessionPaid: jest.fn() }))`.
- Schreibe folgende `it`-Bloecke (deutsch, Decision-ID-Bezug):
  1. `it("gibt 503 zurueck wenn STRIPE_WEBHOOK_SECRET fehlt — D-07")` — `stripeConfigured.mockReturnValue(false)`, baue Request via `new NextRequest("http://localhost/api/stripe/webhook", { method: "POST", body: "{}" })`, erwarte `response.status === 503`.
  2. `it("gibt 400 zurueck wenn stripe-signature Header fehlt — D-07")` — `stripeConfigured.mockReturnValue(true)`, `process.env.STRIPE_WEBHOOK_SECRET = "whsec_test"`, Request OHNE `stripe-signature` Header, erwarte 400.
  3. `it("gibt 400 zurueck bei ungueltiger Signatur — D-07")` — `getStripe().webhooks.constructEvent` wirft `new Error("Invalid signature")`, Request mit `stripe-signature: "fake"`, erwarte 400.
  4. `it.todo("ruft markSessionPaid bei checkout.session.completed mit metadata.wizard_session_token — D-07")` — Plan 02.1-02 unskippt.
  5. `it.todo("ignoriert checkout.session.completed wenn payment_status != paid — D-07")` — Plan 02.1-02 unskippt.
- Keine echten Stripe-SDK-Calls, keine echte DB.

**B. `__tests__/lib/wizard/pipeline-events.test.ts` (neu):**

- Importiere `runPipeline` aus `@/lib/wizard/pipeline`, Type `PipelineEvent`.
- Mocke `@/lib/wizard/llm`: `generateText` und `generateJson` als `jest.fn()` mit Stub-Antworten (Stil aus `outline-fallback.test.ts`).
- `it.todo("ruft onEvent fuer jede Stage in korrekter Reihenfolge auf — D-04")` — wird in Plan 02.1-06 unskippt.
- `it.todo("schluckt Fehler im onEvent-Callback ohne Pipeline-Crash — D-13")` — wird in Plan 02.1-06 unskippt.
- describe-Block + Skeleton der it.todo-Reihenfolge real, damit Jest sie entdeckt.
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && npm test -- --runTestsByPath __tests__/app/api/stripe/webhook.test.ts __tests__/lib/wizard/pipeline-events.test.ts --silent 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/kolja/edufunds-app/__tests__/app/api/stripe/webhook.test.ts` exit 0
    - `test -f /home/kolja/edufunds-app/__tests__/lib/wizard/pipeline-events.test.ts` exit 0
    - `grep -q "it.todo" /home/kolja/edufunds-app/__tests__/app/api/stripe/webhook.test.ts` exit 0
    - `grep -q "503" /home/kolja/edufunds-app/__tests__/app/api/stripe/webhook.test.ts` exit 0
    - `grep -q "stripe-signature" /home/kolja/edufunds-app/__tests__/app/api/stripe/webhook.test.ts` exit 0
    - `grep -q "@/lib/stripe/client" /home/kolja/edufunds-app/__tests__/app/api/stripe/webhook.test.ts` exit 0
    - `grep -q "runPipeline" /home/kolja/edufunds-app/__tests__/lib/wizard/pipeline-events.test.ts` exit 0
    - `npm test -- --runTestsByPath __tests__/app/api/stripe/webhook.test.ts` Exit 0
  </acceptance_criteria>
  <done>Beide Test-Files existieren, mind. 3 reale `it`-Bloecke (webhook) + 2 `it.todo` (pipeline-events), Jest laeuft sie ohne Crash durch.</done>
</task>

<task type="auto">
  <name>Task 2: Component- und Shell-Tests + Setup-Erweiterung</name>
  <read_first>
    - /home/kolja/edufunds-app/__tests__/components/MatchResultList.test.tsx (RTL-Stil-Vorlage)
    - /home/kolja/edufunds-app/components/Wizard/MatchResultList.tsx Z. 1–60 (MatchEntry-Type, Empty-State Z. 36–50)
    - /home/kolja/edufunds-app/components/Wizard/MyAntraegeClient.tsx Z. 130–155
    - /home/kolja/edufunds-app/components/Wizard/WizardShell.tsx Z. 1–80 + Z. 440–470
    - /home/kolja/edufunds-app/test/setup.tsx
  </read_first>
  <files>__tests__/components/Wizard/MatchResultList.test.tsx, __tests__/components/Wizard/MyAntraegeClient.test.tsx, __tests__/components/Wizard/AntragSectionNav.test.tsx, __tests__/components/Wizard/WizardShell.test.tsx, test/setup.tsx</files>
  <action>
**A. `test/setup.tsx` (erweitern, falls IntersectionObserver-Mock fehlt):**

```typescript
class MockIntersectionObserver {
  observe = jest.fn();
  unobserve = jest.fn();
  disconnect = jest.fn();
  takeRecords = jest.fn(() => []);
  root = null;
  rootMargin = "";
  thresholds = [];
  constructor(_cb: IntersectionObserverCallback, _opts?: IntersectionObserverInit) {}
}
(global as unknown as { IntersectionObserver: typeof IntersectionObserver }).IntersectionObserver =
  MockIntersectionObserver as unknown as typeof IntersectionObserver;
```

Wenn der Mock dort schon existiert, NICHT doppelt anhaengen (Idempotenz: `grep -q "IntersectionObserver" test/setup.tsx`).

**B. `__tests__/components/Wizard/MatchResultList.test.tsx` (neu — separates File vom bestehenden `__tests__/components/MatchResultList.test.tsx`):**

- Pattern aus `__tests__/components/MatchResultList.test.tsx` (RTL-Imports, deutsche Test-Beschreibungen).
- describe-Block `'MatchResultList — Empty-State'`.
- Tests (alle als `it.todo`, Plan 02.1-03 macht sie gruen):
  1. `it.todo("rendert Empty-State-Heading 'Kein Programm passt zu diesem Anliegen' bei leerem matches-Array — D-05")`
  2. `it.todo("rendert 3 Reformulierungs-Tipps als Liste — D-05")`
  3. `it.todo("ruft onReset bei Klick auf 'Anliegen neu formulieren' — D-05")`

**C. `__tests__/components/Wizard/MyAntraegeClient.test.tsx` (neu):**

- describe-Block `'MyAntraegeClient — Empty-State'`.
- `it.todo("rendert Empty-State CTA-Label 'Anliegen schildern' bei leerem sessions-Array — D-05")`
- `it.todo("Empty-State-CTA-href verweist auf /antrag/start — D-05")`

**D. `__tests__/components/Wizard/AntragSectionNav.test.tsx` (neu, Component existiert noch nicht — Plan 02.1-04 baut ihn):**

- describe-Block `'AntragSectionNav — D-02'`.
- `it.todo("generiert Slug 'vorhaben-und-anliegen' aus h2-Text 'Vorhaben und Anliegen' — D-02")`
- `it.todo("mapped Umlaute: 'Massnahmen und Foerderung' aus 'Maßnahmen und Förderung' — D-02")` (Hinweis: Tests mit ASCII-Spiegel der Eingabe, weil Source `MatchEntry` UTF-8 ist)
- `it.todo("returns null wenn keine h2-Elemente vorhanden — D-02")`
- `it.todo("setzt active-Klasse auf den Eintrag mit IntersectionObserver-isIntersecting=true — D-02")`

**E. `__tests__/components/Wizard/WizardShell.test.tsx` (neu):**

- Mock `next/navigation` falls Tests auf `useRouter` stossen: `jest.mock("next/navigation", () => ({ useRouter: () => ({ push: jest.fn(), replace: jest.fn() }) }))`.
- Mock `fetch` global per `jest.spyOn(global, "fetch")` mit `mockResolvedValue(new Response(JSON.stringify({ ... }), { status: 200 }))`.
- describe-Block `'WizardShell — Phase-Dispatch + Polling'`.
- `it.todo("rendert letzte Frage bei phase=interviewing — D-12 (Reload waehrend Interview)")`
- `it.todo("startet KEIN /api/wizard/generate POST bei Mount mit phase=generating — D-12 + Pattern-Finding-3")`
- `it.todo("rendert AntragResult bei phase=complete + finalText vorhanden — D-12")`
- `it.todo("rendert Fehler-Block mit Retry-CTA bei phase=failed — D-15")`
- `it.todo("polled gegen /api/wizard/[token] mit cancel-Flag bei phase=generating — D-12 + Pattern-Finding-5 (CheckoutSuccessClient-Pattern)")`

Alle `it.todo` werden in Plan 02.1-06 gruen gemacht.
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && npm test -- --runTestsByPath __tests__/components/Wizard/MatchResultList.test.tsx __tests__/components/Wizard/MyAntraegeClient.test.tsx __tests__/components/Wizard/AntragSectionNav.test.tsx __tests__/components/Wizard/WizardShell.test.tsx --silent 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/kolja/edufunds-app/__tests__/components/Wizard/MatchResultList.test.tsx` exit 0
    - `test -f /home/kolja/edufunds-app/__tests__/components/Wizard/MyAntraegeClient.test.tsx` exit 0
    - `test -f /home/kolja/edufunds-app/__tests__/components/Wizard/AntragSectionNav.test.tsx` exit 0
    - `test -f /home/kolja/edufunds-app/__tests__/components/Wizard/WizardShell.test.tsx` exit 0
    - `grep -q "IntersectionObserver" /home/kolja/edufunds-app/test/setup.tsx` exit 0
    - `grep -q "phase=generating" /home/kolja/edufunds-app/__tests__/components/Wizard/WizardShell.test.tsx` exit 0
    - `grep -q "Anliegen schildern" /home/kolja/edufunds-app/__tests__/components/Wizard/MyAntraegeClient.test.tsx` exit 0
    - `grep -q "phase=failed" /home/kolja/edufunds-app/__tests__/components/Wizard/WizardShell.test.tsx` exit 0
    - `npm test -- --runTestsByPath __tests__/components/Wizard/WizardShell.test.tsx` Exit 0
  </acceptance_criteria>
  <done>4 Component-Test-Files + erweitertes test/setup.tsx mit IntersectionObserver-Mock; alle Tests laufen via `npm test` durch (todos akzeptabel).</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-Skript-Skelette anlegen</name>
  <read_first>
    - /home/kolja/edufunds-app/scripts/smoke-llm.ts (49 Zeilen — Skript-Skelett-Vorlage)
    - /home/kolja/edufunds-app/scripts/smoke-pipeline-with-extractor.ts (220 Zeilen — Env-Loading + DB-Lookup-Pattern)
    - /home/kolja/edufunds-app/lib/stripe/client.ts (getStripe-Singleton-Pattern)
  </read_first>
  <files>scripts/smoke-stripe-webhook.ts, scripts/smoke-reload-resume.ts</files>
  <action>
**A. `scripts/smoke-stripe-webhook.ts` (neu):**

- Header-Kommentar (Stil `smoke-llm.ts`):
  ```
  /**
   * Smoke-Test fuer app/api/stripe/webhook (Plan 02.1-02).
   * Nutzung: `npx tsx --env-file=.env.local scripts/smoke-stripe-webhook.ts`
   * Voraussetzung: Dev-Server laeuft auf http://localhost:3101
   * Voraussetzung: STRIPE_SECRET_KEY + STRIPE_WEBHOOK_SECRET in .env.local
   */
  ```
- Importiere `Stripe from "stripe"` und nutze `stripe.webhooks.generateTestHeaderString({ payload, secret })` zum lokalen Signieren.
- `main()`-Funktion baut eine Test-Payload `{ id: "evt_test_123", type: "checkout.session.completed", data: { object: { id: "cs_test_123", metadata: { wizard_session_token: "test-token-from-script" }, payment_status: "paid", customer_details: { email: "test@example.com" } } } }` als JSON-String, generiert Signatur, POST-t an `http://localhost:3101/api/stripe/webhook`.
- Numbered Logs `[1/3]`, `[2/3]`, `[3/3]` analog `smoke-llm.ts`.
- Erfolgs-Pruefung: `res.status === 200` UND `body.received === true`.
- Negativ-Pfad: zweiter POST mit gefaelschter Signatur (`"v1=fake"`) muss 400 zurueckgeben — auch loggen.
- catch-Wrapper: `main().catch(err => { console.error("SMOKE FAIL:", err); process.exit(1); })`.
- Aktuell wird das Skript noch erfolgreich laufen, weil Plan 02.1-02 die Webhook-Logik haerten wird. In Wave 0 reicht: Skript existiert + ist syntaktisch valide (`npx tsx --check` oder `tsc --noEmit`).

**B. `scripts/smoke-reload-resume.ts` (neu):**

- Header-Kommentar:
  ```
  /**
   * Smoke-Test fuer WIZ-04 Reload-Resume (Plan 02.1-06).
   * Nutzung: `npx tsx --env-file=.env.local scripts/smoke-reload-resume.ts`
   * Voraussetzung: Dev-Server + Dev-DB-Tunnel aktiv (./scripts/dev-db-tunnel.sh --bg)
   */
  ```
- Verwende `pg.Client`-Pattern aus `scripts/smoke-pipeline-with-extractor.ts` Z. 50–60 fuer DATABASE_URL-Lookup.
- `main()` macht (TODO-Kommentar fuer Wave 2):
  - `[1/4]` Lookup einer Test-Session aus DB mit `phase=generating` (oder Skip wenn keine vorhanden + Hinweis-Output).
  - `[2/4]` GET `/api/wizard/{sessionToken}` und logge `body.phase`, `body.generation?.stage`, `body.generation?.stageAt`.
  - `[3/4]` Pruefe: `phase === "generating" || phase === "complete"`. Wenn `generating` und `stageAt > 60s` alt: WARN ausgeben.
  - `[4/4]` Bei `phase === "complete"`: pruefe `generation.finalText` ist nicht-leer.
- catch-Wrapper analog Skript A.
- In Wave 0 reicht: Skript existiert + ist syntaktisch valide. In Wave 2 wird es scharf gestellt.

Beide Skripte muessen TS-clean sein: `npx tsc --noEmit scripts/smoke-stripe-webhook.ts scripts/smoke-reload-resume.ts` exit 0 (bzw. Repo-typisch via `npm run typecheck`).
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && npx tsc --noEmit --skipLibCheck scripts/smoke-stripe-webhook.ts scripts/smoke-reload-resume.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/kolja/edufunds-app/scripts/smoke-stripe-webhook.ts` exit 0
    - `test -f /home/kolja/edufunds-app/scripts/smoke-reload-resume.ts` exit 0
    - `grep -q "generateTestHeaderString" /home/kolja/edufunds-app/scripts/smoke-stripe-webhook.ts` exit 0
    - `grep -q "checkout.session.completed" /home/kolja/edufunds-app/scripts/smoke-stripe-webhook.ts` exit 0
    - `grep -q "process.exit(1)" /home/kolja/edufunds-app/scripts/smoke-stripe-webhook.ts` exit 0
    - `grep -q "phase" /home/kolja/edufunds-app/scripts/smoke-reload-resume.ts` exit 0
    - `grep -q "/api/wizard/" /home/kolja/edufunds-app/scripts/smoke-reload-resume.ts` exit 0
    - `npx tsc --noEmit --skipLibCheck scripts/smoke-stripe-webhook.ts scripts/smoke-reload-resume.ts` Exit 0
  </acceptance_criteria>
  <done>Beide Skripte existieren, sind TS-clean und enthalten klare Numbered-Log-Schritte plus catch-Wrapper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Code → Production-Code | Tests muessen Mocks nutzen statt echte SDKs/DBs zu treffen — verhindert Test-Datenleck in Stripe-Test-Mode-Account / DB-Drift |
| Smoke-Skripte → Lokaler Dev-Server | Smoke-Skripte rufen `localhost:3101` nur ueber gepruefte Routen auf, kein externes Network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1.01-01 | Information Disclosure | `__tests__/app/api/stripe/webhook.test.ts` | mitigate | Tests mocken `getStripe` und `markSessionPaid` vollstaendig — keine echten API-Keys in Tests, keine echten DB-Calls |
| T-02.1.01-02 | Tampering | `scripts/smoke-stripe-webhook.ts` | mitigate | Skript nutzt `STRIPE_WEBHOOK_SECRET` aus `.env.local`, generiert Signaturen lokal — kein Hardcoded-Secret im Repo |
| T-02.1.01-03 | Information Disclosure | `scripts/smoke-reload-resume.ts` | accept | Liest Dev-DB-Sessions; Dev-DB enthaelt nur Test-Schul-Daten und Tokens — Out-of-Scope-Risk fuer 02.1 (PROJECT.md erlaubt Dev-DB-Nutzung) |
</threat_model>

<verification>
- Volle Suite: `npm test -- --silent` exit 0 — alle Tests laufen durch (todos akzeptabel)
- TS-Check: `npx tsc --noEmit --skipLibCheck scripts/smoke-stripe-webhook.ts scripts/smoke-reload-resume.ts` exit 0
- Lint: `npm run lint -- __tests__/app/api/stripe __tests__/components/Wizard __tests__/lib/wizard scripts/smoke-stripe-webhook.ts scripts/smoke-reload-resume.ts` ohne Fehler
- Anzahl `it.todo`-Bloecke: `grep -rh "it.todo" __tests__/app/api/stripe __tests__/components/Wizard __tests__/lib/wizard | wc -l` >= 13 (alle Wave-1/2-Tasks haben Test-Anker)
</verification>

<success_criteria>
- 6 Test-Files + 2 Smoke-Skripte + erweitertes test/setup.tsx existieren
- Volle Suite gruen oder skipped (kein roter Fail), Wave 0 schliesst nicht durch FAILED-Tests
- Jeder `it.todo` referenziert eine Decision-ID aus CONTEXT.md (D-02, D-04, D-05, D-07, D-12, D-13, D-15)
- IntersectionObserver-Mock global verfuegbar
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-01-SUMMARY.md` mit:
- Liste der angelegten Test-Files und ihrer it.todo-Anzahl pro File
- IntersectionObserver-Mock-Zustand in test/setup.tsx
- Hinweise fuer Wave-1/2-Plans, welche `it.todo`-Bloecke sie unskippen muessen
</output>

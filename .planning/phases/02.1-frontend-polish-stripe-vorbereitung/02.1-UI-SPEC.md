---
phase: 02.1
slug: frontend-polish-stripe-vorbereitung
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-06
---

# Phase 02.1 — UI Design Contract

> Visual and interaction contract für Frontend-Polish + Stripe-Vorbereitung.
> Generiert von gsd-ui-researcher, verifiziert von gsd-ui-checker.

---

## Design System

| Property | Value | Quelle |
|----------|-------|--------|
| Tool | none (bespoke Tailwind token system) | tailwind.config.js detektiert, kein components.json |
| Preset | not applicable | kein shadcn |
| Component library | none (Tailwind utility classes direkt) | CONTEXT.md D-03, Code-Scan |
| Icon library | lucide-react | GeneratingProgress.tsx, AntragResult.tsx, MyAntraegeClient.tsx |
| Font (sans) | Plus Jakarta Sans, 400/500/600/700/800 | globals.css @import |
| Font (serif) | DM Serif Display (Headings Marketing-Layer) | globals.css, tailwind.config.js |
| Font (mono) | Fira Code (technische Details) | globals.css |

**Hinweis zum Surface-Dualismus:** Das Projekt hat zwei Oberflächen.
- **Marketing-Layer** (`/`, `/foerderprogramme`, Landing): Parchment-Hintergrund (#f8f5f0), Navy-Text, Gold-Akzent.
- **Wizard-Layer** (`/antrag/*`, `/antrag/meine`, `/antrag/start`): Dunkel-Oberfläche (slate-900/slate-800-Palette), slate-100-Text, Gold-Akzent. Phase 02.1 betrifft ausschließlich den Wizard-Layer.

---

## Spacing Scale

Deklarierte Werte (Tailwind-Standard, Vielfache von 4px):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (p-1, gap-1) | Icon-interne Abstände, Badge-Padding |
| sm | 8px (p-2, gap-2) | Kompakte Element-Abstände, Button-interne Gaps |
| md | 16px (p-4, gap-4) | Standard-Element-Abstand, Card-interne Padding |
| lg | 24px (p-6, gap-6) | Sektions-Padding innerhalb Cards |
| xl | 32px (p-8, gap-8) | Card-Padding (GeneratingProgress, AntragResult) |
| 2xl | 48px (p-12) | Major Section Breaks im Wizard-Flow |
| 3xl | 64px | Page-Level Spacing (nicht aktiv in Wizard-Layer) |

Ausnahmen:
- **Touch-Targets Mobile:** Mindest-Tap-Area 44px für alle interaktiven Elemente im Wizard auf `sm:`-Breakpoint. Betrifft insbesondere `QuestionCard`-Antwort-Buttons, Wizard-Navigation, Match-Auswahl-Links.
- **Stage-Item-Padding:** Pipeline-Stage-Liste in `GeneratingProgress` nutzt p-3 (12px) — bleibt bestehen, ausreichend für Lese-Kontext ohne Touch-Action.

---

## Typography

Wizard-Layer-Typografie (alle Werte aus existierendem Code extrapoliert und konsolidiert):

| Role | Size | Weight | Line Height | Font | Klasse |
|------|------|--------|-------------|------|--------|
| Body / Fließtext | 14px | 400 (regular) | 1.5 | Plus Jakarta Sans | `text-sm` |
| Label / Meta | 12px | 400 (regular) | 1.4 | Plus Jakarta Sans | `text-xs` |
| Card-Heading | 18px | 600 (semibold) | 1.3 | Plus Jakarta Sans | `text-lg font-semibold` |
| Abschnitt-Heading | 20px | 600 (semibold) | 1.2 | Plus Jakarta Sans | `text-xl font-semibold` |

Maximale Gewichte: 400 (regular) + 600 (semibold). Gewicht 500 (medium) nur für Badge-Labels und Fließtext-Hervorhebungen — kein neues Gewicht einführen.

Sonderfall Antrag-Markdown:
- `h1` im Markdown-Render: 24px / 600 / slate-100
- `h2` im Markdown-Render: 18px / 600 / gold-500 (#c9a227)
- `h3` im Markdown-Render: 16px / 600 / slate-100
- Fließtext im Markdown-Render: 14px / 400 / slate-200 / leading-relaxed (~1.625)

Diese Markdown-Typen sind bereits in `AntragResult.tsx` definiert (`MARKDOWN_COMPONENTS`) und werden im Polish nicht neu gesetzt — nur strukturell erweitert (Anker-Navigation, Sektions-Edit-Button).

---

## Color

Wizard-Layer-Farbsystem (Dunkel-Oberfläche):

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `bg-slate-900` (#0f172a) | Primäre Container-Oberfläche: Antrag-Artikel, Stage-Items, Detail-Cards |
| Secondary (30%) | `bg-slate-800/40` (slate-800 mit 40 % Opacity) | Card-Wrapper, Panel-Hintergründe, Listen-Items |
| Accent (10%) | Gold #c9a227 (`text-orange-400`, `border-orange-500/40`, `bg-orange-500/10`) | Reserviert für: Primäre CTA-Buttons, Spinner/Loading-Indikatoren, aktive Stage-Markierung, Sektions-Anker-Links im Antrag-Lesemodus, PDF-Download-Button |
| Destructive | rose-500 #f43f5e (`text-red-300`, `border-red-500/50`) | Ausschließlich: Session-aus-Browser-entfernen-Button (Trash-Icon), Fehler-Badges |

**Hinweis zu "orange" im bestehenden Code:** `text-orange-400` / `bg-orange-500` sind Legacy-Tailwind-Klassen, die im Projekt auf Gold (#c9a227) alias-mapped sind (`tailwind.config.js` orange-Sektion). Neue Implementierungen in Phase 02.1 nutzen ebenfalls `orange-*`-Klassen für Konsistenz mit bestehendem Wizard-Code — KEINE neuen gold-*-Klassen einführen.

Accent reserviert für (exklusive Liste):
1. Primäre CTA-Buttons (z. B. "Antrag starten", "Anliegen schildern", "Zurücksetzen und neu formulieren")
2. Laufender Spinner (`Loader2 text-orange-400`)
3. Aktive Pipeline-Stage-Markierung in `GeneratingProgress`
4. Sektions-Anker-Navigation im Antrag-Lesemodus (aktiver Link-Indikator)
5. PDF-Download-Button-Rahmen und Hintergrund

Text-Hierarchie Wizard-Layer:
- Primärtext: `text-slate-100`
- Sekundärtext: `text-slate-200`
- Metainfo / Subtext: `text-slate-400`
- Deaktiviert / Platzhalter: `text-slate-500` / `text-slate-600`
- Fehlertext: `text-red-300` / `text-red-400`
- Erfolgstext: `text-emerald-300`

---

## Component Inventory

Bestehende Components — kein Neu-Import, nur Polish-Erweiterungen:

| Component | Datei | Phase-02.1-Änderung |
|-----------|-------|---------------------|
| `AntragResult` | `components/Wizard/AntragResult.tsx` | Sektions-Anker-Navigation + Edit-Button pro Sektion (D-02) |
| `MyAntraegeClient` | `components/Wizard/MyAntraegeClient.tsx` | Empty-State Action-orientiert (D-05), Resume-Einstieg (D-12) |
| `GeneratingProgress` | `components/Wizard/GeneratingProgress.tsx` | Aktive Stage-Markierung (current/done/pending) statt alle gleich (D-04) |
| `MatchResultList` | `components/Wizard/MatchResultList.tsx` | Empty-State mit 2-3 Reformulierungs-Tipps + CTA (D-05) |
| `WizardShell` | `components/Wizard/WizardShell.tsx` | Resume-Logik: Reload während interviewing + generating (D-12) |
| `StartClient` | `components/Wizard/StartClient.tsx` | Mobile-Breakpoints Critical-Path Anliegen-Eingabe (D-03) |

Keine neuen Third-Party-Components. Keine shadcn-Registry.

---

## Interaction Contracts

### GeneratingProgress — Stage-Tracking (D-04)

Drei visuelle Zustände pro Stage-Item:

| Zustand | Indikator | Klassen |
|---------|-----------|---------|
| `pending` | Leerer Kreis (Circle Icon) | `text-slate-600` |
| `active` | Animierter Spinner (Loader2) + Gold-Rahmen | `text-orange-400 animate-spin` + `border-orange-500/40 bg-orange-500/5` |
| `done` | Ausgefülltes CheckCircle | `text-emerald-400` + `bg-emerald-500/5` |

Stage-Labels (deutsch, aus `GeneratingProgress.tsx` bereits vorhanden — bestehende Texte bleiben):
1. "Gliederung strukturieren"
2. "Abschnitte schreiben"
3. "Gutachten erstellen"
4. "Revision prüfen"
5. "Finale Fassung"
6. "Finanzplan-Entwurf"
7. "Konsistenz prüfen"

Technische Umsetzung: SSE-Stream oder Stage-Feld-Polling in DB (Researcher-Empfehlung: Stage-Feld in `wizard_sessions`-Tabelle, Polling alle 2s im Frontend über bestehendes `/api/wizard/[token]`-Endpoint — kein neuer WebSocket/SSE nötig).

### Empty-States — Action-orientiert (D-05)

**`/antrag/meine` leer:**
- Icon: `FileText` (Lucide, 40px, `text-slate-500`) — bereits vorhanden, bleibt
- Heading: "Noch kein Antrag begonnen" (16px / semibold / slate-100)
- Body: "Schildere dein Anliegen und finde in Sekunden das passende Förderprogramm für deine Schule." (14px / regular / slate-400)
- CTA: Button "Anliegen schildern" → `/antrag/start` (primary accent: `bg-orange-500 text-white hover:bg-orange-600`, 14px / semibold, px-5 py-2.5, rounded-lg)

**Matcher 0-Treffer (`MatchResultList` leeres matches-Array):**
- Icon: `SearchX` (Lucide, 32px, `text-slate-500`) — neu einführen statt kein Icon
- Heading: "Kein Programm passt zu diesem Anliegen" (18px / semibold / slate-200)
- Body: "Das passiert, wenn das Anliegen zu allgemein oder sehr speziell ist." (14px / regular / slate-400)
- 2-3 Reformulierungs-Tipps als einfache Liste (14px / regular / slate-400):
  - "Nenne Zielgruppe und Zielwirkung konkreter (z. B. 'Leseförderung Klasse 1–2, 30 Kinder')"
  - "Gib ein ungefähres Budget an (z. B. '5.000 bis 10.000 €')"
  - "Erkläre, was die Schule bereits hat und was fehlt"
- CTA: Button "Anliegen neu formulieren" (kein direkter Link, löst `onReset`-Callback aus — existierendes Pattern in `StartClient`)

### Antrag-Detail Lesemodus — Sektions-Navigation (D-02)

Inline-Navigationsliste am Seitenrand (sticky, nur auf `md:` und größer sichtbar):
- Sektionsliste aus `h2`-Headings des Antrag-Markdown extrahiert (client-seitig per `querySelectorAll`)
- Aktiver Link-Indikator: `text-orange-400 border-l-2 border-orange-500 pl-2`
- Inaktiver Link: `text-slate-400 hover:text-slate-200 pl-2`
- Font: 12px / regular

Edit-Button pro Sektion: `PenLine`-Icon (Lucide, 14px), erscheint on-hover rechts neben dem `h2`-Heading, Klasse `text-slate-500 hover:text-slate-200`. Nur sichtbar bei `paid === true`.

### WIZ-04 Reload-Resume — UX-Zustandsmarker

In `MyAntraegeClient.tsx` für Sessions mit `phase === "generating"` oder `phase === "interviewing"`:
- Kein Toast — der bestehende Session-Card zeigt den Phasenstatus bereits via `PHASE_META`-Badge
- Resume-Button: "Weiter →" (bestehende "Öffnen"-Logik reicht, da Wizard-Route Session-Token trägt)
- Generating-Resume: Bei Reload während `phase === "generating"` prüft `WizardShell` das `phase`-Feld in der API-Response — wenn `complete`, rendert `AntragResult`; wenn noch `generating`, rendert `GeneratingProgress` mit Polling (kein Neustart der Pipeline)

### Mobile-Breakpoints Critical-Paths (D-03)

Tailwind-Standard-Breakpoints: `sm: 640px`, `md: 768px`, `lg: 1024px`. Keine Custom-Breakpoints.

Touch-Optimierungen (überall dort, wo heute `py-2` für interaktive Elemente):
- Anliegen-Eingabe-Textarea: `min-h-[120px]` → `min-h-[140px]` auf `sm:` (mehr Tipp-Komfort)
- Wizard-Antwort-Buttons (QuestionCard): `py-2 px-4` → `py-3 px-4` auf `sm:` (44px Mindest-Höhe)
- Match-Karten "Antrag starten"-Links: `py-2` → `py-3` auf `sm:`
- Session-Cards in `MyAntraegeClient`: `flex-wrap` bereits vorhanden — auf `sm:` Stack-Layout (`flex-col`) statt Row

---

## Copywriting Contract

| Element | Copy | Quelle |
|---------|------|--------|
| Primary CTA (Antrag starten) | "Antrag starten" | Bestehend in MatchResultList.tsx |
| Primary CTA (Anliegen) | "Anliegen schildern" | D-05 |
| Primary CTA (Reformulieren) | "Anliegen neu formulieren" | D-05 |
| Empty State /antrag/meine — Heading | "Noch kein Antrag begonnen" | D-05 |
| Empty State /antrag/meine — Body | "Schildere dein Anliegen und finde in Sekunden das passende Förderprogramm für deine Schule." | D-05 / Claude's Discretion |
| Empty State Matcher-0-Treffer — Heading | "Kein Programm passt zu diesem Anliegen" | D-05 |
| Empty State Matcher-0-Treffer — Body | "Das passiert, wenn das Anliegen zu allgemein oder sehr speziell ist." | D-05 / Claude's Discretion |
| Reformulierungs-Tipp 1 | "Nenne Zielgruppe und Zielwirkung konkreter (z. B. 'Leseförderung Klasse 1–2, 30 Kinder')" | Claude's Discretion |
| Reformulierungs-Tipp 2 | "Gib ein ungefähres Budget an (z. B. '5.000 bis 10.000 €')" | Claude's Discretion |
| Reformulierungs-Tipp 3 | "Erkläre, was die Schule bereits hat und was fehlt" | Claude's Discretion |
| GeneratingProgress — Heading | "Pipeline schreibt deinen Antrag" | Bestehend in GeneratingProgress.tsx |
| GeneratingProgress — Subtext | "Typisch 1–3 Minuten. Bleib auf der Seite — das Ergebnis erscheint automatisch." | Bestehend |
| Fehler Matching | "Verbindungsfehler beim Matching — bitte Seite neu laden und erneut versuchen." | Claude's Discretion (ersetzt generisches HTTP-Error) |
| Fehler Generating | "Die Generierung ist fehlgeschlagen. Prüfe deine Verbindung und versuche es erneut." | Claude's Discretion |
| Fehler Session nicht gefunden | "Diese Session existiert nicht mehr auf dem Server." | Bestehend in MyAntraegeClient.tsx |
| Destructive: Session entfernen | "Session entfernen" — Confirm-Dialog: "Diese Session aus dem Browser entfernen? Auf dem Server bleibt sie bestehen." | Bestehend in MyAntraegeClient.tsx (confirm() — bleibt unverändert in 02.1) |
| Antrag-Detail Heading | "Antragsentwurf freigeschaltet" / "Antragsentwurf fertig" | Bestehend in AntragResult.tsx |
| Resume-Phase Generating | "Wird noch geschrieben…" (Badge) | Bestehend via PHASE_META |
| Stage-Label 1 | "Gliederung strukturieren" | Bestehend GeneratingProgress.tsx |
| Stage-Label 2 | "Abschnitte schreiben" | Bestehend |
| Stage-Label 3 | "Gutachten erstellen" | Bestehend |
| Stage-Label 4 | "Revision prüfen" | Bestehend (war "Finale Fassung" + "Recheck" vertauscht — Reihenfolge aus pipeline.ts ist maßgeblich) |
| Stage-Label 5 | "Finale Fassung" | Bestehend |
| Stage-Label 6 | "Finanzplan-Entwurf" | Bestehend |
| Stage-Label 7 | "Konsistenz prüfen" | Bestehend |

Destruktive Aktionen in Phase 02.1:
- **Session aus Browser entfernen:** Bestehender `confirm()`-Dialog bleibt (kein Modal nötig, da Aktion reversibel — Session bleibt auf Server). Kein neuer Destructive-UI-Pattern.
- **Kein weiterer destruktiver Flow** in 02.1 scope.

---

## Stripe-Vorbereitung — UI-Seiteneffekte

Der Webhook-Skeleton (`app/api/stripe/webhook/route.ts`) hat keine direkten UI-Konsequenzen in Phase 02.1. Die Paywall-UI (`PaywallGate.tsx`) bleibt unverändert. Dev-Mock (`NEXT_PUBLIC_PAYWALL_DEV_MOCK=1`) bleibt aktiv.

Keine neuen UI-States für Stripe in 02.1.

---

## UAT-Vorbereitungs-Artefakte — kein UI

`.planning/uat/`-Dateien (UAT-PLAN-TEMPLATE.md, UAT-BEFUNDE-TEMPLATE.md, PILOTEN.md, UAT-ANSCHREIBEN.md) sind reine Dokumentations-Deliverables ohne UI-Konsequenzen. Kein Design-Contract erforderlich.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable (kein shadcn) |
| Third-party | none | not applicable |

Keine dritten Registries deklariert. Gate nicht erforderlich.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 02.1-frontend-polish-stripe-vorbereitung*
*UI-SPEC erstellt: 2026-05-06*
*Quelle: CONTEXT.md (13 Decisions), tailwind.config.js, globals.css, 6 Wizard-Components gelesen*

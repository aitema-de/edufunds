---
phase: 02.1-frontend-polish-stripe-vorbereitung
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/wizard/types.ts
  - lib/wizard/stage-labels.ts
  - lib/wizard/pipeline.ts
  - app/api/wizard/generate/route.ts
  - components/Wizard/GeneratingProgress.tsx
  - components/Wizard/WizardShell.tsx
  - __tests__/lib/wizard/pipeline-events.test.ts
  - __tests__/components/Wizard/WizardShell.test.tsx
autonomous: true
requirements: [WIZ-04, UI-04]
must_haves:
  truths:
    - "PipelineStage-Type ist in lib/wizard/types.ts definiert (Single Source of Truth, Open Question #4 aus RESEARCH); pipeline.ts re-exportiert"
    - "GenerationArtefacts hat optionale Felder `stage?: PipelineStage` und `stageAt?: string` (D-13)"
    - "/api/wizard/generate haengt einen onEvent-Callback an runPipeline, der bei jedem emit() Stage + Timestamp via jsonb_set in antrag_data.generation persistiert (D-13)"
    - "/api/wizard/generate ist idempotent: bei `phase=generating` antwortet sie 200 mit Hinweis statt 409 (D-12 Lücke #2)"
    - "GeneratingProgress STAGES-Array nutzt 'section' (Singular) — Stage-Key-Drift behoben (Pattern-Finding #4)"
    - "GeneratingProgress berechnet pending|active|done pro Stage aus currentStage-Prop und rendert je Status unterschiedlich (Circle/Loader2/CheckCircle, UI-SPEC §Stage-Tracking)"
    - "WizardShell startet bei Mount mit phase=generating KEIN POST /api/wizard/generate, sondern startet direkt Polling auf /api/wizard/[token] (D-12 Lücke #1, Pattern-Finding #3 + #5)"
    - "WizardShell-Polling nutzt cancel-Flag-Pattern aus CheckoutSuccessClient.tsx (Pattern S-1)"
    - "WizardShell rendert bei phase=failed eine Fehler-Card mit 'Erneut versuchen'-CTA (D-15)"
    - "Pipeline-Events-Test ist gruen: onEvent wird mind. 4x aufgerufen (outline, section, critique, revision) in korrekter Reihenfolge UND Pipeline schluckt onEvent-Throws ohne Crash (defensiver try/catch um emit() in pipeline.ts)"
    - "WizardShell-Tests sind gruen: 5 Phase-Dispatch-Branches getestet"
  artifacts:
    - path: lib/wizard/types.ts
      provides: "PipelineStage-Type + GenerationArtefacts.stage/stageAt"
      contains: "PipelineStage"
    - path: lib/wizard/pipeline.ts
      provides: "Re-Export von PipelineStage aus types.ts (single source of truth)"
      contains: "PipelineStage"
    - path: app/api/wizard/generate/route.ts
      provides: "Idempotente Generate-Route + Stage-Heartbeat via jsonb_set"
      contains: "jsonb_set"
    - path: components/Wizard/GeneratingProgress.tsx
      provides: "Stage-Status-Logik (pending/active/done) mit korrigierter STAGES-Reihenfolge ('section' Singular)"
      contains: "stageStatus"
    - path: components/Wizard/WizardShell.tsx
      provides: "Polling bei phase=generating + Reload-Detect + failed-UI mit Retry-CTA"
      contains: "phase === \"failed\""
  key_links:
    - from: app/api/wizard/generate/route.ts
      to: lib/wizard/pipeline.ts
      via: "onEvent-Callback bei runPipeline-Aufruf, schreibt Stage in DB"
      pattern: "jsonb_set.*generation,stage"
    - from: components/Wizard/WizardShell.tsx
      to: app/api/wizard/[token]/route.ts
      via: "Polling-Loop bei phase=generating, fetch alle 2000ms"
      pattern: "/api/wizard/.*\\$\\{state\\.sessionToken\\}"
    - from: components/Wizard/GeneratingProgress.tsx
      to: components/Wizard/WizardShell.tsx
      via: "currentStage-Prop aus state.generation.stage"
      pattern: "currentStage"
---

<objective>
Wave-2-Hauptarbeit: WIZ-04 Reload-Resume schliessen + GeneratingProgress 7-Stage-Anzeige live + failed-Phase-UI Mini-Fix. Drei diskrete Luecken aus 02.1-RESEARCH §2.3:
- Lücke #1: WizardShell ruft bei Reload mit phase=generating /api/wizard/generate NICHT erneut auf
- Lücke #2: /api/wizard/generate ist idempotent fuer phase=generating-Sessions
- Lücke #3: Stage-Persistenz fehlt komplett — Pipeline-emit() versickert heute

Zusatz: Stage-Key-Drift `sections`(Plural) → `section`(Singular) im STAGES-Array, PipelineStage-Type-Move nach types.ts, failed-UI Mini-Fix mit Retry-CTA (D-15).

Purpose: Live-UAT in Phase 6 mit broken Reload = Pilot-Session-Killer (CONTEXT D-11). Nach diesem Plan kann ein Pilot Tab schliessen + neu oeffnen waehrend `phase=interviewing` ODER `phase=generating` ohne State-Verlust.

Output: 5 modifizierte Code-Files + 2 Test-Files mit ehemals todo-Tests gruen.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/kolja/edufunds-app/CLAUDE.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-CONTEXT.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-RESEARCH.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-PATTERNS.md
@/home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-UI-SPEC.md
@/home/kolja/edufunds-app/components/Wizard/CheckoutSuccessClient.tsx
@/home/kolja/edufunds-app/lib/db.ts

<interfaces>
<!-- Aktueller Code-State — Plan modifiziert genau diese Stellen. -->

From `lib/wizard/types.ts` Z. 125–142 (GenerationArtefacts heute):
```typescript
export interface GenerationArtefacts {
  outline?: { titel: string; abschnitte: Array<{ name: string; fokus: string }> };
  sections?: Array<{ name: string; text: string }>;
  critique?: string;
  // ... weitere Felder ...
  finalText?: string;
  finanzplan?: Finanzplan;
  // KEIN stage / stageAt vorhanden — wird hinzugefuegt
}
```

From `lib/wizard/pipeline.ts` Z. 147–157 (heute):
```typescript
export interface PipelineEvent {
  stage: "outline" | "section" | "critique" | "revision"
       | "recheck" | "finanzplan" | "consistency" | "done";
  message: string;
  payload?: unknown;
}
```

From `app/api/wizard/generate/route.ts` Z. 28–60 (heute):
```typescript
if (
  session.data.phase !== "ready_to_generate" &&
  session.data.phase !== "interviewing"
) {
  return NextResponse.json(
    { error: `Session ist in Phase ${session.data.phase}` },
    { status: 409 }    // ← muss gelockert werden fuer phase=generating
  );
}
// ...
const { artefacts, usages } = await runPipeline(
  programm,
  session.data.facts,
  richtlinie,
  undefined,           // ← onEvent ist undefined — wird Heartbeat-Callback
  session.data.messages
);
```

From `lib/db.ts`:
```typescript
export async function query(sql: string, params?: unknown[]): Promise<QueryResult>;
```

From `components/Wizard/GeneratingProgress.tsx` Z. 13–21 (heute, KEIN stageStatus):
```typescript
const STAGES = [
  { key: "outline", label: "Gliederung strukturieren", detail: "..." },
  { key: "sections", label: "Abschnitte schreiben", detail: "..." },  // ← drift: Plural
  { key: "critique", label: "Gutachten erstellen", detail: "..." },
  { key: "revision", label: "Finale Fassung", detail: "..." },
  { key: "recheck", label: "Revision pruefen", detail: "..." },
  { key: "finanzplan", label: "Finanzplan-Entwurf", detail: "..." },
  { key: "consistency", label: "Konsistenz pruefen", detail: "..." },
];
interface Props { stage: string; }   // ← wird zu currentStage?: PipelineStage
```

Wichtig: Die UI-SPEC §"Copywriting Contract" zeigt Stage-Reihenfolge anders ("Revision pruefen" VOR "Finale Fassung"). Aktuell im Code steht "Finale Fassung" (revision) VOR "Revision pruefen" (recheck) — das ist auch die Pipeline-emit-Reihenfolge (RESEARCH §3.1: emit-Reihenfolge ist outline → section → critique → revision → recheck → finanzplan → consistency → done). **Pipeline-Reihenfolge ist Source of Truth** — UI-SPEC-Text muss nach Pipeline-Reihenfolge geordnet werden, nicht umgekehrt. Heutige STAGES-Reihenfolge ist korrekt, nur der `key` `sections` muss auf `section` geaendert werden.

From `components/Wizard/WizardShell.tsx`:
```typescript
const [generationStage, setGenerationStage] = useState<string>("");
// Z. 154–184 useEffect-Mount-Hook (loadSession)
// Z. 291–316 startGeneration-Callback (POST /api/wizard/generate)
// Z. 442–460 Phase-Dispatch (kein failed-Branch)
```

Polling-Pattern aus `CheckoutSuccessClient.tsx` Z. 25–79 (Vorbild):
```typescript
let cancelled = false;
let consecutiveFailures = 0;
const tick = async () => {
  if (cancelled) return;
  try {
    const res = await fetch(`/api/wizard/${sessionToken}`, { cache: "no-store" });
    if (res.ok) {
      consecutiveFailures = 0;
      const body = await res.json();
      if (terminal-condition) return;
    } else { consecutiveFailures += 1; }
  } catch (e) { consecutiveFailures += 1; }
  if (consecutiveFailures >= 5) { setStatus("network-error"); return; }
  if (Date.now() - started > MAX_WAIT_MS) { setStatus("timeout"); return; }
  if (!cancelled) setTimeout(tick, POLL_INTERVAL_MS);
};
tick();
return () => { cancelled = true; clearInterval(interval); };
```

From `app/api/wizard/[token]/route.ts`: bestehende GET-Route gibt `phase`, `generation` (mit `generation.stage` + `generation.stageAt` nach Plan-Aenderungen), `costs`, `paidToken` durch — KEIN neuer Endpoint noetig (Pattern-Finding #3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PipelineStage-Type-Move + Generate-Route Idempotenz + Stage-Heartbeat</name>
  <read_first>
    - /home/kolja/edufunds-app/lib/wizard/types.ts (volle Datei, vor allem GenerationArtefacts Z. 125–142)
    - /home/kolja/edufunds-app/lib/wizard/pipeline.ts Z. 145–180 (PipelineEvent + runPipeline-Signatur)
    - /home/kolja/edufunds-app/app/api/wizard/generate/route.ts (volle 97 Zeilen)
    - /home/kolja/edufunds-app/lib/db.ts (query-Signatur)
    - /home/kolja/edufunds-app/__tests__/lib/wizard/pipeline-events.test.ts (Wave-0-Skelett mit 2 todos)
    - /home/kolja/edufunds-app/__tests__/lib/wizard/outline-fallback.test.ts (LLM-Mock-Stil)
    - /home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-RESEARCH.md §2.3 + §2.4 + §3.3
  </read_first>
  <behavior>
    - Test 1 (vorher todo): runPipeline mit Mock-LLM ruft onEvent mind. 4× auf, in Reihenfolge ["outline", "section", ..., "critique", "revision", ...]
    - Test 2 (vorher todo): runPipeline schluckt Fehler im onEvent-Callback (rejected Promise) ohne abzubrechen
    - Idempotenz: POST /api/wizard/generate auf Session mit `phase=generating` antwortet 200 mit `{ phase: "generating", message: "Pipeline laeuft bereits, polle /api/wizard/[token]" }` statt 409
    - Stage-Heartbeat: Pipeline-emit fuehrt zu UPDATE ki_antraege ... jsonb_set(antrag_data,'{generation,stage}',...) mit aktuellem Stage und ISO-Timestamp
  </behavior>
  <files>lib/wizard/types.ts, lib/wizard/pipeline.ts, app/api/wizard/generate/route.ts, __tests__/lib/wizard/pipeline-events.test.ts</files>
  <action>
**A. `lib/wizard/types.ts`:**

1. Direkt nach `import`-Block oder direkt vor `GenerationArtefacts` (Z. 125) neuen Type-Export:
   ```typescript
   /**
    * Pipeline-Stage-Identifier — Single Source of Truth (RESEARCH Open Question #4).
    * pipeline.ts re-exportiert; GeneratingProgress importiert von hier.
    */
   export type PipelineStage =
     | "outline"
     | "section"
     | "critique"
     | "revision"
     | "recheck"
     | "finanzplan"
     | "consistency"
     | "done";
   ```
2. `GenerationArtefacts` (Z. 125–142) erweitern um:
   ```typescript
     /** D-13 — Aktueller Pipeline-Stage waehrend Generation. Bei jedem emit() in DB persistiert. */
     stage?: PipelineStage;
     /** D-13 — ISO-Timestamp des letzten Stage-Updates. Frontend nutzt fuer Liveness-Check. */
     stageAt?: string;
   ```

**B. `lib/wizard/pipeline.ts`:**

1. Importiere `PipelineStage` aus `./types` ODER (defensiver, falls Circular-Import-Risk besteht): definiere weiterhin den Union-Type vor Ort UND exportiere ihn als `PipelineStage`. Pruefe via `tsc --noEmit` ob ein Re-Export aus `./types` zirkulaer wuerde.

   Empfehlung: Update `PipelineEvent`-Definition (Z. 147–157):
   ```typescript
   import type { PipelineStage } from "./types";  // bestehende type-Imports erweitern

   export type { PipelineStage } from "./types";

   export interface PipelineEvent {
     stage: PipelineStage;
     message: string;
     payload?: unknown;
   }
   ```

2. Pipeline.ts emittiert heute schon mit den 8 keys aus dem Union — keine Aenderung an emit-Calls noetig, nur am Type.

**C. `app/api/wizard/generate/route.ts`:**

1. Imports erweitern: `import { query } from "@/lib/db";` + `import type { PipelineStage } from "@/lib/wizard/types";`
2. Idempotenz-Branch (Z. 28–36) ersetzen: Wenn `session.data.phase === "generating"`, **NICHT 409 werfen** sondern early-return mit Polling-Hinweis:
   ```typescript
   if (session.data.phase === "generating") {
     // D-12 Idempotenz: Pipeline laeuft serverseitig schon — Frontend soll pollen, nicht neu starten.
     return NextResponse.json({
       sessionToken,
       phase: "generating",
       message: "Pipeline laeuft bereits, polle /api/wizard/[token]",
       generation: session.data.generation ?? null,
     });
   }
   if (
     session.data.phase !== "ready_to_generate" &&
     session.data.phase !== "interviewing"
   ) {
     return NextResponse.json(
       { error: `Session ist in Phase ${session.data.phase}` },
       { status: 409 }
     );
   }
   ```
3. Stage-Heartbeat (Z. 54–60) — `undefined` durch `onEvent`-Callback ersetzen:
   ```typescript
   const onEvent = async (event: { stage: PipelineStage; message: string }) => {
     try {
       await query(
         `UPDATE ki_antraege
            SET antrag_data = jsonb_set(
              jsonb_set(COALESCE(antrag_data, '{}'::jsonb), '{generation,stage}', to_jsonb($1::text)),
              '{generation,stageAt}', to_jsonb($2::text)
            ),
            updated_at = CURRENT_TIMESTAMP
          WHERE session_token = $3`,
         [event.stage, new Date().toISOString(), sessionToken]
       );
     } catch (e) {
       // Best-Effort — Pipeline darf nicht wegen DB-Hick crashen.
       console.warn("[wizard/generate] Stage-Heartbeat fehlgeschlagen:", e);
     }
   };

   const { artefacts, usages } = await runPipeline(
     programm,
     session.data.facts,
     richtlinie,
     onEvent,                                // ← war undefined
     session.data.messages
   );
   ```

**D. `__tests__/lib/wizard/pipeline-events.test.ts` — beide it.todos unskippen:**

Mock-Strategie: `jest.mock("@/lib/wizard/llm")` mit `generateText` und `generateJson` als jest.fn(). Stub-Antworten so dass runPipeline durch alle Stages laeuft. Wenn das zu komplex wird (echte runPipeline benoetigt viele LLM-Returns), darf der Test mit deliberated SKip bleiben + Begruendung — aber bevorzugt: 1+ Stage-Reihenfolge-Assertion in einem minimal-stubbed Lauf.

```typescript
import { runPipeline } from "@/lib/wizard/pipeline";
import type { PipelineEvent } from "@/lib/wizard/pipeline";
import type { Foerderprogramm } from "@/lib/foerderSchema";

jest.mock("@/lib/wizard/llm", () => ({
  generateText: jest.fn().mockResolvedValue({
    text: "Stub Section Text",
    usage: { promptTokens: 0, completionTokens: 0, totalTokens: 0 },
  }),
  generateJson: jest.fn().mockResolvedValue({
    data: { findings: [] },        // critique: keine findings → recheck wird skipped
    usage: { promptTokens: 0, completionTokens: 0, totalTokens: 0 },
  }),
  MODEL_PRO: "stub-pro",
  MODEL_FLASH: "stub-flash",
}));

describe("runPipeline — onEvent (D-04 + D-13)", () => {
  it("ruft onEvent fuer jede Stage in korrekter Reihenfolge auf — D-04", async () => {
    const events: PipelineEvent["stage"][] = [];
    const minimalProgramm = {
      id: "stub-program",
      name: "Stub-Programm",
      foerdergeber: "Test",
      foerdergeberTyp: "stiftung",
    } as unknown as Foerderprogramm;
    const facts = {} as never;
    try {
      await runPipeline(
        minimalProgramm,
        facts,
        null,
        (e: PipelineEvent) => { events.push(e.stage); }
      );
    } catch {
      // Pipeline darf scheitern bei minimal-stubbed Mocks; relevant sind die emittierten Stages bis zum Abbruch.
    }
    // Verbindliche Untergrenze (B4): mind. 4 Stages in fester Reihenfolge muessen emittiert worden sein,
    // bevor die Pipeline ggf. an einer spaeteren Stage scheitert.
    expect(events.length).toBeGreaterThanOrEqual(4);
    expect(events.slice(0, 4)).toEqual(["outline", "section", "critique", "revision"]);
  });

  it("schluckt Fehler im onEvent-Callback ohne Pipeline-Crash — D-13", async () => {
    const minimalProgramm = {
      id: "stub-program",
      name: "Stub-Programm",
      foerdergeber: "Test",
      foerdergeberTyp: "stiftung",
    } as unknown as Foerderprogramm;
    const facts = {} as never;
    // Verbindliche Assertion (B4): runPipeline MUSS resolven, auch wenn onEvent throwt.
    // Setzt die Pipeline-emit-Haertung in pipeline.ts voraus (siehe Schritt B unten).
    await expect(
      runPipeline(
        minimalProgramm,
        facts,
        null,
        (() => { throw new Error("boom"); }) as unknown as (e: PipelineEvent) => void
      )
    ).resolves.toBeDefined();
  });
});
```

**Verbindliche emit-Haertung in pipeline.ts (B4 — KEINE Option mehr, sondern Pflicht):**

In `lib/wizard/pipeline.ts` (heute ~Z. 178) steht:
```typescript
const emit = (e: PipelineEvent) => onEvent?.(e);
```

Diese Zeile MUSS in Task 1 verbindlich umgestellt werden auf:
```typescript
const emit = (e: PipelineEvent) => {
  try { onEvent?.(e); } catch (err) { console.warn("[pipeline] onEvent threw, ignoring:", err); }
};
```

Begruendung: Test 2 oben verlangt `resolves.toBeDefined()` — ohne den try/catch crasht runPipeline beim ersten emit() durch den onEvent-Throw, und der Test wird rot. RESEARCH §3.3 fordert das gleiche Pattern. Aenderung ist defensiver Schutz, kein Logik-Eingriff in die Stage-Reihenfolge.

**Verifikation:**
- TS-Check: `npx tsc --noEmit --skipLibCheck` Exit 0
- Tests: `npm test -- --runTestsByPath __tests__/lib/wizard/pipeline-events.test.ts` Exit 0
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && npx tsc --noEmit --skipLibCheck 2>&1 | tail -10 && npm test -- --runTestsByPath __tests__/lib/wizard/pipeline-events.test.ts --silent 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export type PipelineStage" /home/kolja/edufunds-app/lib/wizard/types.ts` exit 0
    - `grep -q "stage?: PipelineStage" /home/kolja/edufunds-app/lib/wizard/types.ts` exit 0
    - `grep -q "stageAt?:" /home/kolja/edufunds-app/lib/wizard/types.ts` exit 0
    - `grep -q "PipelineStage" /home/kolja/edufunds-app/lib/wizard/pipeline.ts` exit 0
    - `grep -q "jsonb_set" /home/kolja/edufunds-app/app/api/wizard/generate/route.ts` exit 0
    - `grep -q "generation,stage" /home/kolja/edufunds-app/app/api/wizard/generate/route.ts` exit 0
    - `grep -q "generation,stageAt" /home/kolja/edufunds-app/app/api/wizard/generate/route.ts` exit 0
    - `grep -q "Pipeline laeuft bereits" /home/kolja/edufunds-app/app/api/wizard/generate/route.ts` exit 0
    - `grep -c "it.todo" /home/kolja/edufunds-app/__tests__/lib/wizard/pipeline-events.test.ts` Output 0
    - `npm test -- --runTestsByPath __tests__/lib/wizard/pipeline-events.test.ts` Exit 0
    - `npx tsc --noEmit --skipLibCheck` Exit 0
  </acceptance_criteria>
  <done>Stage-Type ist Single-Source-of-Truth in types.ts; Generate-Route ist idempotent; Stage-Heartbeat schreibt jsonb_set; pipeline-events Tests gruen (oder dokumentiert weich-passed mit Hinweis).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GeneratingProgress Stage-Status-Logik + Stage-Key-Drift fix</name>
  <read_first>
    - /home/kolja/edufunds-app/components/Wizard/GeneratingProgress.tsx (volle 57 Zeilen)
    - /home/kolja/edufunds-app/lib/wizard/types.ts (mit neuem PipelineStage aus Task 1)
    - /home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-UI-SPEC.md §"GeneratingProgress — Stage-Tracking (D-04)"
    - /home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-RESEARCH.md §3.3
  </read_first>
  <behavior>
    - STAGES-Array nutzt key="section" (nicht "sections") — Pattern-Finding #4 fix
    - Wenn currentStage="critique": outline + section haben done-Visual, critique hat active-Visual, rest pending
    - Wenn currentStage="done": alle Stages done
    - Wenn currentStage=undefined: alle Stages pending
  </behavior>
  <files>components/Wizard/GeneratingProgress.tsx</files>
  <action>
Datei komplett ersetzen:

```typescript
"use client";

import { Circle, Loader2, CheckCircle } from "lucide-react";
import type { PipelineStage } from "@/lib/wizard/types";

interface Props {
  /** Optional: dt. Klartext-Status fuer den unteren Hinweistext (z. B. "Pipeline startet"). */
  stage?: string;
  /** Aktueller Pipeline-Stage aus DB-Heartbeat — bestimmt pending/active/done je Item. */
  currentStage?: PipelineStage;
}

const STAGES: Array<{ key: PipelineStage; label: string; detail: string }> = [
  { key: "outline", label: "Gliederung strukturieren", detail: "Folgt der offiziellen Antragsstruktur, falls Richtlinie hinterlegt ist." },
  { key: "section", label: "Abschnitte schreiben", detail: "Pro Abschnitt ein eigener KI-Call mit den Fakten." },
  { key: "critique", label: "Gutachten erstellen", detail: "Ein strenger Durchgang sucht Floskeln, Lücken, Richtlinien-Verstöße." },
  { key: "revision", label: "Finale Fassung", detail: "Einarbeitung des Gutachtens in den Antragstext." },
  { key: "recheck", label: "Revision prüfen", detail: "Jedes Gutachten-Finding gegen die finale Fassung geprüft." },
  { key: "finanzplan", label: "Finanzplan-Entwurf", detail: "Programm-spezifische Kostenpositionen nach Richtlinie." },
  { key: "consistency", label: "Konsistenz prüfen", detail: "Antragstext und Finanzplan müssen zusammenpassen." },
];

const ORDER: PipelineStage[] = ["outline", "section", "critique", "revision", "recheck", "finanzplan", "consistency"];

function stageStatus(
  stageKey: PipelineStage,
  currentStage?: PipelineStage
): "pending" | "active" | "done" {
  if (!currentStage) return "pending";
  if (currentStage === "done") return "done";
  const currentIdx = ORDER.indexOf(currentStage);
  const myIdx = ORDER.indexOf(stageKey);
  if (myIdx < 0 || currentIdx < 0) return "pending";
  if (myIdx < currentIdx) return "done";
  if (myIdx === currentIdx) return "active";
  return "pending";
}

export function GeneratingProgress({ stage, currentStage }: Props) {
  return (
    <div className="rounded-xl border border-orange-500/40 bg-slate-800/40 p-10">
      <div className="mb-6 flex items-center gap-3">
        <Loader2 className="h-8 w-8 animate-spin text-orange-400" />
        <div>
          <h3 className="text-xl font-semibold text-slate-100">
            Pipeline schreibt deinen Antrag
          </h3>
          <p className="text-sm text-slate-400">
            Typisch 1–3 Minuten. Bleib auf der Seite — das Ergebnis erscheint automatisch.
          </p>
        </div>
      </div>

      <ul className="space-y-3">
        {STAGES.map((s) => {
          const status = stageStatus(s.key, currentStage);
          const containerClass =
            status === "active"
              ? "flex items-start gap-3 rounded-lg border border-orange-500/40 bg-orange-500/5 p-3"
              : status === "done"
                ? "flex items-start gap-3 rounded-lg border border-slate-700/40 bg-emerald-500/5 p-3"
                : "flex items-start gap-3 rounded-lg border border-slate-700/40 bg-slate-900/40 p-3";
          return (
            <li key={s.key} className={containerClass}>
              <div className="mt-0.5">
                {status === "pending" && <Circle className="h-5 w-5 text-slate-600" />}
                {status === "active" && <Loader2 className="h-5 w-5 animate-spin text-orange-400" />}
                {status === "done" && <CheckCircle className="h-5 w-5 text-emerald-400" />}
              </div>
              <div className="flex-1">
                <div className="text-sm font-medium text-slate-200">{s.label}</div>
                <div className="text-xs text-slate-500">{s.detail}</div>
              </div>
            </li>
          );
        })}
      </ul>

      {stage && (
        <p className="mt-6 text-center text-sm text-orange-300">{stage}</p>
      )}
    </div>
  );
}
```

Wichtig: Existing-Caller-Compat — `stage`-Prop bleibt bestehen (alte Caller wie `<GeneratingProgress stage={...} />` aus WizardShell brechen nicht). Neue Prop `currentStage` ist optional. WizardShell wird in Task 3 aktualisiert um `currentStage` zu setzen.
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && npx tsc --noEmit --skipLibCheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "key: \"section\"" /home/kolja/edufunds-app/components/Wizard/GeneratingProgress.tsx` exit 0
    - `! grep -q "key: \"sections\"" /home/kolja/edufunds-app/components/Wizard/GeneratingProgress.tsx` (Drift behoben)
    - `grep -q "stageStatus" /home/kolja/edufunds-app/components/Wizard/GeneratingProgress.tsx` exit 0
    - `grep -q "import type { PipelineStage }" /home/kolja/edufunds-app/components/Wizard/GeneratingProgress.tsx` exit 0
    - `grep -q "CheckCircle" /home/kolja/edufunds-app/components/Wizard/GeneratingProgress.tsx` exit 0
    - `grep -q "currentStage" /home/kolja/edufunds-app/components/Wizard/GeneratingProgress.tsx` exit 0
    - `npx tsc --noEmit --skipLibCheck` Exit 0
  </acceptance_criteria>
  <done>GeneratingProgress hat 3-Status-Logik, Drift behoben, weiterhin abwaertskompatibel mit `stage`-Prop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WizardShell Reload-Detect + Polling + failed-UI + Tests gruen</name>
  <read_first>
    - /home/kolja/edufunds-app/components/Wizard/WizardShell.tsx (volle Datei, vor allem Z. 1–80, Z. 154–184, Z. 280–330, Z. 440–470)
    - /home/kolja/edufunds-app/components/Wizard/CheckoutSuccessClient.tsx Z. 25–80 (Polling-Vorbild)
    - /home/kolja/edufunds-app/components/Wizard/WizardErrorBlock.tsx (existing Error-Block-Component)
    - /home/kolja/edufunds-app/__tests__/components/Wizard/WizardShell.test.tsx (Wave-0-Skelett mit 5 todos)
    - /home/kolja/edufunds-app/.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-RESEARCH.md §2.3 (Polling-Pattern komplett)
  </read_first>
  <behavior>
    - Test 1 (vorher todo): Mount mit phase=interviewing → letzte Frage wird gerendert
    - Test 2 (vorher todo): Mount mit phase=generating → KEIN POST /api/wizard/generate (fetch-Mock-Spy verifiziert)
    - Test 3 (vorher todo): Polling-Response phase=complete → setState fuehrt zu AntragResult-Render (oder Phase-Wechsel)
    - Test 4 (vorher todo): Polling-Response phase=failed → Fehler-Block + Retry-CTA (D-15)
    - Test 5 (vorher todo): cancel-Flag-Pattern: zwei Mount/Unmount-Zyklen erzeugen nicht zwei parallele Polling-Loops
  </behavior>
  <files>components/Wizard/WizardShell.tsx, __tests__/components/Wizard/WizardShell.test.tsx</files>
  <action>
**A. `components/Wizard/WizardShell.tsx` (Polling-useEffect + Phase-Dispatch + failed-UI):**

Schritt 1 — neuer State + Stage-Type-Import oben in der Datei:
```typescript
import type {
  WizardFacts,
  WizardMessage,
  WizardPhase,
  GenerationArtefacts,
  PipelineStage,         // ← neu
} from "@/lib/wizard/types";

// In der WizardApiState-Interface (Z. 35–45):
interface WizardApiState {
  sessionToken: string;
  phase: WizardPhase;
  question: { content: string; rationale?: string } | null;
  ready: { summary: string } | null;
  totalQuestions: number;
  maxQuestions: number;
  facts: WizardFacts;
  costs?: CostLedger | null;
  paidToken?: string | null;
  generation?: GenerationArtefacts | null;   // ← neu, fuer Stage-Heartbeat
}
```

Schritt 2 — `loadSession`-Funktion (Z. 68–95) erweitern um `generation` durchzureichen:
- Pruefe heutigen Code: `body.generation` wird ggf. als `setGeneration(body.generation ?? null)` gesetzt. **Zusaetzlich** in den `setState`-Block aufnehmen, damit `state.generation` verfuegbar ist:
  ```typescript
  setState({
    sessionToken: body.sessionToken,
    phase: body.phase,
    // ... bestehende Felder ...
    generation: body.generation ?? null,
  });
  ```

Schritt 3 — neuer Polling-`useEffect` direkt NACH dem Mount-Hook (Z. 184):
```typescript
// D-12 Reload-Resume: Bei phase=generating polle die GET-Route, statt /api/wizard/generate neu zu starten.
useEffect(() => {
  if (state?.phase !== "generating" || !state.sessionToken) return;
  let cancelled = false;
  let consecutiveFailures = 0;

  const tick = async () => {
    if (cancelled) return;
    try {
      const res = await fetch(`/api/wizard/${state.sessionToken}`, { cache: "no-store" });
      if (res.ok) {
        consecutiveFailures = 0;
        const body = await res.json();
        if (cancelled) return;
        // Stage-Heartbeat aus DB
        const stageKey = body.generation?.stage as PipelineStage | undefined;
        if (stageKey) setGenerationStage(localizeStage(stageKey));
        // Phase-Uebergang erkennen
        if (body.phase === "complete" && body.generation?.finalText) {
          setGeneration(body.generation);
          setState((s) => s ? { ...s, phase: "complete", costs: body.costs ?? s.costs, generation: body.generation } : s);
          return;  // Polling stoppt durch Effect-Re-Run (state.phase != generating)
        }
        if (body.phase === "failed") {
          setState((s) => s ? { ...s, phase: "failed" } : s);
          setError("Generierung fehlgeschlagen — bitte neu versuchen.");
          return;
        }
        // Andernfalls: state.generation aktualisieren fuer GeneratingProgress.currentStage
        if (body.generation) {
          setState((s) => s ? { ...s, generation: body.generation } : s);
        }
      } else {
        consecutiveFailures += 1;
      }
    } catch (e) {
      consecutiveFailures += 1;
    }
    if (consecutiveFailures >= 5) {
      setError("Verbindung zur Server-Pipeline verloren — bitte Seite neu laden.");
      return;
    }
    if (!cancelled) setTimeout(tick, 2000);
  };
  tick();
  return () => { cancelled = true; };
}, [state?.phase, state?.sessionToken]);

// Stage-Localization (deutsch fuer Hinweis-Text — Single Source of Truth in lib/wizard/stage-labels.ts, W15)
import { STAGE_LABELS } from "@/lib/wizard/stage-labels";
function localizeStage(s: PipelineStage): string {
  return STAGE_LABELS[s] ?? s;
}
```

**Verbindlich (W15 — Single Source of Truth):** Lege die Konstante in einer NEUEN Datei `lib/wizard/stage-labels.ts` an, NICHT inline in `WizardShell.tsx`. `GeneratingProgress.tsx` (Schritt 3 weiter unten) importiert dieselbe Konstante.

```typescript
// lib/wizard/stage-labels.ts
import type { PipelineStage } from "./types";

export const STAGE_LABELS: Record<PipelineStage, string> = {
  outline: "Gliederung",
  section: "Abschnitte",
  critique: "Gutachten",
  revision: "Finale Fassung",
  recheck: "Recheck",
  finanzplan: "Finanzplan",
  consistency: "Konsistenz",
  done: "Fertig",
};
```

Schritt 4 — `startGeneration`-Callback (Z. 291–316) bleibt unveraendert (wird vom User-Klick "Antrag generieren" aufgerufen, NICHT mehr beim Mount). Wichtig: `setState((s) => (s ? { ...s, phase: "generating" } : s))` in Z. 296 triggert den neuen Polling-useEffect — zwei Birds, ein Stein.

Schritt 5 — Phase-Dispatch (Z. 442–460) erweitern um `failed`-Branch:
```typescript
if (state.phase === "generating") {
  return <GeneratingProgress
    stage={generationStage}
    currentStage={state.generation?.stage as PipelineStage | undefined}
  />;
}
if (state.phase === "failed") {
  return (
    <WizardErrorBlock
      message={error ?? "Die Generierung ist fehlgeschlagen. Pruefe deine Verbindung und versuche es erneut."}
      onRetry={() => {
        setError(null);
        setState((s) => s ? { ...s, phase: "ready_to_generate" } : s);
      }}
      busy={false}
    />
  );
}
if (state.phase === "complete" && generation?.finalText) {
  // ... bestehender Code ...
}
```

Wichtig: Pruefe `WizardErrorBlock`-Signatur — wenn die Component `onRetry`-Callback nicht akzeptiert oder anders heisst, anpassen oder einen einfachen Fehler-Block inline rendern (mit `<button onClick={...}>Erneut versuchen</button>`).

**B. `__tests__/components/Wizard/WizardShell.test.tsx` (5 todos unskippen):**

Tests sind aufwaendig (fetch-Mock + Polling-Sequence). **Verbindliche Mindest-Abdeckung (B5):** Test 2 (kein-POST-bei-generating) UND Test 4 (failed-Phase -> Fehler-Block + "Erneut versuchen"-CTA) MUESSEN als reale `it()`-Bloecke ausgefuehrt werden — KEIN `it.skip`. Mock fuer Test 4 ist trivial: 1 Polling-Response mit `phase: "failed"`.

Tests 3 und 5 (Polling-complete-Race, Mount/Unmount-Doppel-Polling) DUERFEN als `it.skip` mit Begruendungs-Kommentar bleiben, weil sie setTimeout-basiertes Mocking voraussetzen. Test 1 (interviewing-Render) ist optional, aber empfohlen.

Beispiel-Skeleton fuer Test 2:
```typescript
import { render, waitFor } from "@testing-library/react";
import { WizardShell } from "@/components/Wizard/WizardShell";

jest.mock("next/navigation", () => ({
  useRouter: () => ({ push: jest.fn(), replace: jest.fn() }),
}));

const stubProgramm = {
  id: "stub-prog",
  name: "Stub-Programm",
  // weitere Pflichtfelder analog zu MatchResultList.test.tsx
} as never;

describe("WizardShell — Phase-Dispatch + Polling (D-12 + D-15)", () => {
  let fetchSpy: jest.SpyInstance;
  beforeEach(() => {
    fetchSpy = jest.spyOn(global, "fetch");
    // Default: Phase generating
    fetchSpy.mockResolvedValue(new Response(JSON.stringify({
      sessionToken: "test-token",
      phase: "generating",
      messages: [],
      facts: {},
      interviewer: {},
      generation: { stage: "outline", stageAt: new Date().toISOString() },
    }), { status: 200 }));
    localStorage.setItem("edufunds.wizard.session.stub-prog", "test-token");
  });
  afterEach(() => {
    fetchSpy.mockRestore();
    localStorage.clear();
  });

  it("startet KEIN /api/wizard/generate POST bei Mount mit phase=generating — D-12 + Pattern-Finding-3", async () => {
    render(<WizardShell programm={stubProgramm} />);
    await waitFor(() => {
      const calls = fetchSpy.mock.calls.map(c => c[0] as string);
      const generatePosts = calls.filter(url => url.includes("/api/wizard/generate"));
      expect(generatePosts.length).toBe(0);
    });
  });

  // Test 4 (B5) — VERBINDLICH als reales it(), KEIN it.skip:
  it("rendert Fehler-Block + 'Erneut versuchen'-CTA bei phase=failed — D-15", async () => {
    fetchSpy.mockReset();
    fetchSpy.mockResolvedValueOnce(new Response(JSON.stringify({
      sessionToken: "test-token",
      phase: "failed",
      messages: [],
      facts: {},
      interviewer: {},
      generation: null,
    }), { status: 200 }));
    const { findByText } = render(<WizardShell programm={stubProgramm} />);
    expect(await findByText(/Erneut versuchen|Nochmal versuchen/i)).toBeInTheDocument();
  });

  // Test 3 (Polling complete) + Test 5 (Mount/Unmount-Doppel-Polling) duerfen it.skip bleiben mit Begruendung.
});
```

Akzeptiere bei Polling-Tests Race-Condition-Toleranzen: setTimeout-basiertes Polling braucht `jest.useFakeTimers()` ODER `await waitFor(..., { timeout: 3000 })`. Wenn Tests instabil sind, lieber it.skip + Hinweis-Kommentar `// SKIP: Polling-Mock ist instabil — Plan 02.1-06 deferiert auf manuellen Browser-Smoke`.

**Verifikation:**
- `npm test -- --runTestsByPath __tests__/components/Wizard/WizardShell.test.tsx` Exit 0 (todos und skipped akzeptabel, aber Test 2 ("startet KEIN POST bei generating") muss real + gruen sein)
- `npx tsc --noEmit --skipLibCheck` Exit 0
- Volle Suite: `npm test -- --silent` Exit 0
  </action>
  <verify>
    <automated>cd /home/kolja/edufunds-app && npx tsc --noEmit --skipLibCheck 2>&1 | tail -10 && npm test -- --silent 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "phase === \"failed\"" /home/kolja/edufunds-app/components/Wizard/WizardShell.tsx` exit 0
    - `grep -q "currentStage" /home/kolja/edufunds-app/components/Wizard/WizardShell.tsx` exit 0
    - `grep -q "let cancelled = false" /home/kolja/edufunds-app/components/Wizard/WizardShell.tsx` exit 0
    - `grep -q "setTimeout(tick, 2000)" /home/kolja/edufunds-app/components/Wizard/WizardShell.tsx` exit 0
    - `grep -q "fetch.*\`/api/wizard/" /home/kolja/edufunds-app/components/Wizard/WizardShell.tsx` exit 0  # W12 — Polling-fetch auf /api/wizard/<token>
    - `grep -q "Erneut versuchen\|Nochmal versuchen" /home/kolja/edufunds-app/components/Wizard/WizardShell.tsx` exit 0
    - Anzahl realer it-Bloecke (nicht todo, nicht skip) in `__tests__/components/Wizard/WizardShell.test.tsx` >= 2, davon mind. 1 mit "phase=generating" + "POST" UND mind. 1 mit "phase=failed" + "Erneut versuchen"-Assertion (B5)
    - `grep -q "STAGE_LABELS" /home/kolja/edufunds-app/lib/wizard/types.ts /home/kolja/edufunds-app/lib/wizard/stage-labels.ts 2>/dev/null` exit 0  # W15 — Single Source of Truth
    - `! grep -q "const STAGE_HINT_LABELS" /home/kolja/edufunds-app/components/Wizard/WizardShell.tsx` exit 0  # W15 — KEIN Duplikat in WizardShell
    - `npx tsc --noEmit --skipLibCheck` Exit 0
    - `npm test -- --silent` Exit 0
  </acceptance_criteria>
  <done>WizardShell hat Polling-Loop bei phase=generating, failed-UI mit Retry, Tests verifizieren mind. den No-Re-POST-Branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/wizard/[token] | Polling liest Session-State; Authorization heute ueber session_token-Besitz (kein User-Account) |
| Browser → /api/wizard/generate | POST nur waehrend ready_to_generate / interviewing erlaubt, jetzt mit idempotentem Branch fuer phase=generating |
| Pipeline → DB-Heartbeat | jsonb_set-Updates schreiben Stage; Race-Condition bei zwei parallelen Pipelines moeglich, aber durch idempotenten Generate-Endpoint praktisch verhindert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1.06-01 | Information Disclosure | Polling-URL `/api/wizard/{sessionToken}` | accept | session_token ist UUIDv4 (crypto-strong, RESEARCH §Security V3) — Brute-Force-resistant. Anonyme Sessions sind Design-Choice (PROJECT.md Out of Scope Auth). Wer den Token hat, hat Zugriff — gleiches Risiko wie heute. |
| T-02.1.06-02 | Tampering | Stage-Heartbeat-Race (zwei Generate-Calls parallel) | mitigate | Generate-Route ist nun idempotent: zweiter Call mit phase=generating → Polling-Hinweis, KEIN zweites runPipeline. Verhindert paralleles emit() auf gleichem Token. |
| T-02.1.06-03 | Spoofing | Cross-Session Polling (User A pollt Session B) | accept | Polling-URL braucht den exakten Token; ohne Token kein Lookup. Risiko gleich wie heutiger Wizard-Pfad. |
| T-02.1.06-04 | Denial of Service | Endlos-Polling bei crashed Pipeline (Lücke #2 RESEARCH §2.3) | mitigate | consecutiveFailures-Counter (5 Fehler → setError + Stop). stageAt-Liveness-Check (>60s alt) waere v2-Erweiterung, hier akzeptiert (RESEARCH §2.3 Lücke #2 Fix-Empfehlung "Minimum-viable"). |
| T-02.1.06-05 | Tampering | jsonb_set-Race zwischen Heartbeat und Final-Save | accept | jsonb_set ist atomic in Postgres; Final-Save (`updateWizardSession` mit phase=complete) ueberschreibt antrag_data komplett — letzter gewinnt. Bei normalem Lifecycle harmlos. |
| T-02.1.06-06 | Information Disclosure | Polling-Logs koennten Session-Token leaken | mitigate | Plan-Code logged keine Tokens in console — nur Stage-Namen + ISO-Timestamp |
</threat_model>

<verification>
- TS-Check: `npx tsc --noEmit --skipLibCheck` Exit 0
- Volle Test-Suite: `npm test -- --silent` Exit 0 (alle bestehenden Tests + Plan 02.1-01-Tests + neue gruen)
- Pipeline-Events-Test: mind. 1 von 2 gruen, dokumentierte Note bei skipped
- WizardShell-Test: mind. 1 realer Test fuer "kein POST bei phase=generating"
- Optional Live-Smoke: Mit Dev-Server + Dev-DB + bestehender Test-Session → `npx tsx --env-file=.env.local scripts/smoke-reload-resume.ts` zeigt phase + stage Info
</verification>

<success_criteria>
- WIZ-04 D-12 erfuellt: Reload waehrend phase=generating fuehrt zu Polling, NICHT zu erneutem /api/wizard/generate-POST
- WIZ-04 D-13 erfuellt: Stage-Heartbeat in DB persistiert, Frontend liest und rendert
- D-04 erfuellt: GeneratingProgress zeigt 7 Stages mit pending/active/done
- D-15 erfuellt: phase=failed zeigt Fehler-Card mit Retry-CTA
- Pattern-Finding #4 behoben: STAGES-Array key="section" (Singular)
- Tests: pipeline-events + WizardShell jeweils mind. 1+ realer gruener Test (rest darf it.skip mit Begruendung sein)
- TS-Check + volle Test-Suite gruen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/02.1-frontend-polish-stripe-vorbereitung/02.1-06-SUMMARY.md` mit:
- Diff-Summary pro File (types.ts / pipeline.ts / generate-route.ts / GeneratingProgress.tsx / WizardShell.tsx)
- Test-Status pro File (gruen / skipped / blocked)
- Hinweis: manueller Browser-Reload-Smoke ist als Manual-Only-Verification in 02.1-VALIDATION.md noch offen — sollte vor /gsd-verify-work in Chrome+Safari je 2× durchgespielt werden
</output>

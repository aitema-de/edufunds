# Phase 02.1: Frontend-Polish + Stripe-Vorbereitung — Pattern Map

**Mapped:** 2026-05-06
**Files analyzed:** 22 (3 neu/zu erstellen + 11 zu ändern + 4 UAT-Doku-Templates + 4 Test-Files)
**Analogs found:** 19 / 22

## Wichtiger Befund vorab

`app/api/stripe/webhook/route.ts` (83 Zeilen) **existiert bereits** und ist nahezu identisch mit dem RESEARCH.md-Skelett. Phase 02.1 muss diese Datei nur **erweitern** (TODO-Stub-Granularität, ggf. weitere Events) — sie ist **kein Greenfield**. Ebenso ist `lib/stripe/client.ts` (23 Zeilen Singleton) bereits da. Die antizipierte Liste hat das verfehlt.

Das ist eine pattern-relevante Verschiebung: 02.1 ist hier ein „Audit + Härten + dokumentieren" statt „neu bauen".

---

## File Classification

| Datei | Status | Rolle | Data Flow | Closest Analog | Match Quality |
|-------|--------|-------|-----------|----------------|---------------|
| `app/api/stripe/webhook/route.ts` | **EXISTIERT** (erweitern) | API-Route (POST, Event-Dispatch) | request-response, idempotent | Selbst (Audit) + `app/api/wizard/checkout/route.ts` | exact (sich selbst) |
| `lib/stripe/client.ts` | **EXISTIERT** | Lib (Singleton-Client) | n/a | — | n/a |
| `app/api/wizard/[sessionToken]/progress/route.ts` | **NEU** (laut Forschung optional, RESEARCH.md empfiehlt KEINE neue Route — bestehender `[token]/route.ts` reicht) | API-Route (GET, polling-target) | request-response, polling | `app/api/wizard/[token]/route.ts` (33 Zeilen) | **NICHT BAUEN** — siehe Empfehlung unten |
| `lib/wizard/pipeline.ts` (ändern) | EXISTIERT (Stage-Heartbeat) | Lib (Pipeline-Orchestrator) | event-driven (emit) | Selbst | exact |
| `lib/wizard/session.ts` (evtl. ändern) | EXISTIERT | Lib (DB-Persistenz) | CRUD | Selbst | exact |
| `app/api/wizard/generate/route.ts` (ändern) | EXISTIERT | API-Route (POST, async-pipeline) | request-response + side-effect (DB-Writes) | Selbst | exact |
| `components/Wizard/WizardShell.tsx` (ändern) | EXISTIERT | React-Component (Container) | event-driven + polling | `components/Wizard/CheckoutSuccessClient.tsx` (206 Zeilen — bewährtes Polling-Pattern) | exact |
| `components/Wizard/GeneratingProgress.tsx` (ändern) | EXISTIERT | React-Component (Anzeige) | props-only | Selbst (STAGES-Array bleibt, Status-Logik kommt dazu) | exact |
| `components/Wizard/AntragResult.tsx` (ändern) | EXISTIERT | React-Component (Markdown-Render) | props-only + DOM-effects | Selbst (`MARKDOWN_COMPONENTS`-Map) | exact |
| `components/Wizard/AntragSectionNav.tsx` | **NEU** | React-Component (Sidebar-Nav, IntersectionObserver) | DOM-driven | `components/Wizard/CheckoutSuccessClient.tsx` (Polling-Pattern für Cleanup-Flag); `components/Wizard/AntragResult.tsx` (sibling, ähnlicher Visual-Style) | role-match |
| `components/Wizard/MyAntraegeClient.tsx` (ändern) | EXISTIERT | React-Component (List/Empty-State) | request-response (fetch loop) | Selbst (Empty-State-Block Z. 133–154) | exact |
| `components/Wizard/MatchResultList.tsx` (ändern) | EXISTIERT | React-Component (List/Empty-State) | props-only | Selbst (Z. 36–50) | exact |
| `components/Wizard/StartClient.tsx` (ändern) | EXISTIERT | React-Component (Container, Reset-Callback) | state-machine | Selbst | exact |
| `components/Wizard/AnliegenForm.tsx` (ändern) | EXISTIERT | React-Component (Form) | props-only | Selbst | exact |
| `components/Wizard/QuestionCard.tsx` (ändern) | EXISTIERT | React-Component (Form) | props-only | Selbst | exact |
| `lib/wizard/types.ts` (ändern) | EXISTIERT | Type-Definitionen | n/a | Selbst (`GenerationArtefacts` Z. 125–142) | exact |
| `scripts/smoke-stripe-webhook.ts` | **NEU** | Smoke-Skript (Live-Run, signed-payload) | one-shot | `scripts/smoke-pipeline-with-extractor.ts` (220 Zeilen) + `scripts/smoke-llm.ts` (49 Zeilen, kompakter) | role-match |
| `scripts/smoke-reload-resume.ts` | **NEU** (optional, RESEARCH.md erwähnt nicht zwingend nötig) | Smoke-Skript (DB + HTTP) | one-shot | `scripts/smoke-pipeline-with-extractor.ts` | role-match |
| `__tests__/app/api/stripe/webhook.test.ts` | **NEU** | Test-Datei (Jest, API-Route) | n/a | `__tests__/lib/wizard/facts-extractor.test.ts` (Pure-Function-Test als Stil-Vorlage) | partial — keine direkten API-Route-Tests im Repo, aber Pure-Function-Tests gibt es |
| `__tests__/lib/wizard/session.test.ts` | **NEU** | Test-Datei (Jest, DB-Layer) | n/a | `__tests__/lib/wizard/facts-extractor.test.ts` | partial — DB-Test-Setup fehlt im Repo |
| `__tests__/lib/wizard/pipeline-events.test.ts` | **NEU** | Test-Datei (Jest, onEvent-Mock) | n/a | `__tests__/lib/wizard/outline-fallback.test.ts` + `facts-extractor.test.ts` | partial |
| `__tests__/components/Wizard/WizardShell.test.tsx` | **NEU** | Test-Datei (Jest + RTL, Polling) | n/a | `__tests__/components/MatchResultList.test.tsx` (RTL-Pattern) | role-match |
| `__tests__/components/Wizard/AntragSectionNav.test.tsx` | **NEU** | Test-Datei (Jest + RTL, IntersectionObserver-Mock) | n/a | `__tests__/components/MatchResultList.test.tsx` | role-match |
| `.planning/uat/UAT-PLAN-TEMPLATE.md` | **NEU** | Doku-Template | n/a | RESEARCH.md §5.2 enthält Komplettstruktur | template-from-research |
| `.planning/uat/UAT-BEFUNDE-TEMPLATE.md` | **NEU** | Doku-Template | n/a | RESEARCH.md §5.3 + 28.04.-Memo-Format | template-from-research |
| `.planning/uat/PILOTEN.md` | **NEU** | Doku-Template (Tabelle) | n/a | RESEARCH.md §5.4 | template-from-research |
| `.planning/uat/UAT-ANSCHREIBEN.md` | **NEU** | Doku-Template (Mail) | n/a | RESEARCH.md §5.5 | template-from-research |

---

## Pattern Assignments

### `app/api/stripe/webhook/route.ts` (existiert, evtl. minimal erweitern)

**Befund:** Datei mit 83 Zeilen ist da. Sie folgt bereits dem RESEARCH.md-Pattern fast 1:1 (Raw-Body via `req.text()`, `getStripe().webhooks.constructEvent`, idempotentes `markSessionPaid`, Switch über Event-Type, 503 bei fehlender Konfig, 400 bei Verification-Fail, 500 bei Dispatch-Fail).

**Pattern-Excerpt (existierend, Z. 11–37):**
```typescript
export async function POST(req: NextRequest) {
  if (!stripeConfigured() || !process.env.STRIPE_WEBHOOK_SECRET) {
    return NextResponse.json(
      { error: "Stripe nicht konfiguriert" },
      { status: 503 }
    );
  }
  const sig = req.headers.get("stripe-signature");
  if (!sig) {
    return NextResponse.json({ error: "stripe-signature Header fehlt" }, { status: 400 });
  }
  const rawBody = await req.text();
  const stripe = getStripe();
  let event: Stripe.Event;
  try {
    event = stripe.webhooks.constructEvent(
      rawBody,
      sig,
      process.env.STRIPE_WEBHOOK_SECRET!
    );
  } catch (err) {
    console.error("[stripe/webhook] Signatur-Pruefung fehlgeschlagen:", err);
    return NextResponse.json({ error: "Invalid signature" }, { status: 400 });
  }
```

**Was 02.1 ergänzt (Plan-Empfehlung):**
- Granulare TODO-Kommentare nach RESEARCH.md §1.5 (Mail-Hook / State-Hook / DB-Schema-Hook) für PAY-03/-04
- Optional `event.id`-Logging für Replay-Audit-Trail
- `runtime = "nodejs"` und `dynamic = "force-dynamic"` sind bereits gesetzt — bleiben

**1-Zeilen-Hinweis:** Übernehmen wie es ist; Plan-Phase fügt nur TODO-Stubs feiner ein und dokumentiert in `STRIPE_SETUP.md` § Webhook-Manual-Verifikation.

---

### `app/api/wizard/[token]/route.ts` (Polling-Target — bleibt unverändert)

**Befund:** Die Route gibt heute schon `phase`, `generation`, `costs`, `paidToken` zurück (Z. 16–32). Stage-Polling kann diese Route 1:1 nutzen, **kein neuer Endpoint nötig**.

**Pattern-Excerpt (Z. 16–32):**
```typescript
return NextResponse.json({
  sessionToken: session.sessionToken,
  foerderprogrammId: session.foerderprogrammId,
  foerderprogrammName: session.foerderprogrammName,
  phase: session.data.phase,
  messages: session.data.messages,
  facts: session.data.facts,
  interviewer: session.data.interviewer,
  generation: session.data.generation ?? null,  // ← Stage-Heartbeat landet in generation.stage
  costs: session.data.costs ?? null,
  status: session.status,
  paidToken: session.paidToken ?? null,
  // ...
});
```

**1-Zeilen-Hinweis:** Da `generation` bereits durchgereicht wird und `generation.stage`/`generation.stageAt` per `jsonb_set` in `antrag_data.generation` gepflegt werden, **keine Code-Änderung nötig** — der Polling-Path ist in der Route bereits vollständig.

**Empfehlung Plan-Phase:** Antizipierten Plan-Eintrag „NEU: `app/api/wizard/[sessionToken]/progress/route.ts`" **streichen**. Nutze die existierende Route.

---

### `app/api/wizard/generate/route.ts` (ändern — onEvent + Idempotenz)

**Analog:** Selbst (97 Zeilen).

**Pattern-Excerpt (Z. 28–60, heute):**
```typescript
if (
  session.data.phase !== "ready_to_generate" &&
  session.data.phase !== "interviewing"
) {
  return NextResponse.json(
    { error: `Session ist in Phase ${session.data.phase}` },
    { status: 409 }
  );
}
// ...
const { artefacts, usages } = await runPipeline(
  programm,
  session.data.facts,
  richtlinie,
  undefined,                          // ← onEvent ist undefined — Stage-Events versickern
  session.data.messages
);
```

**Was 02.1 ergänzt:**
1. **Idempotenz:** `phase === "generating"` darf NICHT 409 werfen — stattdessen 200 mit Hinweis „Pipeline läuft bereits, polle `/api/wizard/[token]`". Damit greift Reload-Resume sauber.
2. **`onEvent`-Callback** mit `jsonb_set` für Stage-Heartbeat (RESEARCH.md §2.3 + Code-Beispiel-Block).

**Pattern für jsonb_set-Heartbeat (aus RESEARCH.md §2.3, an `lib/db.ts:query`-Pattern angepasst):**
```typescript
import { query } from "@/lib/db";

const onEvent = async (event: { stage: string; message: string }) => {
  try {
    await query(
      `UPDATE ki_antraege
         SET antrag_data = jsonb_set(
           jsonb_set(COALESCE(antrag_data, '{}'::jsonb), '{generation,stage}', to_jsonb($1::text)),
           '{generation,stageAt}', to_jsonb($2::text)
         )
       WHERE session_token = $3`,
      [event.stage, new Date().toISOString(), sessionToken]
    );
  } catch (e) {
    console.warn("[wizard/generate] stage-heartbeat failed:", e);
  }
};
```

**1-Zeilen-Hinweis:** Identische Struktur wie bestehender Body, nur `undefined` durch `onEvent` ersetzt + 409-Branch zu 200/Polling-Hinweis aufgeweicht.

---

### `lib/wizard/types.ts` (ändern — `GenerationArtefacts` erweitern)

**Analog:** Selbst.

**Pattern-Excerpt (Z. 125–142, heute):**
```typescript
export interface GenerationArtefacts {
  outline?: { titel: string; abschnitte: Array<{ name: string; fokus: string }> };
  sections?: Array<{ name: string; text: string }>;
  critique?: string;
  critiqueFindings?: CritiqueFinding[];
  // ... bestehende Felder ...
  finalText?: string;
  finanzplan?: Finanzplan;
}
```

**Erweiterung (02.1):**
```typescript
  /** Pipeline-Stage-Heartbeat (D-04). Wird bei jedem emit() in DB persistiert. */
  stage?: PipelineStage;
  /** ISO-Timestamp des letzten Stage-Updates. Wird vom Frontend für Liveness-Check genutzt. */
  stageAt?: string;
```

Plus: `export type PipelineStage = "outline" | "section" | "critique" | "revision" | "recheck" | "finanzplan" | "consistency" | "done";` aus `lib/wizard/pipeline.ts:148–157` hierher ziehen, in `pipeline.ts` re-exportieren (RESEARCH.md Open Question #4 — Single Source of Truth).

**1-Zeilen-Hinweis:** Reine Type-Erweiterung, kein Funktions-Refactor.

---

### `components/Wizard/WizardShell.tsx` (ändern — Polling + Reload-Detect)

**Analog 1 (für Polling-Pattern):** `components/Wizard/CheckoutSuccessClient.tsx` (206 Zeilen) — bewährter Polling-Pattern mit Cancel-Flag, MAX_WAIT, Network-Error-Counter.

**Analog 2 (für State-Setup):** Selbst (Z. 154–184 useEffect-Mount, Z. 442–460 Phase-Dispatch).

**Polling-Pattern-Excerpt aus `CheckoutSuccessClient.tsx` (Z. 25–79):**
```typescript
useEffect(() => {
  if (!sessionToken) {
    setStatus("no-token");
    return;
  }
  const started = Date.now();
  let cancelled = false;
  let consecutiveFailures = 0;

  const interval = setInterval(() => {
    if (!cancelled) setElapsedMs(Date.now() - started);
  }, 250);

  const tick = async () => {
    if (cancelled) return;
    try {
      const res = await fetch(`/api/wizard/${sessionToken}`, { cache: "no-store" });
      if (res.ok) {
        consecutiveFailures = 0;
        setNetworkError(null);
        const body = await res.json();
        if (body.paidToken) {
          // ... terminal state, weiterleiten
          return;
        }
      } else {
        consecutiveFailures += 1;
      }
    } catch (e) {
      consecutiveFailures += 1;
      setNetworkError(e instanceof Error ? e.message : "Netzwerkfehler");
    }
    if (consecutiveFailures >= 5) { setStatus("network-error"); return; }
    if (Date.now() - started > MAX_WAIT_MS) { setStatus("timeout"); return; }
    if (!cancelled) setTimeout(tick, POLL_INTERVAL_MS);
  };
  tick();

  return () => {
    cancelled = true;
    clearInterval(interval);
  };
}, [sessionToken, router, restartKey]);
```

**Was WizardShell übernimmt:**
1. `cancelled`-Flag-Pattern (StrictMode-sicher — RESEARCH.md Pitfall 6).
2. `consecutiveFailures`-Counter mit Schwelle (5) für Network-Fehler-State.
3. `setTimeout(tick, 2000)` mit Re-Schedule statt `setInterval`.
4. Terminal-States: `phase=complete` (→ `setGeneration` + Phase-Wechsel), `phase=failed` (→ Fehler-CTA, D-15).

**Reload-Detect-Pattern (neuer useEffect, hängt an `state?.phase`):**
```typescript
useEffect(() => {
  if (state?.phase !== "generating" || !state.sessionToken) return;
  // KEIN startGeneration() Aufruf — Pipeline läuft serverseitig schon.
  // Nur Polling starten.
  let cancelled = false;
  const tick = async () => { /* analog CheckoutSuccessClient, plus Stage-Update */ };
  tick();
  return () => { cancelled = true; };
}, [state?.phase, state?.sessionToken]);
```

**Heutiger Reload-Bug-Excerpt (Z. 154–164):**
```typescript
useEffect(() => {
  if (typeof window === "undefined") return;
  setSchoolProfile(loadSchoolProfile());
  const existing = localStorage.getItem(storageKey);
  if (existing) {
    loadSession(existing);  // → fetch /api/wizard/[token], setState mit phase aus body.phase
  } else {
    const h = consumeHandoff();
    if (h) setHandoff(h);
  }
}, [storageKey, loadSession, programm.id]);
```

**1-Zeilen-Hinweis:** Polling-Pattern aus `CheckoutSuccessClient.tsx` 1:1 übernehmen (Cancel-Flag, Failure-Counter, Re-Schedule); D-15 `failed`-Branch in der bestehenden Phase-Dispatch-Cascade ergänzen (Z. 442–460).

---

### `components/Wizard/GeneratingProgress.tsx` (ändern — Stage-Status pro Item)

**Analog:** Selbst (57 Zeilen).

**Pattern-Excerpt (Z. 13–50, heute):**
```typescript
const STAGES = [
  { key: "outline", label: "Gliederung strukturieren", detail: "..." },
  { key: "sections", label: "Abschnitte schreiben", detail: "..." },
  { key: "critique", label: "Gutachten erstellen", detail: "..." },
  { key: "revision", label: "Finale Fassung", detail: "..." },
  { key: "recheck", label: "Revision prüfen", detail: "..." },
  { key: "finanzplan", label: "Finanzplan-Entwurf", detail: "..." },
  { key: "consistency", label: "Konsistenz prüfen", detail: "..." },
];

export function GeneratingProgress({ stage }: Props) {
  return (
    <div className="rounded-xl border border-orange-500/40 bg-slate-800/40 p-10">
      {/* Header mit Loader2 ... */}
      <ul className="space-y-3">
        {STAGES.map((s) => (
          <li key={s.key} className="flex items-start gap-3 rounded-lg border border-slate-700/40 bg-slate-900/40 p-3">
            <div className="mt-0.5">
              <Circle className="h-5 w-5 text-slate-600" />  {/* ← heute alle gleich */}
            </div>
            {/* ... */}
          </li>
        ))}
      </ul>
    </div>
  );
}
```

**Hinweis Stage-Key-Drift:** STAGES nutzt `"sections"` (Plural), Pipeline emittiert `"section"` (Singular). RESEARCH.md §3.1 gibt Single-Source-of-Truth-Empfehlung. Plan-Phase muss EINEN der beiden anpassen — Empfehlung: STAGES-Array auf `"section"` umstellen, weil Pipeline als Truth-Source schwerer zu refactoren ist.

**Stage-Status-Logik (RESEARCH.md §3.3, Wave 2):**
```typescript
function stageStatus(stageKey: string, currentStage?: string): "pending" | "active" | "done" {
  if (!currentStage) return "pending";
  if (currentStage === "done") return "done";
  const order = ["outline", "section", "critique", "revision", "recheck", "finanzplan", "consistency"];
  const currentIdx = order.indexOf(currentStage);
  const myIdx = order.indexOf(stageKey);
  if (myIdx < 0 || currentIdx < 0) return "pending";
  if (myIdx < currentIdx) return "done";
  if (myIdx === currentIdx) return "active";
  return "pending";
}
```

**Visual-Klassen je Status (UI-SPEC):**
- `pending`: `Circle` + `text-slate-600`
- `active`: `Loader2` + `text-orange-400 animate-spin` + Item: `border-orange-500/40 bg-orange-500/5`
- `done`: `CheckCircle` + `text-emerald-400` + Item: `bg-emerald-500/5`

**Failed-UI-Mini-Fix (D-15):** Eigener Render-Branch in `GeneratingProgress` ODER bevorzugt in `WizardShell` (Phase-Dispatch). Letzteres ist sauberer — `GeneratingProgress` bleibt für laufende Pipeline, `failed` rendert separate Fehler-Card mit `WizardErrorBlock` + Retry-CTA.

**1-Zeilen-Hinweis:** STAGES-Array bleibt, Status-Logik wird props-getrieben aus `currentStage` abgeleitet; Stage-Key-Drift `sections`→`section` korrigieren.

---

### `components/Wizard/AntragResult.tsx` (ändern — Anker-IDs auf h2)

**Analog:** Selbst (427 Zeilen, `MARKDOWN_COMPONENTS`-Map Z. 26–54 ist die Schlüsselstelle).

**Pattern-Excerpt (Z. 26–54, heute):**
```typescript
const MARKDOWN_COMPONENTS = {
  h1: ({ children }: { children?: React.ReactNode }) => (
    <h1 className="mb-6 text-2xl font-semibold text-slate-100">{children}</h1>
  ),
  h2: ({ children }: { children?: React.ReactNode }) => (
    <h2 className="mb-3 mt-8 text-lg font-semibold text-[#c9a227]">{children}</h2>
  ),
  // ...
};
```

**Erweiterung (02.1, RESEARCH.md §4.1):**
```typescript
h2: ({ children }: { children?: React.ReactNode }) => {
  const text = typeof children === "string" ? children
    : Array.isArray(children) ? children.filter(c => typeof c === "string").join("")
    : "";
  const id = text.toLowerCase()
    .replace(/[äöüß]/g, c => ({ä:"ae",ö:"oe",ü:"ue",ß:"ss"}[c]!))
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-|-$/g, "")
    .slice(0, 60);
  return <h2 id={id} className="mb-3 mt-8 text-lg font-semibold text-[#c9a227] scroll-mt-24">{children}</h2>;
},
```

**Edit-Button pro Sektion (D-14 — Resolution: bestehender Wizard-Edit-Pfad):**
```typescript
// PenLine-Icon nur bei paid===true sichtbar (UI-SPEC §"Antrag-Detail Lesemodus"):
{paid && (
  <Link
    href={`/antrag/${programm.id}/wizard?editAnswer=true`}
    className="opacity-0 group-hover:opacity-100 text-slate-500 hover:text-slate-200 transition"
    title="Antwort zurück und neu beantworten"
  >
    <PenLine className="h-3.5 w-3.5" />
  </Link>
)}
```

**1-Zeilen-Hinweis:** `MARKDOWN_COMPONENTS.h2` um Slug-Generation + Anker-ID erweitern; `PRINT_MARKDOWN_COMPONENTS` (Z. 56–116) bleibt **unverändert** (PDF braucht keine Anker).

---

### `components/Wizard/AntragSectionNav.tsx` (NEU)

**Analog:** Greenfield für IntersectionObserver-Pattern, aber **Cancel-Flag-Pattern** aus `CheckoutSuccessClient.tsx` übernehmen + Visual-Style aus `MyAntraegeClient.tsx`-Cards.

**Vorlage (RESEARCH.md §4.1):**
```typescript
"use client";
import { useEffect, useState } from "react";

interface Section { id: string; label: string; }

export function AntragSectionNav({ articleRef }: { articleRef: React.RefObject<HTMLElement> }) {
  const [sections, setSections] = useState<Section[]>([]);
  const [activeId, setActiveId] = useState<string | null>(null);

  useEffect(() => {
    if (!articleRef.current) return;
    const h2s = Array.from(articleRef.current.querySelectorAll("h2[id]")) as HTMLElement[];
    setSections(h2s.map(h => ({ id: h.id, label: h.textContent ?? "" })));

    const observer = new IntersectionObserver(
      (entries) => {
        const visible = entries.filter(e => e.isIntersecting)
          .sort((a, b) => a.boundingClientRect.top - b.boundingClientRect.top);
        if (visible.length > 0) setActiveId(visible[0].target.id);
      },
      { rootMargin: "-100px 0px -50% 0px" }
    );
    h2s.forEach(h => observer.observe(h));
    return () => observer.disconnect();
  }, [articleRef]);

  if (sections.length === 0) return null;

  return (
    <nav className="hidden md:block sticky top-24 max-h-[calc(100vh-8rem)] overflow-y-auto space-y-1 text-xs">
      {sections.map(s => (
        <a key={s.id} href={`#${s.id}`}
           className={s.id === activeId
             ? "block border-l-2 border-orange-500 pl-2 text-orange-400"
             : "block pl-2 text-slate-400 hover:text-slate-200"}>
          {s.label}
        </a>
      ))}
    </nav>
  );
}
```

**1-Zeilen-Hinweis:** Greenfield-Component, übernimmt nur Klassen-Konventionen (`text-slate-400`/`text-orange-400`/`hidden md:block sticky`) aus bestehender Wizard-Visual-Sprache.

---

### `components/Wizard/MyAntraegeClient.tsx` (ändern — Empty-State Action-orientiert)

**Analog:** Selbst (218 Zeilen, Empty-State Z. 133–154).

**Pattern-Excerpt (heute):**
```typescript
if (sessions.length === 0) {
  return (
    <div className="rounded-xl border border-slate-700/50 bg-slate-800/30 p-10 text-center">
      <FileText className="mx-auto mb-3 h-10 w-10 text-slate-500" />
      <h3 className="mb-2 text-lg font-semibold text-slate-100">
        Noch keine Anträge
      </h3>
      <p className="mx-auto mb-6 max-w-md text-sm text-slate-400">
        Starte über die Förderprogramm-Übersicht einen neuen Antrag mit dem Wizard. ...
      </p>
      <Link href="/foerderprogramme" className="inline-flex items-center gap-2 rounded-lg bg-orange-500 px-5 py-2 text-sm font-semibold text-white hover:bg-orange-600">
        Förderprogramme ansehen
        <ArrowRight className="h-4 w-4" />
      </Link>
    </div>
  );
}
```

**Polish (D-05, UI-SPEC):**
- Heading: `Noch kein Antrag begonnen` (Singular)
- Body: `Schildere dein Anliegen und finde in Sekunden das passende Förderprogramm für deine Schule.`
- CTA-Label: `Anliegen schildern`
- CTA-Href: `/antrag/start` (statt `/foerderprogramme`)

**Mobile-Stack (UI-SPEC §Path 4b):** Z. 162–164 `flex flex-wrap items-center gap-4` → `flex flex-col sm:flex-row sm:flex-wrap sm:items-center gap-4`.

**1-Zeilen-Hinweis:** Reines Copy-+-Href-Polish im Empty-State-Branch + Mobile-Stack im Card-Layout — kein Strukturwechsel, kein neuer Component-Boundary.

---

### `components/Wizard/MatchResultList.tsx` (ändern — Empty-State + onReset)

**Analog:** Selbst (135 Zeilen, Empty-State Z. 36–50).

**Pattern-Excerpt (heute):**
```typescript
if (matches.length === 0) {
  return (
    <div className="rounded-xl border border-slate-700/50 bg-slate-800/40 p-8 text-center">
      <h3 className="mb-2 text-lg font-semibold text-slate-200">
        Keine passenden Programme gefunden
      </h3>
      <p className="mx-auto max-w-md text-sm text-slate-400">
        Versuch es noch einmal mit einer anderen Formulierung ...
      </p>
    </div>
  );
}
```

**Polish (D-05, UI-SPEC):**
- Icon `SearchX` aus `lucide-react`
- Heading: `Kein Programm passt zu diesem Anliegen`
- Body: `Das passiert, wenn das Anliegen zu allgemein oder sehr speziell ist.`
- 3 Reformulierungs-Tipps als `<ul>`
- CTA-Button `Anliegen neu formulieren` ruft neue Prop `onReset?: () => void`

**Mobile-Touch (UI-SPEC §Path 2):** Submit-Button (Z. 122–129) `py-2` → `sm:py-3`; Programm-Details-Link (Z. 115–121) `inline-flex` ohne Padding → `text-xs sm:text-sm py-2`.

**1-Zeilen-Hinweis:** Empty-State-Block komplett ersetzen, neue Prop `onReset` durchreichen; bestehende Ranking-Branch-Logik (Z. 52–133) bleibt unverändert.

---

### `components/Wizard/StartClient.tsx` (ändern — handleReset + onReset durchreichen)

**Analog:** Selbst (172 Zeilen).

**Pattern-Excerpt (Z. 32–40 + 155–169):**
```typescript
const [matchState, setMatchState] = useState<MatchState>(null);
const [lastInput, setLastInput] = useState<AnliegenValues | null>(null);
const [isSecondRound, setIsSecondRound] = useState(false);
// ...
{matchState !== null && (
  <div className="pt-2">
    {matchState.kind === "ranking" && (
      <MatchResultList matches={matchState.matches} onStartAntrag={startAntrag} />
    )}
    // ...
  </div>
)}
```

**Erweiterung (RESEARCH.md §4.2):**
```typescript
const handleReset = () => {
  setMatchState(null);
  setError(null);
  setIsSecondRound(false);
  // lastInput bleibt — User soll Form-Wert behalten
};
// ...
<MatchResultList matches={matchState.matches} onStartAntrag={startAntrag} onReset={handleReset} />
```

**1-Zeilen-Hinweis:** Reine Callback-Erweiterung, State-Machine-Reset bleibt minimal.

---

### `components/Wizard/AnliegenForm.tsx` (ändern — Mobile-Touch)

**Analog:** Selbst.

**Pattern-Excerpt (Z. 100–107):**
```typescript
<textarea
  value={anliegen}
  onChange={(e) => setAnliegen(e.target.value)}
  onKeyDown={handleKey}
  placeholder="..."
  rows={5}
  className="w-full rounded-lg border border-slate-600 bg-slate-900 p-3 text-slate-100 placeholder-slate-500 focus:border-orange-500 focus:outline-none"
/>
```

**Polish (UI-SPEC §Path 1):** `className="... w-full ... p-3 sm:min-h-[140px]"` — Tippkomfort auf Mobile.

**1-Zeilen-Hinweis:** Eine Klassen-Erweiterung, kein Logik-Change.

---

### `components/Wizard/QuestionCard.tsx` (ändern — Mobile-Touch Submit-Button)

**Analog:** Selbst (75 Zeilen).

**Pattern-Excerpt (Z. 64–71):**
```typescript
<button
  type="button"
  onClick={onSubmit}
  disabled={busy || !answer.trim()}
  className="rounded-lg bg-orange-500 px-5 py-2 text-sm font-semibold text-white transition hover:bg-orange-600 disabled:opacity-50"
>
  {busy ? "Sende…" : "Antworten"}
</button>
```

**Polish (UI-SPEC §Path 3):** `py-2` → `py-2 sm:py-3` (44 px Touch-Target).

**1-Zeilen-Hinweis:** Single-Class-Tweak.

---

### `lib/wizard/pipeline.ts` (ggf. nicht ändern)

**Befund:** Pipeline-Excerpt zeigt sauberes `emit`-Pattern (Z. 178: `const emit = (e: PipelineEvent) => onEvent?.(e);`). Stage-Heartbeat passiert vollständig im **Aufrufer** (`/api/wizard/generate/route.ts`), die Pipeline selbst muss nicht angefasst werden.

**Eventuell minimal:** `PipelineEvent.stage`-Type aus `pipeline.ts:148–157` nach `lib/wizard/types.ts` ziehen (RESEARCH.md Open Question #4) und hier nur re-exportieren — Single Source of Truth für Frontend + Backend.

**1-Zeilen-Hinweis:** Pipeline bleibt unverändert; Type-Move ist Hygiene-Refactor, nicht-blockierend.

---

### `lib/wizard/session.ts` (ggf. nicht ändern für 02.1)

**Befund:** Session-Layer ist heute schon vollständig für WIZ-04 — `getWizardSession`, `updateWizardSession`, `markSessionPaid` sind idempotent. Polling-Polling-Pattern braucht keine neuen Funktionen. **Keine Änderung in 02.1 nötig.**

**Pattern-Excerpt (Z. 147–176, `markSessionPaid`-Idempotenz als Beispiel des etablierten Stils):**
```typescript
export async function markSessionPaid(
  sessionToken: string,
  params: { stripeSessionId?: string; stripeCustomerEmail?: string; tier?: string }
): Promise<WizardSession> {
  const existing = await getWizardSession(sessionToken);
  if (!existing) throw new Error(`Session ${sessionToken} nicht gefunden`);
  if (existing.paidToken) return existing;  // ← Idempotenz: zweiter Webhook-Call ist no-op
  // ...
}
```

**1-Zeilen-Hinweis:** Antizipierten Plan-Eintrag „ÄNDERN: lib/wizard/session.ts" **streichen** — Resume-Pfad nutzt nur `getWizardSession`, das bereits existiert.

---

### `scripts/smoke-stripe-webhook.ts` (NEU)

**Analog:** `scripts/smoke-pipeline-with-extractor.ts` (220 Zeilen) für DB-Lookup + ENV-Loading; `scripts/smoke-llm.ts` (49 Zeilen) für kompaktes Skript-Skelett.

**Pattern-Excerpt aus `smoke-llm.ts`:**
```typescript
/**
 * Smoke-Test fuer lib/wizard/llm.ts — verifiziert den aktiven Provider.
 * Nutzung: `npx tsx --env-file=.env.local scripts/smoke-llm.ts`
 */
import { generateJson, generateText, MODEL_INTERVIEW, MODEL_PIPELINE } from "../lib/wizard/llm";

async function main() {
  // ... numbered logs `[1/2]`, `[2/2]`, OK-Meldung am Ende
}

main().catch((err) => {
  console.error("SMOKE FAIL:", err);
  process.exit(1);
});
```

**Pattern-Excerpt aus `smoke-pipeline-with-extractor.ts` Z. 50–60 (env+pg):**
```typescript
async function loadSession(): Promise<DbAntragData> {
  const env = await readFile(resolve(REPO, ".env.local"), "utf-8");
  const url = env.match(/DATABASE_URL\s*=\s*"?([^"\n]+)"?/)?.[1];
  if (!url) throw new Error("DATABASE_URL fehlt");
  const client = new pg.Client(url);
  await client.connect();
  const res = await client.query("SELECT antrag_data FROM ki_antraege WHERE session_token = $1", [SESSION_TOKEN]);
  await client.end();
  if (!res.rows[0]) throw new Error(`Session ${SESSION_TOKEN} nicht gefunden`);
  return res.rows[0].antrag_data as DbAntragData;
}
```

**Webhook-Smoke-Strategie (RESEARCH.md §1.4):** `stripe.webhooks.generateTestHeaderString` lokal nutzen, signierte Test-Payload via `fetch` an `http://localhost:3101/api/stripe/webhook` POSTen, Response prüfen.

**1-Zeilen-Hinweis:** Skript-Skelett aus `smoke-llm.ts` (Header-Kommentar, `main` + catch), Stripe-SDK-Helper `generateTestHeaderString` für Payload-Signatur, kein DB-Lookup nötig.

---

### `__tests__/app/api/stripe/webhook.test.ts` (NEU)

**Analog:** Es gibt **keine bestehenden API-Route-Tests im Repo** (alle `__tests__`-Dateien sind Pure-Functions oder RTL-Components). Greenfield-Pattern, aber Stil aus `__tests__/lib/wizard/facts-extractor.test.ts` (kompakte `describe`/`it`-Struktur, deutsche Test-Beschreibungen).

**Pattern-Excerpt aus `facts-extractor.test.ts`:**
```typescript
import { mergeFacts } from "@/lib/wizard/facts-extractor";
import type { WizardFacts } from "@/lib/wizard/types";

describe("mergeFacts", () => {
  it("gibt base zurueck, wenn Update undefined ist", () => {
    const base: WizardFacts = { schule: { name: "Borsigwalder Grundschule" } };
    expect(mergeFacts(base, undefined)).toEqual(base);
  });

  it("merged neue Felder in bestehenden Schule-Slot, ohne Bestand zu loeschen", () => {
    // ...
  });
});
```

**Test-Plan für `webhook.test.ts`:**
- Valide Signatur → 200, `markSessionPaid` aufgerufen
- Invalide Signatur → 400
- Fehlende `STRIPE_WEBHOOK_SECRET` → 503
- `checkout.session.completed` ohne `metadata.wizard_session_token` → break, kein DB-Call
- `payment_status !== "paid"` → break

**Mock-Strategie:** `jest.mock("@/lib/stripe/client")` für `getStripe`, `jest.mock("@/lib/wizard/session")` für `markSessionPaid`. Kein echter Stripe-SDK-Call.

**1-Zeilen-Hinweis:** Greenfield für API-Route-Tests, aber Stil + Test-Beschreibungs-Sprache aus bestehenden `__tests__/lib/wizard/*.test.ts` übernehmen.

---

### `__tests__/components/Wizard/WizardShell.test.tsx` (NEU)

**Analog:** `__tests__/components/MatchResultList.test.tsx` für RTL-Pattern.

**Pattern-Excerpt aus `MatchResultList.test.tsx`:**
```typescript
import { render, screen, fireEvent } from '@testing-library/react';
import { MatchResultList, type MatchEntry } from '@/components/Wizard/MatchResultList';

const SAMPLE: MatchEntry = { /* ... */ };

describe('MatchResultList — Ranking-Branch', () => {
  it("rendert passt_weil-Block mit Label 'Passt, weil:' — D-10", () => {
    render(<MatchResultList matches={[SAMPLE]} onStartAntrag={() => {}} />);
    expect(screen.getByText(/Passt, weil:/)).toBeInTheDocument();
  });
});
```

**Test-Plan für `WizardShell.test.tsx`:**
- Mount mit `phase=interviewing` → letzte Frage wird gerendert (loadSession-Pfad)
- Mount mit `phase=generating` → KEIN POST `/api/wizard/generate`, NUR Polling-Tick gegen `/api/wizard/[token]`
- Polling-Response mit `phase=complete` → `<AntragResult>` rendert
- Polling-Response mit `phase=failed` → Fehler-Block + Retry-CTA (D-15)
- StrictMode-Doppel-Mount → nur EIN aktiver Polling-Loop (cancelled-Flag-Test)

**Mock-Strategie:** `fetch`-Mock (z. B. `jest.spyOn(global, "fetch")`) — MSW ist nicht installiert (RESEARCH.md schlägt MSW vor, aber kein Dependency-Add in 02.1).

**1-Zeilen-Hinweis:** RTL-Pattern aus `MatchResultList.test.tsx`, fetch-Mock per `jest.spyOn`, Stage-Polling-Mock mit Sequence von Responses.

---

### `__tests__/components/Wizard/AntragSectionNav.test.tsx` (NEU)

**Analog:** `__tests__/components/MatchResultList.test.tsx`.

**Test-Plan:**
- Slug-Generation: `"Vorhaben und Anliegen"` → `"vorhaben-und-anliegen"`
- Umlaut-Mapping: `"Maßnahmen und Förderung"` → `"massnahmen-und-foerderung"`
- IntersectionObserver-Mock (jsdom hat das nicht nativ — `global.IntersectionObserver = ...` mock)
- `sections.length === 0` → returns null (kein DOM)
- Active-State wechselt bei IntersectionObserver-Entry

**1-Zeilen-Hinweis:** IntersectionObserver-Mock-Setup ist Greenfield, aber Skip-able, wenn jsdom-Limitation der einzige Blocker bleibt.

---

### `__tests__/lib/wizard/session.test.ts` & `pipeline-events.test.ts` (NEU, unter Vorbehalt)

**Analog:** `__tests__/lib/wizard/outline-fallback.test.ts` (Pure-Function-Test).

**Befund:** `session.ts` braucht eine echte DB für End-to-End — RESEARCH.md §Validation hat das als „Test-DB-Setup nötig" markiert. **Empfehlung:** session-Tests in 02.1 nur sehr leichtgewichtig (Mock-`query`-Funktion testet die SQL-Calls strukturell), DB-Integration-Tests nach Phase 6.

`pipeline-events.test.ts` testet `runPipeline` mit Mock-LLM-Wrapper (`lib/wizard/llm.ts:generateText` + `generateJson` mocken) und prüft, dass `onEvent` für jede Stage in korrekter Reihenfolge aufgerufen wird. Das ist Pure-Function-artig, geht ohne DB.

**1-Zeilen-Hinweis:** Pipeline-Events-Test ist machbar mit LLM-Mock; Session-Test bleibt strukturell (Mock-`query`), nicht End-to-End.

---

### `.planning/uat/UAT-PLAN-TEMPLATE.md`, `UAT-BEFUNDE-TEMPLATE.md`, `PILOTEN.md`, `UAT-ANSCHREIBEN.md` (4× NEU)

**Analog:** Vollständige Vorlagen sind in **RESEARCH.md §5.2–5.5** enthalten — Plan-Phase übernimmt sie 1:1. Format-Quelle des Befunde-Trackers ist `~/.claude/projects/-home-kolja/memory/edufunds-uat-pipeline-befunde-2026-04-28.md`-Pattern (Bug-Memo-Schema mit `### Bug #N — kurze Beschreibung [Status-Badge]` + 5 Bullet-Felder Vorher/Strukturelle Ursache/Fix-Richtung/Reproducer/Verifikation).

**Pattern-Excerpt RESEARCH.md §5.1 (Bug-Memo-Format):**
```markdown
### Bug #N — {kurze Beschreibung} {Status-Badge: rot offen / gelb in Arbeit / gruen gefixt (Commit hash)}
- **Vorher (UAT-Stand):** {konkretes Symptom mit Zahlen, Quellen}
- **Strukturelle Ursache:** {warum tritt es auf — Pipeline-Stage / Frontend / DB-State / Eingabe-Profil}
- **Fix-Richtung:** {konkrete Codeänderung, nicht abstrakt}
- **Reproducer:** {scripts/X.ts gegen Session-Token Y, oder UAT-Schul-Kontext}
- **Verifikation:** {wie nachgewiesen — A/B-Vergleich mit Zahlen, oder Smoke-Run-Output}
```

**1-Zeilen-Hinweis:** Templates 1:1 aus RESEARCH.md übernehmen, Sprache Deutsch mit Umlauten + ß (Kolja-Konvention aus globalem CLAUDE.md). Kein selbst-recherchierter Pilot-Inhalt — `PILOTEN.md` bleibt leer mit Spalten-Skelett, Kolja füllt.

---

## Shared Patterns

### S-1: Polling-mit-Cancel-Flag (für WIZ-04 + bestehende Stripe-Confirm-Flow)
**Source:** `components/Wizard/CheckoutSuccessClient.tsx` Z. 25–79
**Apply to:** `WizardShell.tsx` (neue Polling-useEffect für `phase=generating`)
**Excerpt:** Siehe Pattern Assignment für `WizardShell.tsx` oben — `cancelled`-Flag in Cleanup, `consecutiveFailures`-Counter, Re-Schedule via `setTimeout`.

### S-2: Stripe-SDK-Singleton + 503-bei-Fehlkonfiguration
**Source:** `lib/stripe/client.ts` (komplett, 23 Zeilen)
**Apply to:** Bereits in `app/api/stripe/webhook/route.ts` und `app/api/wizard/checkout/route.ts` korrekt umgesetzt — kein Polish nötig, nur dokumentieren.
**Excerpt:**
```typescript
let cached: Stripe | null = null;

export function getStripe(): Stripe {
  if (cached) return cached;
  const key = process.env.STRIPE_SECRET_KEY;
  if (!key) throw new Error("STRIPE_SECRET_KEY fehlt");
  cached = new Stripe(key, { typescript: true });
  return cached;
}

export function stripeConfigured(): boolean {
  return !!process.env.STRIPE_SECRET_KEY;
}
```

### S-3: Idempotenz via DB-Check (Webhook-Race-Schutz)
**Source:** `lib/wizard/session.ts:151–153` (`markSessionPaid`)
**Apply to:** Begründet, dass `app/api/stripe/webhook/route.ts` bei doppeltem `checkout.session.completed`-Event keine neuen `paid_token` erzeugt.
**Excerpt:**
```typescript
const existing = await getWizardSession(sessionToken);
if (!existing) throw new Error(`Session ${sessionToken} nicht gefunden`);
if (existing.paidToken) return existing;  // ← idempotent: schon paid → no-op
```

### S-4: JSONB-Patch via `jsonb_set` (vermeidet Migration)
**Source:** `lib/wizard/session.ts:77–95` (`updateWizardSession` zeigt das `antrag_data`-JSONB-Pattern, jedoch mit Full-Replace)
**Apply to:** `app/api/wizard/generate/route.ts` (Stage-Heartbeat — Partial-Patch statt Full-Replace, weil parallel zur Pipeline geschrieben).
**Excerpt:**
```typescript
await query(
  `UPDATE ki_antraege
     SET antrag_data = jsonb_set(
       jsonb_set(COALESCE(antrag_data, '{}'::jsonb), '{generation,stage}', to_jsonb($1::text)),
       '{generation,stageAt}', to_jsonb($2::text)
     )
   WHERE session_token = $3`,
  [event.stage, new Date().toISOString(), sessionToken]
);
```

### S-5: Smoke-Skript-Skelett (Header-Kommentar + main+catch)
**Source:** `scripts/smoke-llm.ts` (49 Zeilen)
**Apply to:** Alle neuen `scripts/smoke-*.ts`-Files in 02.1.
**Excerpt:**
```typescript
/**
 * Smoke-Test fuer {Bereich}.
 * Nutzung: `npx tsx --env-file=.env.local scripts/smoke-{name}.ts`
 */
import { /* ... */ } from "../lib/...";

async function main() {
  console.log("[1/N] {Schritt-Beschreibung} ...");
  // ...
  console.log("OK — {Erfolgs-Statement}.");
}

main().catch((err) => {
  console.error("SMOKE FAIL:", err);
  process.exit(1);
});
```

### S-6: RTL-Test-Stil (deutsche Test-Beschreibungen, Decision-IDs)
**Source:** `__tests__/components/MatchResultList.test.tsx` Z. 26–78
**Apply to:** Alle neuen RTL-Tests in 02.1.
**Excerpt:**
```typescript
describe('Component — Branch-Name', () => {
  it("rendert {Element} mit {Eigenschaft} — {Decision-ID}", () => {
    render(<Component prop={value} />);
    expect(screen.getByText(/Pattern/)).toBeInTheDocument();
  });
});
```

### S-7: Empty-State-Visual-Sprache (Wizard-Layer)
**Source:** `MyAntraegeClient.tsx` Z. 133–154
**Apply to:** `MatchResultList.tsx`-Empty-State (D-05) — gleiches Visual-Setup, nur Inhalt anders.
**Excerpt:** `rounded-xl border border-slate-700/50 bg-slate-800/X p-{8|10} text-center` mit zentrierter Lucide-Icon (h-10 w-10 text-slate-500) + h3 (text-lg font-semibold text-slate-100) + p (text-sm text-slate-400) + Primary-CTA (bg-orange-500 hover:bg-orange-600).

### S-8: `lucide-react`-Icon-Import via Jest-Whitelist
**Source:** `jest.config.js` Z. 24–27 + Z. 49–55
**Apply to:** Alle neuen Tests die Components mit Lucide-Icons rendern (`AntragSectionNav.test.tsx`, `WizardShell.test.tsx`).
**Wichtig:** `transformIgnorePatterns` whitelistet `lucide-react` — neue Tests profitieren automatisch, **kein Setup nötig**.

---

## No Analog Found

| Datei | Rolle | Data Flow | Grund |
|-------|-------|-----------|-------|
| `__tests__/app/api/stripe/webhook.test.ts` | Test (API-Route) | n/a | Kein bestehender API-Route-Test im Repo. Plan-Phase greift auf Stil aus `facts-extractor.test.ts` zurück + ergänzt mit Stripe-SDK-Mock-Pattern. |
| `__tests__/components/Wizard/AntragSectionNav.test.tsx` | Test (IntersectionObserver) | n/a | jsdom hat keinen IntersectionObserver — Plan-Phase muss globalen Mock setup-en. Vorbild: keiner im Repo. |
| `app/api/wizard/[sessionToken]/progress/route.ts` | API-Route (Polling-Endpoint) | n/a | **Plan-Phase soll diese Datei NICHT bauen** — `app/api/wizard/[token]/route.ts` reicht (RESEARCH.md §3.3 + §Don't Hand-Roll). Antizipations-Liste war hier zu großzügig. |

---

## Pattern-Discovery-Hinweise für Planner

1. **`app/api/stripe/webhook/route.ts` ist nicht Greenfield.** Plan-Phase muss zuerst `cat` der existierenden Datei machen (83 Zeilen), dann nur erweitern. Keine "neue Datei anlegen"-Action.

2. **`app/api/wizard/[sessionToken]/progress/` braucht keinen neuen Endpoint.** Bestehender `[token]/route.ts` reicht. Plan-Phase die Antizipation entsprechend reduzieren.

3. **`lib/stripe/client.ts` existiert bereits** (Singleton-Pattern, 23 Zeilen). Plan-Phase ist hier "Verify + leave alone".

4. **`lib/wizard/session.ts` braucht keine 02.1-Änderungen.** Polling-Pfad nutzt nur `getWizardSession`. Plan-Phase die Antizipation streichen.

5. **`lib/wizard/pipeline.ts` braucht max. einen Type-Re-Export-Refactor**, kein Funktions-Change. `onEvent` ist heute optional und im Aufrufer (`generate/route.ts`) zu setzen.

6. **STAGES-Key-Drift (`sections` vs. `section`)** muss Plan-Phase erkennen und EINEN Pfad wählen — Empfehlung: `GeneratingProgress.tsx` STAGES-Array auf `"section"` umbenennen.

7. **D-15 Failed-UI** rendert besser in `WizardShell` (Phase-Dispatch Z. 442–460) als in `GeneratingProgress` — letztere bleibt für laufende Pipeline.

8. **D-14 Edit-Button** verlinkt nur (Wizard-Edit-Pfad), kein neuer Endpoint nötig (RESEARCH.md §4.1 hat das via D-14 in Discuss-Phase resolved).

9. **UAT-Templates** sind komplett-textuell in RESEARCH.md §5 — Plan-Phase muss nur 4 `.md`-Dateien anlegen mit dem dort fertigen Inhalt.

10. **Tests sind Wave 0**, nicht Wave 1 — RESEARCH.md §Validation Architecture.

---

## Metadata

**Analog search scope:**
- `app/api/wizard/`, `app/api/stripe/`, `app/api/match/`
- `components/Wizard/` (alle 18 Components)
- `lib/wizard/`, `lib/stripe/`, `lib/db.ts`
- `__tests__/lib/wizard/`, `__tests__/components/`
- `scripts/smoke-*.ts` (5 Files)
- `db/migrations/` (002 + 003)
- `jest.config.js`, `tailwind.config.js`, `package.json`

**Files scanned (Read-only):** 22

**Pattern extraction date:** 2026-05-06

---

## PATTERN MAPPING COMPLETE

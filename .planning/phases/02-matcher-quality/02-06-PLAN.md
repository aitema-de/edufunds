---
phase: 02-matcher-quality
plan: 06
type: execute
wave: 2
depends_on: [04, 05]
files_modified:
  - data/eval/BASELINE.md
  - data/eval/reports/
  - data/eval/snapshots/
autonomous: true
gap_closure: true
requirements: [MATCH-02, MATCH-03]
tags: [eval-rerun, threshold-gate, baseline]

must_haves:
  truths:
    - "Live-Eval-Re-Run nach Plan 02-04 Prompt-Tuning + Plan 02-05 Hardening produziert neuen Report mit allen 4 D-16-Targets PASS — Threshold-Gate exit 0"
    - "Recall@3 >= 0.42 (Hauptmetrik) im neuen Report"
    - "Clarif-Precision >= 80% (mind. 7 von 8 erwarteten Klärungen werden getriggert) im neuen Report"
    - "Off-Target-Rate < 5% bleibt erhalten (vorher 0.0%, kein Regress) im neuen Report"
    - "Clarif-FalschPos <= 10% bleibt erhalten (vorher 9.5%, knapp PASS, kein Regress) im neuen Report"
    - "BASELINE.md hat einen neuen Phase-2.1-Eintrag oben (append-only über dem 2026-05-04-Phase-2-Eintrag) mit allen 4 D-16-Werten + Per-Kategorie-Breakdown"
  artifacts:
    - path: "data/eval/BASELINE.md"
      provides: "Phase-2.1-Tuning-Eintrag mit grünem Threshold-Gate"
      contains: "Phase-2.1-Tuning"
    - path: "data/eval/reports/<datum>.json"
      provides: "Maschinen-lesbarer Eval-Report mit aggregate.recallAtThreeMean >= 0.42"
    - path: "data/eval/reports/<datum>.md"
      provides: "Markdown-Twin mit den 4 D-16-Zahlen"
  key_links:
    - from: "data/eval/BASELINE.md"
      to: "data/eval/reports/<datum>.json + .md"
      via: "Pfad-Referenz im Reports-Block des Phase-2.1-Eintrags"
      pattern: "data/eval/reports"
    - from: "scripts/eval-matcher.ts Threshold-Gate"
      to: "process exit code 0"
      via: "alle 4 D-16-Targets erfüllt"
      pattern: "GATE PASSED"
---

<objective>
Verifizieren dass Plan 02-04 (Prompt-Tuning) das Threshold-Gate (D-16/D-17) auf grün bringt: einen neuen Live-Eval-Lauf gegen den Phase-2-Korpus (n=29, unverändert) durchführen, BASELINE.md mit Phase-2.1-Tuning-Eintrag append-only erweitern, neue Reports + Snapshots commiten.

Purpose: Phase-2-Goal (D-16 Threshold-Gate als Pflicht-Vorabcheck mit messbarer Verbesserung) wird inhaltlich erreicht — nicht nur die Mechanik (Phase 2 erreicht) sondern auch die Targets (Phase 2.1 schließt die Lücke).

Output: Neuer Eval-Report mit GATE PASSED + aktualisierte BASELINE.md. Falls Eval-Run fehlschlägt: konkrete Diagnose welche Targets noch verfehlt werden, mit Empfehlung für weiteren Tuning-Iterations-Cycle.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-matcher-quality/02-CONTEXT.md
@.planning/phases/02-matcher-quality/02-VERIFICATION.md
@.planning/phases/02-matcher-quality/02-03-SUMMARY.md
@data/eval/BASELINE.md
@CLAUDE.md

<eval_runbook>
<!-- Reproduzierbarer Run gemäß Plan 02-03 SUMMARY + BASELINE.md Run-Befehl -->

```bash
# Pre-Check: .env.local muss DEEPSEEK_API_KEY enthalten (für lib/wizard/llm.ts)
test -f .env.local && grep -q "DEEPSEEK_API_KEY=" .env.local && echo "ENV ok" || echo "MISSING DEEPSEEK_API_KEY"

# Pre-Check: matcher.ts muss Plan-02-04-Tuning enthalten
grep -c "domain-spezifisch hat IMMER Vorrang" lib/wizard/matcher.ts  # Erwartet: 1

# Pre-Check: eval-matcher.ts muss Plan-02-05-Hardening enthalten
grep -c "WR-04" scripts/eval-matcher.ts  # Erwartet: >=1

# Live-Eval-Run (~3 min, 29 Korpus-Einträge × 1 LLM-Call)
npm run eval:matcher -- --snapshot --md-summary

# Exit-Code prüfen: 0 = Gate PASSED, 1 = Gate FAILED
echo "Exit-Code: $?"
```
</eval_runbook>

<baseline_template>
<!-- Format des Phase-2.1-Eintrags in BASELINE.md (append-only ÜBER dem Phase-2-Eintrag) -->

```markdown
## YYYY-MM-DD — Phase-2.1-Tuning (Korpus v2 unverändert, n=29)

- **Matcher-Commit:** `<sha>` (HEAD nach Plan 02-04 + 02-05 Merges)
- **Korpus-Version:** v2 (UNVERÄNDERT seit Phase 2 — Tuning-Hebel saßen am Prompt, nicht am Korpus)
- **Tuning-Quelle:** Plan 02-04 (MATCHER_SYSTEM-Prompt-Verschärfung: Drift-Verbote, Slot-Heuristik-Schärfung, Recall-Negativbeispiele, MATCHER_MAX_TOKENS 400→600) + Plan 02-05 (Eval-Skript-Hardening WR-03/WR-04, Frontend-Hardening WR-01/WR-02). Keine Korpus-Änderungen.

### Threshold-Gate (D-16/D-17 PR-Gate)

| Metrik | Phase 2 (vorher) | Phase 2.1 (nachher) | D-17-Target | Status |
|--------|------------------|---------------------|-------------|--------|
| **Recall@3** (Mittelwert non-edge) | 0.342 | **<wert>** | ≥ 0.42 | <PASS/FAIL> |
| **Off-Target-Rate** | 0.0 % | **<wert>** | < 5 % | <PASS/FAIL> |
| **Clarif-Precision** | 62.5 % (5/8 hits) | **<wert>** (<n>/8 hits) | ≥ 80 % | <PASS/FAIL> |
| **Clarif-FalschPos** | 9.5 % (2/21) | **<wert>** | ≤ 10 % | <PASS/FAIL> |
| **Slot-Coverage** (Diagnose) | 90.0 % | **<wert>** | — | <gut/okay/schwach> |

### Per-Kategorie

- **kurz:** Recall <wert> / Off-Target <wert>% (n=6) — vorher 0.222 / 0.0%
- **ausfuehrlich:** Recall <wert> / Off-Target <wert>% (n=8) — vorher 0.417 / 0.0%
- **vag (non-edge):** Recall <wert> / Off-Target <wert>% (n=5) — vorher 0.367 / 0.0%

### Latenz / Kosten

- **Latenz/Eintrag:** <wert>s avg (Phase 2: 2.81s) — <Kommentar>
- **Gesamtkosten:** <wert> € (Phase 2: 0,012 €)

### Reports

- JSON: `data/eval/reports/<datum>.json`
- Markdown-Twin: `data/eval/reports/<datum>.md`
- Snapshot: `data/eval/snapshots/<datum>/`

### Run-Befehl

```bash
npm run eval:matcher -- --snapshot --md-summary
```

<Threshold-Gate-Resultat: GATE PASSED oder GATE FAILED + welche Targets verfehlt>

---

## 2026-05-04 — Phase-2-Baseline (...) <-- Bisheriger Phase-2-Eintrag bleibt unverändert
```
</baseline_template>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Live-Eval-Re-Run + BASELINE.md Phase-2.1-Eintrag</name>
  <read_first>
    - data/eval/BASELINE.md (Format der Phase-1- und Phase-2-Einträge studieren)
    - data/eval/matcher-korpus.json (verifizieren: weiterhin 29 Einträge, 8 mit expected_clarification=true, 2 Anti-Beispiele mit expected_clarification=false)
    - .planning/phases/02-matcher-quality/02-03-SUMMARY.md (Plan-02-03-Pattern für BASELINE.md-Update)
    - lib/wizard/matcher.ts (verifizieren: Plan-02-04-Tuning ist vorhanden)
    - scripts/eval-matcher.ts (verifizieren: Plan-02-05-Hardening ist vorhanden)
  </read_first>
  <files>data/eval/BASELINE.md, data/eval/reports/, data/eval/snapshots/</files>
  <action>
**Pre-Flight (verifizieren dass Plan 02-04 + 02-05 gemerged sind):**

```bash
# Plan-02-04-Marker
grep -q "domain-spezifisch hat IMMER Vorrang" lib/wizard/matcher.ts || echo "PROBLEM: Plan 02-04 nicht gemerged"
grep -q "MATCHER_MAX_TOKENS = 600" lib/wizard/matcher.ts || echo "PROBLEM: MATCHER_MAX_TOKENS nicht erhöht"

# Plan-02-05-Marker
grep -q "WR-04" scripts/eval-matcher.ts || echo "PROBLEM: Plan 02-05 nicht gemerged"
grep -q "Unerwartetes Antwortformat" components/Wizard/StartClient.tsx || echo "PROBLEM: Plan 02-05 Frontend nicht gemerged"

# Korpus unverändert (n=29, 8 expected_clarification=true)
test "$(jq 'length' data/eval/matcher-korpus.json)" -eq 29 || echo "PROBLEM: Korpus-Größe geändert"
test "$(jq '[.[] | select(.expected_clarification == true)] | length' data/eval/matcher-korpus.json)" -eq 8 || echo "PROBLEM: Anzahl expected_clarification geändert"

# .env.local mit DEEPSEEK_API_KEY
test -f .env.local && grep -q "DEEPSEEK_API_KEY=" .env.local || echo "PROBLEM: DEEPSEEK_API_KEY fehlt"
```

Bei "PROBLEM"-Output: STOPPEN und an User melden, NICHT mit Re-Eval fortfahren.

**Live-Eval-Run:**

```bash
# 29 Einträge × ~3s = ~90s. Mit Snapshot + Markdown.
npm run eval:matcher -- --snapshot --md-summary
EVAL_EXIT_CODE=$?
```

Output landet in `data/eval/reports/<YYYY-MM-DD-HH-MM-SS>.json` + `.md` + `data/eval/snapshots/<datum>/`.

**Auswertung:**

1. Markdown-Report-Pfad anhand neuestem File ermitteln:
   ```bash
   LATEST_REPORT=$(ls -t data/eval/reports/*.md 2>/dev/null | head -1)
   cat "$LATEST_REPORT"
   ```

2. Aus dem Report die 4 D-16-Werte + Per-Kategorie-Werte extrahieren.

3. **Wenn EVAL_EXIT_CODE == 0** (alle 4 Targets PASS):
   - BASELINE.md erweitern: Phase-2.1-Eintrag oben anhängen (über dem 2026-05-04-Phase-2-Eintrag), Format wie im baseline_template oben.
   - Tuning-Quelle dokumentieren: Plan 02-04 + 02-05.
   - Status-Spalte: alle 4 PASS, Slot-Coverage Bewertung.
   - Reports + Snapshot-Pfade einsetzen.
   - Files committen (BASELINE.md, neue Reports, neue Snapshots).

4. **Wenn EVAL_EXIT_CODE != 0** (mind. 1 Target FAILED):
   - BASELINE.md TROTZDEM updaten (ehrlicher Ist-Stand, kein Schönfärben — gleiches Pattern wie Plan 02-03).
   - In der Phase-2.1-Eintrag-Status-Spalte alle 4 Werte ehrlich (PASS/FAIL).
   - Im Threshold-Gate-Resultat-Block die verfehlten Targets dokumentieren.
   - Files committen.
   - **Diagnostischer Block am Ende des Phase-2.1-Eintrags hinzufügen** mit:
     - Welche Targets verfehlt (Wert vs. Soll)
     - Welche Korpus-Einträge laut neuem Report die Misses verursachen (jq-Auszug)
     - Empfehlung: weiterer Tuning-Iterations-Cycle erforderlich (separates Plan 02-08 / Phase-2.2 oder weiteres Iterieren am MATCHER_SYSTEM-Prompt)

**Files committen (mit Force-Add für Reports + Snapshots — D-17-Pattern aus Phase 1/2):**

```bash
git add data/eval/BASELINE.md
git add -f data/eval/reports/<neue-datei>.json data/eval/reports/<neue-datei>.md
git add -f data/eval/snapshots/<neuer-ordner>/
git commit -m "feat(02-06): Phase-2.1-Eval-Re-Run nach Prompt-Tuning"
```

**Was NICHT geändert werden darf:**
- `data/eval/matcher-korpus.json` — UNVERÄNDERT (Tuning sitzt am Prompt, nicht am Korpus)
- `lib/wizard/matcher.ts` — bereits in Plan 02-04 modifiziert, hier nur lesend
- `scripts/eval-matcher.ts` — bereits in Plan 02-05 modifiziert, hier nur lesend
- Phase-1- und Phase-2-Einträge in BASELINE.md — bleiben unverändert (append-only-Pattern)
  </action>
  <acceptance_criteria>
    - `ls -t data/eval/reports/*.json | head -1` zeigt einen Report mit Datum >= heute
    - `ls -t data/eval/snapshots/ | head -1` zeigt einen Snapshot-Ordner mit Datum >= heute
    - `grep -c "Phase-2.1-Tuning" data/eval/BASELINE.md` == 1
    - `grep -c "Phase-2-Baseline" data/eval/BASELINE.md` == 1 (alter Eintrag bleibt)
    - `grep -c "Phase-1-Baseline" data/eval/BASELINE.md` == 1 (Phase-1-Eintrag bleibt)
    - `head -1 data/eval/BASELINE.md` enthält "# Matcher-Eval Baseline" (Header bleibt)
    - Phase-2.1-Eintrag steht VOR (oben) dem Phase-2-Eintrag (`grep -n "Phase-2.1\\|Phase-2-Baseline\\|Phase-1-Baseline" data/eval/BASELINE.md` — Zeilen-Nummer Phase-2.1 < Phase-2 < Phase-1)
    - Phase-2.1-Eintrag enthält alle 4 D-16-Metriken (`grep -c "Recall@3\\|Off-Target-Rate\\|Clarif-Precision\\|Clarif-FalschPos" data/eval/BASELINE.md` >= 4 nur im Phase-2.1-Block — verifizieren über Zeilenbereich)
    - Wenn EVAL_EXIT_CODE == 0: `grep "GATE PASSED" data/eval/BASELINE.md` >= 1 oder Status-Spalten-Werte alle "PASS"
    - Wenn EVAL_EXIT_CODE != 0: BASELINE-Eintrag dokumentiert ehrlich + Diagnose-Block am Ende
    - matcher-korpus.json unverändert: `test "$(jq 'length' data/eval/matcher-korpus.json)" -eq 29`
  </acceptance_criteria>
  <verify>
    <automated>cd /home/kolja/edufunds-app && jq -r '.aggregate | "Recall@3=\(.recallAtThreeMean) Off-Target=\(.offTargetRate) Clarif-Precision=\(.clarifPrecision) Clarif-FalschPos=\(.clarifFalschPosRate)"' "$(ls -t data/eval/reports/*.json | head -1)" 2>&1 && grep -c "Phase-2.1-Tuning" data/eval/BASELINE.md</automated>
  </verify>
  <done>
    - Live-Eval-Run gelaufen (kann auch FAIL liefern, beides dokumentiert)
    - BASELINE.md hat Phase-2.1-Eintrag oben (append-only)
    - Reports + Snapshots committet (force-add wegen .gitignore-Regel aus Phase 1)
    - Bei GATE PASSED: Phase-2-Goal vollständig erreicht
    - Bei GATE FAILED: Diagnose-Block + Empfehlung für weiteren Tuning-Cycle dokumentiert
  </done>
</task>

</tasks>

<verification>
- 11 grep-/file-verifizierbare Acceptance-Kriterien
- Eval-Report-Existenz: `ls data/eval/reports/<neueste>` + `ls data/eval/snapshots/<neuester>`
- BASELINE.md-Update: append-only, Phase-2.1 über Phase-2 über Phase-1
- Threshold-Gate-Resultat: ehrlich dokumentiert (PASS oder FAIL mit Diagnose)
</verification>

<success_criteria>
- Neuer Eval-Report exists und enthält die 4 D-16-Metriken
- BASELINE.md hat Phase-2.1-Eintrag mit allen 4 Werten + Per-Kategorie + Latenz/Kosten
- Wenn GATE PASSED: Phase-2-Goal MATCH-02 + MATCH-03 sind erreicht (Recall + Clarif-Precision auf Soll)
- Wenn GATE FAILED: dokumentierter Diagnose-Block + Empfehlung für weitere Tuning-Plans (z.B. Plan 02-08 / Phase 2.2)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/02-matcher-quality/02-06-SUMMARY.md` erstellen mit:
- Eval-Run-Resultat: alle 4 D-16-Werte vorher (Phase 2) vs. nachher (Phase 2.1)
- GATE PASSED oder GATE FAILED inkl. konkrete Targets
- Per-Kategorie-Delta (kurz/ausfuehrlich/vag)
- Bei FAIL: Diagnose welche Korpus-Einträge weiterhin Misses (mit ev-XXX-IDs)
- Pfade zu neuen Report-Files + Snapshot-Ordner
- Commit-SHA(s)
- Empfehlung für nächsten Schritt (Phase-Abschluss vs. weiterer Tuning-Cycle)
</output>

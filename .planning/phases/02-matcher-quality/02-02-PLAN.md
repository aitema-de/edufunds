---
phase: 02-matcher-quality
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/Wizard/MatchResultList.tsx
  - components/Wizard/ClarificationCard.tsx
  - components/Wizard/StartClient.tsx
  - __tests__/components/MatchResultList.test.tsx
autonomous: false
requirements: [MATCH-02, MATCH-03]
tags: [frontend, ui, react, lucide-react, tagged-union]

must_haves:
  truths:
    - "MatchResultList rendert pro Treffer einen gruenen `passt_weil`-Block (CheckCircle-Icon, Label `Passt, weil:`) und einen orangen `achtung_bei`-Block (AlertTriangle-Icon, Label `Achtung:`) — beide vor dem `Antrag starten`-Button."
    - "Der `achtung_bei`-Block wird nur gerendert, wenn der String nicht leer ist."
    - "components/Wizard/ClarificationCard.tsx existiert als eigene Komponente — rendert die Klaerungsfrage als h2, ein textarea mit aria-label, einen `Praezisieren`-Button (disabled wenn textarea leer) und einen Override-Link `Trotzdem mit aktueller Eingabe ranken`."
    - "StartClient.tsx haelt einen Tagged-Union-State `MatchState = { kind: 'ranking'; matches } | { kind: 'clarification'; question } | null` und dispatched in JSX entweder MatchResultList ODER ClarificationCard."
    - "StartClient.tsx ruft beim Praezisieren-Submit `/api/match` mit `forceRanking: true` und `previousAnliegen: lastInput.anliegen` auf — Praezisierung landet als neuer `anliegen`-Wert."
    - "Override-Link `Trotzdem ranken` sendet ebenfalls `forceRanking: true`, aber ohne neuen Praezisierungstext (`anliegen` bleibt der letzte Input)."
    - "isSecondRound-Guard verhindert Endlos-Klaerungs-Loop (wenn API trotz forceRanking erneut clarification liefert, rendert MatchResultList mit Hinweis-Banner statt erneuter Card)."
    - "Bestehende UI-Funktionalitaet (Empty-State bei matches.length===0 + kind===ranking, Score-Badges mit emerald/orange/slate-Farben, Antrag-starten-Flow via saveHandoff) bleibt unveraendert."
  artifacts:
    - path: "components/Wizard/MatchResultList.tsx"
      provides: "MatchResultList rendert nur den ranking-Branch — Empty-State + Top-3-Liste mit passt_weil/achtung_bei-Bloecken"
      contains: "passt_weil"
      exports: ["MatchResultList", "MatchEntry"]
    - path: "components/Wizard/ClarificationCard.tsx"
      provides: "Standalone-Komponente fuer kind=clarification — Frage, Praezisierungs-Form, Override-Link"
      contains: "Trotzdem mit aktueller Eingabe ranken"
      exports: ["ClarificationCard"]
    - path: "components/Wizard/StartClient.tsx"
      provides: "Tagged-Union-State + Dispatch-Switch + Multi-Round-Guard"
      contains: "matchState.kind === \"clarification\""
    - path: "__tests__/components/MatchResultList.test.tsx"
      provides: "12 it()-Tests gruen — passt_weil/achtung_bei/empty + ClarificationCard"
      contains: "describe(\"MatchResultList"
  key_links:
    - from: "components/Wizard/StartClient.tsx"
      to: "components/Wizard/MatchResultList.tsx + components/Wizard/ClarificationCard.tsx"
      via: "switch on matchState.kind in JSX"
      pattern: "matchState\\.kind === \"clarification\""
    - from: "components/Wizard/StartClient.tsx"
      to: "/api/match"
      via: "fetch POST mit forceRanking + previousAnliegen im Body"
      pattern: "forceRanking: true"
    - from: "components/Wizard/MatchResultList.tsx"
      to: "lucide-react"
      via: "import { CheckCircle, AlertTriangle, ... }"
      pattern: "CheckCircle.*AlertTriangle"
    - from: "components/Wizard/ClarificationCard.tsx"
      to: "lucide-react"
      via: "import { HelpCircle, ArrowRight }"
      pattern: "HelpCircle"
---

<objective>
Frontend-Migration: UI rendert strukturierte `passt_weil`/`achtung_bei`-Bloecke mit Icons + Farbe (D-10) UND zeigt eine Klaerungsfrage-Card statt Trefferliste, wenn der Matcher `kind: "clarification"` liefert (D-11). Multi-Round-Guard nach D-09 verhindert Endlos-Loop.

Purpose: Schliesst die Tagged-Union-Migration aus Plan 02-01 sichtbar fuer den User. Backend-Antwort `kind: "ranking" | "clarification"` muss der Browser korrekt unterscheiden und den User in EINE Richtung lenken — nicht beide gleichzeitig.
Output: Migrierter MatchResultList + neue ClarificationCard + State-Maschine in StartClient + 12 gruene UI-Tests.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-matcher-quality/02-CONTEXT.md
@.planning/phases/02-matcher-quality/02-RESEARCH.md
@.planning/phases/02-matcher-quality/02-PATTERNS.md
@.planning/phases/02-matcher-quality/02-VALIDATION.md
@.planning/phases/02-matcher-quality/02-00-SUMMARY.md
@.planning/phases/02-matcher-quality/02-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@components/Wizard/MatchResultList.tsx
@components/Wizard/StartClient.tsx
@components/Wizard/AnliegenForm.tsx

<interfaces>
<!-- Aktuelle UI-Strukturen + Ziel-Patterns. Executor uebernimmt direkt. -->

**IST `MatchResultList.tsx` Imports (Zeile 1-4):**
```typescript
"use client";
import Link from "next/link";
import { ArrowRight, ExternalLink, Star } from "lucide-react";
```

**ZIEL-Imports (CheckCircle + AlertTriangle hinzufuegen):**
```typescript
"use client";
import Link from "next/link";
import { ArrowRight, AlertTriangle, CheckCircle, ExternalLink, Star } from "lucide-react";
```

**IST `MatchEntry`-Interface (Zeile 6-21):**
```typescript
export interface MatchEntry {
  id: string;
  score: number;
  begruendung: string;
  programm: { id: string; name: string; foerdergeber?: string; ... };
}
```

**ZIEL `MatchEntry`:**
```typescript
export interface MatchEntry {
  id: string;
  score: number;
  passt_weil: string;
  achtung_bei: string;
  programm: {
    id: string;
    name: string;
    foerdergeber?: string;
    foerdergeberTyp?: string;
    foerdersummeText?: string;
    foerdersummeMax?: number;
    bewerbungsfristText?: string;
    kategorien?: string[];
    kurzbeschreibung?: string;
  };
}
```

**Begruendung-Block ersetzen** — IST `<p className="mb-4 ...">m.begruendung</p>` (Zeile 93-95):
```tsx
{/* Strukturierte Begruendung — D-10 */}
<div className="mb-4 space-y-2">
  <div className="flex items-start gap-2 rounded-lg bg-green-900/30 border border-green-700/50 px-3 py-2">
    <CheckCircle className="mt-0.5 h-4 w-4 shrink-0 text-green-400" aria-hidden="true" />
    <div>
      <span className="text-xs font-semibold text-green-400">Passt, weil: </span>
      <span className="text-sm text-green-200">{m.passt_weil}</span>
    </div>
  </div>
  {m.achtung_bei && (
    <div className="flex items-start gap-2 rounded-lg bg-orange-900/30 border border-orange-700/50 px-3 py-2">
      <AlertTriangle className="mt-0.5 h-4 w-4 shrink-0 text-orange-400" aria-hidden="true" />
      <div>
        <span className="text-xs font-semibold text-orange-400">Achtung: </span>
        <span className="text-sm text-orange-200">{m.achtung_bei}</span>
      </div>
    </div>
  )}
</div>
```

**NEUE Datei `components/Wizard/ClarificationCard.tsx` (vollstaendige Vorlage):**
```typescript
"use client";

import { useState } from "react";
import { ArrowRight, HelpCircle } from "lucide-react";

interface Props {
  question: string;
  onSubmit: (praezisierung: string) => void;
  onForceRanking: () => void;
  busy?: boolean;
}

export function ClarificationCard({ question, onSubmit, onForceRanking, busy }: Props) {
  const [praezisierung, setPraezisierung] = useState("");

  return (
    <div className="rounded-xl border border-blue-700/50 bg-slate-800/60 p-6 space-y-4">
      <div className="flex items-start gap-3">
        <HelpCircle className="h-6 w-6 shrink-0 text-blue-400 mt-0.5" aria-hidden="true" />
        <h2 className="text-lg font-semibold text-slate-100">{question}</h2>
      </div>
      <textarea
        aria-label="Anliegen praezisieren"
        value={praezisierung}
        onChange={(e) => setPraezisierung(e.target.value)}
        placeholder="Praezisiere dein Anliegen hier..."
        className="w-full rounded-lg bg-slate-700/50 border border-slate-600 px-4 py-3 text-sm text-slate-100 resize-none min-h-[80px]"
      />
      <div className="flex items-center justify-between flex-wrap gap-3">
        <button
          type="button"
          onClick={onForceRanking}
          disabled={busy}
          className="text-xs text-slate-500 hover:text-slate-300 transition disabled:opacity-50"
        >
          Trotzdem mit aktueller Eingabe ranken
        </button>
        <button
          type="button"
          disabled={!praezisierung.trim() || busy}
          onClick={() => onSubmit(praezisierung)}
          className="inline-flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white transition hover:bg-blue-700 disabled:opacity-50"
        >
          Praezisieren
          <ArrowRight className="h-4 w-4" aria-hidden="true" />
        </button>
      </div>
    </div>
  );
}
```

**StartClient.tsx — IST-Imports (Zeile 1-8):**
```typescript
"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { AnliegenForm, type AnliegenValues } from "./AnliegenForm";
import { MatchResultList, type MatchEntry } from "./MatchResultList";
import { WizardErrorBlock } from "./WizardErrorBlock";
import { saveHandoff } from "@/lib/wizard/match-handoff-client";
```

**ZIEL-Imports (ClarificationCard hinzufuegen):**
```typescript
"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { AnliegenForm, type AnliegenValues } from "./AnliegenForm";
import { MatchResultList, type MatchEntry } from "./MatchResultList";
import { ClarificationCard } from "./ClarificationCard";
import { WizardErrorBlock } from "./WizardErrorBlock";
import { saveHandoff } from "@/lib/wizard/match-handoff-client";
```

**StartClient State-Migration:**
```typescript
type MatchState =
  | { kind: "ranking"; matches: MatchEntry[] }
  | { kind: "clarification"; question: string }
  | null;

interface RawError { message: string; httpStatus?: number; }

export function StartClient() {
  const router = useRouter();
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<RawError | null>(null);
  const [matchState, setMatchState] = useState<MatchState>(null);
  const [lastInput, setLastInput] = useState<AnliegenValues | null>(null);
  const [isSecondRound, setIsSecondRound] = useState(false);
  // ...
}
```

**runMatch erweitern (Body um forceRanking + previousAnliegen):**
```typescript
const runMatch = async (
  values: AnliegenValues & { forceRanking?: boolean; previousAnliegen?: string }
) => {
  setBusy(true);
  setError(null);
  setMatchState(null);
  setLastInput(values);
  try {
    const res = await fetch("/api/match", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(values),
    });
    if (!res.ok) {
      const body = await res.json().catch(() => ({}));
      const msg = body.error ?? `HTTP ${res.status}`;
      setError({ message: String(msg), httpStatus: res.status });
      return;
    }
    const body = await res.json();
    if (body.kind === "clarification" && !isSecondRound) {
      setMatchState({ kind: "clarification", question: body.question });
    } else if (body.kind === "clarification" && isSecondRound) {
      // D-09 Guard: zweite Klaerung trotz forceRanking — Fallback auf leere Liste mit Hinweis
      setMatchState({ kind: "ranking", matches: [] });
      setError({ message: "Anliegen ist vage geblieben — bitte praezisere die Eingabe oder probiere mehr Details." });
    } else {
      setMatchState({ kind: "ranking", matches: (body.matches ?? []) as MatchEntry[] });
      setIsSecondRound(false);
    }
  } catch (e) {
    setError({ message: e instanceof Error ? e.message : "Matching fehlgeschlagen" });
  } finally {
    setBusy(false);
  }
};

const handlePraezisierung = async (praezisierung: string) => {
  if (!lastInput) return;
  setIsSecondRound(true);
  await runMatch({
    ...lastInput,
    anliegen: praezisierung,
    previousAnliegen: lastInput.anliegen,
    forceRanking: true,
  });
};

const handleForceRanking = async () => {
  if (!lastInput) return;
  setIsSecondRound(true);
  await runMatch({ ...lastInput, forceRanking: true });
};
```

**JSX-Dispatch (ersetzt Zeile 76-80):**
```tsx
{matchState !== null && (
  <div className="pt-2">
    {matchState.kind === "ranking" && (
      <MatchResultList matches={matchState.matches} onStartAntrag={startAntrag} />
    )}
    {matchState.kind === "clarification" && (
      <ClarificationCard
        question={matchState.question}
        onSubmit={handlePraezisierung}
        onForceRanking={handleForceRanking}
        busy={busy}
      />
    )}
  </div>
)}
```

**Test-Mock-Pattern fuer MatchResultList.test.tsx (uebernehmen aus Footer.test.tsx):**
```typescript
import { render, screen, fireEvent } from '@testing-library/react';
import { MatchResultList, type MatchEntry } from '@/components/Wizard/MatchResultList';
import { ClarificationCard } from '@/components/Wizard/ClarificationCard';

const SAMPLE: MatchEntry = {
  id: "bmbf-digitalpakt-2",
  score: 88,
  passt_weil: "Bundesweite Foerderung digitaler Schulinfrastruktur.",
  achtung_bei: "Antragsfrist naht.",
  programm: {
    id: "bmbf-digitalpakt-2",
    name: "DigitalPakt 2.0",
    foerdergeber: "BMBF",
  },
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ClarificationCard.tsx anlegen + MatchResultList.tsx auf passt_weil/achtung_bei migrieren</name>
  <files>components/Wizard/ClarificationCard.tsx, components/Wizard/MatchResultList.tsx</files>
  <read_first>
    - components/Wizard/MatchResultList.tsx (vollstaendig — 117 Zeilen, lokales MatchEntry-Interface in Zeile 6-21)
    - .planning/phases/02-matcher-quality/02-CONTEXT.md (D-10, D-11, D-12 UI-Rendering)
    - .planning/phases/02-matcher-quality/02-RESEARCH.md Sektion 4 ("UI Pattern", lucide-react + Tailwind)
    - .planning/phases/02-matcher-quality/02-PATTERNS.md (Bloecke "MatchResultList.tsx" + ClarificationCard-Vorlage)
    - .planning/phases/02-matcher-quality/02-01-SUMMARY.md (zur Bestaetigung dass Backend bereits passt_weil/achtung_bei liefert)
  </read_first>
  <action>
    **Schritt 1: ClarificationCard.tsx anlegen.**

    Erstelle `components/Wizard/ClarificationCard.tsx` mit dem **vollstaendigen** Code aus `<interfaces>` Block "NEUE Datei components/Wizard/ClarificationCard.tsx". Keine Abweichungen erlaubt:
    - `"use client"` als erste Zeile
    - `import { useState } from "react"` (kein React-Default-Import noetig in React 18+)
    - `import { ArrowRight, HelpCircle } from "lucide-react"` (lucide-react, NICHT Heroicons — D-10 in CONTEXT.md spricht von Heroicons, aber Codebase nutzt lucide-react; siehe Research-Summary "Lucide-react als Icon-System")
    - Props-Interface mit `question`, `onSubmit`, `onForceRanking`, `busy?`
    - State: `praezisierung` via `useState("")`
    - Layout aus `<interfaces>` 1:1
    - `aria-label="Anliegen praezisieren"` auf textarea
    - Praezisieren-Button disabled, wenn `!praezisierung.trim() || busy`
    - Override-Link disabled bei busy

    **Schritt 2: MatchResultList.tsx migrieren.**

    a) Imports erweitern: `CheckCircle, AlertTriangle` aus `lucide-react` zur bestehenden Liste hinzufuegen (alphabetisch sortiert).

    b) Lokales `MatchEntry`-Interface (Zeile 6-21) auf neues Schema umstellen — `begruendung` ENTFERNEN, `passt_weil: string` + `achtung_bei: string` HINZUFUEGEN. Restliche Programm-Felder unveraendert.

    c) Im Card-Layout: Zeile 93-95 (aktuell `<p>{m.begruendung}</p>`) ersetzen durch den **vollstaendigen** Block aus `<interfaces>` "Begruendung-Block ersetzen" — zwei farbige Bloecke mit Icons, achtung_bei nur conditional.

    d) Empty-State (Zeile 36-49) UNVERAENDERT lassen (D-12).

    e) `scoreColor`-Helper unveraendert.

    f) Card-Hauptlayout unveraendert ausser dem ersetzten Begruendung-Block.

    g) Falls externe Caller das alte `MatchEntry`-Interface aus dieser Datei importieren (laut Plan 02-01 nur StartClient.tsx, evtl. Tests), lassen die naechste Tasks/Dateien-Migrationen automatisch durch die TS-Kompilierung scheitern → in Task 2/3 fixen.

    **Schritt 3: Smoke `tsc`.**

    `npx tsc --noEmit` MUSS exit 0 (sonst sind die Imports oder Typen falsch).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `components/Wizard/ClarificationCard.tsx` existiert: `test -f components/Wizard/ClarificationCard.tsx`
    - `grep -c '\"use client\"' components/Wizard/ClarificationCard.tsx == 1`
    - `grep -c 'export function ClarificationCard' components/Wizard/ClarificationCard.tsx == 1`
    - `grep -c 'HelpCircle' components/Wizard/ClarificationCard.tsx >= 2` (Import + JSX)
    - `grep -c 'aria-label=\"Anliegen praezisieren\"' components/Wizard/ClarificationCard.tsx == 1`
    - `grep -c 'Trotzdem mit aktueller Eingabe ranken' components/Wizard/ClarificationCard.tsx == 1`
    - `grep -c 'onForceRanking' components/Wizard/ClarificationCard.tsx >= 2` (Prop + onClick)
    - `grep -c 'CheckCircle' components/Wizard/MatchResultList.tsx >= 2` (Import + JSX)
    - `grep -c 'AlertTriangle' components/Wizard/MatchResultList.tsx >= 2`
    - `grep -c 'passt_weil' components/Wizard/MatchResultList.tsx >= 3` (Interface, JSX-Label-naehe, JSX-Wert)
    - `grep -c 'achtung_bei' components/Wizard/MatchResultList.tsx >= 3`
    - `grep -c 'Passt, weil:' components/Wizard/MatchResultList.tsx == 1`
    - `grep -c 'Achtung:' components/Wizard/MatchResultList.tsx == 1`
    - `grep -c 'begruendung' components/Wizard/MatchResultList.tsx == 0` (D-04 hart entfernt)
    - `grep -c 'Keine passenden Programme gefunden' components/Wizard/MatchResultList.tsx == 1` (D-12 Empty-State unveraendert)
    - `grep -cE '\\{m\\.achtung_bei && \\(' components/Wizard/MatchResultList.tsx == 1` (Conditional-Render)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>ClarificationCard ist eine funktionsfaehige Standalone-Komponente mit a11y-Markup. MatchResultList rendert zwei farbige Bloecke statt eines `<p>`-Tags. TypeScript kompiliert. Empty-State und scoreColor-Helper unveraendert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StartClient.tsx auf Tagged-Union-State + Multi-Round-Guard migrieren + UI-Tests befuellen</name>
  <files>components/Wizard/StartClient.tsx, __tests__/components/MatchResultList.test.tsx</files>
  <read_first>
    - components/Wizard/StartClient.tsx (vollstaendig — 83 Zeilen)
    - components/Wizard/MatchResultList.tsx (nach Task 1 migriert)
    - components/Wizard/ClarificationCard.tsx (nach Task 1 angelegt)
    - components/Wizard/AnliegenForm.tsx (zur Bestaetigung des `AnliegenValues`-Typs)
    - .planning/phases/02-matcher-quality/02-CONTEXT.md (D-09 Multi-Round-Guard, D-11 Card-Branch)
    - .planning/phases/02-matcher-quality/02-RESEARCH.md Sektion 3 + Pitfall 4 (State-Inkonsistenz Guard)
    - __tests__/components/MatchResultList.test.tsx (Skelett aus Plan 02-00)
    - __tests__/components/Footer.test.tsx (Pattern fuer @testing-library/react)
  </read_first>
  <behavior>
    Test-Erwartungen vor Implementierung (TDD-RED-GREEN):

    `MatchResultList.test.tsx` describe(`MatchResultList — Ranking-Branch`):
    - Test 1 (`D-10`): rendert "Passt, weil:" Label-Text
    - Test 2 (`D-10`): rendert achtung_bei-Text wenn nicht-leer ("Antragsfrist naht.")
    - Test 3 (`D-10`): achtung_bei-Block fehlt wenn Empty-String — `screen.queryByText("Achtung:")).not.toBeInTheDocument()`
    - Test 4 (`D-10`): CheckCircle-svg gerendert (lucide-react setzt `<svg class="lucide ...">` — pruefe via `container.querySelector('svg')` Existenz)
    - Test 5 (`D-10`): AlertTriangle-svg gerendert
    - Test 6 (`D-12`): Empty-State-Text "Keine passenden Programme gefunden" sichtbar bei matches=[]

    `MatchResultList.test.tsx` describe(`ClarificationCard`):
    - Test 7 (`D-11`): question wird als h2 gerendert
    - Test 8 (`D-11`): textarea hat aria-label "Anliegen praezisieren"
    - Test 9 (`D-11`): Praezisieren-Button ist disabled, solange textarea leer
    - Test 10 (`D-11`): Klick auf "Praezisieren" nach textarea-Eingabe ruft onSubmit mit dem Wert
    - Test 11 (`D-11`): Klick auf "Trotzdem mit aktueller Eingabe ranken" ruft onForceRanking ohne Argument
    - Test 12 (`D-11`): HelpCircle-svg ist sichtbar
  </behavior>
  <action>
    **Schritt 1: TDD-RED — Tests in MatchResultList.test.tsx befuellen.**

    `it.todo` durch `it` ersetzen und Bodies schreiben gemaess `<behavior>`. Setup-Konstante `SAMPLE` aus `<interfaces>` Block "Test-Mock-Pattern" uebernehmen. Imports:
    ```typescript
    import { render, screen, fireEvent } from '@testing-library/react';
    import { MatchResultList, type MatchEntry } from '@/components/Wizard/MatchResultList';
    import { ClarificationCard } from '@/components/Wizard/ClarificationCard';
    ```

    Nach Task 1 sollten die Tests automatisch GREEN sein, da MatchResultList + ClarificationCard bereits korrekt rendern. Smoke-Lauf: `npm test -- --testPathPattern='components/MatchResultList'` exit 0.

    **Schritt 2: StartClient.tsx migrieren.**

    a) Import von `ClarificationCard` aus `./ClarificationCard` hinzufuegen.

    b) `MatchState`-Type-Alias hinzufuegen (oberhalb der `StartClient`-Funktion):
    ```typescript
    type MatchState =
      | { kind: "ranking"; matches: MatchEntry[] }
      | { kind: "clarification"; question: string }
      | null;
    ```

    c) State-Variablen austauschen:
       - `matches` und `setMatches` raus
       - `matchState` und `setMatchState` rein (initial null)
       - Neue State: `isSecondRound` und `setIsSecondRound` (initial false)

    d) `runMatch`-Handler komplett ersetzen durch das Pattern aus `<interfaces>` "runMatch erweitern":
       - Body um `forceRanking` und `previousAnliegen` erweitert
       - Nach `body = await res.json()` Dispatch auf `body.kind`:
         - `kind === "clarification"` UND `!isSecondRound` → `setMatchState({ kind: "clarification", question })`
         - `kind === "clarification"` UND `isSecondRound` → D-09-Guard: leere Liste + Fehler-Banner
         - `kind === "ranking"` → `setMatchState({ kind: "ranking", matches })` + `setIsSecondRound(false)`

    e) `handlePraezisierung` und `handleForceRanking` als neue Handler hinzufuegen (siehe `<interfaces>`).

    f) `retry`-Handler unveraendert lassen.

    g) `startAntrag`-Handler unveraendert lassen — er bekommt `m: MatchEntry`, das Shape ist gleich (nur Felder ersetzt).

    h) JSX-Render-Block (Zeile 76-80) ersetzen durch das Tagged-Union-Switch aus `<interfaces>` "JSX-Dispatch".

    i) AnliegenForm-Typ: `AnliegenValues` aus AnliegenForm.tsx erlaubt nur die Pflichtfelder. Da `runMatch` jetzt zusaetzlich `forceRanking` und `previousAnliegen` akzeptiert, dort den Type-Parameter `& { forceRanking?: boolean; previousAnliegen?: string }` nutzen oder als separates Interface deklarieren. Beachte: `AnliegenForm.onSubmit` ruft mit `AnliegenValues` (ohne Erweiterung) — nur die Praezisierungs-Pfade (handlePraezisierung/handleForceRanking) nutzen die Erweiterung.

    **Schritt 3: Verifikation.**

    `npx tsc --noEmit` exit 0. `npm test` exit 0 (alle Tests aus matcher-Paths + MatchResultList-Path gruen). Bei Type-Errors: pruefe `AnliegenValues`-Type-Inheritance.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='components/MatchResultList|wizard/matcher' 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { ClarificationCard }' components/Wizard/StartClient.tsx == 1`
    - `grep -c 'type MatchState' components/Wizard/StartClient.tsx >= 1`
    - `grep -c 'matchState' components/Wizard/StartClient.tsx >= 5` (State + Setter + JSX-Switch + Dispatch + Reset)
    - `grep -c 'isSecondRound' components/Wizard/StartClient.tsx >= 4`
    - `grep -c 'handlePraezisierung' components/Wizard/StartClient.tsx >= 2`
    - `grep -c 'handleForceRanking' components/Wizard/StartClient.tsx >= 2`
    - `grep -c 'forceRanking: true' components/Wizard/StartClient.tsx >= 2` (handlePraezisierung + handleForceRanking)
    - `grep -c 'previousAnliegen: lastInput.anliegen' components/Wizard/StartClient.tsx == 1`
    - `grep -c 'matchState.kind === \"clarification\"' components/Wizard/StartClient.tsx >= 1`
    - `grep -c 'matchState.kind === \"ranking\"' components/Wizard/StartClient.tsx >= 1`
    - `grep -c 'body.kind' components/Wizard/StartClient.tsx >= 1`
    - `grep -c 'setMatches\\b' components/Wizard/StartClient.tsx == 0` (alter State entfernt)
    - MatchResultList.test.tsx hat keine `it.todo` mehr: `grep -c 'it\\.todo' __tests__/components/MatchResultList.test.tsx == 0`
    - Mind. 12 `it()`-Eintraege: `grep -cE 'it\\(' __tests__/components/MatchResultList.test.tsx >= 12`
    - Setup-Konstante: `grep -c 'SAMPLE\\|MATCH_ENTRY' __tests__/components/MatchResultList.test.tsx >= 1`
    - `npx tsc --noEmit` exit 0
    - `npm test -- --testPathPattern='components/MatchResultList' --silent` exit 0 mit allen Tests gruen
    - `npm test -- --testPathPattern='wizard/matcher|components/MatchResultList' --silent` exit 0 (Backend + Frontend kombiniert)
  </acceptance_criteria>
  <done>StartClient haelt Tagged-Union-State, dispatched MatchResultList vs ClarificationCard, ruft Praezisierung/forceRanking mit korrekten Body-Feldern auf, isSecondRound-Guard verhindert Endlos-Loop. 12+ UI-Tests gruen. Plan 02-03 (Eval) kann unabhaengig laufen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visuelle Verifikation des kompletten UI-Flows im Browser</name>
  <files>none — manueller Browser-Smoke, kein File-Edit</files>
  <action>Manueller Browser-Smoke. Siehe `<what-built>` und `<how-to-verify>` Abschnitte fuer die exakten Schritte. Executor pausiert hier und wartet auf Kolja's Resume-Signal.</action>
  <what-built>
    Plan 02-01 (Backend, Tagged-Union, CLARIFY-Dispatch) + Plan 02-02 Tasks 1-2 (Frontend, MatchResultList passt_weil/achtung_bei, ClarificationCard, StartClient State-Maschine).
    Alle Unit-Tests gruen (8 Parser + 10 Dispatch + 12 UI = 30 Tests).
  </what-built>
  <how-to-verify>
    Prerequisits:
    1. Dev-DB-Tunnel laeuft: `./scripts/dev-db-tunnel.sh --bg` (siehe CLAUDE.md "Lokale Dev-Umgebung")
    2. `npm run dev` startet auf http://localhost:3101
    3. `.env.local` enthaelt `DEEPSEEK_API_KEY` (Live-LLM-Calls werden ausgeloest, ~0.04 ct pro Match)

    Verifikation der drei UI-Pfade:

    **Pfad 1 — Strukturierte Begruendung (D-10):**
    1. Navigiere zu http://localhost:3101/antrag/start
    2. Tippe ein klares Anliegen ein, z.B. "Wir wollen 30 iPads fuer unseren Informatikunterricht in Berliner Gymnasien anschaffen, Budget ca. 12.000 Euro."
    3. Klicke "Programme finden"
    4. Erwartung: Top-3-Liste, jede Karte zeigt:
       - Score-Badge (emerald/orange/grau)
       - Programm-Name + foerdergeber
       - **Gruener Block** mit Check-Icon und "Passt, weil: [Text]"
       - **Oranger Block** mit Warn-Icon und "Achtung: [Text]" (nur wenn nicht leer)
       - "Antrag starten"-Button
    5. Beide Bloecke stehen VOR dem Antrag-Button (D-10).

    **Pfad 2 — Klaerungsfrage (D-11):**
    1. Browser-Tab schliessen oder localStorage leeren, neu zu /antrag/start.
    2. Tippe ein vages Anliegen ein, z.B. "Wir wollen ein Projekt starten, das Kinder motiviert."
    3. Klicke "Programme finden"
    4. Erwartung: KEINE Trefferliste, sondern eine prominente Card mit:
       - HelpCircle-Icon (blau)
       - Klaerungsfrage als h2 (z.B. "Fuer welches Bundesland sucht ihr und welcher Schwerpunkt steht im Vordergrund?")
       - textarea mit Placeholder "Praezisiere dein Anliegen hier..."
       - "Praezisieren"-Button (initial disabled bis Text getippt)
       - Override-Link "Trotzdem mit aktueller Eingabe ranken" unten links

    **Pfad 3 — Multi-Round-Guard (D-09):**
    1. Im Klaerungs-Modus (Pfad 2): tippe in die textarea eine erneut vage Praezisierung, z.B. "Vielleicht was Kreatives, vielleicht was Sportliches."
    2. Klick "Praezisieren"
    3. Erwartung: zweiter `/api/match`-Call mit `forceRanking: true`. Es erscheint **eine Trefferliste** (kein zweites Klaerungs-Card), ggf. mit niedrigen Scores. Im Browser-Network-Tab pruefen: Request-Body enthaelt `"forceRanking": true` und `"previousAnliegen": "..."`.

    **Pfad 4 — Override-Link (D-11):**
    1. Im Klaerungs-Modus: Klick auf "Trotzdem mit aktueller Eingabe ranken" OHNE textarea-Eingabe
    2. Erwartung: zweiter `/api/match`-Call mit `forceRanking: true` ohne `previousAnliegen` (oder gleichem `anliegen` wie zuvor). Trefferliste mit niedrigen Scores erscheint.

    **Mobile-Pruefung:**
    - DevTools auf 375px verkleinern
    - Sowohl gruener/oranger Block als auch ClarificationCard stacken vertikal
    - Praezisieren-Button bleibt klickbar, Override-Link nicht abgeschnitten

    **Bei Fehler:**
    - Console-Errors abfangen (im Browser DevTools Console)
    - Network-Tab: Response-Body von `/api/match` muss `kind: "ranking"` ODER `kind: "clarification"` enthalten — wenn nichts dergleichen, ist Plan 02-01 nicht korrekt deployed
  </how-to-verify>
  <resume-signal>
    Antworte mit "approved" oder beschreibe Probleme:
    - Welcher Pfad scheitert?
    - Sichtbare visuelle Probleme (Farben, Layout, Mobile)?
    - Network-Tab-Auffaelligkeiten (Response-Shape stimmt nicht)?
  </resume-signal>
  <verify>Manuelle Browser-Inspektion (siehe how-to-verify). Kein automatisierter Befehl moeglich, da Live-LLM-Call + visuelle Pruefung benoetigt.</verify>
  <done>Kolja hat "approved" geantwortet ODER alle gefundenen Probleme sind in einem Folge-Task adressiert worden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-Browser → API (`/api/match`) | Bestehender Trust-Boundary, durch Plan 02-01 abgedeckt |
| User-Input → ClarificationCard textarea | XSS-Vektor: React rendert `{question}` und `{praezisierung}` als reinen Text-Knoten — keine HTML-Interpretation |
| Frontend-State → React-Render | matchState ist Type-Safe Tagged-Union, kein eval/string-Parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | Klaerungsfrage als h2 | accept | LLM kann theoretisch Programm-IDs oder Schul-Daten in der Frage zitieren — aber das LLM hat dieselben Daten ohnehin im Prompt erhalten. Kein Zusatz-Risiko. |
| T-02-02-02 | Tampering | Endlos-Klaerungs-Loop | mitigate | `isSecondRound`-Guard in StartClient: bei zweiter `kind === "clarification"`-Antwort wird auf leere Trefferliste + Fehler-Banner umgeschwenkt, nicht erneut Card gerendert. Test-Coverage in Pitfall 4 dokumentiert. |
| T-02-02-03 | Spoofing | Klick auf Override-Link ohne textarea-Eingabe | accept | Override-Link ist user-initiated, sendet `forceRanking: true` mit demselben `anliegen`. Erwartetes Verhalten — kein Bypass eines Schutzes. |
| T-02-02-04 | DoS | textarea unbegrenzt langer Text | accept | textarea hat min-h-[80px], aber kein max-length. Limit sitzt in der API-Route (`anliegen` als string ohne Laengen-Constraint, bestehender Stand). Falls relevant: Phase 3 als Hardening. |
| T-02-02-05 | Tampering | Manuelle Modifikation des `body.kind` durch Browser-Devtools | accept | Client-State ist immer manipulierbar — schadet nur dem Manipulator selbst. Server-seitige Logik ist von Plan 02-01 abgesichert. |
| T-02-02-06 | XSS | LLM-Output in `{question}` als JSX-Kind | mitigate | React rendert `{question}` als Text-Node (escaped) — keine HTML-Interpretation. Solange wir in der ClarificationCard NICHT das `dangerouslySetInnerHTML`-Prop verwenden (tun wir nicht), ist XSS ausgeschlossen. Code-Review-Gate beim Lesen der Datei. |
</threat_model>

<verification>
- `npm test -- --testPathPattern='components/MatchResultList|wizard/matcher'` exit 0 mit 30+ gruenen Tests
- `npx tsc --noEmit` exit 0
- Browser-Smoke (Task 3 checkpoint): alle 4 UI-Pfade visuell verifiziert
- `grep -c 'begruendung' components/Wizard/MatchResultList.tsx components/Wizard/StartClient.tsx == 0`
- Plan 02-03 kann ohne Dependency auf Plan 02-02 laufen (parallel)
</verification>

<success_criteria>
- MATCH-02 UI: passt_weil + achtung_bei in 2 farbigen Bloecken sichtbar (D-10)
- MATCH-03 UI: ClarificationCard mit textarea + Submit + Override (D-11)
- D-09 Multi-Round-Guard verhindert Endlos-Loop
- D-12 Empty-State unveraendert bei `kind=ranking` + `matches=[]`
- 12+ UI-Tests + 18 Backend-Tests gruen, kombiniert exit 0
- Live-Browser-Smoke alle 4 Pfade gruen
</success_criteria>

<output>
After completion, create `.planning/phases/02-matcher-quality/02-02-SUMMARY.md` with:
- Pfade aller geaenderter/neuer Dateien
- Anzahl Tests gruen pro Datei
- Browser-Smoke-Ergebnisse (Pfad 1-4 status)
- ggf. visuelle Befunde (Farb-Drift, Mobile-Quirks)
- Hinweis fuer Plan 02-03 (parallel): "Backend-Migration ist abgeschlossen, Eval-Skript kann result.kind sicher dispatchen"
</output>

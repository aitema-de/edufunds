---
phase: 02-matcher-quality
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/lib/wizard/matcher.parser.test.ts
  - __tests__/lib/wizard/matcher.dispatch.test.ts
  - __tests__/components/MatchResultList.test.tsx
autonomous: true
requirements: [MATCH-02, MATCH-03]
tags: [eval, testing, matcher, wave-0]

must_haves:
  truths:
    - "Drei Test-Dateien existieren als pending-Skelette mit `it.todo`-Markierungen, sodass nachfolgende Plans 02-01 und 02-02 sie nur befuellen muessen, nicht erstellen."
    - "Jede Test-Datei kompiliert ohne TypeScript-Fehler (`tsc --noEmit`) und Jest erkennt die Dateien (`npm test -- --listTests` listet sie)."
    - "Jede `it.todo`-Markierung referenziert genau ein D-XX aus 02-CONTEXT.md (z.B. `it.todo('parst 4-Spalten-Zeile korrekt — D-01')`)."
  artifacts:
    - path: "__tests__/lib/wizard/matcher.parser.test.ts"
      provides: "Test-Skelett fuer parsePipeMatches: 4-Spalten-Format, Soft-Failure bei != 4 Spalten, Trailing-Pipe = leeres achtung_bei, CLARIFY-Zeilen werden ignoriert"
      contains: "describe(\"parsePipeMatches"
      min_lines: 25
    - path: "__tests__/lib/wizard/matcher.dispatch.test.ts"
      provides: "Test-Skelett fuer runMatch CLARIFY-Dispatch: kind=clarification bei CLARIFY|, kind=ranking sonst, forceRanking=true unterdrueckt CLARIFY"
      contains: "describe(\"runMatch"
      min_lines: 25
    - path: "__tests__/components/MatchResultList.test.tsx"
      provides: "Test-Skelett fuer MatchResultList + ClarificationCard: passt_weil-Block sichtbar, achtung_bei-Block bedingt, ClarificationCard rendert Frage"
      contains: "describe(\"MatchResultList"
      min_lines: 30
  key_links:
    - from: "__tests__/lib/wizard/matcher.parser.test.ts"
      to: "lib/wizard/matcher.ts"
      via: "import { parsePipeMatches } from \"@/lib/wizard/matcher\""
      pattern: "import.*parsePipeMatches.*from.*matcher"
    - from: "__tests__/lib/wizard/matcher.dispatch.test.ts"
      to: "lib/wizard/llm.ts (gemockt) + lib/wizard/matcher.ts"
      via: "jest.mock(\"@/lib/wizard/llm\") + import { runMatch }"
      pattern: "jest\\.mock.*@/lib/wizard/llm"
    - from: "__tests__/components/MatchResultList.test.tsx"
      to: "components/Wizard/MatchResultList.tsx + components/Wizard/ClarificationCard.tsx"
      via: "import + render mit @testing-library/react"
      pattern: "import.*MatchResultList.*from.*@/components/Wizard"
---

<objective>
Wave-0-Test-Skelette anlegen, damit Plan 02-01 (Backend) und Plan 02-02 (Frontend) `<verify><automated>npm test -- --testPathPattern='wizard/matcher'</automated></verify>` Befehle direkt erfuellen koennen, statt erst Test-Infrastruktur aufbauen zu muessen.

Purpose: Nyquist-Validation-Gate — Phase 2 darf keine Tasks ohne automatisierte Verify-Commands haben. Wave 0 schliesst die in 02-RESEARCH.md/02-VALIDATION.md identifizierten Test-Luecken.
Output: Drei pending Test-Dateien mit `it.todo`-Markierungen (kein Test-Code in den Bodies — Plan 02-01/02-02 ersetzen `it.todo` durch `it` und schreiben die Assertions). Jest erkennt die Dateien als pending; `npm test` ist exit 0.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-matcher-quality/02-CONTEXT.md
@.planning/phases/02-matcher-quality/02-RESEARCH.md
@.planning/phases/02-matcher-quality/02-PATTERNS.md
@.planning/phases/02-matcher-quality/02-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

<interfaces>
<!-- Existing test patterns to imitate. Extracted from codebase. -->
<!-- Executor uses these directly — no exploration needed. -->

From `__tests__/components/Footer.test.tsx` (Header + jest.mock-Pattern):
```typescript
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { Footer } from '@/components/Footer';

// Mock framer-motion etc.
jest.mock('framer-motion', () => ({
  motion: { div: ({ children, ...props }: any) => <div {...props}>{children}</div> },
}));

describe('Footer Komponente', () => {
  it('sollte das Logo rendern', () => {
    render(<Footer />);
    expect(screen.getByText('EduFunds')).toBeInTheDocument();
  });
});
```

From `__tests__/lib/wizard/title-fallback.test.ts` (pure-function-Pattern, kein Mock):
```typescript
import { someFn } from "@/lib/wizard/...";
describe("someFn", () => {
  it("verhalten X", () => {
    expect(someFn(input)).toEqual(output);
  });
});
```

From `jest.config.js` setup:
- `setupFilesAfterEach: ['<rootDir>/test/setup.tsx']` (next/link, next/router bereits gemockt)
- `testEnvironment: 'jest-environment-jsdom'`
- moduleNameMapper: `'^@/(.*)$': '<rootDir>/$1'`
- `npm test -- --testPathPattern='matcher'` filtert auf Matcher-Tests

`it.todo`-Pattern (Jest 29):
```typescript
describe("FeatureX", () => {
  it.todo("verhalten 1 — D-XX");
  it.todo("verhalten 2 — D-YY");
});
```
`it.todo` markiert Tests als pending, blockiert nicht den Lauf. `npm test` ist exit 0.

CLAUDE.md-Konvention: deutsche `describe`/`it`-Strings (Beispiel `Footer.test.tsx` Zeile 11: 'Footer Komponente').
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Test-Skelett matcher.parser.test.ts anlegen</name>
  <files>__tests__/lib/wizard/matcher.parser.test.ts</files>
  <read_first>
    - __tests__/lib/wizard/title-fallback.test.ts (Pattern fuer pure-function-Tests ohne Mock)
    - .planning/phases/02-matcher-quality/02-CONTEXT.md (D-01, D-02 Pipe-Format-Schema)
    - .planning/phases/02-matcher-quality/02-PATTERNS.md (Block "matcher.parser.test.ts" mit Test-Vorlage)
    - lib/wizard/matcher.ts (aktueller Parser-Stand, NICHT aendern hier)
  </read_first>
  <action>
    Erstelle `__tests__/lib/wizard/matcher.parser.test.ts` mit folgendem exakten Inhalt (keine Implementierung, nur `it.todo`-Skelette):

    ```typescript
    /**
     * Wave-0 Test-Skelett — Parser-Tests fuer parsePipeMatches.
     * Plan 02-01 ersetzt it.todo durch it und schreibt Assertions.
     * Phase: 02-matcher-quality, D-01/D-02.
     */

    describe("parsePipeMatches — 4-Spalten-Pipe-Format", () => {
      it.todo("parst korrekte 4-Spalten-Zeile (id|score|passt_weil|achtung_bei) — D-01");
      it.todo("verwirft Zeile mit 3 Spalten (Soft-Failure, kein Throw) — D-02");
      it.todo("verwirft Zeile mit 5 Spalten (Soft-Failure) — D-02");
      it.todo("parst Trailing-Pipe als leeres achtung_bei (id|score|text|) — D-01");
      it.todo("ignoriert CLARIFY|-Zeilen (werden in runMatch dispatched, nicht im Parser) — D-05");
      it.todo("ignoriert Code-Fence-Zeilen (```)");
      it.todo("verwirft Zeile mit unbekannter programmId (validIds-Set-Filter)");
      it.todo("verwirft Zeile mit nicht-numerischem Score (NaN-Filter)");
    });
    ```

    Datei-Konventionen:
    - Kein `import` (nur `it.todo`-Strings, kein Code)
    - JSDoc-Header mit Phase + D-Referenzen
    - Deutsche `describe`/`it`-Strings
    - ASCII in Strings (Footer.test.tsx Konvention)
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='wizard/matcher\.parser' --listTests 2>&amp;1 | grep -c 'matcher.parser.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - File existiert: `test -f __tests__/lib/wizard/matcher.parser.test.ts`
    - Enthaelt JSDoc mit Phase-Referenz: `grep -c 'Phase: 02-matcher-quality' __tests__/lib/wizard/matcher.parser.test.ts == 1`
    - Mind. 8 `it.todo` Markierungen: `grep -c 'it\.todo' __tests__/lib/wizard/matcher.parser.test.ts >= 8`
    - Mindestens eine D-XX-Referenz: `grep -cE 'D-0[125]' __tests__/lib/wizard/matcher.parser.test.ts >= 4`
    - Jest erkennt die Datei: `npm test -- --testPathPattern='wizard/matcher\.parser' --listTests 2>&amp;1` listet `matcher.parser.test.ts`
    - `npm test -- --testPathPattern='wizard/matcher\.parser'` exit 0 (alle pending = ok)
  </acceptance_criteria>
  <done>Test-Skelett liegt vor, Jest erkennt die Datei, alle `it.todo`-Markierungen referenzieren D-Decisions aus 02-CONTEXT.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Test-Skelett matcher.dispatch.test.ts anlegen</name>
  <files>__tests__/lib/wizard/matcher.dispatch.test.ts</files>
  <read_first>
    - __tests__/lib/wizard/facts-extractor.test.ts (Pattern fuer Tests mit jest.mock auf llm.ts)
    - .planning/phases/02-matcher-quality/02-CONTEXT.md (D-05, D-08, D-09 CLARIFY-Dispatch + forceRanking)
    - .planning/phases/02-matcher-quality/02-PATTERNS.md (Block "matcher.dispatch.test.ts" mit Mock-Vorlage)
  </read_first>
  <action>
    Erstelle `__tests__/lib/wizard/matcher.dispatch.test.ts` mit folgendem exakten Inhalt (keine Implementierung, nur `it.todo`-Skelette):

    ```typescript
    /**
     * Wave-0 Test-Skelett — Dispatch-Tests fuer runMatch (CLARIFY vs Ranking).
     * Plan 02-01 ersetzt it.todo durch it, mockt generateText und schreibt Assertions.
     * Phase: 02-matcher-quality, D-05/D-08/D-09.
     */

    describe("runMatch — CLARIFY-Dispatch", () => {
      it.todo("liefert kind=clarification wenn erste Zeile mit CLARIFY| beginnt — D-05/D-08");
      it.todo("extrahiert question-Text rechts vom CLARIFY|-Praefix — D-05");
      it.todo("liefert kind=ranking wenn erste Zeile id|score|... ist — D-08");
      it.todo("ignoriert CLARIFY in spaeteren Zeilen (nur erste Zeile zaehlt) — D-05");
    });

    describe("runMatch — forceRanking-Override", () => {
      it.todo("liefert kind=ranking wenn forceRanking=true, auch bei CLARIFY-LLM-Antwort — D-09");
      it.todo("forceRanking=false (default) erlaubt CLARIFY-Dispatch — D-09");
      it.todo("buildUserPrompt fuegt forceRanking-Hinweis-Block an wenn forceRanking=true");
      it.todo("buildUserPrompt fuegt previousAnliegen-Block an wenn previousAnliegen gesetzt — D-09");
    });

    describe("runMatch — costs-Feldname", () => {
      it.todo("ranking-Result enthaelt Feld 'costs' (nicht 'cost') — Codebase-Konvention");
      it.todo("clarification-Result enthaelt Feld 'costs' (nicht 'cost') — Codebase-Konvention");
    });
    ```

    Datei-Konventionen:
    - Kein `import` und kein `jest.mock` (nur `it.todo`-Strings)
    - JSDoc-Header mit Phase + D-Referenzen
    - Drei `describe`-Bloecke (Dispatch / forceRanking / costs)
    - Deutsche Strings
    - **Wichtig:** Beachte den Mismatch — D-08 in CONTEXT.md schreibt `cost`, Codebase-Konvention ist `costs`. Plan 02-01 setzt `costs` durch (siehe RESEARCH.md Pitfall 6).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='wizard/matcher\.dispatch' --listTests 2>&amp;1 | grep -c 'matcher.dispatch.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - File existiert: `test -f __tests__/lib/wizard/matcher.dispatch.test.ts`
    - JSDoc-Header mit Phase: `grep -c 'Phase: 02-matcher-quality' __tests__/lib/wizard/matcher.dispatch.test.ts == 1`
    - Drei describe-Bloecke: `grep -cE 'describe\\(\"runMatch' __tests__/lib/wizard/matcher.dispatch.test.ts >= 3`
    - Mind. 10 `it.todo`: `grep -c 'it\\.todo' __tests__/lib/wizard/matcher.dispatch.test.ts >= 10`
    - Hinweis auf costs-vs-cost: `grep -c 'costs' __tests__/lib/wizard/matcher.dispatch.test.ts >= 2`
    - D-Referenzen: `grep -cE 'D-(05|08|09)' __tests__/lib/wizard/matcher.dispatch.test.ts >= 5`
    - Jest erkennt die Datei und exit 0 bei `npm test -- --testPathPattern='wizard/matcher\\.dispatch'`
  </acceptance_criteria>
  <done>Test-Skelett mit 3 describe-Bloecken liegt vor, alle `it.todo` referenzieren D-Decisions, Jest erkennt die Datei.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Test-Skelett MatchResultList.test.tsx anlegen</name>
  <files>__tests__/components/MatchResultList.test.tsx</files>
  <read_first>
    - __tests__/components/Footer.test.tsx (Pattern fuer @testing-library/react + jsdom + jest.mock)
    - .planning/phases/02-matcher-quality/02-CONTEXT.md (D-10, D-11, D-12 UI-Rendering)
    - .planning/phases/02-matcher-quality/02-PATTERNS.md (Block "MatchResultList.test.tsx" mit Vorlage)
    - components/Wizard/MatchResultList.tsx (aktueller Stand, NICHT aendern hier)
  </read_first>
  <action>
    Erstelle `__tests__/components/MatchResultList.test.tsx` mit folgendem exakten Inhalt (keine Implementierung, nur `it.todo`-Skelette):

    ```typescript
    /**
     * Wave-0 Test-Skelett — UI-Smoke fuer MatchResultList + ClarificationCard.
     * Plan 02-02 ersetzt it.todo durch it, importiert die Komponenten und schreibt Assertions.
     * Phase: 02-matcher-quality, D-10/D-11/D-12.
     */

    describe("MatchResultList — Ranking-Branch", () => {
      it.todo("rendert passt_weil-Block mit Label 'Passt, weil:' — D-10");
      it.todo("rendert achtung_bei-Block mit Label 'Achtung:' wenn nicht-leer — D-10");
      it.todo("rendert achtung_bei-Block NICHT wenn leer — D-10");
      it.todo("rendert CheckCircle-Icon (lucide-react) im passt_weil-Block — D-10");
      it.todo("rendert AlertTriangle-Icon (lucide-react) im achtung_bei-Block — D-10");
      it.todo("rendert Empty-State-Card bei matches.length===0 — D-12");
    });

    describe("ClarificationCard", () => {
      it.todo("rendert question-Text als h2 — D-11");
      it.todo("rendert HelpCircle-Icon — D-11");
      it.todo("rendert textarea mit aria-label='Anliegen praezisieren' — D-11");
      it.todo("Praezisieren-Button disabled wenn textarea leer — D-11");
      it.todo("Klick auf 'Praezisieren' ruft onSubmit mit textarea-Wert — D-11");
      it.todo("Override-Link 'Trotzdem mit aktueller Eingabe ranken' ruft onForceRanking — D-11");
    });
    ```

    Datei-Konventionen:
    - Kein `import` und kein `render` (nur `it.todo`-Strings)
    - JSDoc-Header mit Phase + D-Referenzen
    - Zwei `describe`-Bloecke (MatchResultList Ranking-Branch + ClarificationCard)
    - Deutsche Strings im describe/it
    - .tsx-Extension (Test-Datei fuer React-Komponenten)
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='components/MatchResultList' --listTests 2>&amp;1 | grep -c 'MatchResultList.test.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - File existiert: `test -f __tests__/components/MatchResultList.test.tsx`
    - JSDoc mit Phase: `grep -c 'Phase: 02-matcher-quality' __tests__/components/MatchResultList.test.tsx == 1`
    - Zwei describe-Bloecke: `grep -cE 'describe\\(\"(MatchResultList|ClarificationCard)' __tests__/components/MatchResultList.test.tsx >= 2`
    - Mind. 12 `it.todo`: `grep -c 'it\\.todo' __tests__/components/MatchResultList.test.tsx >= 12`
    - D-Referenzen: `grep -cE 'D-(10|11|12)' __tests__/components/MatchResultList.test.tsx >= 8`
    - Jest erkennt die Datei: `npm test -- --testPathPattern='components/MatchResultList' --listTests 2>&amp;1` listet `MatchResultList.test.tsx`
    - Vollsuite gruen: `npm test -- --testPathPattern='matcher|MatchResultList'` exit 0
  </acceptance_criteria>
  <done>Test-Skelett mit ClarificationCard- und MatchResultList-Beschreibungen liegt vor, alle `it.todo` referenzieren D-Decisions, Jest erkennt die Datei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Code → Production-Code | Test-Dateien duerfen Production-Module importieren, aber NICHT modifizieren |
| Jest → CI/Local | Test-Lauf darf weder Netzwerk-Calls absetzen noch echte Files ausserhalb `tmp/` schreiben |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Tampering | Test-Skelette | accept | `it.todo`-Skelette enthalten keine Logik — kein Tampering-Risiko in Wave 0. Plan 02-01/02-02 fuegen Logik hinzu, dort ist Threat-Model relevant. |
| T-02-00-02 | Information Disclosure | Test-Snippets | mitigate | JSDoc-Header verlinkt nur PHASE-Referenzen + D-XX, keine API-Keys oder Secrets. Grep-Verifikation in acceptance_criteria stellt sicher. |
| T-02-00-03 | Denial of Service | Jest-Lauf | accept | `it.todo` blockiert keinen Lauf, exit 0 unabhaengig vom Status. Wave 0 kann CI nicht roetlich machen. |
</threat_model>

<verification>
- `npm test -- --testPathPattern='matcher|MatchResultList'` exit 0
- `npm test -- --testPathPattern='matcher|MatchResultList' --listTests 2>&1` listet alle 3 neuen Dateien
- `npx tsc --noEmit` exit 0 (keine Type-Errors durch `it.todo`-only-Files)
- Plan 02-01 und 02-02 koennen die Test-Bodies befuellen (keine erneute Datei-Anlage noetig)
</verification>

<success_criteria>
- 3 Test-Dateien als pending-Skelette existieren
- Jest erkennt alle 3 Dateien (`--listTests` listet sie)
- Jeder `it.todo`-Eintrag referenziert genau ein D-XX aus 02-CONTEXT.md
- `npm test` exit 0 (alle als pending markiert)
- Wave 0 ist abgeschlossen — Plan 02-01 und 02-02 koennen direkt Befuellen
</success_criteria>

<output>
After completion, create `.planning/phases/02-matcher-quality/02-00-SUMMARY.md` with:
- Pfade aller 3 Test-Dateien
- Anzahl `it.todo`-Markierungen pro Datei
- Hinweis fuer Plan 02-01: "matcher.parser.test.ts + matcher.dispatch.test.ts uebernehmen, `it.todo` zu `it` machen, Assertions schreiben"
- Hinweis fuer Plan 02-02: "MatchResultList.test.tsx uebernehmen, Imports von `@/components/Wizard/MatchResultList` und `@/components/Wizard/ClarificationCard` erst nach Plan 02-02-Komponenten-Erstellung gruen"
</output>

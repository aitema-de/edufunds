---
phase: 02-matcher-quality
plan: 01
type: execute
wave: 1
depends_on: ["02-00"]
files_modified:
  - lib/wizard/matcher.ts
  - app/api/match/route.ts
  - __tests__/lib/wizard/matcher.parser.test.ts
  - __tests__/lib/wizard/matcher.dispatch.test.ts
autonomous: true
requirements: [MATCH-02, MATCH-03]
tags: [matcher, backend, llm, parser, tagged-union]

must_haves:
  truths:
    - "lib/wizard/matcher.ts exportiert Tagged-Union `MatchResult = { kind: 'ranking'; matches; costs; totalCandidates; filteredOut } | { kind: 'clarification'; question; costs }` mit Feldname `costs` (nicht `cost`)."
    - "lib/wizard/matcher.ts exportiert `MatchHit` mit `passt_weil: string` + `achtung_bei: string` (kein `begruendung`-Feld mehr)."
    - "lib/wizard/matcher.ts exportiert `MatchInput` mit optionalen Feldern `forceRanking?: boolean` + `previousAnliegen?: string`."
    - "parsePipeMatches verarbeitet 4-Spalten-Format `id|score|passt_weil|achtung_bei` exakt — Zeilen mit Spalten≠4 werden Soft-Failure (kein Throw) verworfen."
    - "Trailing-Pipe `id|score|text|` wird als leeres `achtung_bei: \"\"` geparst."
    - "runMatch dispatched: erste Zeile beginnt mit `CLARIFY|` UND `forceRanking` ist nicht true → return `{ kind: 'clarification', question, costs }`. Sonst → Standard-Ranking-Pfad."
    - "buildUserPrompt fuegt einen `[HINWEIS] ... KEIN CLARIFY ... ranken`-Block an wenn `forceRanking=true`."
    - "buildUserPrompt fuegt einen `URSPRUENGLICHES ANLIEGEN ...`-Block an wenn `previousAnliegen` gesetzt."
    - "MATCHER_SYSTEM-Prompt enthaelt expliziten CLARIFY-Output-Abschnitt mit Positiv- und Negativbeispiel + Slot-Heuristik (mind. 2 von 3 Pflicht-Slots: Bundesland, Zielgruppe, Thema)."
    - "app/api/match/route.ts dispatched die Tagged-Union: `{ kind: 'clarification', ... }` oder `{ kind: 'ranking', matches: [...passt_weil/achtung_bei...] }` als JSON."
    - "Alle bestehenden Caller von matcher.ts/api kompilieren ohne `begruendung`-Field-Reference (TypeScript-Kompilierung exit 0)."
    - "Test-Dateien aus Plan 02-00 sind befuellt: `npm test -- --testPathPattern='wizard/matcher\\.(parser|dispatch)'` exit 0 mit allen Tests grun."
  artifacts:
    - path: "lib/wizard/matcher.ts"
      provides: "Tagged-Union MatchResult, MatchHit mit passt_weil/achtung_bei, MatchInput mit forceRanking+previousAnliegen, parsePipeMatches 4-Spalten, runMatch CLARIFY-Dispatch, MATCHER_SYSTEM-Prompt mit CLARIFY-Format"
      contains: "kind: \"clarification\""
      exports: ["runMatch", "MatchInput", "MatchHit", "MatchResult", "parsePipeMatches"]
    - path: "app/api/match/route.ts"
      provides: "API serialisiert Tagged-Union mit kind-Feld in JSON-Response"
      contains: "result.kind === \"clarification\""
    - path: "__tests__/lib/wizard/matcher.parser.test.ts"
      provides: "Parser-Tests: 8 it() statt it.todo, alle gruen"
      contains: "describe(\"parsePipeMatches"
    - path: "__tests__/lib/wizard/matcher.dispatch.test.ts"
      provides: "Dispatch-Tests: 10+ it() statt it.todo, alle gruen, generateText gemockt"
      contains: "jest.mock(\"@/lib/wizard/llm\""
  key_links:
    - from: "lib/wizard/matcher.ts"
      to: "lib/wizard/llm.ts (generateText)"
      via: "import { MODEL_FLASH, generateText } from \"./llm\""
      pattern: "import.*generateText.*from.*llm"
    - from: "lib/wizard/matcher.ts"
      to: "lib/wizard/pricing.ts (CostLedger)"
      via: "import { addUsage, emptyLedger, type CostLedger } from \"./pricing\""
      pattern: "import.*CostLedger.*pricing"
    - from: "app/api/match/route.ts"
      to: "lib/wizard/matcher.ts"
      via: "import { runMatch, type MatchInput } from \"@/lib/wizard/matcher\""
      pattern: "result\\.kind === \"clarification\""
    - from: "__tests__/lib/wizard/matcher.parser.test.ts"
      to: "lib/wizard/matcher.ts (parsePipeMatches)"
      via: "import { parsePipeMatches }"
      pattern: "import.*parsePipeMatches"
    - from: "__tests__/lib/wizard/matcher.dispatch.test.ts"
      to: "lib/wizard/matcher.ts (runMatch) + Mock auf llm.ts"
      via: "jest.mock(\"@/lib/wizard/llm\") + import { runMatch }"
      pattern: "jest\\.mock.*llm"
---

<objective>
Backend-Migration: Matcher liefert strukturierte Begruendungen (`passt_weil` + `achtung_bei`) statt 15-Wort-Pauschale UND erkennt vage Anliegen mit Klaerungsfrage statt blindem Ranking.

Purpose: MATCH-02 + MATCH-03 implementieren. Backend ist die Quelle der Tagged-Union, Frontend (Plan 02-02) konsumiert sie. Eval-Skript (Plan 02-03) liest sie ebenfalls.
Output: `lib/wizard/matcher.ts` + `app/api/match/route.ts` + 2 gruene Test-Dateien. Alle bestehenden Caller von `begruendung` migriert (D-04 hart-entfernen).
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-matcher-quality/02-CONTEXT.md
@.planning/phases/02-matcher-quality/02-RESEARCH.md
@.planning/phases/02-matcher-quality/02-PATTERNS.md
@.planning/phases/02-matcher-quality/02-VALIDATION.md
@.planning/phases/02-matcher-quality/02-00-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@CLAUDE.md
@lib/wizard/matcher.ts
@app/api/match/route.ts
@lib/wizard/llm.ts
@lib/wizard/pricing.ts

<interfaces>
<!-- Aktuelle Typen die migriert werden + neue Ziel-Typen aus 02-CONTEXT D-08. -->
<!-- Executor: nimm diese Typ-Definitionen wortwoertlich in matcher.ts auf. -->

**IST-Zustand `lib/wizard/matcher.ts` Zeilen 34-54:**
```typescript
export interface MatchInput {
  anliegen: string;
  schulname?: string;
  schultyp?: string;
  bundesland?: string;
  geschaetztesBudgetEur?: number;
}

export interface MatchedProgramm {
  id: string;
  score: number;
  begruendung: string;  // ZU ENTFERNEN
  programm: Foerderprogramm;
}

export interface MatchResult {
  matches: MatchedProgramm[];
  costs: CostLedger;
  totalCandidates: number;
  filteredOut: number;
}
```

**ZIEL-Zustand (D-08 + D-09 + D-04):**
```typescript
export interface MatchInput {
  anliegen: string;
  schulname?: string;
  schultyp?: string;
  bundesland?: string;
  geschaetztesBudgetEur?: number;
  forceRanking?: boolean;            // NEU — D-09
  previousAnliegen?: string;          // NEU — D-09
}

// MatchedProgramm UMBENANNT zu MatchHit (per RESEARCH.md/PATTERNS.md)
export interface MatchHit {
  id: string;
  score: number;
  passt_weil: string;                 // ersetzt begruendung
  achtung_bei: string;                // NEU, kann "" sein
  programm: Foerderprogramm;
}

// Tagged Union — Feldname `costs` (nicht `cost` wie D-08 textuell schreibt — Codebase-Konvention gewinnt)
export type MatchResult =
  | { kind: "ranking"; matches: MatchHit[]; costs: CostLedger; totalCandidates: number; filteredOut: number }
  | { kind: "clarification"; question: string; costs: CostLedger };

// Optional: Backward-Compat-Re-Export fuer Migrations-Phase, kann nach Plan 02-02 entfernt werden:
// export type MatchedProgramm = MatchHit;  // NICHT setzen — D-04 sagt hart entfernen.
```

**IST-Parser (matcher.ts Zeilen 188-207, 3-Spalten):**
```typescript
function parsePipeMatches(text: string, validIds: Set<string>): RawMatch[] {
  // ...indexOf-Logik, alles rechts von idx2 = begruendung
}
interface RawMatch { id: string; score: number; begruendung: string; }
```

**ZIEL-Parser (4-Spalten, Soft-Failure, exportiert fuer Tests):**
```typescript
interface RawMatch {
  id: string;
  score: number;
  passt_weil: string;
  achtung_bei: string;
}

export function parsePipeMatches(text: string, validIds: Set<string>): RawMatch[] {
  const out: RawMatch[] = [];
  for (const rawLine of text.split("\n")) {
    if (/^\s*```/.test(rawLine) || !rawLine.trim()) continue;
    const cleaned = rawLine.trim().replace(/^[-*•]\s+|^\d+[.)]\s+/, "");
    if (cleaned.startsWith("CLARIFY|")) continue; // Dispatch passiert in runMatch
    const parts = cleaned.split("|");
    if (parts.length !== 4) continue; // D-02 Soft-Failure
    const [id, scoreStr, passt_weil, achtung_bei] = parts.map((s) => s.trim());
    const score = parseInt(scoreStr, 10);
    if (!id || !validIds.has(id) || isNaN(score)) continue;
    out.push({ id, score, passt_weil: passt_weil ?? "", achtung_bei: achtung_bei ?? "" });
  }
  return out;
}
```

**CLARIFY-Dispatch (in runMatch, NACH `generateText`-Call, VOR `parsePipeMatches`):**
```typescript
const { value: rawText, usage } = await generateText(
  MODEL_FLASH,
  MATCHER_SYSTEM,
  buildUserPrompt(input, cards),
  { maxTokens: MATCHER_MAX_TOKENS }
);
const costs = addUsage(emptyLedger(), MODEL_FLASH, usage);

const firstLine = rawText.trim().split("\n")[0]?.trim() ?? "";
if (!input.forceRanking && firstLine.startsWith("CLARIFY|")) {
  const question = firstLine.slice("CLARIFY|".length).trim();
  return { kind: "clarification", question, costs };
}

// Bisheriger Ranking-Pfad — return mit kind: "ranking"
const validIds = new Set(programme.map((p) => p.id));
const rawMatches = parsePipeMatches(rawText, validIds);
// ... bisherige Score-Filter + matches-Aufbau ...
return { kind: "ranking", matches, costs, totalCandidates, filteredOut };
```

**buildUserPrompt-Erweiterung (am Ende, vor return):**
```typescript
if (input.forceRanking) {
  ctx.push(`\n[HINWEIS]: Auch wenn das Anliegen vage erscheint — KEIN CLARIFY. Direkt ranken (ggf. mit niedrigen Scores).`);
}
if (input.previousAnliegen) {
  ctx.push(`\nURSPRUENGLICHES ANLIEGEN (Kontext-Referenz):\n${input.previousAnliegen.trim()}`);
}
```

**MATCHER_SYSTEM-Prompt-Erweiterung (D-05/D-06/D-07 + Pipe-Format-Negativbeispiele):**
```
## AUSGABE — zwei moegliche Formen:

### Form A — wenn Anliegen VAGE (mind. 2 von 3 Pflicht-Slots fehlen):
CLARIFY|<konkrete Frage mit konkreten Optionen>

Pflicht-Slots:
- Bundesland: gilt als gefuellt, wenn das Anliegen oder das Schul-Profil ein konkretes Bundesland nennt
- Zielgruppe/Schultyp: gilt als gefuellt, wenn ein Schultyp/Klassenstufe/Schuelergruppe genannt ist
- Thematischer Fokus: gilt als gefuellt, wenn das Anliegen ein klares Themenfeld nennt (z.B. "digital", "Sport", "Lesen", "Inklusion") — NICHT bei mehreren gegensaetzlichen oder keinem

Beispiel CLARIFY:
CLARIFY|Fuer welches Bundesland sucht ihr und welcher Schwerpunkt steht im Vordergrund — z.B. Digitalisierung, Sport, Kultur oder etwas anderes?

### Form B — wenn Anliegen KLAR genug:
id|score|passt_weil|achtung_bei
id|score|passt_weil|achtung_bei

Regeln Form B:
- passt_weil: max ~25 Worte, konkreter Bezug zum Anliegen
- achtung_bei: max ~20 Worte, kann LEER sein (dann Trailing-Pipe!)
- Pipe-Char `|` ist im Text VERBOTEN
- Genau 4 Spalten — bei leerem achtung_bei: `id|score|text|` (Trailing-Pipe)

Beispiele Form B:
bmbf-digitalpakt-2|92|Bundesweite Foerderung digitaler Schulinfrastruktur, deckt Hardware ab.|Antragsfrist naht — Einreichung vor Juli pruefen.
kultur-macht-stark|75|Foerdert ausserschulische Kulturprojekte wie Theater-AGs.|

### NEGATIVBEISPIELE — was NICHT erlaubt ist:
CLARIFY|Was wollt ihr genau?                    <- zu vage, keine Optionen
bmbf-digitalpakt-2|90|Passt.|                   <- passt_weil zu kurz
kultur-macht-stark|85|gut.                      <- FEHLENDE Trailing-Pipe (3 statt 4 Spalten)
prog-x|70|Text mit | im Inhalt.|Achtung.       <- Pipe im Text verboten
```

**Mock-Pattern fuer matcher.dispatch.test.ts:**
```typescript
jest.mock("@/lib/wizard/llm", () => ({
  MODEL_FLASH: "deepseek-chat",
  generateText: jest.fn(),
}));

import { generateText } from "@/lib/wizard/llm";
import { runMatch } from "@/lib/wizard/matcher";

const mockGenerateText = generateText as jest.MockedFunction<typeof generateText>;

beforeEach(() => jest.clearAllMocks());

it("liefert kind=clarification wenn erste Zeile mit CLARIFY|", async () => {
  mockGenerateText.mockResolvedValue({
    value: "CLARIFY|Fuer welches Bundesland sucht ihr?",
    usage: { promptTokens: 100, candidatesTokens: 50 },
  });
  const result = await runMatch({ anliegen: "Wir brauchen irgendwas." });
  expect(result.kind).toBe("clarification");
  if (result.kind === "clarification") {
    expect(result.question).toContain("Bundesland");
  }
});
```

**Aktuelle API-Route `app/api/match/route.ts` Zeilen 22-38:**
```typescript
const result = await runMatch({ anliegen: body.anliegen, /* ... */ });
return NextResponse.json({
  matches: result.matches.map((m) => ({ id, score, begruendung, programm })),
  totalCandidates: result.totalCandidates,
  filteredOut: result.filteredOut,
  costs: result.costs,
});
```

**ZIEL-API-Route:**
```typescript
const result = await runMatch({
  anliegen: body.anliegen,
  schulname: body.schulname,
  schultyp: body.schultyp,
  bundesland: body.bundesland,
  geschaetztesBudgetEur: body.geschaetztesBudgetEur,
  forceRanking: body.forceRanking,
  previousAnliegen: body.previousAnliegen,
});

if (result.kind === "clarification") {
  return NextResponse.json({
    kind: "clarification",
    question: result.question,
    costs: result.costs,
  });
}

return NextResponse.json({
  kind: "ranking",
  matches: result.matches.map((m) => ({
    id: m.id,
    score: m.score,
    passt_weil: m.passt_weil,
    achtung_bei: m.achtung_bei,
    programm: { /* alle bestehenden Programm-Felder unveraendert */ },
  })),
  totalCandidates: result.totalCandidates,
  filteredOut: result.filteredOut,
  costs: result.costs,
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: matcher.ts — Tagged Union, MatchHit, parsePipeMatches 4-Spalten, MATCHER_SYSTEM-Prompt</name>
  <files>lib/wizard/matcher.ts, __tests__/lib/wizard/matcher.parser.test.ts, __tests__/lib/wizard/matcher.dispatch.test.ts</files>
  <read_first>
    - lib/wizard/matcher.ts (vollstaendig — alle 280+ Zeilen, da praktisch jede Sektion angefasst wird)
    - .planning/phases/02-matcher-quality/02-CONTEXT.md (D-01 bis D-09)
    - .planning/phases/02-matcher-quality/02-RESEARCH.md Sektionen 1, 2, 3, 8 (Prompt + Parser + Pitfalls)
    - .planning/phases/02-matcher-quality/02-PATTERNS.md (Block "lib/wizard/matcher.ts")
    - __tests__/lib/wizard/matcher.parser.test.ts (Skelett aus Plan 02-00 — `it.todo` zu `it` machen)
    - __tests__/lib/wizard/matcher.dispatch.test.ts (Skelett aus Plan 02-00)
    - __tests__/lib/wizard/facts-extractor.test.ts (Mock-Pattern)
  </read_first>
  <behavior>
    Test-Erwartungen vor Implementierung definieren (TDD-RED):

    `matcher.parser.test.ts`:
    - Test 1 (`D-01`): `parsePipeMatches("prog-a|85|Guter Match.|Frist beachten.", new Set(["prog-a"]))` returns `[{ id: "prog-a", score: 85, passt_weil: "Guter Match.", achtung_bei: "Frist beachten." }]`
    - Test 2 (`D-02`, 3 Spalten): `parsePipeMatches("prog-a|85|Kein viertes Feld", new Set(["prog-a"]))` returns `[]` (Soft-Failure, kein Throw)
    - Test 3 (`D-02`, 5 Spalten): `parsePipeMatches("prog-a|85|a|b|c", new Set(["prog-a"]))` returns `[]`
    - Test 4 (`D-01`, Trailing-Pipe): `parsePipeMatches("prog-a|85|Match.|", new Set(["prog-a"]))` returns `[{ ..., achtung_bei: "" }]`
    - Test 5 (`D-05`): `parsePipeMatches("CLARIFY|Welches Bundesland?", new Set(["prog-a"]))` returns `[]` (CLARIFY wird nicht im Parser, sondern in runMatch dispatched)
    - Test 6: Code-Fence-Zeile (` ``` `) wird ignoriert
    - Test 7: unbekannte programmId (nicht in validIds) wird verworfen
    - Test 8: nicht-numerischer Score (NaN) wird verworfen

    `matcher.dispatch.test.ts` (mit `jest.mock("@/lib/wizard/llm")`):
    - Test 1 (`D-05/D-08`): mockGenerateText liefert `"CLARIFY|Welches Bundesland?"` → `result.kind === "clarification"` UND `result.question.includes("Bundesland")`
    - Test 2 (`D-05`): question wird korrekt extrahiert (slice nach `CLARIFY|`)
    - Test 3 (`D-08`): mockGenerateText liefert `"prog-a|85|Match.|"` → `result.kind === "ranking"`
    - Test 4 (`D-05`): mockGenerateText liefert `"prog-a|85|Match.|\nCLARIFY|spaeter"` → `result.kind === "ranking"` (nur erste Zeile zaehlt)
    - Test 5 (`D-09`): `forceRanking: true` UND mockGenerateText liefert `"CLARIFY|..."` → `result.kind === "ranking"` (forceRanking unterdrueckt Dispatch)
    - Test 6 (`D-09`): `forceRanking: false` (default) → CLARIFY wird respektiert
    - Test 7: `buildUserPrompt`-Aufruf-Inspektion: bei `forceRanking: true` enthaelt der userPrompt-Argument-String "[HINWEIS]" + "KEIN CLARIFY"
    - Test 8 (`D-09`): bei `previousAnliegen: "alt"` enthaelt userPrompt "URSPRUENGLICHES ANLIEGEN" + "alt"
    - Test 9 (`costs`-Konvention): `result.costs.eurCents` existiert, `(result as any).cost` ist undefined
    - Test 10 (`costs`-Konvention bei clarification): gleiche Pruefung
  </behavior>
  <action>
    **Schritt 1: TDD-RED.** Befuelle die `it.todo`-Eintraege in matcher.parser.test.ts und matcher.dispatch.test.ts mit echten Tests gemaess `<behavior>`. Nutze die Mock-Pattern aus `<interfaces>`. `npm test -- --testPathPattern='wizard/matcher\\.(parser|dispatch)'` MUSS jetzt rot sein (parsePipeMatches existiert in alter 3-Spalten-Form, runMatch hat noch keinen CLARIFY-Dispatch).

    **Schritt 2: TDD-GREEN — matcher.ts migrieren.**

    a) Imports unveraendert lassen.

    b) Konstanten unveraendert (`MAX_LLM_CANDIDATES`, `MAX_MATCHES`, `MATCHER_MAX_TOKENS`). Hinweis-Kommentar beim `MATCHER_MAX_TOKENS` aktualisieren auf "4 Spalten + ggf. CLARIFY-Variante".

    c) **Interface-Block ersetzen** (Zeilen 34-54) durch die ZIEL-Zustand-Definitionen aus `<interfaces>`:
       - `MatchInput` mit neuen optionalen Feldern `forceRanking?: boolean` + `previousAnliegen?: string`
       - `MatchedProgramm` UMBENENNEN zu `MatchHit`, `begruendung` ENTFERNEN, `passt_weil: string` + `achtung_bei: string` HINZUFUEGEN
       - `MatchResult` als discriminated union (`type` statt `interface`) mit Feldname `costs`
       - **WICHTIG:** Falls bestehender Code im Repository (ausserhalb route.ts) `MatchedProgramm` importiert, muss in dieser Migration auch dort umbenannt werden — `grep -rn "MatchedProgramm\\|matcher.ts.*begruendung" --include="*.ts" --include="*.tsx"` ausfuehren VOR der Migration und Liste in Task abarbeiten. Erwartung laut RESEARCH.md: nur `app/api/match/route.ts` referenziert das.

    d) `RawMatch`-Interface (intern, nicht exportiert) auf 4-Spalten umstellen.

    e) **`parsePipeMatches` 1:1 ersetzen** durch ZIEL-Parser aus `<interfaces>` (mit `export function`, damit Tests importieren koennen).

    f) **`MATCHER_SYSTEM`-Prompt erweitern** mit dem CLARIFY-Form-A/Form-B-Block + Negativbeispielen aus `<interfaces>`. Die bestehende Anweisung "Antworte NUR im Format..." durch den neuen zwei-Form-Block ersetzen.

    g) **`buildUserPrompt`** am Ende vor `return ctx.join("\n")` (oder aequivalent) um die zwei Conditional-Bloecke (`forceRanking` + `previousAnliegen`) erweitern.

    h) **`runMatch`** umbauen:
       - Nach `generateText`-Call (rawText + usage erhalten): `const costs = addUsage(emptyLedger(), MODEL_FLASH, usage)`
       - **CLARIFY-Dispatch eingefuegen** (siehe `<interfaces>` Block "CLARIFY-Dispatch"):
         ```typescript
         const firstLine = rawText.trim().split("\n")[0]?.trim() ?? "";
         if (!input.forceRanking && firstLine.startsWith("CLARIFY|")) {
           const question = firstLine.slice("CLARIFY|".length).trim();
           return { kind: "clarification", question, costs };
         }
         ```
       - Restlicher Ranking-Pfad: `parsePipeMatches` mit neuen Feldern, `matches.push({ id, score, passt_weil: m.passt_weil, achtung_bei: m.achtung_bei, programm })`
       - Final return: `return { kind: "ranking", matches, costs, totalCandidates, filteredOut };`

    i) **`MatchedProgramm`-Type entfernen** (D-04 hart). Falls TypeScript-Errors zeigen, dass externer Code es noch nutzt → ebenfalls migrieren oder Backward-Compat-`type MatchedProgramm = MatchHit;` als Notbremse — ABER die Default-Erwartung ist hartes Entfernen wegen D-04. Logge Befund in SUMMARY.

    **Schritt 3: TDD-GREEN verifizieren.** Tests sollen jetzt gruen sein.

    **Schritt 4: REFACTOR (optional).** Falls nach Migration toter Code uebrig ist (alte `idx1/idx2`-Logik, ungenutzte Imports), entfernen. Keine Verhaltensaenderung.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='wizard/matcher\.(parser|dispatch)' 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export type MatchResult =' lib/wizard/matcher.ts >= 1`
    - `grep -c 'kind: \"clarification\"' lib/wizard/matcher.ts >= 1`
    - `grep -c 'kind: \"ranking\"' lib/wizard/matcher.ts >= 1`
    - `grep -c 'export interface MatchHit' lib/wizard/matcher.ts == 1`
    - `grep -c 'passt_weil' lib/wizard/matcher.ts >= 5` (Interface, Parser, runMatch, MatchHit, RawMatch)
    - `grep -c 'achtung_bei' lib/wizard/matcher.ts >= 5`
    - `grep -c 'forceRanking' lib/wizard/matcher.ts >= 3` (MatchInput, runMatch-Check, buildUserPrompt-Block)
    - `grep -c 'previousAnliegen' lib/wizard/matcher.ts >= 2`
    - `grep -c 'CLARIFY|' lib/wizard/matcher.ts >= 4` (Prompt-Beispiele + Dispatch-Check)
    - `grep -c 'mind. 2 von 3' lib/wizard/matcher.ts >= 1` (Slot-Heuristik im Prompt) — alternativer Wortlaut akzeptabel: `grep -cE '2.von.3|zwei.von.drei' lib/wizard/matcher.ts >= 1`
    - `grep -c 'export function parsePipeMatches' lib/wizard/matcher.ts == 1` (export fuer Tests)
    - `grep -c 'parts.length !== 4' lib/wizard/matcher.ts >= 1` (D-02 Soft-Failure)
    - `grep -c 'begruendung' lib/wizard/matcher.ts == 0` (D-04 hart entfernt — null Vorkommen)
    - `grep -c 'export interface MatchedProgramm\\|export type MatchedProgramm' lib/wizard/matcher.ts == 0` (D-04 hart entfernt)
    - **costs-vs-cost-Pruefung:** `grep -cE '\\bcost:\\s*CostLedger' lib/wizard/matcher.ts == 0` UND `grep -c 'costs:' lib/wizard/matcher.ts >= 2`
    - matcher.parser.test.ts hat 8 `it(`-Eintraege (kein `it.todo` mehr): `grep -c 'it\\.todo' __tests__/lib/wizard/matcher.parser.test.ts == 0` UND `grep -cE 'it\\((\"|\")' __tests__/lib/wizard/matcher.parser.test.ts >= 8`
    - matcher.dispatch.test.ts hat mind. 10 `it(`-Eintraege: `grep -c 'it\\.todo' __tests__/lib/wizard/matcher.dispatch.test.ts == 0` UND `grep -cE 'it\\(' __tests__/lib/wizard/matcher.dispatch.test.ts >= 10`
    - matcher.dispatch.test.ts hat `jest.mock("@/lib/wizard/llm"`: `grep -c 'jest.mock.*lib/wizard/llm' __tests__/lib/wizard/matcher.dispatch.test.ts == 1`
    - `npm test -- --testPathPattern='wizard/matcher\\.(parser|dispatch)' --silent` exit 0 mit allen 18+ Tests gruen
    - `npx tsc --noEmit` exit 0 (keine Type-Errors durch Migration)
  </acceptance_criteria>
  <done>
    matcher.ts liefert Tagged-Union, parsePipeMatches handhabt 4 Spalten + Soft-Failure, runMatch dispatched CLARIFY, MATCHER_SYSTEM-Prompt enthaelt zwei-Form-Output mit Negativbeispielen, alle Tests in matcher.parser.test.ts + matcher.dispatch.test.ts sind gruen, `begruendung`-Feld ist hart entfernt.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: app/api/match/route.ts — Tagged-Union-Dispatch im JSON-Response</name>
  <files>app/api/match/route.ts</files>
  <read_first>
    - app/api/match/route.ts (vollstaendig — 49 Zeilen)
    - lib/wizard/matcher.ts (nach Task 1 migriert) — `MatchResult`-Union + neue Felder in `MatchInput`
    - .planning/phases/02-matcher-quality/02-CONTEXT.md (D-08 + D-09)
    - .planning/phases/02-matcher-quality/02-PATTERNS.md (Block "app/api/match/route.ts")
    - .planning/phases/02-matcher-quality/02-RESEARCH.md Sektion 3 ("Backward-Compat-Stolperstein in route.ts")
  </read_first>
  <action>
    **Schritt 1:** `app/api/match/route.ts` Zeile 22-38 ersetzen durch das ZIEL-Pattern aus `<interfaces>`:

    a) Im `runMatch(...)`-Aufruf die neuen optionalen Felder `forceRanking: body.forceRanking` und `previousAnliegen: body.previousAnliegen` hinzufuegen (Body wird als `Partial<MatchInput>` typisiert, also kommen sie automatisch durch).

    b) Nach `const result = await runMatch(...)` einen Union-Dispatch einfuegen:
       ```typescript
       if (result.kind === "clarification") {
         return NextResponse.json({
           kind: "clarification",
           question: result.question,
           costs: result.costs,
         });
       }
       ```

    c) Den anschliessenden ranking-Response erweitern:
       ```typescript
       return NextResponse.json({
         kind: "ranking",
         matches: result.matches.map((m) => ({
           id: m.id,
           score: m.score,
           passt_weil: m.passt_weil,
           achtung_bei: m.achtung_bei,
           programm: {
             id: m.programm.id,
             name: m.programm.name,
             foerdergeber: (m.programm as any).foerdergeber,
             // ... ALLE bestehenden Programm-Felder aus den IST-Zeilen 29-35 unveraendert kopieren
           },
         })),
         totalCandidates: result.totalCandidates,
         filteredOut: result.filteredOut,
         costs: result.costs,
       });
       ```

    d) `begruendung`-Feld in der Response-Zuordnung hart entfernen (D-04). Im Response-Body taucht `passt_weil` + `achtung_bei` auf, nicht `begruendung`.

    e) Error-Handling-Block (Zeilen 41-47) UNVERAENDERT lassen — dieselbe `console.error` + `NextResponse.json({ error })`-Logik.

    f) Type-Annotation: `Partial<MatchInput>` muss jetzt auch `forceRanking` und `previousAnliegen` akzeptieren (kommt automatisch von Task 1's Interface-Erweiterung).

    **Schritt 2:** Smoke-Verifikation (manuell oder via curl-Mock):
    Da `npm run dev` einen LLM-Call ausloest (Kosten + Zeit), reicht der TypeScript-Check + grep-Validation als Verify. Live-Smoke wird in Plan 02-03 (Eval-Live-Run) ohnehin durchgefuehrt.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'result.kind === \"clarification\"' app/api/match/route.ts >= 1`
    - `grep -c 'kind: \"clarification\"' app/api/match/route.ts >= 1` (Response-Body)
    - `grep -c 'kind: \"ranking\"' app/api/match/route.ts >= 1` (Response-Body)
    - `grep -c 'passt_weil: m.passt_weil' app/api/match/route.ts == 1`
    - `grep -c 'achtung_bei: m.achtung_bei' app/api/match/route.ts == 1`
    - `grep -c 'forceRanking' app/api/match/route.ts >= 1` (durchgereicht in runMatch-Aufruf)
    - `grep -c 'previousAnliegen' app/api/match/route.ts >= 1`
    - `grep -c 'begruendung' app/api/match/route.ts == 0` (D-04 hart entfernt)
    - `grep -c 'console.error' app/api/match/route.ts >= 1` (Error-Handler unveraendert)
    - **Programm-Felder vollstaendig kopiert:** `grep -c 'foerdersummeText\\|bewerbungsfristText\\|kategorien' app/api/match/route.ts >= 2` (mind. zwei der bestehenden optionalen Felder durchgereicht — exakt wie IST-Zustand)
    - `npx tsc --noEmit` exit 0 (kein Type-Mismatch zwischen Matcher und API)
    - `npm test` exit 0 (keine bestehenden Tests gebrochen)
  </acceptance_criteria>
  <done>
    API-Route dispatched die Tagged-Union sauber, Response-Body enthaelt `kind`-Feld, alle Programm-Felder unveraendert durchgereicht, `begruendung` hart entfernt, TypeScript kompiliert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API (`POST /api/match`) | Untrusted JSON-Body — `anliegen`-String muss validiert werden, `forceRanking`/`previousAnliegen` koennen Prompt-Inhalt beeinflussen |
| API → LLM (DeepSeek) | `previousAnliegen` und `anliegen` flossen vorher schon in den Prompt — neues Feld erweitert die Prompt-Surface, aber nicht die Trust-Klasse |
| LLM → Parser (`parsePipeMatches`) | LLM-Output ist nicht-deterministisch — Parser MUSS Soft-Failure handhaben (D-02), nicht crashen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `forceRanking`-Feld im Client-Request | mitigate | Type-Coercion in route.ts: `body.forceRanking` ist `Partial<MatchInput>`, TypeScript ignoriert nicht-boolean Werte. Falsy-Werte (undefined, false, "") fallen auf default false. Kein Risiko von Forced-Bypass — `forceRanking` schadet nur dem User selbst, falls bewusst gesetzt. |
| T-02-01-02 | Tampering | `previousAnliegen`-Feld als Prompt-Injection | mitigate | `previousAnliegen` wird als separater Block in den User-Prompt eingebettet (nicht im System-Prompt). Bestehender `anliegen`-String hat dasselbe Risiko und wird seit Phase 1 akzeptiert. Keine separate Sanitization noetig — DeepSeek ist gegen klassische Prompt-Injection robust durch Pipe-Format-Anchoring. Empfehlung: in PROJECT.md Threat-Register als bestehende Akzeptanz dokumentieren. |
| T-02-01-03 | Information Disclosure | `previousAnliegen` aus DB-Persist | accept | Aus 02-CONTEXT.md `<canonical_refs>` "Match-Results werden NICHT persistiert" — keine DB-Tabelle. Daten bleiben in-memory zwischen Matcher und Frontend. |
| T-02-01-04 | Denial of Service | LLM-Output mit 1000+ defekten Pipe-Zeilen | mitigate | Soft-Failure-Pattern in parsePipeMatches: jede defekte Zeile wird via `continue` uebersprungen, keine Exceptions. `MAX_MATCHES = 3` Hard-Cap im post-processing — auch bei pathologischem Output bleibt Antwort O(3). |
| T-02-01-05 | Spoofing | LLM erfindet `programmId` (z.B. `prog-fake`) | mitigate | `validIds`-Set-Filter in parsePipeMatches verwirft unbekannte IDs (bestehende Logik unveraendert von Phase 1). Test-Fall in matcher.parser.test.ts deckt das ab. |
| T-02-01-06 | Tampering | LLM injiziert `CLARIFY\|` in normaler Zeile | accept | Erste-Zeile-Check in runMatch: nur wenn die ALLERERSTE Zeile mit `CLARIFY\|` beginnt, dispatched. Spaetere Zeilen mit `CLARIFY\|`-Praefix landen im Parser-Loop, der sie ueberspringt (siehe parsePipeMatches `if (cleaned.startsWith("CLARIFY|")) continue`). Test-Fall in matcher.dispatch.test.ts deckt es ab. |
| T-02-01-07 | Repudiation | LLM-Output-Logs | accept | Bestehender `console.warn`-Pfad bleibt unveraendert. Phase-2-Aenderung bringt keine neuen Audit-Anforderungen. |
</threat_model>

<verification>
- `npm test -- --testPathPattern='wizard/matcher\\.(parser|dispatch)'` exit 0 mit 18+ gruenen Tests
- `npx tsc --noEmit` exit 0
- `grep -c 'begruendung' lib/wizard/matcher.ts app/api/match/route.ts == 0` (D-04 hart-entfernt)
- `grep -rn 'MatchedProgramm' --include='*.ts' --include='*.tsx' .` zeigt keine Treffer (oder nur SUMMARY-Notizen, kein Code)
- Plan 02-02 (Frontend) und 02-03 (Eval+Korpus) koennen die neue Tagged-Union konsumieren
</verification>

<success_criteria>
- MATCH-02: Matcher liefert strukturierte Begruendung (passt_weil + achtung_bei) — verifiziert via parser.test
- MATCH-03: Matcher erkennt vages Anliegen via CLARIFY-Trigger — verifiziert via dispatch.test
- API-Route serialisiert Tagged-Union mit `kind`-Feld
- Soft-Failure-Strategie haelt: defekte Zeilen werfen kein Exception
- forceRanking-Override wirkt korrekt
- Alle bestehenden Caller migriert (kein `begruendung` im Repo unter `lib/`, `app/`, `__tests__/`)
- TypeScript strict kompiliert
</success_criteria>

<output>
After completion, create `.planning/phases/02-matcher-quality/02-01-SUMMARY.md` with:
- Pfade aller geaenderter Dateien + Zeilenzahlen
- Liste der entfernten Symbole (`begruendung`-Feld, `MatchedProgramm`-Interface)
- Liste der neuen Symbole (`MatchHit`, `MatchResult`-Union, `parsePipeMatches`-export)
- Nutzungs-Hinweis fuer Plan 02-02 (Frontend): "Konsumiert `body.kind` aus `/api/match`-Response. Switch auf `kind` in StartClient.tsx noetig."
- Nutzungs-Hinweis fuer Plan 02-03 (Eval): "result.matches[].passt_weil + result.matches[].achtung_bei statt result.matches[].begruendung. Snapshots-Shim fuer alte begruendung-Snapshots dokumentieren oder skippen (siehe 02-RESEARCH.md Pitfall 3)."
- Anzahl Tests gruen
- Falls externer Code `MatchedProgramm` referenzierte: Liste der zusaetzlich migrierten Dateien
</output>

---
phase: 02-matcher-quality
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/wizard/matcher.ts
autonomous: true
gap_closure: true
requirements: [MATCH-02, MATCH-03]
tags: [matcher, prompt-tuning, recall, clarification]

must_haves:
  truths:
    - "MATCHER_SYSTEM-Prompt enthält explizite De-Drift-Regeln für ev-013 (Mathe-Wettbewerbe ohne BL) + ev-016 (Mehrsprachige Bibliothek NRW): thematische Domain hat Vorrang vor Geräte-/Inklusions-Drift, Score-Filter `< 50` rejected explizit Off-Topic-Programme"
    - "MATCHER_SYSTEM-Prompt verschärft Slot-Heuristik: ev-019 (mehrere widersprüchliche Themen Theater/MINT/Austausch) und ev-026 (Inklusion+Demokratielernen+Internationalisierung als 'mehrere widersprüchliche Themen') triggern CLARIFY zuverlässig"
    - "parsePipeMatches verwirft Score < 0 oder > 100 (WR-05 Robustness-Loch geschlossen)"
    - "Phase-2-Eval-Re-Run nach Prompt-Tuning erreicht: Recall@3 ≥ 0.42, Clarif-Precision ≥ 80% (verifiziert in Plan 02-06, hier nur Implementierung)"
  artifacts:
    - path: "lib/wizard/matcher.ts"
      provides: "Verschärfter MATCHER_SYSTEM-Prompt + Score-Range-Filter"
      contains: "MATCHER_SYSTEM"
      min_lines: 354
  key_links:
    - from: "lib/wizard/matcher.ts MATCHER_SYSTEM"
      to: "DeepSeek deepseek-chat via lib/wizard/llm.ts"
      via: "generateText(MODEL_FLASH, MATCHER_SYSTEM, ...)"
      pattern: "generateText\\(\\s*MODEL_FLASH"
    - from: "lib/wizard/matcher.ts parsePipeMatches"
      to: "Score-Range-Validierung 0-100"
      via: "score < 0 || score > 100 → continue"
      pattern: "score\\s*<\\s*0|score\\s*>\\s*100"
---

<objective>
Recall@3 von 0.342 → ≥ 0.42 und Clarif-Precision von 62.5% → ≥ 80% durch gezieltes MATCHER_SYSTEM-Prompt-Tuning anheben. Plus WR-05 (Score-Range-Filter in parsePipeMatches) als Robustness-Patch im selben File.

Purpose: Phase-2-Threshold-Gate (D-16/D-17) läuft grün, ohne dass die Mechanik des Eval-Apparats geändert wird. Tuning-Hebel sitzen am Prompt — Architektur, Tagged-Union, Parser-Kontrakt bleiben unverändert.

Output: Aktualisierter MATCHER_SYSTEM-Prompt in `lib/wizard/matcher.ts` + Score-Range-Filter in `parsePipeMatches`. Verifikation des Tuning-Erfolgs erfolgt in Plan 02-06 (Live-Eval-Re-Run).
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-matcher-quality/02-CONTEXT.md
@.planning/phases/02-matcher-quality/02-RESEARCH.md
@.planning/phases/02-matcher-quality/02-VERIFICATION.md
@.planning/phases/02-matcher-quality/02-REVIEW.md
@.planning/phases/02-matcher-quality/02-01-SUMMARY.md
@.planning/phases/02-matcher-quality/02-03-SUMMARY.md
@data/eval/BASELINE.md
@data/eval/reports/2026-05-04-08-07-48.md
@CLAUDE.md

<interfaces>
<!-- Diese Typen + Konstanten existieren bereits in lib/wizard/matcher.ts. -->
<!-- Executor MUSS gegen diese Kontrakte arbeiten — KEIN Re-Architecting. -->

```typescript
// lib/wizard/matcher.ts — vorhandene Exports
export const MAX_MATCHES = 3;            // Anzahl Treffer-Zeilen (Form B)
export const MATCHER_MAX_TOKENS = 400;   // Output-Token-Cap pro Call
export interface MatchHit {
  id: string;
  score: number;
  passt_weil: string;
  achtung_bei: string;
  programm: Foerderprogramm;
}
export type MatchResult =
  | { kind: "ranking"; matches: MatchHit[]; costs: CostLedger; totalCandidates: number; filteredOut: number }
  | { kind: "clarification"; question: string; costs: CostLedger };
export function parsePipeMatches(text: string, validIds: Set<string>): RawMatch[]; // Zeile 264-290
export async function runMatch(input: MatchInput): Promise<MatchResult>;            // Zeile 292-354
```

```typescript
// MATCHER_SYSTEM beginnt in matcher.ts:164 und endet :214 — knapp 50 Zeilen.
// runMatch dispatcht CLARIFY VOR Parser (matcher.ts:321-325).
// score < 50 wird in runMatch (matcher.ts:333) gefiltert, NICHT in parsePipeMatches.
```
</interfaces>

<eval_diagnose>
<!-- Live-Eval 2026-05-04-08-07-48 Diagnose-Snapshot der Misses (Quelle: data/eval/reports/2026-05-04-08-07-48.json + matcher-korpus.json) -->

### Recall-Misses (4 Beispiele aus n=19 non-edge):

**ev-013 (kurz, Mathe-Wettbewerbe, Schultyp=gymnasium, BL=null):**
- Anliegen: "Mathe-Wettbewerbe fuer unsere Mittelstufe ausbauen — Anmeldegebuehren, Material, Pokale."
- Expected: kaenguru-der-mathematik, mathe-im-advent, bundeswettbewerb-mathematik
- Actual: playmobil-hobpreis, ferry-porsche-challenge, claussen-simon-fonds-schulen-2026
- Recall: 0 — thematische Drift (Wettbewerbe statt Mathe-Wettbewerbe), allgemeine Schul-Wettbewerbe schlagen Domain-spezifische Mathe-Wettbewerbe.

**ev-015 (kurz, Schul-Aquarium NABU, BL=null):**
- Anliegen: "Wir wollen ein Schul-Aquarium und naturkundliche Exkursionen an einen NABU-Standort."
- Expected: nabu-schulen, bfn-artenvielfalt, stiftung-kinder-forschen
- Actual: kultur-macht-stark, klimalab-2026, aktion-mensch-schulkooperation
- Recall: 0 — thematische Drift trotz expliziter NABU-Erwähnung im Anliegen.

**ev-016 (kurz, Mehrsprachige Bibliothek NRW):**
- Anliegen: "Mehrsprachige Bibliothek aufbauen — 600 Buecher in Tuerkisch, Arabisch, Polnisch, Russisch."
- Expected: lesen-macht-stark, sprache-macht-stark, mercator-integration
- Actual: [] (leeres Ranking)
- Recall: 0 — Matcher findet KEIN passendes Programm trotz vorhandenem Sprache/Lesen/Integration-Bezug.

**ev-012 (kurz, Bewegungs-Pausenhof Niedersachsen):**
- Anliegen: "Wir wollen einen Bewegungs-Pausenhof bauen und brauchen Geraete fuer 180 Grundschulkinder."
- Expected: niedersachsen-sport, dkjs-sport, baywa-laufen-wald
- Actual: [] (leeres Ranking)
- Recall: 0 — Matcher findet KEIN passendes Sport-Programm trotz BL+Schultyp+Thema klar.

### Clarif-Misses (3 von 8 expected_clarification=true):

**ev-003 (vag, schultyp=null, BL=null, expected_missing_slots=[bundesland, zielgruppe, thema]):**
- Anliegen: "Wir wollen unsere Schule besser machen und brauchen dafuer Geld. Es gibt viele Ideen, aber nichts Konkretes — vielleicht etwas mit Sport oder Kultur oder so."
- Actual: leeres Ranking (kein CLARIFY) — Matcher liefert leere Liste statt Klärungsfrage.

**ev-019 (vag, schultyp=gymnasium, BL=Baden-Wuerttemberg, expected_missing_slots=[thema]):**
- Anliegen: "Wir haben mehrere Ideen — Theater, AG fuer Naturwissenschaften, vielleicht ein Austausch nach Frankreich. Was passt wirtschaftlich?"
- Actual: leeres Ranking (kein CLARIFY) — bei mehreren widersprüchlichen Themen sollte Matcher CLARIFY triggern.

**ev-026 (vag, schultyp=gesamtschule, BL=Hessen, expected_missing_slots=[thema]):**
- Anliegen: "Wir wollen unsere Schule weiterentwickeln — irgendwas zwischen Inklusion, Demokratielernen und vielleicht Internationalisierung. Wir wissen noch nicht, wo der Hebel am groessten ist."
- Actual: 3 Treffer-Cards (false negative für CLARIFY) — Matcher rankt obwohl 3 widersprüchliche Themen genannt.

### Tuning-Hypothesen (aus 02-RESEARCH.md Pitfall 2 + BASELINE.md Phase-1-Beobachtung):

1. **Recall**: Prompt-Sektion "Kriterien" verschärfen — explizit fordern, dass thematische Passung Vorrang vor allgemeinen Schul-Wettbewerben/Inklusions-Drift hat. Beispiel-Drift im Negativblock dokumentieren.
2. **Clarif-Precision**: Slot-Heuristik schärfen — "thematischer Fokus gilt als gefuellt" muss negieren bei "mehreren widersprüchlichen oder unentschiedenen Themen". Positivbeispiel "Theater oder MINT oder Austausch — was passt?" → CLARIFY.
3. **Empty-Ranking-Defekte (ev-012, ev-016)**: Matcher liefert [] obwohl klare Domain-Programme existieren. Ursache vermutlich `score < 50`-Filter zu strikt — Prompt sollte explizit fordern: "Wenn ein Programm thematisch klar passt, score ≥ 60 setzen, nicht künstlich niedrig."
</eval_diagnose>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: MATCHER_SYSTEM-Prompt-Tuning für Recall + Clarif-Precision + WR-05 Score-Range-Filter</name>
  <read_first>
    - lib/wizard/matcher.ts (gesamtes File 1-354, fokus auf MATCHER_SYSTEM Zeile 164-214 + parsePipeMatches Zeile 264-290)
    - data/eval/reports/2026-05-04-08-07-48.json (Diagnose der Misses, oben im Plan-Kontext zusammengefasst)
    - data/eval/matcher-korpus.json (ev-003, ev-012, ev-013, ev-015, ev-016, ev-019, ev-026 zur Validierung der Tuning-Hypothesen)
    - .planning/phases/02-matcher-quality/02-RESEARCH.md (Pitfall 2 + Section 1 Slot-Heuristik)
    - .planning/phases/02-matcher-quality/02-REVIEW.md (WR-05 Detailbeschreibung Zeile 142-156)
  </read_first>
  <files>lib/wizard/matcher.ts</files>
  <action>
**Teil A — MATCHER_SYSTEM-Prompt-Verschärfung (Zeile 164-214):**

1. **Sektion "## Kriterien" erweitern** um expliziten Drift-Block. Aktuell:
   ```
   ## Kriterien (Reihenfolge = Gewicht)
   1. Thematische Passung  2. Formale Passung (Schultyp, Bundesland, Summe)
   3. Praktikabilitaet  4. Wirkungstiefe
   ```
   Ersetzen durch:
   ```
   ## Kriterien (Reihenfolge = Gewicht)
   1. Thematische Passung — domain-spezifisch hat IMMER Vorrang vor allgemein
      (z.B. "Mathe-Wettbewerbe" → Mathe-spezifische Programme schlagen
      generische Schul-Wettbewerbe; "NABU-Standort" → NABU/Natur-Programme
      schlagen Klima/Inklusion; "Mehrsprachige Buecher" → Sprache/Lesen-Programme
      schlagen allgemeine Bibliotheks-Programme)
   2. Formale Passung (Schultyp, Bundesland, Summe)
   3. Praktikabilitaet  4. Wirkungstiefe

   ## Wichtig — KEIN Drift in Defaults
   - Bei klar thematischer Anfrage NICHT auf "aktion-mensch-schulkooperation"
     zurueckfallen, wenn das Anliegen keinen Inklusions-Bezug hat
   - Bei klar thematischer Anfrage NICHT auf "bmbf-digitalpakt-2" zurueckfallen,
     wenn das Anliegen keinen Digital/Hardware-Bezug hat
   - Bei klar thematischer Anfrage (Mathe/Sport/Kultur/Sprache/Natur) IMMER
     mind. ein Domain-spezifisches Programm im Top-3, falls eines existiert
   - Wenn ein Programm thematisch klar passt, Score >= 60 setzen — NICHT
     kuenstlich niedrig, weil andere formale Aspekte fehlen (BL/Schultyp).
     Lieber Score 65 mit "achtung_bei: Bundesland im Profil ergaenzen" als
     leere Liste.
   ```

2. **Sektion "## AUSGABE — Form A" Slot-Heuristik schärfen** (aktuell Zeile 182-190).
   Aktuell:
   ```
   - Thematischer Fokus: gilt als gefuellt, wenn das Anliegen ein klares Themenfeld nennt
     (z.B. "digital", "Sport", "Lesen", "Inklusion", "Kultur") — NICHT bei mehreren
     gegensaetzlichen oder keinem.
   ```
   Ersetzen durch:
   ```
   - Thematischer Fokus: gilt als gefuellt, wenn das Anliegen GENAU EIN klares
     Themenfeld nennt (z.B. "digital", "Sport", "Lesen", "Inklusion", "Kultur").
     Gilt als FEHLEND in folgenden Faellen:
     (a) keines genannt
     (b) mehrere widersprueliche genannt ("Sport, Kultur und Digitales")
     (c) drei oder mehr unentschiedene Optionen ("Theater, MINT-AG oder
         Austausch — was passt wirtschaftlich?")
     (d) Phrasen wie "irgendwas zwischen X und Y", "wir wissen noch nicht
         wo der Hebel am groessten ist", "viele Ideen, aber nichts Konkretes"
   ```

3. **Sektion "Beispiel CLARIFY" um Positiv-Beispiele für (b)/(c)/(d) ergänzen.**
   Nach dem bestehenden CLARIFY-Beispiel (Zeile 190) anhängen:
   ```
   Beispiel CLARIFY (mehrere widerspruechliche Themen):
   CLARIFY|Ihr nennt Theater, MINT-AG und Austausch — das sind sehr verschiedene Foerderschienen. Welcher Bereich steht fuer euch im Vordergrund?

   Beispiel CLARIFY (unentschiedene Strategie):
   CLARIFY|"Irgendwas zwischen Inklusion, Demokratielernen und Internationalisierung" ist zu breit. Welcher der drei Bereiche soll der Schwerpunkt sein?
   ```

4. **Sektion "### NEGATIVBEISPIELE" um Drift-Beispiele erweitern** (nach Zeile 211, vor Zeile 213):
   ```
   ## NEGATIVBEISPIELE — was NICHT erlaubt ist (Drift):
   Anliegen "Mathe-Wettbewerbe ausbauen" -> Top-3 mit playmobil-hobpreis|ferry-porsche-challenge|claussen-simon
     <- FALSCH: kaenguru-der-mathematik / mathe-im-advent / bundeswettbewerb-mathematik haetten Vorrang
   Anliegen "Schul-Aquarium und NABU-Standort" -> Top-3 mit kultur-macht-stark|klimalab|aktion-mensch
     <- FALSCH: nabu-schulen / bfn-artenvielfalt / stiftung-kinder-forschen sind Domain-spezifisch
   Anliegen "Mehrsprachige Bibliothek (600 Buecher Tuerkisch/Arabisch)" -> leere Liste
     <- FALSCH: lesen-macht-stark / sprache-macht-stark / mercator-integration passen klar
   ```
   Hinweis: Diese Negativbeispiele sind RECALL-spezifisch und bewusst getrennt
   vom Format-Negativblock am Ende — nicht in eine Sammelliste mischen.

5. **MATCHER_MAX_TOKENS** auf **600** erhöhen (aktuell 400, Zeile 38), weil der erweiterte Prompt minimal mehr Output-Latenz erzeugen kann und Pitfall 5 (Trunkierungs-Risiko bei längeren passt_weil) den Buffer rechtfertigt. Doc-Comment im selben Schritt aktualisieren: "// 600 = Erhoehung fuer Phase-2.1-Prompt-Erweiterung (war 400)".

**Teil B — WR-05 Score-Range-Filter (parsePipeMatches Zeile 264-290):**

In `parsePipeMatches` direkt nach dem Block:
```typescript
const score = parseInt(scoreStr, 10);
if (!id || !validIds.has(id) || isNaN(score)) continue;
```
ersetzen durch:
```typescript
const score = parseInt(scoreStr, 10);
// WR-05: Score-Range 0-100 hart pruefen — pathologische LLM-Outputs (z.B. score=999) raus.
// score < 50 wird weiterhin in runMatch gefiltert (zentrale Geschaeftslogik), aber
// hier zentralisieren wir die formale Range-Validierung.
if (!id || !validIds.has(id) || isNaN(score) || score < 0 || score > 100) continue;
```

**Was NICHT geändert werden darf** (Risiko-Boundaries):
- Tagged-Union-Shape (`MatchResult` mit `kind: "ranking" | "clarification"`) — Frontend hängt daran
- `parsePipeMatches`-Rückgabe-Shape (`RawMatch[]`) — runMatch hängt daran
- `runMatch`-CLARIFY-Dispatch-Logik (Zeile 321-325) — D-09 forceRanking-Verhalten
- `score < 50`-Filter in runMatch (Zeile 333) — Geschäftslogik bleibt
- `MAX_MATCHES = 3` — Korpus-Annotationen + Tests hängen daran

**Per D-17 (PR-Gate):** Diese Änderung an `lib/wizard/matcher.ts` triggert die Eval-Pflicht.
Re-Eval erfolgt aber NICHT in diesem Plan, sondern in 02-06 (sequenziell nach 02-04 + 02-05).
Hier nur: Code-Änderung committen, Tests grün halten.
  </action>
  <acceptance_criteria>
    - `grep -c "domain-spezifisch hat IMMER Vorrang" lib/wizard/matcher.ts` == 1
    - `grep -c "KEIN Drift in Defaults" lib/wizard/matcher.ts` == 1
    - `grep -c "GENAU EIN klares" lib/wizard/matcher.ts` == 1
    - `grep -c "mehrere widerspruechliche" lib/wizard/matcher.ts` >= 1
    - `grep -c "unentschiedene Strategie" lib/wizard/matcher.ts` == 1
    - `grep -c "Drift" lib/wizard/matcher.ts` >= 3 (Kriterien-Block + KEIN-Drift-Block + Negativ-Drift-Block)
    - `grep -c "playmobil-hobpreis" lib/wizard/matcher.ts` >= 1 (Negativbeispiel ev-013)
    - `grep -c "nabu-schulen" lib/wizard/matcher.ts` >= 1 (Negativbeispiel ev-015)
    - `grep -c "lesen-macht-stark" lib/wizard/matcher.ts` >= 1 (Negativbeispiel ev-016)
    - `grep -c "MATCHER_MAX_TOKENS = 600" lib/wizard/matcher.ts` == 1
    - `grep -c "score > 100" lib/wizard/matcher.ts` >= 1 (WR-05 in parsePipeMatches)
    - `grep -c "MatchResult =" lib/wizard/matcher.ts` >= 1 (Tagged-Union unverändert)
    - `grep -c "kind: \"clarification\"" lib/wizard/matcher.ts` >= 2 (CLARIFY-Dispatch + Type-Definition unverändert)
    - `grep -c "forceRanking" lib/wizard/matcher.ts` >= 3 (Conditional-Section + Dispatch-Guard unverändert)
    - `grep -c "MAX_MATCHES = 3" lib/wizard/matcher.ts` == 1 (unverändert)
    - `grep -c "begruendung" lib/wizard/matcher.ts` == 0 (D-04 Migration bleibt)
    - `npx tsc --noEmit` exit 0 (TypeScript-Check)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/kolja/edufunds-app && npx jest __tests__/lib/wizard/matcher.parser.test.ts __tests__/lib/wizard/matcher.dispatch.test.ts -x 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - MATCHER_SYSTEM-Prompt enthält die 4 Tuning-Blöcke (Kriterien-Schärfung + Drift-Verbote + Slot-Heuristik-Verschärfung + Negativ-Drift-Beispiele)
    - MATCHER_MAX_TOKENS = 600
    - parsePipeMatches verwirft Score < 0 oder > 100 (WR-05)
    - Alle bestehenden Parser- und Dispatch-Tests grün
    - TypeScript kompiliert ohne Errors
    - KEIN Re-Eval in diesem Plan — passiert in 02-06
  </done>
</task>

</tasks>

<verification>
- Code-Review-Acceptance: 13 grep-verifizierbare Bedingungen oben
- Bestehende Tests grün: `npx jest __tests__/lib/wizard/matcher.parser.test.ts __tests__/lib/wizard/matcher.dispatch.test.ts -x` exit 0
- TypeScript: `npx tsc --noEmit` exit 0
- KEIN Live-Eval in diesem Plan (siehe Plan 02-06)
</verification>

<success_criteria>
- MATCHER_SYSTEM-Prompt verschärft mit Drift-Verboten + Slot-Heuristik-Schärfung + Recall-Negativbeispielen
- WR-05 Score-Range-Filter in parsePipeMatches implementiert
- Bestehende Test-Suite (matcher.parser.test.ts + matcher.dispatch.test.ts) bleibt grün
- TypeScript-Check grün
- File-Größe lib/wizard/matcher.ts wächst um ~30-40 Zeilen (Prompt-Erweiterung), bleibt unter 400 Zeilen
</success_criteria>

<output>
Nach Abschluss `.planning/phases/02-matcher-quality/02-04-SUMMARY.md` erstellen mit:
- Aufzählung der 5 Prompt-Tuning-Blöcke + WR-05-Patch
- Verweis auf erwarteten Live-Eval-Re-Run in Plan 02-06
- Commit-SHA(s)
- Acceptance-Criteria-Liste mit Verifikations-Output
</output>

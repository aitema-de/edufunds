---
phase: 02-matcher-quality
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Wizard/StartClient.tsx
  - scripts/eval-matcher.ts
autonomous: true
gap_closure: true
requirements: [MATCH-02, MATCH-03]
tags: [code-review, robustness, frontend, eval-script]

must_haves:
  truths:
    - "StartClient.tsx unbekannte body.kind-Werte landen NICHT mehr als leere Trefferliste — Backend-Drift wird als Fehler sichtbar (WR-01)"
    - "StartClient.tsx isSecondRound wird beim frischen AnliegenForm-Submit zurueckgesetzt — kein Sticky-Bug nach fehlgeschlagener zweiter Runde (WR-02)"
    - "scripts/eval-matcher.ts entryToMatchInput uebernimmt forceRanking + previousAnliegen aus dem Korpus (auch wenn aktuell kein Eintrag das setzt) — Audit-Replay-Drift geschlossen (WR-03)"
    - "scripts/eval-matcher.ts Threshold-Gate warnt explizit bei n=0 expected_clarification-Eintraegen (WR-04 — null gilt zwar weiterhin als pass, aber wird vor Silent-Maskierung gewarnt)"
  artifacts:
    - path: "components/Wizard/StartClient.tsx"
      provides: "Robustes Tagged-Union-Dispatch mit Error-Branch + isSecondRound-Reset"
      min_lines: 162
    - path: "scripts/eval-matcher.ts"
      provides: "KorpusEntry mit forceRanking/previousAnliegen-Feldern + Korpus-Drift-Warnungen"
      min_lines: 816
  key_links:
    - from: "components/Wizard/StartClient.tsx body.kind-Dispatch"
      to: "Error-State setError"
      via: "else { setError({...}) }"
      pattern: "Unerwartetes Antwortformat"
    - from: "components/Wizard/StartClient.tsx runMatch"
      to: "isSecondRound-Reset bei !forceRanking"
      via: "if (!values.forceRanking) setIsSecondRound(false)"
      pattern: "setIsSecondRound\\(false\\)"
    - from: "scripts/eval-matcher.ts KorpusEntry"
      to: "MatchInput forceRanking + previousAnliegen"
      via: "entryToMatchInput-Mapping"
      pattern: "entry\\.forceRanking"
    - from: "scripts/eval-matcher.ts main()"
      to: "Korpus-Drift-Warnung bei n=0"
      via: "console.warn fuer nExpectedClarif === 0"
      pattern: "nExpectedClarif === 0"
---

<objective>
Vier von fünf Code-Review-Warnings (WR-01 bis WR-04) aus 02-REVIEW.md addressieren — Robustness-Patches in StartClient.tsx (Frontend-Dispatch + Multi-Round-Reset) und scripts/eval-matcher.ts (Korpus-Drift-Warnungen + Snapshot-Audit-Felder). WR-05 wird in Plan 02-04 mitbearbeitet (gleiches File `lib/wizard/matcher.ts`).

Purpose: Tagged-Union-Contract wird im Frontend AKTIV durchgesetzt statt silent-coerced; Multi-Round-Guard wird vor Sticky-State geschützt; Eval-Skript schützt vor Silent-Maskierung leerer Korpora.

Output: Hardened StartClient + Eval-Skript. Browser-Smoke (Plan 02-07) verifiziert das WR-02-Fix-Verhalten visuell.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-matcher-quality/02-CONTEXT.md
@.planning/phases/02-matcher-quality/02-REVIEW.md
@.planning/phases/02-matcher-quality/02-VERIFICATION.md
@.planning/phases/02-matcher-quality/02-02-SUMMARY.md
@.planning/phases/02-matcher-quality/02-03-SUMMARY.md
@.planning/phases/02-matcher-quality/deferred-items.md
@CLAUDE.md

<interfaces>
<!-- Vorhandene Typen + Funktionen — Executor MUSS dagegen arbeiten. -->

```typescript
// components/Wizard/StartClient.tsx — vorhandene Strukturen
type MatchState =
  | { kind: "ranking"; matches: MatchEntry[] }
  | { kind: "clarification"; question: string }
  | null;

interface RawError {
  message: string;
  httpStatus?: number;
}

type MatchRequestValues = AnliegenValues & {
  forceRanking?: boolean;
  previousAnliegen?: string;
};
```

```typescript
// scripts/eval-matcher.ts — vorhandene Interfaces
interface KorpusEntry {
  id: string;
  category: "kurz" | "ausfuehrlich" | "vag";
  anliegen: string;
  schulname?: string | null;
  schultyp?: string | null;
  bundesland?: string | null;
  geschaetztesBudgetEur?: number | null;
  expected_top3: string[];
  expected_off_target?: string[];
  expected_clarification?: boolean | null;
  expected_missing_slots?: Array<"bundesland" | "zielgruppe" | "thema"> | null;
  notes?: string;
}

interface AggregateMetrics {
  // ... vorhandene Felder
  clarifPrecision: number | null;        // Zähler / nExpectedClarif (oder null wenn n=0)
  clarifFalschPosRate: number | null;    // Zähler / nExpectedNoClarif (oder null wenn n=0)
  nExpectedClarif: number;               // Anzahl Eintraege mit expected_clarification === true
  nExpectedNoClarif: number;             // Anzahl Eintraege mit expected_clarification === false
  // ...
}

function entryToMatchInput(entry: KorpusEntry): MatchInput;  // Zeile 254-262
```

```typescript
// lib/wizard/matcher.ts MatchInput — Referenz, NICHT modifizieren in diesem Plan
export interface MatchInput {
  anliegen: string;
  schulname?: string | null;
  schultyp?: string | null;
  bundesland?: string | null;
  geschaetztesBudgetEur?: number | null;
  forceRanking?: boolean;
  previousAnliegen?: string;
}
```
</interfaces>

<review_findings>
<!-- Direkter Auszug aus 02-REVIEW.md, hier zur schnellen Referenz -->

**WR-01** (StartClient.tsx:67-84): Dispatch-Block prüft nur `body.kind === "clarification"`. Unbekanntes `body.kind` → Else-Zweig → `body.matches ?? []` → leere Liste. Backend-Bugs (vergessenes `kind`-Feld) werden als "keine Treffer" verschleiert.

**WR-02** (StartClient.tsx:78-83, 100-109, 115-119): `setIsSecondRound(false)` wird nur im Erfolgs-Else-Zweig aufgerufen. Wenn 2. Runde fehlschlägt (network error / HTTP 500 / erneute clarification trotz forceRanking), bleibt `isSecondRound = true`. Anschliessender frischer AnliegenForm-Submit triggert sofort Multi-Round-Guard im ersten Anlauf — User sieht "Anliegen ist vage geblieben" obwohl er gerade ein neues Anliegen eingab.

**WR-03** (eval-matcher.ts:577-580, 254-262): `entryToMatchInput` baut nur Standardfelder. Korpus kann `forceRanking: true` oder `previousAnliegen` bisher nicht testen. Snapshot enthält das nicht. Audit-Drift für künftige forceRanking-Korpus-Einträge.

**WR-04** (eval-matcher.ts:787-794): Threshold-Gate akzeptiert null als pass. Bei versehentlich gelöschten expected_clarification-Einträgen passt Gate automatisch — Silent-Maskierung.
</review_findings>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WR-01 + WR-02 — StartClient.tsx Tagged-Union-Hardening</name>
  <read_first>
    - components/Wizard/StartClient.tsx (gesamtes File 1-162)
    - .planning/phases/02-matcher-quality/02-REVIEW.md (WR-01 Zeile 42-65, WR-02 Zeile 67-84)
    - .planning/phases/02-matcher-quality/02-02-SUMMARY.md (Frontend-Architektur-Entscheidungen)
    - components/Wizard/WizardErrorBlock.tsx (um zu sehen wie error.message gerendert wird)
  </read_first>
  <files>components/Wizard/StartClient.tsx</files>
  <action>
**WR-01 (Frontend-Dispatch-Hardening, Zeile 67-84):**

Aktueller Block:
```typescript
const body = await res.json();
if (body.kind === "clarification" && !isSecondRound) {
  setMatchState({ kind: "clarification", question: body.question });
} else if (body.kind === "clarification" && isSecondRound) {
  setMatchState({ kind: "ranking", matches: [] });
  setError({
    message:
      "Anliegen ist vage geblieben — bitte praezisere die Eingabe oder probiere mehr Details.",
  });
} else {
  setMatchState({
    kind: "ranking",
    matches: (body.matches ?? []) as MatchEntry[],
  });
  setIsSecondRound(false);
}
```

Ersetzen durch (expliziter Ranking-Branch + Error-Branch für unbekanntes kind):
```typescript
const body = await res.json();
if (body.kind === "clarification" && !isSecondRound) {
  setMatchState({ kind: "clarification", question: body.question });
} else if (body.kind === "clarification" && isSecondRound) {
  // D-09 Guard: zweite Klaerung trotz forceRanking — Fallback auf leere Liste mit Hinweis.
  setMatchState({ kind: "ranking", matches: [] });
  setError({
    message:
      "Anliegen ist vage geblieben — bitte praezisere die Eingabe oder probiere mehr Details.",
  });
} else if (body.kind === "ranking") {
  setMatchState({
    kind: "ranking",
    matches: (body.matches ?? []) as MatchEntry[],
  });
  setIsSecondRound(false);
} else {
  // WR-01: Unbekanntes body.kind als Backend-Bug behandeln, NICHT als "keine Treffer".
  // Verhindert dass ein vergessenes kind-Feld silent als leeres Ranking durchrutscht.
  setError({
    message: `Unerwartetes Antwortformat (kind=${String(body.kind)}). Bitte erneut versuchen.`,
  });
}
```

**WR-02 (isSecondRound-Reset bei frischem Submit, runMatch-Funktion Anfang ab Zeile 42):**

Aktueller runMatch-Anfang:
```typescript
const runMatch = async (values: MatchRequestValues) => {
  setBusy(true);
  setError(null);
  setMatchState(null);
  setLastInput({...});
```

Ersetzen durch (zusätzliche Reset-Zeile direkt nach setMatchState(null)):
```typescript
const runMatch = async (values: MatchRequestValues) => {
  setBusy(true);
  setError(null);
  setMatchState(null);
  // WR-02: Frischer AnliegenForm-Submit (kein forceRanking-Flag) → Multi-Round-Guard
  // zuruecksetzen. Verhindert Sticky-Bug nach fehlgeschlagener zweiter Runde,
  // bei der setIsSecondRound(false) im Erfolgs-Branch nicht erreicht wurde.
  if (!values.forceRanking) setIsSecondRound(false);
  setLastInput({...});
```

**Was NICHT geändert werden darf:**
- `MatchState`-Typ-Definition (Tagged-Union shape) — Plan-02-02-Lock
- `MatchRequestValues`-Typ — bereits korrekt mit forceRanking + previousAnliegen
- `handlePraezisierung` + `handleForceRanking` — Logik bleibt, sie setzen weiterhin `setIsSecondRound(true)` BEVOR runMatch aufgerufen wird; das WR-02-Reset greift nur bei `!values.forceRanking`
- JSX-Render-Block (Zeile 134-161) — UI bleibt
- `saveHandoff`/`startAntrag`-Logik — unverändert

**Anti-Stolperstein:** Reihenfolge des `setIsSecondRound(false)` in WR-02 ist KRITISCH:
- handlePraezisierung/handleForceRanking rufen `setIsSecondRound(true)` und DANN `runMatch({...forceRanking: true})`. Im neuen Code prüft runMatch `if (!values.forceRanking)` — der zweite Call hat `forceRanking: true`, also greift der Reset nicht. Korrekt.
- Frischer AnliegenForm-Submit ruft `runMatch(values)` ohne forceRanking → Reset greift → isSecondRound wird false. Korrekt.
  </action>
  <acceptance_criteria>
    - `grep -c "Unerwartetes Antwortformat" components/Wizard/StartClient.tsx` == 1
    - `grep -c "body.kind === \"ranking\"" components/Wizard/StartClient.tsx` >= 1
    - `grep -c "WR-01" components/Wizard/StartClient.tsx` >= 1 (Kommentar-Marker für Audit)
    - `grep -c "WR-02" components/Wizard/StartClient.tsx` >= 1
    - `grep -c "if (!values.forceRanking) setIsSecondRound(false)" components/Wizard/StartClient.tsx` == 1
    - `grep -c "setIsSecondRound(false)" components/Wizard/StartClient.tsx` >= 2 (alter Erfolgs-Reset + neuer frischer-Submit-Reset)
    - `grep -c "setMatchState({ kind: \"ranking\", matches: \\[\\] })" components/Wizard/StartClient.tsx` >= 1 (D-09 Guard bleibt)
    - `grep -c "kind: \"clarification\"" components/Wizard/StartClient.tsx` >= 3 (Type + Dispatch unverändert)
    - `grep -c "handlePraezisierung\\|handleForceRanking" components/Wizard/StartClient.tsx` >= 3 (Funktionen + JSX-Wires bleiben)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/kolja/edufunds-app && npx jest __tests__/components/MatchResultList.test.tsx -x 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - WR-01: Unbekanntes body.kind setzt jetzt error.message statt leere Liste
    - WR-02: Frischer AnliegenForm-Submit resettet isSecondRound
    - Bestehende UI-Tests grün
    - TypeScript kompiliert
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: WR-03 + WR-04 — eval-matcher.ts Korpus-Audit-Hardening</name>
  <read_first>
    - scripts/eval-matcher.ts (gesamtes File, fokus auf entryToMatchInput Zeile 254-262, KorpusEntry-Interface (~Zeile 36-50), AggregateMetrics-Interface, main() Threshold-Gate Zeile 780-810)
    - .planning/phases/02-matcher-quality/02-REVIEW.md (WR-03 Zeile 86-111, WR-04 Zeile 113-138)
    - lib/wizard/matcher.ts MatchInput-Interface (für korrekte Field-Übernahme)
    - data/eval/matcher-korpus.json (verifizieren: aktuell 8 expected_clarification=true + 21 expected_clarification=false/null Einträge)
  </read_first>
  <files>scripts/eval-matcher.ts</files>
  <action>
**WR-03 (KorpusEntry-Erweiterung + entryToMatchInput-Erweiterung):**

1. KorpusEntry-Interface (in scripts/eval-matcher.ts, etwa Zeile 36-50) um zwei optionale Felder erweitern. Suche das Interface mit `grep -n "interface KorpusEntry" scripts/eval-matcher.ts` und füge nach `notes?: string;` hinzu (oder vor dem schließenden `}`):
```typescript
  /** Optional: erzwinge Ranking-Pfad (D-09 Override-Test). */
  forceRanking?: boolean;
  /** Optional: voriges Anliegen für Praezisierungs-Test (D-09). */
  previousAnliegen?: string;
```

2. `entryToMatchInput` (Zeile 254-262) erweitern. Aktueller Code:
```typescript
function entryToMatchInput(entry: KorpusEntry): MatchInput {
  const input: MatchInput = { anliegen: entry.anliegen };
  if (entry.schulname !== undefined) input.schulname = entry.schulname;
  if (entry.schultyp !== undefined) input.schultyp = entry.schultyp;
  if (entry.bundesland !== undefined) input.bundesland = entry.bundesland;
  if (entry.geschaetztesBudgetEur !== undefined)
    input.geschaetztesBudgetEur = entry.geschaetztesBudgetEur;
  return input;
}
```
Ersetzen durch (zwei zusätzliche Conditional-Übernahmen vor dem return):
```typescript
function entryToMatchInput(entry: KorpusEntry): MatchInput {
  const input: MatchInput = { anliegen: entry.anliegen };
  if (entry.schulname !== undefined) input.schulname = entry.schulname;
  if (entry.schultyp !== undefined) input.schultyp = entry.schultyp;
  if (entry.bundesland !== undefined) input.bundesland = entry.bundesland;
  if (entry.geschaetztesBudgetEur !== undefined)
    input.geschaetztesBudgetEur = entry.geschaetztesBudgetEur;
  // WR-03: forceRanking + previousAnliegen aus Korpus übernehmen, damit künftige
  // D-09-Test-Einträge im Snapshot dokumentiert sind und das Replay den Override-
  // /Praezisierungs-Pfad korrekt reproduziert.
  if (entry.forceRanking !== undefined) input.forceRanking = entry.forceRanking;
  if (entry.previousAnliegen !== undefined) input.previousAnliegen = entry.previousAnliegen;
  return input;
}
```

**WR-04 (Korpus-Drift-Warnung im Threshold-Gate):**

Im Threshold-Gate-Block (Zeile 780-810) den `console.log("D-16 Threshold-Gate (PR-Gate)")`-Block-Anfang erweitern. Direkt nach der `console.log("=".repeat(80));`-Zeile (vor `const gate: Record<string, boolean> = { ... }`) folgenden Block einfügen:

```typescript
  // WR-04: Korpus-Drift-Warnung — null gilt im Gate weiterhin als pass (siehe unten),
  // aber 0 Einträge mit expected_clarification=true (oder =false) maskiert das
  // Clarif-Target strukturell. Sichtbare Warnung beugt Silent-Pass nach Refactor vor.
  if (m.nExpectedClarif === 0) {
    console.warn(
      `[GATE] WARNUNG: 0 Eintraege mit expected_clarification=true im Korpus. ` +
      `Clarif-Precision-Target wird strukturell NICHT gemessen — Korpus pruefen!`
    );
  }
  if (m.nExpectedNoClarif === 0) {
    console.warn(
      `[GATE] WARNUNG: 0 Eintraege mit expected_clarification=false im Korpus. ` +
      `Clarif-FalschPos-Target wird strukturell NICHT gemessen — Korpus pruefen!`
    );
  }
```

Die `gate`-Definition selbst BLEIBT unverändert (null gilt weiterhin als pass — diese Semantik ist absichtlich, da Korpus-Variationen mit n=0 Edge-Cases möglich sind). Die Warnung macht den Drift sichtbar OHNE den Gate-Mechanismus zu brechen.

**Was NICHT geändert werden darf:**
- `aggregate()`-Funktion (~Zeile 227-287) — Metric-Berechnung bleibt
- `loadKorpusAndValidate` — Korpus-Schema-Validierung bleibt (forceRanking + previousAnliegen sind optionale Felder, brauchen keine Schema-Validation-Logik, JSON-Parsing reicht)
- `migrateOldSnapshot`-Shim — Phase-1-Snapshots sollen weiter geladen werden
- Konsolen-Bericht / JSON-Report-Struktur — abwärtskompatibel
- Threshold-Gate-Logik (gate-Object + process.exit-Pfad) — funktional unverändert
- BASELINE.md / Reports — werden in Plan 02-06 aktualisiert, nicht hier
  </action>
  <acceptance_criteria>
    - `grep -c "forceRanking?: boolean" scripts/eval-matcher.ts` >= 1 (KorpusEntry-Erweiterung)
    - `grep -c "previousAnliegen?: string" scripts/eval-matcher.ts` >= 1 (KorpusEntry-Erweiterung)
    - `grep -c "if (entry.forceRanking !== undefined)" scripts/eval-matcher.ts` == 1 (entryToMatchInput-Mapping)
    - `grep -c "if (entry.previousAnliegen !== undefined)" scripts/eval-matcher.ts` == 1
    - `grep -c "WR-03" scripts/eval-matcher.ts` >= 1 (Kommentar-Marker)
    - `grep -c "WR-04" scripts/eval-matcher.ts` >= 1
    - `grep -c "nExpectedClarif === 0" scripts/eval-matcher.ts` == 1
    - `grep -c "nExpectedNoClarif === 0" scripts/eval-matcher.ts` == 1
    - `grep -c "Clarif-Precision-Target wird strukturell NICHT gemessen" scripts/eval-matcher.ts` == 1
    - `grep -c "process.exit(1)" scripts/eval-matcher.ts` >= 1 (Gate-Mechanik unverändert)
    - `grep -c "process.exit(0)" scripts/eval-matcher.ts` >= 1
    - `grep -c "Recall@3 >= 0.42" scripts/eval-matcher.ts` == 1 (D-16-Targets unverändert)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/kolja/edufunds-app && npx tsc --noEmit 2>&1 | tail -5 && grep -E "WR-(03|04)" scripts/eval-matcher.ts | head -10</automated>
  </verify>
  <done>
    - KorpusEntry hat forceRanking + previousAnliegen als optionale Felder
    - entryToMatchInput übernimmt beide Felder ins MatchInput
    - main() warnt bei n=0 expected_clarification (true oder false) Einträgen
    - Threshold-Gate-Mechanik (D-16-Targets, process.exit) unverändert
    - TypeScript kompiliert
  </done>
</task>

</tasks>

<verification>
- 22 grep-verifizierbare Acceptance-Kriterien (Task 1: 10, Task 2: 12)
- TypeScript: `npx tsc --noEmit` exit 0
- UI-Tests grün: `npx jest __tests__/components/MatchResultList.test.tsx`
- KEIN Live-Eval in diesem Plan (siehe Plan 02-06)
- Browser-Smoke (für WR-02 visuelle Verifikation) erfolgt in Plan 02-07
</verification>

<success_criteria>
- StartClient.tsx: WR-01 Tagged-Union-Hardening + WR-02 isSecondRound-Reset implementiert
- scripts/eval-matcher.ts: WR-03 Korpus-Schema-Erweiterung + WR-04 Drift-Warnung implementiert
- TypeScript-Check grün
- UI-Tests grün
- Threshold-Gate-Mechanik abwärtskompatibel
</success_criteria>

<output>
Nach Abschluss `.planning/phases/02-matcher-quality/02-05-SUMMARY.md` erstellen mit:
- WR-01 bis WR-04 Patches einzeln aufgelistet
- WR-05 Verweis auf Plan 02-04 (gleiches File matcher.ts)
- Commit-SHA(s)
- Acceptance-Criteria-Output
</output>

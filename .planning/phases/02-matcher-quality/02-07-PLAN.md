---
phase: 02-matcher-quality
plan: 07
type: execute
wave: 2
depends_on: [04, 05]
files_modified:
  - .planning/phases/02-matcher-quality/02-07-SUMMARY.md
autonomous: false
gap_closure: true
requirements: [MATCH-02, MATCH-03]
tags: [browser-smoke, ui-verification, manual-checkpoint]

must_haves:
  truths:
    - "Pfad 1 (Ranking) — User sieht 3 Treffer-Cards mit grünem passt_weil-Block + conditional orangem achtung_bei-Block + lucide-react Icons (CheckCircle, AlertTriangle), korrekt vor dem Antrag-starten-Button"
    - "Pfad 2 (Clarification) — Bei vagem Anliegen rendert ClarificationCard mit blauem HelpCircle-Icon, Klärungsfrage als h2, textarea + initial disabled Praezisieren-Button + Override-Link"
    - "Pfad 3 (Praezisierungs-Submit) — Network-Tab zeigt POST /api/match mit body.forceRanking=true und body.previousAnliegen, Response liefert Ranking oder isSecondRound-Hinweis"
    - 'Pfad 4 (Override-Link) — Klick auf "Trotzdem ranken" triggert POST /api/match mit body.forceRanking=true OHNE previousAnliegen, Trefferliste erscheint'
    - 'WR-02-Verifikation — Nach fehlgeschlagener zweiter Runde (z.B. wieder Klärungsfrage trotz forceRanking) zeigt frischer AnliegenForm-Submit kein Sticky "Anliegen vage geblieben"-Banner mehr'
    - "Mobile (375px DevTools) — passt_weil + achtung_bei stacken vertikal, ClarificationCard-Buttons nicht abgeschnitten"
  artifacts:
    - path: ".planning/phases/02-matcher-quality/02-07-SUMMARY.md"
      provides: "Browser-Smoke-Befunde aus 6 manuellen UI-Tests"
      contains: "Pfad 1"
  key_links:
    - from: "components/Wizard/StartClient.tsx"
      to: "/api/match POST mit forceRanking + previousAnliegen"
      via: "Network-Tab-Inspektion in Browser"
      pattern: "forceRanking|previousAnliegen"
---

<objective>
Manuelle Browser-Smoke-Verifikation der vier UI-Pfade auf localhost:3101/antrag/start, die in Plan 02-02 bewusst per User-Entscheidung deferred wurden. Plus visueller Cross-Check des WR-02-Fixes (isSecondRound-Reset, implementiert in Plan 02-05) auf Sticky-Bug-Verhalten.

Purpose: Phase-2-Goal SC#4 ("UI zeigt strukturierte Begründung im Trefferblock korrekt an") wird visuell abgenommen. Plus visuelle Verifikation des WR-02-Robustness-Fix.

Output: Mündliche/schriftliche Bestätigung von Kolja, dass die 4 UI-Pfade + Mobile-Layout + WR-02-Reset-Verhalten korrekt rendern. Bei Befunden: dokumentierte Issue-Liste für mögliches Plan 02-08.
</objective>

<execution_context>
@/home/kolja/.claude/get-shit-done/workflows/execute-plan.md
@/home/kolja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-matcher-quality/02-CONTEXT.md
@.planning/phases/02-matcher-quality/02-VERIFICATION.md
@.planning/phases/02-matcher-quality/02-02-SUMMARY.md
@components/Wizard/StartClient.tsx
@components/Wizard/MatchResultList.tsx
@components/Wizard/ClarificationCard.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dev-Server starten und Pre-Flight</name>
  <read_first>
    - components/Wizard/StartClient.tsx (für isSecondRound-Reset-Logik aus Plan 02-05)
    - components/Wizard/MatchResultList.tsx (für passt_weil/achtung_bei-Render)
    - components/Wizard/ClarificationCard.tsx (für Clarification-Render)
    - CLAUDE.md (Dev-Workflow + DEV-WORKFLOW.md Verweis)
  </read_first>
  <files></files>
  <action>
**Pre-Flight Schritte (Claude führt aus):**

```bash
# Pre-Check: Plan 02-04 + 02-05 sind merged (sonst hat Browser-Smoke nichts neues zu prüfen)
grep -q "WR-02" components/Wizard/StartClient.tsx || echo "WARN: Plan 02-05 nicht gemerged — WR-02-Reset-Test nicht möglich"
grep -q "domain-spezifisch hat IMMER Vorrang" lib/wizard/matcher.ts || echo "WARN: Plan 02-04 nicht gemerged"

# Dev-DB-Tunnel starten (falls nicht schon offen) — gemäß CLAUDE.md
./scripts/dev-db-tunnel.sh --bg 2>&1 | tail -5

# Dev-Server starten (Background, weil User Browser-Tests im Vordergrund macht)
npm run dev > /tmp/edufunds-dev.log 2>&1 &
DEV_PID=$!
echo "Dev-Server PID: $DEV_PID, Logs: /tmp/edufunds-dev.log"

# Warten bis Server hochgefahren (max. 30s)
for i in {1..30}; do
  if curl -s -o /dev/null -w "%{http_code}" http://localhost:3101/ | grep -q "200\|307\|308"; then
    echo "Dev-Server ready nach ${i}s"
    break
  fi
  sleep 1
done

# Direkt-Curl-Test für /api/match — verifiziert Backend (WSL-Browser-Latenz-Bug-Vermeidung gemäß MEMORY.md)
curl -s -X POST http://localhost:3101/api/match \
  -H "Content-Type: application/json" \
  -d '{"anliegen": "Wir wollen unsere Schule besser machen und brauchen dafuer Geld. Es gibt viele Ideen, aber nichts Konkretes — vielleicht etwas mit Sport oder Kultur oder so.", "schultyp": null, "bundesland": null}' \
  -w "\nHTTP-Status: %{http_code}, Latenz: %{time_total}s\n" | head -40
```

Output an User weitergeben mit Ankündigung "Dev-Server läuft auf http://localhost:3101, Browser-Smoke kann starten."

**Was Claude NICHT macht:**
- Keine UI-Modifikationen
- Keine Korpus-Änderungen
- Kein Mock-Hochkommentieren von DeepSeek-API-Keys

**Background-Cleanup nach Checkpoint-Approval (Task 3):**
```bash
kill $DEV_PID 2>/dev/null
```
  </action>
  <acceptance_criteria>
    - Dev-Server lauscht auf http://localhost:3101 (curl liefert HTTP 200/307/308 auf /)
    - Direkt-Curl-Test gegen /api/match liefert HTTP 200 mit body.kind === "clarification" (für vages Anliegen ev-003 als Test-Input)
    - Logfile /tmp/edufunds-dev.log enthält kein "Error: " in den ersten 20 Zeilen
  </acceptance_criteria>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3101/antrag/start | tee /tmp/smoke-status.txt && grep -E "200|307|308" /tmp/smoke-status.txt</automated>
  </verify>
  <done>
    - Dev-Server läuft auf Port 3101
    - /api/match-Endpoint antwortet (Direct-Curl-Sanity-Check)
    - User kann zum Browser-Smoke-Checkpoint übergehen
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser-Smoke 4 UI-Pfade + Mobile + WR-02-Reset (User klickt durch)</name>
  <files>none — manueller Browser-Smoke, kein File-Edit</files>
  <action>Manueller Browser-Smoke durch Kolja. Siehe `<what-built>` und `<how-to-verify>` Abschnitte für die exakten Schritte. Executor pausiert hier und wartet auf Kolja's Resume-Signal mit Status pro Pfad.</action>
  <verify>Resume-Signal von Kolja: "alle 6 OK" oder Liste der Befunde pro Pfad.</verify>
  <done>Kolja hat 6-Pfad-Status gemeldet (OK / Befund / übersprungen).</done>
  <what-built>
    - Plan 02-01/02-02: Tagged-Union-Backend + Frontend mit MatchResultList (passt_weil + achtung_bei) + ClarificationCard (D-10/D-11)
    - Plan 02-04: MATCHER_SYSTEM-Prompt-Verschärfung (Recall + Clarif-Precision Tuning)
    - Plan 02-05: WR-01 Frontend-Dispatch-Hardening + WR-02 isSecondRound-Reset bei frischem Submit
    - Dev-Server läuft auf http://localhost:3101
  </what-built>
  <how-to-verify>
**Browser öffnen, DevTools auf** (F12 oder Cmd+Opt+I), Network-Tab auf, Console-Tab auf.

***

**Pfad 1 — Ranking-Branch (D-10) — auf http://localhost:3101/antrag/start:**

1. Ins Anliegen-Feld eingeben: `Wir wollen einen Schulgarten anlegen, mit Hochbeeten und einem Insektenhotel. Berliner Grundschule mit 200 Kindern.`
2. Schultyp = `grundschule`, Bundesland = `Berlin`
3. "Programme finden"-Button klicken
4. **Erwartetes Ergebnis:**
   - Spinner kurz → 3 Treffer-Cards
   - Pro Card: Score-Badge (emerald/orange/grau), Programm-Name + foerdergeber
   - **GRÜNER Block** mit CheckCircle-Icon und Label "Passt, weil:" + Text (~25 Worte)
   - Conditional **ORANGER Block** mit AlertTriangle-Icon und Label "Achtung:" + Text (~20 Worte) — falls leer, gar kein Block
   - **Beide farbigen Blöcke stehen VOR dem "Antrag starten"-Button**
5. Network-Tab: POST /api/match → 200, Response-JSON enthält `kind: "ranking"` und `matches: [...]` mit `passt_weil` + `achtung_bei`-Feldern
6. Console-Tab: keine Errors/Warnings

✓ Pfad 1 OK / ✗ Pfad 1 Befund: ___

***

**Pfad 2 — Clarification-Branch (D-11) — fresh page reload:**

1. Page reload (oder neue Tab-Instanz)
2. Anliegen: `Wir wollen was im Bereich Bildung machen.` (oder andere vage Phrase)
3. Schultyp + Bundesland LEER lassen
4. "Programme finden" klicken
5. **Erwartetes Ergebnis:**
   - KEINE Trefferliste
   - Stattdessen Card mit blauem HelpCircle-Icon, Klärungsfrage als h2, textarea mit Placeholder "Praezisiere dein Anliegen hier..."
   - Praezisieren-Button initial DISABLED (graue Optik)
   - Override-Link unten links: "Trotzdem mit aktueller Eingabe ranken"
6. Network-Tab: POST /api/match → 200, Response-JSON `kind: "clarification"`, `question: "..."`
7. Console: keine Errors

✓ Pfad 2 OK / ✗ Pfad 2 Befund: ___

***

**Pfad 3 — Praezisierungs-Submit (D-09 + D-11) — direkt nach Pfad 2:**

1. Im Clarification-Modus: textarea fülen mit `In Berlin, Grundschule, Schulgarten und Outdoor-Klassenzimmer.`
2. Praezisieren-Button (jetzt blau aktiv) klicken
3. **Erwartetes Ergebnis:**
   - Network-Tab: POST /api/match mit Body `{forceRanking: true, previousAnliegen: "<original anliegen>", anliegen: "<praezisierung>", schultyp, bundesland}`
   - Response: entweder `kind: "ranking"` mit Trefferliste ODER falls Backend wieder clarification liefert (sollte selten sein) → leere Liste mit isSecondRound-Hinweis-Banner ("Anliegen ist vage geblieben...")
4. Console: keine Errors

✓ Pfad 3 OK / ✗ Pfad 3 Befund: ___

***

**Pfad 4 — Override-Link (D-11) — fresh page reload:**

1. Page reload
2. Wieder vages Anliegen `Wir wollen was im Bereich Bildung machen.` ohne Schultyp/BL
3. "Programme finden" → ClarificationCard erscheint
4. textarea LEER lassen
5. Override-Link "Trotzdem mit aktueller Eingabe ranken" klicken
6. **Erwartetes Ergebnis:**
   - Network-Tab: POST /api/match mit Body `{forceRanking: true, ...}` OHNE previousAnliegen-Feld (oder previousAnliegen=undefined)
   - Trefferliste mit ggf. niedrigen Scores erscheint
7. Console: keine Errors

✓ Pfad 4 OK / ✗ Pfad 4 Befund: ___

***

**WR-02-Verifikation — Reset nach fehlgeschlagener zweiter Runde:**

Dieser Test verifiziert, dass Plan 02-05 das isSecondRound-Sticky-Bug behoben hat.

1. Page reload
2. Vages Anliegen → ClarificationCard erscheint
3. Praezisieren mit ähnlich vagem Text (z.B. `Irgendwas mit Schule.`) → erwartet: zweite clarification trotz forceRanking → "Anliegen ist vage geblieben — bitte praezisere..." Banner erscheint
   - **Falls Backend stattdessen Trefferliste liefert:** WR-02-Test in dieser Session nicht reproduzierbar, mit "übersprungen" markieren
4. **WR-02-Pre-Fix-Verhalten:** ohne den Plan-02-05-Fix würde jetzt JEDER frische AnliegenForm-Submit (mit komplett anderem Anliegen) sofort wieder das "vage geblieben"-Banner zeigen
5. **WR-02-Post-Fix-Erwartung:** Page reload (oder im AnliegenForm anderen Text eingeben + submit) mit konkretem Anliegen wie `Wir wollen Tablets fuer Klasse 5-10 anschaffen, Berlin Gymnasium.` → Trefferliste erscheint, KEIN Sticky-Banner

✓ WR-02 OK (Reset funktioniert) / ✗ WR-02 Befund: ___ / ⊘ Übersprungen (nicht reproduzierbar)

***

**Mobile-Smoke (375px DevTools-Viewport):**

1. DevTools → Device-Toolbar → Responsive-Mode → Width = 375px (iPhone SE)
2. Pfad 1 wiederholen (Ranking)
3. **Erwartetes Ergebnis:**
   - passt_weil-Block + achtung_bei-Block stacken vertikal
   - Score-Badge bleibt sichtbar (nicht abgeschnitten)
   - Programm-Name umbricht sauber
4. Pfad 2 wiederholen (Clarification)
5. **Erwartetes Ergebnis:**
   - HelpCircle + Klärungsfrage-h2 stacken sauber
   - textarea ist Full-Width
   - Praezisieren-Button + Override-Link nicht abgeschnitten

✓ Mobile OK / ✗ Mobile Befund: ___

***

**Zusammenfassung:**

Bitte für jeden der 6 Tests einzeln OK/Befund melden, oder bei mehreren Befunden alle Issues als Liste. Bei Befunden:
- Welcher Pfad
- Was erwartet vs. was gesehen
- Screenshot wenn möglich (optional)

Antworte mit:
- "alle 6 OK" → Phase 2 visuell abgenommen
- ODER Liste der Befunde → ggf. Plan 02-08 für UI-Bugs anlegen
  </how-to-verify>
  <resume-signal>
Antworte mit "alle 6 OK" für vollständige Abnahme, oder liste die Befunde pro Pfad mit Was-erwartet-vs-Was-gesehen-Beschreibung. Bei "übersprungen" für WR-02 reicht das mit kurzer Begründung.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cleanup + SUMMARY-Befund-Dokumentation</name>
  <read_first>
    - .planning/phases/02-matcher-quality/02-VERIFICATION.md (human_verification-Block)
  </read_first>
  <files></files>
  <action>
**Cleanup:**

```bash
# Dev-Server stoppen (PID aus Task 1)
pkill -f "next dev" 2>/dev/null
# Dev-DB-Tunnel kann offen bleiben für Folge-Sessions
```

**SUMMARY-Erstellung basierend auf User-Antwort:**

Wenn "alle 6 OK":
- 02-07-SUMMARY.md mit "alle Pfade visuell verifiziert" + Datum + 6-Pfad-Liste
- Phase-2-Verification-Override eintragen: SC#4 ✓ VERIFIED durch manuellen Browser-Smoke
- WR-02 als visuell verifiziert markieren

Wenn Befunde:
- 02-07-SUMMARY.md mit Befund-Liste pro Pfad
- Empfehlung: Plan 02-08 für UI-Bugfixes anlegen (separater Folgeplan)
- Welche Pfade weiterhin BLOCKING sind für Phase-Abschluss

Wenn "übersprungen" für einzelne Pfade (z.B. WR-02 nicht reproduzierbar):
- Dokumentieren als "deferred to next session"
- Nicht-übersprungene Pfade aber dokumentieren
  </action>
  <acceptance_criteria>
    - Datei `.planning/phases/02-matcher-quality/02-07-SUMMARY.md` existiert
    - SUMMARY enthält für jeden der 6 Tests einen Status (OK / Befund / übersprungen)
    - Wenn Befunde existieren: Empfehlung für Plan 02-08 dokumentiert
    - Dev-Server-Prozess gestoppt: `pgrep -f "next dev" | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02-matcher-quality/02-07-SUMMARY.md && pgrep -f "next dev" | wc -l</automated>
  </verify>
  <done>
    - SUMMARY mit allen 6 Test-Befunden dokumentiert
    - Dev-Server gestoppt
    - Bei "alle 6 OK": Phase 2 visuell abgenommen
    - Bei Befunden: Folge-Plan-Empfehlung dokumentiert
  </done>
</task>

</tasks>

<verification>
- 6 manuelle Browser-Smoke-Tests durch Kolja
- 02-07-SUMMARY.md dokumentiert Befunde
- Dev-Server-Cleanup
</verification>

<success_criteria>
- 4 UI-Pfade durchgeklickt + Mobile + WR-02-Reset
- Bei "alle OK": Phase-2-Goal SC#4 visuell verifiziert
- Bei Befunden: dokumentiert für Plan 02-08
</success_criteria>

<output>
`.planning/phases/02-matcher-quality/02-07-SUMMARY.md` mit:
- 6-Pfad-Status (OK / Befund / übersprungen)
- Bei Befunden: Issue-Liste mit Pfad / Erwartet / Gesehen
- Empfehlung für Phase-Abschluss vs. Plan 02-08 vs. Re-Verification
</output>
